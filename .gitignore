# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# packages/core 빌드 산출 (tsc composite) — Phase 1.5
packages/*/dist/

# production
/build

# misc
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# OS
Thumbs.db
Desktop.ini
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo

# nul (Windows)
nul

# 백업 파일
*.bak

/src/generated/prisma

# Playwright MCP artifacts (세션 17~)
/.playwright-mcp/
/e2e-*.png

# Playwright test output (세션 27~)
/test-results/
/playwright-report/

# Next.js standalone 배포 패키지 (scripts/pack-standalone.sh 재생성 가능)
# /standalone/ 대신 /standalone/* 사용 — 디렉토리 자체는 추적하되 내부만 ignore + 아래 화이트리스트만 재포함
/standalone/*
# 기동 헬퍼는 커밋 대상
!/standalone/start.sh
!/standalone/ecosystem.config.cjs
!/standalone/install-native-linux.sh
!/standalone/.env.production.example
!/standalone/README.md

# Claude Code 로컬/임시 산출 (세션 83 정리)
# 기본 ignore + 추적 자산만 화이트리스트 (standalone/ 패턴과 동일).
# 화이트리스트 외 신규 파일은 자동 무시 — settings.local.json, scheduled_tasks.lock,
# worktrees/, plans/, projects/ 등 ephemeral/local 산출이 다시 생겨도 안전.
/.claude/*
# 팀 공유 추적 자산
!/.claude/settings.json
!/.claude/hooks-template.json
!/.claude/hooks/
!/.claude/hooks/**
!/.claude/skills/
!/.claude/skills/**

# kdyswarm 락 파일 (세션 단위 역사 기록은 lock.completed-*.json 로 일부 추적,
# 진행 중 lock.json 은 ephemeral)
/.kdyswarm/lock.json
