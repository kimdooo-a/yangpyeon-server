# 인수인계서 마스터 목록

> 상위: [CLAUDE.md](../../CLAUDE.md) → **여기**

---

## 다음 세션 프롬프트

**[next-dev-prompt.md](./next-dev-prompt.md)** — 다음 세션 시작 시 Claude에게 전달할 컨텍스트

---

## 인수인계서 목록

<!-- 날짜 그룹별로 정리합니다. 최신이 위로 올라갑니다. -->

### 2026-05-05

| 90 (sweep cont chunk) | [260505-session90-silent-catch-sweep-cont.md](./260505-session90-silent-catch-sweep-cont.md) | **silent catch sweep cont — 30 후보 grep + 6 추가 fix (commit `5f64675`)** — S89 마무리 chunk 의 `sticky-board.tsx:35` + `filebox/page.tsx:79` 2 위치 표면화 자연 follow-up. 30 catch 후보 (components 7 + protected 23) grep 후 위험도×UX 매트릭스 분류: HIGH 1 (`command-menu.tsx:87` PM2 restart silent fail) + primary 2 (`filebox:64` 폴더 내용 / `processes:133` detail 클릭) + secondary 2 (`page.tsx:90` 대시보드 PM2 / `sql-editor:40` saved queries) + polling 1 (`realtime:40` channels 10s) = **6 추가 fix**, 합리적 skip 23건 보존 (JSON parse / capability fallback / re-throw / UI state error / polling 재시도 / SSE malformed / fallback inline). 차등 패턴: primary→`console.error + toast.error`, secondary/polling→`console.error` 만 (toast spam 회피). import 누락 보강 (`filebox/page.tsx` `import { toast } from "sonner"` 추가) — sweep PR 의 부산물 안전망. 검증: vitest 585 PASS / 91 skipped (S87 baseline 정확 일치, 회귀 0) + tsc 사전 존재 2건만. 다른 터미널 동시 commit `67461da` (S89 마무리 chunk /cs docs) = 영역 분리 (본 세션 src 6 vs 다른 터미널 docs) 머지 충돌 0. **잔여 P3**: `sticky-note-card.tsx:107` paired capability fallback 주석 정합 (cosmetic, refactor scope). **이월 (S91+)**: P0 사용자 휴대폰 재시도 final 검증 / P1 ops live 검증 (운영자) / S85-F2 단독 chunk / S87 이월 그대로 유지. |
| 89 (마무리 chunk) | [260505-session89-s88-followup.md](./260505-session89-s88-followup.md) | **S88 마무리 chunk — silent catch 표면화 + PR 게이트 룰 #4 BYPASSRLS=t 확장 + audit 보강 (commits `d18154e` 흡수 + `d10b5e9` follow-up)** — 본 세션 진입 시 다른 터미널 GRANT hotfix 산출 7 untracked + 3 modified docs + handover 미완 발견 → 마무리 commit 처리 (commit A `d18154e` 13 files, hook 통과 = false positive 회피 메타-검증). 추가 audit (`scripts/diag-readwrite-grants.sh` 신규): raw pg client 경로 (`src/lib/pg/pool.ts:58/101` SQL Editor / Webhooks 콘솔) 가 사용하는 `app_readonly` (37/0/0/0 의도) + `app_readwrite` (37/37/37/37 ALL granted) 정상 확인 = `app_admin` 가 유일한 갭. follow-up 3건 (commit B `d10b5e9` 3 files): (1) `sticky-board.tsx:35-44` silent catch → toast.error + console.error + success=false 분기 명시화, (2) `filebox/page.tsx:79-82` silent catch → console.error (사이드바 표시용 비-blocking 위계), (3) `CLAUDE.md` line 167 PR 게이트 룰 #4 확장 — BYPASSRLS=t 운영 role 라이브 SET ROLE 테스트 게이트 추가 + S88 마이그레이션 명시 참조 + memory `feedback_grant_check_for_bypassrls_roles.md` 신규 자매 룰. 다른 터미널 동시 작업 (commit `e33a318` `docs(s88): /cs 보조 chunk`) = CK `2026-05-05-app-admin-bypassrls-grants-trap.md` + next-dev-prompt 갱신, 영역 분리 자연 정합 (본 세션 = 코드 + 룰, 다른 터미널 = docs/solutions + 우선순위 표). 검증: vitest 585 PASS / 91 skipped (S87 baseline 그대로, 회귀 0) + tsc 본 변경 무관 (사전 존재 phase-14c-alpha-ui.spec.ts 2건). **이월 (S90+)**: P0 사용자 휴대폰 재시도 final 검증 / P1 silent catch SWEEP cont. (ops route 17개 client component grep) / P1 ops live 검증 (운영자) / S87 이월 그대로 유지. |
| 88 (보조 chunk) | [260505-session88-app-admin-grants-fix.md](./260505-session88-app-admin-grants-fix.md) | **sticky_notes 운영 콘솔 fix — `app_admin` PostgreSQL role 의 `BYPASSRLS=t + zero GRANT` systemic 결함 (4개월 prod hidden latent)** — 사용자 보고 "iPhone Safari /notes 미작동" → systematic-debugging Phase 1~4 전체 사이클 → 1차 가설 "모바일 Safari" 폐기 (데스크톱도 동일) → PM2 stderr 의 `permission denied for table sticky_notes` (PG 42501) 12+9 건 발견 (sticky_notes/webhooks/sql_queries/cron_jobs 4 ops 테이블 동시 broken) → 1차 fix 가설 "GRANT 누락" 자가 반박 (postgres BYPASSRLS=t + ACL 모두 부여) → CLAUDE.md PR 게이트 룰 #3 단서 = `tenantPrismaFor({bypassRls: true})` → `SET LOCAL ROLE app_admin` → 2차 가설 확정 (`has_table_privilege('app_admin', ...)` 4/4 false) → 스코프 확장 (`feedback_verification_scope_depth`) = **public schema 37 테이블 + 1 시퀀스 + DEFAULT PRIVILEGES 모두 누락 systemic** = app_admin role 자체가 처음부터 GRANT 받은 적 없는 시스템 결함 (S82 4 latent bug 패턴 5번째 사례). **마이그레이션 작성**: `prisma/migrations/20260505000000_grant_app_admin_all_public/migration.sql` (123 lines) — role 가드 + GRANT ALL TABLES/SEQUENCES/FUNCTIONS + 3종 ALTER DEFAULT PRIVILEGES (향후 신설 객체 자동 GRANT, 동일 latent bug 재발 차단) + 검증 블록 (실패 시 RAISE EXCEPTION 자동 rollback). **직접 적용**: psql `--single-transaction -f` + `_prisma_migrations` 메타 row 수동 (sha256sum + uuidgen) — prisma migrate deploy cross-mount 안정성 의심 회피. **검증**: NOTICE "37 테이블 모두 ALL 권한 부여" + 라이브 SET ROLE app_admin 4 broken 테이블 SELECT 통과 (sticky_notes=1/webhooks=0/sql_queries=0/cron_jobs=6) + has_table_privilege 37/37 + 30s PM2 stderr 0 new lines + curl probe 401/307 정상. **메타 발견**: `sticky-board.tsx:35` `} catch { /* 무시 */ }` silent catch 가 PM2 stderr 의 명백한 42501 을 UI 에서 차단 → 디버깅 비용 9배 증폭 잠재력. 후속 PR 권고 = `toast.error` 교체 + 다른 ops route catch{} 전수 점검. **PM2 restart 의도적 SKIP**: CLAUDE.md "PM2 운영 서버 임의 종료 금지" + GRANT catalog-only 변경 = restart 불필요, 두 제약 정합. **CK 신규**: `2026-05-05-app-admin-bypassrls-grants-trap.md` — PostgreSQL BYPASSRLS=t ≠ 모든 권한 (ACL 검사 RLS 보다 먼저). **이월 (S89+)**: P0 사용자 휴대폰 재시도 final 검증 / P1 silent catch 패턴 전수 정리 / P1 다른 ops route 라이브 검증 / P1 PR 게이트 룰 #4 → app_admin (BYPASSRLS=t) 도 라이브 테스트 게이트 확장 / S87 이월 그대로 유지. |
| 87 (메인 chunk) | [260505-session87-cron-verify-cleanup-ck-hook-tdd.md](./260505-session87-cron-verify-cleanup-ck-hook-tdd.md) | **S87 다중 작업 5건 압축 — S87-CRON-VERIFY + S87-ENV-CLEANUP + S87-CK-WSL 2건 + S86-SEC-2 + S85-WAVE-1 (commits `b46bf2e` cleanup+hook+CK, `effd6fa` aggregator TDD 32 case)** — (1) **S87-CRON-VERIFY P0**: PGPASSWORD direct + `AT TIME ZONE 'Asia/Seoul'` 측정으로 6 cron 모두 wall clock 정확 일치 + cf=0 + last_success_at 갱신 검증. **3 fix 동시 영구 통과** (TimeZone=UTC PrismaPg shift 소멸 + S86 P2 recordResult 자연 path + S84-D dedupe Fix A/B 회복). 메타 발견: next-dev-prompt 예측 schedule (23:00/23:30/03:00) 가 실제 (`every 30m` / `every 6h` / `0 3 * * *`) 와 다름 — 인수인계서 추정 vs 실측 차이. (2) **S87-ENV-CLEANUP P2**: `.gitignore` `*.bak` → `*.bak*` 확장 (`.bak-{timestamp}` 변형 매칭, 검증 PASS). `.env.bak-20260504-223541` Windows + WSL build 양측 삭제. (3) **S87-CK-WSL P2**: `docs/solutions/2026-05-04-wsl2-background-sighup-trap.md` (84 lines, WSL2 HCS session 종료 시 child SIGHUP 패턴 + 단일 foreground 표준화) + `docs/solutions/2026-05-04-tsx-env-not-loaded.md` (98 lines, tsx CLI 가 .env 자동 로드 안 함 + PowerShell `$env:` 우회 + dotenv-cli + `import "dotenv/config"` 4종 우회 매트릭스). 두 CK 모두 memory 룰 후보. (4) **S86-SEC-2 P1 보안**: git native `core.hooksPath` (husky 회피). `.githooks/pre-commit` 90 lines 차단 패턴 (운영 password 변형 + API key prefix 4종 Anthropic/OpenAI/GitHub/AWS + bash `${VAR:-secret}` + TS `process.env.X ?? "secret"` + `.env` 직접 staging 차단). `scripts/setup-githooks.sh` 26 lines 1회 실행. **5/5 자체 검증 PASS** (BLOCK 3 + PASS 1 + SKIP_SECRET_HOOK=1 우회 1). 본 commit 자체가 hook 통과 = false positive 회피 메타-검증. (5) **S85-WAVE-1 P1**: wave 평가 §5.4 R-W1 갭 (Track B TDD 81% → 32 case 미달) 해소. llm.test.ts 13→27 (+14: getDailyBudget 폴백 / systemInstruction / config 정확도 / catch / 타입 가드 4종 / prompt 폴백 / client 싱글톤 testTimeout 8000 / 일자 reset / 빈 배열 구분), promote.test.ts 14→27 (+13: slugify 4분기 / batch / upsert update / Set unique / boundary slice / tags 폴백 / tx throw 격리), runner.test.ts 10→15 (+5: cleanup 분기 / buildPendingRow boundary 5종 / classifier 격리 / promoter FAILURE 분기 / item filter). 1차 2 fail (T21 ruleResult.track undefined 가능 → 가드 방향 반전, T25 throttle 6500ms × 2 → testTimeout 8000ms) 즉시 fix. **검증 매트릭스**: vitest 585 PASS / 91 skipped (S86 baseline 553 + 32 신규, 회귀 0) + tsc 0 + hook 5/5 + prod cron 6/6 wall clock 일치. **터치 안 함**: S85-F2 M4 UI Phase 2 (5-6 작업일 단독 세션 권장, wave §5.1 진입 패턴) / S87-RSS-ACTIVATE (운영자) / S87-TZ-MONITOR 24h / S86-SEC-1 (운영자) / 다른 터미널 산출 7건 (memory `feedback_concurrent_terminal_overlap`). **이월 (S88+)**: P0 S85-F2 단독 chunk / P1 S85-WAVE-1 sweep cont. (R-W2/R-W6/R-W7) / P2 S87-CK-WSL memory 룰 승격 / P3 S85-INFRA-1 SWR + jsdom 도입. |

### 2026-05-04

| 85 (메인 chunk) | [260504-session85-wave-completion-eval.md](./260504-session85-wave-completion-eval.md) | **(다른 터미널) Wave 진척도 종합 평가 — S58~S84 27 세션 누적, A-(87/100, 보수화 -0.5)** — kdywavecompletion 스킬 + 3 병렬 Explore 에이전트 (Track A/B/C). **Track A BaaS 95%** (검증 PASS, 잔여=prod TimeZone=UTC 의사결정) / **Track B Aggregator 100% 코드 + 81% TDD** (170 약속 → 139 실측, llm/promote/runner 32 case 미달, PR 게이트 룰 #4 미적용 영역) / **Track C Messenger Phase 1 70% 실측** (60% 주장 보수적, M4 UI Phase 1 진입 + Phase 2~6 + M5 + M6 ~15작업일 잔여) / **Track D Filebox stabilized** (S78~S81 multipart + standalone proxy 정착). **최대 발견 갭**: Track B 100% 주장은 코드 기준이며 TDD 81% — non-BYPASSRLS 라이브 테스트 미적용 영역. 다음 단일 가장 큰 가치 = Track C M4 UI Phase 2~6 + M5 + M6. 자매 chunk = 본 row 아래. |
| 85 (보조 chunk) | [260504-session85-secret-recovery-history-purge.md](./260504-session85-secret-recovery-history-purge.md) | **GitGuardian 시크릿 평문 회수 + git history purge (commit `a4e1ef9` 26 files / filter-repo 300 commits rewrite / `--force-with-lease` push: main `847dbe3`→`fd29ca7`, spec/aggregator-fixes `9957798`→`3ae830f`)** — admin 비밀번호 + Postgres superuser 비밀번호 평문 **21파일 31위치** 노출 (이전 분석 5파일 한정 → grep 결과 6배 확대). (1) **노출 매트릭스**: scripts/ 10건 + src/lib/password.test.ts 1건 + docs/ 14건. 두 변형 동시 노출 (`Knp13579!yan` 운영 + `Knp13579yan` postgres). (2) **1순위 코드 정리** (`a4e1ef9`, 26 files +54/-47): scripts/ env-only 강제 패턴 (`${VAR:?msg}`, fallback default 제거), `.spec.ts` 명시적 throw. password.test.ts 운영-fixture 분리 (`"test-password-fixture-only"`). docs/ placeholder 치환 (`<ADMIN_PASSWORD>` / `<DB_PASSWORD>`). 잔존 검증 0건. (3) **memory 룰 신설** `feedback_no_secret_defaults_in_scripts.md` + MEMORY.md 색인 — 다음 세션 발화 차단 게이트. (4) **사용자 회전 거부** ("회전은 안할꺼야. 2순위 진행해") → 2순위 force purge 직행 (memory `feedback_autonomy.md` 적용). (5) **filter-repo 안전망 3중**: pip 설치 + 백업 브랜치 `sec/before-purge-{main,spec}` + working tree stash (cron/registry WIP, S85 wave eval handover, anthropic RSS WIP) + `--force-with-lease`. (6) **filter-repo 실행**: 300 commits rewrite 4.31초, origin 자동 제거 후 수동 복구. HEAD = `3ae830f fix(cron): runNow에 recordResult` — 다른 터미널 cron/runNow circuit-breaker P2 fix 자연 합류 (보너스). (7) **force push** main + spec/aggregator-fixes 양 forced update. stash pop: anthropic RSS 복원 ✓, cron/registry no-op (`3ae830f` 흡수, 정상). (8) **백업 브랜치 즉시 삭제** (사용자 "백업 브랜치를 지금 지우면 안돼?" → 검증 완료 + GitHub reflog 90일 잔존, 안전 즉시 삭제). **CK 신규**: `2026-05-04-secret-recovery-fallback-default-pattern.md` — fallback default = git history 영구 노출 매개체 패턴. **이월 (S86+)**: GitHub repo public/private 확인 + Archive Program 영향 / pre-commit hook gitleaks / 사용자 비밀번호 회전 (선택, 거부 영역) / S84 이월 그대로 유지. |

### 2026-05-03

| 84 (메인 터미널) | [260503-session84-main-wave-eval-dedupe-cleanup.md](./260503-session84-main-wave-eval-dedupe-cleanup.md) | **wave 평가 + S84-D dedupe Fix A/B + cleanup cron 부채 해소 + wave-tracker 영속화 + PR 리뷰 게이트 룰 5종 (4 commits: `da39576` `0bcc283` `f4bdf8f` `f8caa26`)** — (1) wave 진척도 종합 평가 (4-Track 매트릭스: A ~95% / B 100% / C 60% M3까지 / D stabilized, 압축 실행 통계 S81 5x). (2) **다른 터미널 위임 프롬프트 작성** (`s84-parallel-prompt-m4-ui-phase1.md`, frontend M4 chunk, 영역 분리 충돌 0). (3) **wave 영속화** — 외부 `~/.claude/plans/wave-wiggly-axolotl.md` → `docs/research/baas-foundation/04-architecture-wave/wave-tracker.md` 이식. (4) **CLAUDE.md PR 리뷰 게이트 룰 5 항목** — RLS / withTenant / tenantPrismaFor closure / **non-BYPASSRLS 라이브 테스트** / timezone-sensitive 비교. S82 4 latent bug 재발 차단. (5) **S84-D dedupe Fix A** (`da39576`, TDD 26): root cause = WHERE tenantId 누락 + prod BYPASSRLS + 130 legacy default-tenant rows (S82 fix 이전 fetcher 잔재). 수정 = `where: { tenantId: ctx.tenantId, urlHash: { in: hashes } }` defense-in-depth. (6) **Fix B 데이터 마이그레이션** (직접 SQL): 사전 검증 0 충돌 → `UPDATE 130 default→almanac` → 사후 cross-tenant FK 0. ingested=131 / items=131 정합. (7) **cleanup cron 부채 해소** (`f4bdf8f`, TDD 6): SQL kind readonly 풀 한계 → AGGREGATOR module=cleanup 신설 (`src/lib/aggregator/cleanup.ts`). DB row UPDATE (kind:SQL→AGGREGATOR). **b8-runnow 라이브 검증 SUCCESS 23ms deleted=0**. prod 배포는 다른 터미널 WIP 안정화 후. (8) 검증: tsc 0 / vitest 139/139 PASS / 회귀 0. **이월 (S85+)**: prod 배포 (cleanup + M4 Phase 1 + 메인 4 commits 흡수) / S84-A timezone fix (사용자 의사결정) / S84-C 24h 관찰. **자매 (다른 터미널)** = 같은 세션 84 frontend chunk → row 아래. |
| 84 (다른 터미널) | [260503-session84-m4-ui-phase1.md](./260503-session84-m4-ui-phase1.md) | **M4 UI 보드 Phase 1 — 다른 터미널 작업 (frontend chunk, 13 파일 = 12 신규 + 1 수정, 충돌 0%)** — 위임 프롬프트 `s84-parallel-prompt-m4-ui-phase1.md` (commit `0bcc283`) 정독 후 진입. 산출: (1) 사이드바 "커뮤니케이션" 그룹 신설 4 항목 (대화/알림설정/신고운영/헬스, MANAGER_PLUS·ADMIN_ONLY 권한 매트릭스 추가). (2) 시각 분류 헬퍼 2종 + 단위 테스트 8 PASS — `getConversationItemClasses(active/unread/muted/mention)` + `getMessageBubbleVariant(own/other/system/recalled)`. **TDD 전략 결정**: vitest `environment: "node"` + `@testing-library/react` 미설치 환경에서 시각 분류 로직 순수 함수 추출 → `.test.ts` 8 케이스로 위임 프롬프트의 "props/렌더 4 케이스" 정신 유지 + 인프라 변경 회피. (3) 컴포넌트 4종 — ConversationListItem (role=button + aria) / ConversationList (loading/error/empty/list 4 분기) / MessageBubble (4 variant + 본문 분기) / MessageList (역방향 표시 + 자동 스크롤 + aria-live=polite). (4) 데이터 fetch hook 2종 — useConversations + useMessages, `useState + useEffect + fetch` 패턴 (SWR 미설치). tenantSlug = 'default' 하드코드 (memory `project_tenant_default_sentinel`). (5) 라우트 2종 — `(protected)/messenger/page.tsx` (3-col 데스크톱, 모바일 list only) + `(protected)/messenger/[id]/page.tsx` (헤더 h-14 + 메시지 + composer placeholder). 위치 = (protected) 그룹 (auth layout 상속, 위임 프롬프트의 `src/app/messenger/` 에서 변경). composer/⋮/ⓘ/첨부/이모지/멘션 모두 disabled (Phase 2 활성화). **위임 프롬프트 vs 실제 환경 차이 4건**: 페이지 위치 (protected) / SWR 미설치 → 순수 fetch / @testing-library 미설치 → 순수 함수 8 케이스 / lucide-react 정상. **검증**: tsc 0 / vitest **547 pass** + 91 skip (회귀 0, 신규 8) / production build PASS, `/messenger` + `/messenger/[id]` 라우트 (ƒ Dynamic) 등록 확인. WSL 빌드+배포 = SKIP (위임 프롬프트 명시, PR 머지 후 운영자/다음 세션). **이월 (S85+)**: P0 Composer 인터랙티브 + SSE wiring + User name lookup / P1 정보패널 + 검색 + 컨슈머 generic / P2 SWR 도입 + jsdom + 모바일 사이드바 / 운영 WSL 배포 + 시각 fidelity + a11y 게이트. |

### 2026-05-02

| 83 | [260502-session83-timezone-audit-source-expand-cron-cleanup.md](./260502-session83-timezone-audit-source-expand-cron-cleanup.md) | **S82 follow-up 6 task 일괄 (코드 commit 0, 신규 untracked 2 + WSL 빌드+재배포 1회)** — S82 종료 시 5 follow-up 인계 + 사용자 P1 격상 PrismaPg timezone audit. **(1) 24h 관찰**: cron 정상 + items=60 stable + PrismaPg -9h shift prod 발현 empirical 재확인 (10:55 wall → DB 01:57:23+09 정확 -9h). **(2) 60 sources 5 확장**: 9 RSS active (github/huggingface/stripe/kakao/techcrunch + 기존 4 - anthropic 1). runNow PASS sources=8 fetched=130 errors=0 (단 inserted=0 duplicates=130 — 신규 소스 canonical URL 중첩 의심, 별도 진단 후보). **(3) anthropic-news 비활**: RSS endpoint 4 path 모두 404 → active=FALSE + notes. **(4) S78-H multipart cleanup cron**: scripts/seaweedfs-clean-multipart.sh + WSL crontab 매주 일요일 04:00 KST. 수동 PASS. **(5) M3 SSE 라이브 e2e**: events route 가 마지막 deploy 이후 추가 미배포 발견 → WSL 빌드+재배포 (PM2 pid 252403 ↺=21, ELF Linux). 401 가드 응답 확인. publish/subscribe 풀 e2e = browser auth 필요 (별도). 부수: Windows port 3000 leftover node.exe (pid 6608) 발견. **(6) [P1] PrismaPg timezone audit** (`docs/solutions/2026-05-02-prismapg-timezone-prod-audit.md`): 12 코드 위치 분류 — Prisma round-trip cancel ✅ / DB-side calc only ✅ / raw SQL workaround 인지 ⚠️ (sessions 7일 → 6일 15시간, 사용자 미체감) / 외부 timestamp 파싱 ✅. prod 데이터 손실 0, 사용자 체감 거의 0. **권고 = `?options=-c TimeZone=UTC` prod 적용** (재로그인 1회 외 영향 미미) — 사용자 의사결정 대기. 검증: tsc 0 / WSL 빌드+배포 PASS / `/events` 401. **이월 (S84+)**: timezone fix 적용 결정 / 60 sources 자연 cron 24h+ 안정 후 추가 5 / inserted=0 dedupe 진단 / M3 SSE browser e2e / M4 UI 보드 / M5+M6 / Windows port 3000 leftover 정리. |
| 82 | [260502-session82-m3-user-m2-live-prisma-rls-fixes.md](./260502-session82-m3-user-m2-live-prisma-rls-fixes.md) | **M3 user 채널 wiring + M2 통합 라이브 활성화 + Prisma/RLS/timezone 4 latent bug fix (3 commits `152562d` `8bef896` `5449f9e`, +625 LOC, 17 파일)** — S81 6 후보 중 진행 가능 3 작업 동시 압축. **M3 user 채널** (`152562d`, +292 LOC, 6 파일): mention/dm/report/block 4 이벤트 wiring + sendMessage 반환 확장 (conversationKind + otherMemberId) + buildSnippet 80자 컷 + block.created blocker 본인 채널 (stalker risk 차단). **M2 라이브 + 4 latent fix** (`8bef896`, +180 LOC, 8 파일): WSL postgres `luckystyle4u_test` 신규 DB 셋업 시 67 fail/25 pass 첫 실행 → (A.1) tenant_memberships ON CONSTRAINT 명 mismatch + files/folders cascade 누락, (A.2) AbuseReport.targetKind @map 누락 (prod 미운영 라우트라 latent, session 80 추가 시 누락), (A.3) **Prisma extension query(args) escape — tx 와 다른 conn 으로 SET LOCAL 무효, prod BYPASSRLS 로 가려져 있던 latent bug** → tx[modelCamel][operation](args) 직접 라우팅 fix, (A.4) **PrismaPg + Asia/Seoul timezone +9hr 시프트** → admin pool (raw pg) + Prisma 혼용 시 노출, `?options=-c TimeZone=UTC` 회피 (prod follow-up 별도). 인프라 영구 정착: scripts/setup-test-db-role.sh + scripts/run-integration-tests.sh + .env.test.example. 메신저 92/92 PASS. **M3 SSE wire format** (`5449f9e`, +153 LOC, 3 파일): encodeSseEvent/Comment 헬퍼 추출 + 7 신규 테스트 (wire format / nested payload / 한글·이모지 / ReadableStream+TextEncoder 통합 / multiple subscribers). **M4 = UI 보드 5-7일 chunk** (PRD 확인 결과 mis-naming, web push 자체 Phase 1 미포함) → 진입 안 함. 검증: tsc 0 / vitest 532 pass + 91 skip / live 92/92. **이월 (S83+)**: prod PrismaPg timezone 시프트 가시화 + 영향 분석 (P1, ~2h, rate-limit/edit/recall/session 비교 audit) / 24h+ 후 60 소스 점진 확장 / M4 UI 보드 (5-7일) / M5+M6 / totp.test.ts flake 수정 / Phase 2 plugin / anthropic-news RSS 갱신. **세션 82 후속 (/cs 2차, `04e441b`)**: CLAUDE.md §"PM2 운영 서버 — 임의 종료 절대 금지 규칙" 신설 + memory/feedback_pm2_servers_no_stop.md (광범위 표현은 세션 기동분 한정). |

| 세션 | 파일 | 주요 작업 |
|------|------|-----------|
| 81 | [260502-session81-aggregator-launch-messenger-m2-m3.md](./260502-session81-aggregator-launch-messenger-m2-m3.md) | **B7+B8 라이브 + Track C M2 23 ops + (다른 터미널) M3 SSE + M2 통합테스트 32 케이스 (3 commits `ffdd2dd` `72b5ebd` `069705c`, +3,164 LOC, 26 파일)** — 사용자 단일 요청 "B7,b8 + C 전체" 수렴, wave 5 세션 매핑 단일 압축. **B7+B8** (`ffdd2dd`, +477 LOC, 5 파일): seed-aggregator-cron + b8-list/activate/check/runnow. WSL 빌드+배포 pid 220187 ↺=19. 5 소스 활성화 (anthropic-news/openai-blog/vercel-blog/toss-tech/hn-algolia-front). **runNow 라이브 PASS** — rss-fetch 60 INSERT (anthropic-news 404 격리) → classify 50 → promote 50 → **items=50 첫 production Almanac 카드** (4개월 P0 본진 라이브). **Track C M2** (`72b5ebd`, +1,360 LOC, 17 파일): 23 ops 19 routes 4 그룹 (C1 5 / C2 5 / C3 5 / C4 8) + route-utils.ts (HTTP 매핑 20 코드). 패턴 단일 (가드+Zod+helper+audit+errorMap). tsc 0 / 17 라우트 unauth 401 / WSL 재배포 pid 226263 ↺=20. **다른 터미널 M3** (`069705c`, +1,332 LOC, 4 신규 + 5 갱신): src/lib/messenger/sse.ts + events/route.ts SSE GET + tests/messenger/m2-integration.test.ts 32 케이스 + sse.test.ts. 5 라우트 publishConvEvent 통합. **§3 격리 production 첫 실증** (anthropic-news 404 다른 4 차단 0). **이월 (S82+)**: 24h 후 60 소스 점진 확장 / M3 SSE 라이브 검증 / M4 운영자 시나리오 / anthropic-news 갱신 / Phase 2 plugin. |
| 80 | [260502-session80-aggregator-bpre-b1-b2.md](./260502-session80-aggregator-bpre-b1-b2.md) | **Track B Aggregator 본진 P0 — B-pre + B1 + B2 + B3 + B4 + B5 + B6 (10 commits, +6,112 LOC)** — wave plan + baseline 검증 + s79 leftover 회복 + B-pre 슬러그 매핑 + B1 의존성+env + B2 types+dedupe + B3 classify + 1차 /cs (`b46918c`) + B3 정합화 (`0ad2f9a`) + **B4 4 fetchers** (`100ae5c`, TDD 30, 5 파일 +1,481 LOC, ArXiv link order spec bug fix) + **B5 llm+promote multi-tenant tx** (`58a526a`, TDD 27, 4 파일 +981 LOC, slugify NFKD 한글 음절 분해 spec bug fix) + **B6 runner+cron AGGREGATOR dispatcher + kind union 확장 4 파일 동시** (`7c50c9f`, TDD 15, 8 파일 +815 LOC). 본진 spec 6 모듈 (types/dedupe/classify/fetchers/llm/promote/runner) 모두 multi-tenant 적응 완료. **spec port-time bug 4건 누적 차단** (B2 multi-value + B3 한글 boundary + B4 ArXiv link order + B5 slugify NFKD). 검증: tsc 0 / 509/569 pass / 신규 110 케이스 PASS / 회귀 0. **CK 신규 3건** (multi-value / 한글 boundary / 4-cases 통합) + **memory 신규 1건** (vi.mock 호이스팅 분기). **이월 (S81+)**: B7 시드+배포 ~3h (P0 본진 마지막 코드 작업) / B8 5 소스 활성화 ~2h / 세션 85 24h 관찰 / 86~ Track C M2 19 라우트. |

### 2026-05-01

| 세션 | 파일 | 주요 작업 |
|------|------|-----------|
| 79 | [260501-session79-multipart-body-truncation-fix.md](./260501-session79-multipart-body-truncation-fix.md) | **세션 79 — S78-C 본진 검증 + multipart body truncation 회귀 fix (commits `fd4d666` + `ff05a07` + /cs commit, 1+2+5=8 파일)** — 세션 78 직후 운영자 71.3MB ZIP 1차 시도 → PM2 err.log `Request body exceeded 10MB` 발견 → body-streams.js + next-server.js + router-server.js + config-shared.d.ts 코드 수술로 메커니즘 정확 진단 (Next.js 16 standalone router-server proxy 가 default 10MB 로 모든 request 에 cloneBodyStream attach + finalize() 가 truncated PassThrough 로 원본 request body replace) → `next.config.ts` 에 `experimental.proxyClientMaxBodySize: '100mb'` 6 줄 추가 → tsc 0 / WSL 빌드 8단계 PASS (pid 210927→213048, ELF Linux x86-64) → 운영자 동일 71.3MB 재업로드 PASS (DB `files.size = 74,724,323` + SeaweedFS `yangpyeon-filebox_8.dat = 74,724,808`, volume header 485B 정확 일치 / created_at = deploy 후 5.5분 / ALS "Tenant context missing" 0건). **검증 사슬 완성**: s77 PHASE 4 (auth-gate ping → broken 머지) → s78 (architectural gap 발견 + multipart 4 라우트 신설) → s79 (infrastructure layer 회귀 발견 → fix → 결정적 검증). 3 세션 동일 4-step 함정 패턴 반복 (기능 X 추가 → X 자체 정확 → infrastructure layer silently 변형 → 표면 응답만 검증). **memory rule 신규**: `reference_nextjs_proxy_body_limit.md` — Next.js 16 standalone proxyClientMaxBodySize 함정 baked-in (메커니즘 + setting 변천 + 검증 임계). **CK 신규**: `2026-05-02-nextjs16-standalone-proxy-body-truncation.md` — `2026-05-01-verification-scope-depth-auth-gate-only-insufficient.md` 자매. **터치 안 함**: S78-E Almanac aggregator (~28h, 별도 트랙) / S78-H multipart cleanup cron (P1) / S78-D 폰 모바일 (P1 보너스). **이월** (S80+): S78-E (P0 본진) / S78-H (P1) / S78-D (P1) / S78-I (P2) / S78-J (P2) / 메신저 M2-Step1. |
| 78 | [260501-session78-multipart-upload-x1-server-proxy.md](./260501-session78-multipart-upload-x1-server-proxy.md) | **세션 78 — SeaweedFS multipart upload (X1 server proxy) S78-A (commits `963eba5` + `6c5c195`, 8 파일 +585/-184)** — s77 옵션 C C1 머지 직후 첫 후속. 운영자 자율 모드 → S78-F CGNAT 5초 검증 (반증, KT 정상 IPv4) → S78-A 진입 시점에 **architectural gap 발견** (browser → localhost:8333 도달 불가능, s77 PHASE 4 가 auth-gate ping 만 검증한 결과로 broken 머지) → X1 server proxy 채택 (vs X2 cloudflared S3 ingress, X3 hostname rewrite — SigV4 깨짐) → multipart 4 함수 + 4 라우트 + frontend 리팩터 + r2-presigned/r2-confirm 삭제. 검증: tsc 0 / WSL 빌드 + 배포 PASS / 회귀 ping 9 라우트 401 / 신규 ALS 0. **자동화 불가**: 인증 50MB+ PUT 실측 = S78-C (운영자 본인 60MB+ 드래그, 10분, 다음 세션 P0) — ALS + C1 + S78-A multipart 3종 결정적 동시 검증. **CK 신규**: `2026-05-01-verification-scope-depth-auth-gate-only-insufficient.md` (cc231fd CK `plan-estimate-vs-reality-gap-infrastructure-blind-spot.md` 자매 — verification-depth 측면). **memory rule 신규**: `feedback_verification_scope_depth.md`. **이월** (S79+): S78-C P0 운영자 본인 실측 / S78-H multipart cleanup cron / S78-I filer leveldb / Almanac aggregator(28h). |
| 77 (옵션 C 실행) | [260501-session77-option-c-new-terminal-execution.md](./260501-session77-option-c-new-terminal-execution.md) | **옵션 C 새 터미널 = R2 → SeaweedFS 자가호스팅 7 PHASE 일괄 실행** — 자매 handover (260501-session77-r2-questioning-seaweedfs-pivot.md) 의 옵션 C 결정 + 7 PHASE 풀 패키지 프롬프트를 본 conversation = 새 터미널 역할로 직접 실행. **5 commits** (ee68a07/87de464/28273a0/63521d2 + 다른 터미널 19454d4): PHASE 0 §S77-A SUPERSEDED note (운영자 R2 콘솔 PHASE 0 시점 이미 삭제 = PHASE 6 무용 신호) / **PHASE 1 SP-016 50GB 정량 검증 ACCEPTED 4/4 임계 PASS** (throughput 566.53MB/s 11.3× 마진 / mem 608.8MB 40% 여유 / SIGKILL→재시작 22.1s 5.4× 마진 / 무결성 5/5 + 재시작후 file-250 PASS, `scripts/sp016-load-test.py` 자동 측정 도구) / PHASE 2 PM2 seaweedfs daemon 등록 + bucket 생성 (`weed shell s3.user.provision -name=ypserver -bucket=yangpyeon-filebox -role=readwrite`) / **PHASE 3 R2 → SeaweedFS endpoint 교체 C1 옵션 A** (~62줄: r2.ts endpoint+forcePathStyle+region us-east-1, 다운로드 302 redirect → ypserver `transformToWebStream` forward, .env 3계층 동기 windows+ypserver, 함수명 R2_* 그대로 의미 재정의) / PHASE 4 `/ypserver` 8단계 통과 + 회귀 ping 9 라우트 (401×6+403×1+405×2 auth gate 차단) + 신규 에러 0건 / **PHASE 5 ADR-032 SUPERSEDED + ADR-033 ACCEPTED 신규 (235줄, §"운영자 가치관 정합성 점검 6항목" 매트릭스 적용 1호) + r2-monitoring SUPERSEDED + seaweedfs-monitoring 신규 (디스크 80%/90%/process/filer 응답/restart 5종 트리거) + memory rule supersede + 신규** / PHASE 6 운영자 본인 작업 무용 (이미 완료) / PHASE 7 본 handover + push. **핵심 발견** (운영자 가설 검증 정확): "IP 가 고정이 아니라 cloudflare 의존 강제" — 동적 IP + ISP CGNAT 매트릭스 5종 분석으로 cloudflare tunnel outbound-only persistent connection 이 사실상 강제 → 100MB hard limit 인프라 측 회피 불가 → 코드 측 multipart upload 가 유일한 회수 경로. **작동 범위**: <50MB local / 50MB~90MB single PUT presigned 즉시 작동 / 90MB+ 회귀 (multipart 후속 PR S78-A ~530줄 ~3.5h 대기). **이월** (S78+): A multipart 통합 / B multipart cleanup cron / C filer leveldb 전환 / D PM2 startup / E 인증 50MB+ PUT 실측 / F CGNAT 확인 / Almanac aggregator 28h. |
| 77 | [260501-session77-r2-questioning-seaweedfs-pivot.md](./260501-session77-r2-questioning-seaweedfs-pivot.md) | **비대칭 분할 → 운영자 가치관 충돌 표면화 → 옵션 C SeaweedFS 자가호스팅 전환 결정** — (1) S77-W `/ypserver` 8단계 통과 + 회귀 ping 7 라우트 (405×6 + 401×1, 500 0건) + 13:32:52 KST 이후 신규 에러 0건 (단 인증 50MB+ PUT 실측 미완 — 진짜 ALS 회귀 검증 다음 세션 이월). (2) S77-B Step 2 — Cloudflare Notifications UI 가 USD 가 아닌 bytes 만 받음 발견 → T1 정수 매핑 `10737418240` bytes (10 GiB) 채택, 운영자 본인 콘솔 적용 완료. 가이드 §2.1 정정 + USD↔bytes 변환표 신규 + 메모리 룰 `reference_r2_alarm_threshold.md` 등록. (3) **운영자 핵심 질문**: "왜 R2 같은 지출 서비스를 쓰게 만들었나? 내 컴퓨터, 돈 안 쓰는 자가 서버 운영." → ADR-032 §"결정자" = 운영자 본인 sign-off (s71~72 토큰 직접 발급) 인정 + 결정 시점 가치관 충돌 점검 누락 인정. 4 옵션 (A 유지 / B 외부 채널 / C SeaweedFS / D 보류) 제시. (4) **옵션 C 채택** → 새 터미널용 7 PHASE 풀 패키지 프롬프트 작성: PHASE 0 S77-A 캔슬 / 1 SP-016 50GB 정량 검증 6종 임계 / 2 SeaweedFS 운영 모드 (filer leveldb + S3 API + PM2) / 3 R2→SeaweedFS endpoint 교체 (~10줄, S3 API 호환) / 4 빌드+배포+50MB PUT 실측 / 5 ADR-032 supersede + ADR-033 ACCEPTED + SP-016 ACCEPTED + 가이드/메모리 supersede / 6 운영자 R2 콘솔 정리 / 7 /cs. 비용 $0 영구 / 외부 의존 0 / 가치관 ◎. **CK 후보**: 외부 서비스 도입 ADR 결정 시 운영자 가치관 점검 누락 패턴 — "결정자=운영자 sign-off" 가 가치관 충돌 점검을 보장하지 않음. **이월** (S78+): 새 터미널 PHASE 1~7 / R2 콘솔 정리 / SeaweedFS 모니터링 가이드 / Almanac aggregator(28h) / 메신저 M2. |
| 76 | [260501-session76-r2-deleteobject.md](./260501-session76-r2-deleteobject.md) | **R2 객체 즉시 삭제 best-effort (S76-D 1·2단계, commit `8bf1b5f`, 2 파일 +38/-4)** — `deleteR2Object(key)` 신규(`src/lib/r2.ts`, `DeleteObjectCommand` + NotFound swallow + 그 외 throw) + `deleteFile()` R2 분기 보강(`src/lib/filebox-db.ts`, `prisma.file.delete()` 후 `try/catch console.warn` best-effort). DB row 는 이미 사라졌으므로 R2 잔존 객체는 24h cleanup cron 의 회수 대상 — eventually consistent 모델 채택, 동기 트랜잭션 외부 SDK 확장 회피. **3단계 분리**: 24h pending cleanup cron 은 cron runner kind enum (SQL/FUNCTION/WEBHOOK) 가 R2 SDK 호출 미지원 → 새 kind 또는 별도 스케줄러 필요 → 별도 PR (§S77-A). **검증**: `tsc --noEmit` exit 0. **본 conversation /cs 산출**: journal §"세션 76" / current.md row 76 / logs/2026-05.md s76 entry / 본 handover / next-dev-prompt s77 갱신. **이월** (S77+): §S77-A 24h pending cleanup cron(~3h) + R2 콘솔 CORS(운영자 3분) + 50MB+ 브라우저 PUT 실측 + Almanac aggregator(28h) + 메신저 M2. |
| 75 | [260501-session75-r2-doc-cleanup-overlap.md](./260501-session75-r2-doc-cleanup-overlap.md) | **R2 ACCEPTED 추적성 보강 (M1~M4) — redundant verification, 다른 터미널 commit `6061cdc` 와 byte-identical 결과 → no-op, session-end 문서만 산출** (commit `594387f`). 직전 검증에서 commit `275464c` (S72 R2 V1) 핵심 8/8 통과 후 마무리 6건(M1~M6) 식별 → M5/M6 = 4h+ V1 진화는 다른 터미널 s73 적용 완료, 본 세션은 M1~M4 (spike-032 §4.2 PoC 표 5컬럼 / §3 헤더 정정 / §9 v0.3 / ADR-032 §7 게이트 [x] / wsl-build-deploy.sh /.env exclude) 자율 진행 의도. 그러나 같은 워킹트리 다른 터미널이 동일 cleanup 을 commit `6061cdc s73-followup` 으로 먼저 적용 → 본 세션 edits byte-identical no-op. **메모리 룰 신규**: `feedback_concurrent_terminal_overlap.md` — `/cs` 또는 새 작업 진입 전 `git log --oneline -5` + `git status --short` 로 동일 영역 사전 확인 (baseline_check_before_swarm 자매). **이월** (S76+): R2 콘솔 CORS / 50MB+ 실측 / R2 객체 cleanup(S76 에서 1·2단계 진행) / Almanac aggregator. |
| 74 | [260501-session74-als-migration-mobile-drag.md](./260501-session74-als-migration-mobile-drag.md) | **메모 사고 후속: 모바일 드래그 PointerEvent + 28→31 라우트 ALS 마이그레이션** — 직전 메모 fix 세션 commits(9621480/c4c7798/7978535) 권고 후속 2건 직행. **(1) 모바일 드래그**: `sticky-note-card.tsx` `onMouseDown`/`mousemove`/`mouseup` → `onPointerDown`/`pointermove`/`pointerup`/`pointercancel` 통합. `setPointerCapture(pointerId)` + `style={{ touchAction: isOwner ? 'none' : 'auto' }}` (헤더 owner-only 차단, read-only 사용자 page scroll 보존) + `e.preventDefault()`. **(2) 28→31 라우트 ALS 마이그레이션**: Grep 36 파일 분류 → 운영 콘솔 22(`OPS_CTX = { tenantId, bypassRls: true } as const` 패턴, 단 functions/log-drains 7 파일은 bypassRls 미설정 정책 유지) + 테넌트 5(`tenantPrismaFor({ tenantId: tenant.id })` 가드 arg 활용) + 메신저 라이브러리 4(`getCurrentTenant() + ctx 캐시` 패턴, `withTenantTx` 그대로 유지). 다중 statement `runWithTenant` 블록은 `db = tenantPrismaFor(...)` 한 번 캐시 + sequence 분해(원래도 atomic 아님). **(3) 의도적 제외 3건**: filebox-db.ts (raw prisma + ADR-024 부속결정 T1.5), tenant-router/membership.ts (tenant 결정 *전* 단계 base prisma 정당), r2-presigned route (TODO T1.5). **검증**: `tsc --noEmit` exit 0, 잔여 `prismaWithTenant.X` 호출 0건 (인프라 파일 2개 단어 잔존만). **메모리 갱신**: `project_workspace_singleton_globalthis.md` 함정 2 섹션에 마이그레이션 완료 사실 추가 (31 파일 + bypassRls 정책 차이 + 의도적 제외 3건). **터치 안 함**: 다른 무관 미커밋 영역(세션 72 R2 V1, 세션 73 R2 UI), WSL 배포(사용자 단계), 폰 실측(사용자 단계). **이월** (S75+): commit + WSL 배포 + 폰 실측 → 자주 안 쓰는 라우트 ping smoke → 세션 72/73 미커밋 정리. |
| 73 | [260501-session73-r2-ui-download.md](./260501-session73-r2-ui-download.md) | **R2 UI 50MB 분기 + XHR 진행률 + 다운로드 302 redirect (~30분)** — 세션 72 next-dev-prompt §S72-A line 3~5 직행. `FileUploadZone` 50MB 임계점 자동 분기(이하 multipart, 초과 presigned PUT) + `XMLHttpRequest.upload.onprogress` 실시간 % 표시(기존 60% pulse 페이크 제거). `getFileForDownload` R2 분기(filePath: null union) + `[id]/route.ts` storageType='r2' 시 `presignR2GetUrl(...)` + `NextResponse.redirect(url, 302)` (Cloudflare Tunnel 우회). `presignR2GetUrl` `responseContentDisposition` 옵션 확장(R2 응답 헤더로 set, RFC 5987 한국어 파일명 보존). `deleteFile` R2 분기(DB row만 삭제, R2 객체 잔존은 다음 PR — TODO 코드 주석). **`scripts/r2-cors-apply.mjs` 자동화 스크립트** (PutBucketCorsCommand → AccessDenied 403, **현재 토큰 Object 전용 제약 발견** → 콘솔 작업으로 운영자 위임). **검증**: tsc 0 / WSL 빌드+배포 8/8 / PM2 ypserver pid 190213 online / 라우트 401 정상. **터치 안 함**: aggregator 비즈니스, 메신저, 다른 무관 미커밋 영역 보존. **이월** (S74+): R2 콘솔 CORS 1회 적용 → 50MB+ 브라우저 PUT 실측 → R2 deleteObject + 24h cleanup cron → R2 quota UI(P2) → Almanac aggregator ~28h. |
| 72 | [260501-session72-r2-v1-applied.md](./260501-session72-r2-v1-applied.md) | **S72-A 트랙 A R2 V1 옵션 A 적용 완수 + 메모리 +1 + commit 275464c** — 세션 71 next-dev-prompt §S72-A 추천 직행. R2 토큰 발급(MCP Chrome 봇 차단으로 사용자 직접 진행) → V1 옵션 A 코드 src/ 적용(r2.ts + 라우트 2개) + Prisma 마이그레이션 직접 적용(`20260501100000_add_file_storage_type` 1/1, 3 row 모두 'local' 검증) + npm install(@aws-sdk/client-s3@3.1040 + s3-request-presigner) + WSL 빌드+배포+PM2 재시작(pid 187964) → **PoC 6/6 PASS** (presigned avg **1.8ms** 목표 <50ms 28× 마진 / 1MB+100MB PUT 100% / fetch PUT 200) → ADR-032/spike-032 PROPOSED→ACCEPTED 동일 세션 승격 → **단일 commit `275464c`** 18 파일 +6180/-3037. **함정 발견**: `wsl-build-deploy.sh` [1/8] rsync에 `--exclude '/.env'` 부재 → windows측 .env가 build측 .env를 덮어 R2 키 사라짐 (1차 PoC 실패 원인). [5/8]은 ypserver측 보호 있음 — 비대칭 정책. **메모리 룰 등록** `feedback_env_propagation.md` (3계층 .env 동기화: windows + build + ypserver). **CK +1**: `2026-05-01-wsl-build-deploy-env-not-protected.md` (workaround/high). **터치 안 함**: 다운로드 라우트(별도 PR), UI 50MB 분기, E2E 테스트, 24h cleanup cron, 다른 무관 미커밋 영역. **이월 (S73+)**: S73-A R2 후속(다운로드 + UI + CORS 실측, ~6h) / S73-B wsl-build-deploy 패치(~10분) / S73-C cleanup cron / S73-D 모니터링. |
| 71 | [260501-session71-r2-spike-adr-032.md](./260501-session71-r2-spike-adr-032.md) | **S71-A 파일박스 large-file R2 hybrid spike-032 + ADR-032 PROPOSED + V1 사전 코드 6 파일(.txt) + SP-013/016 측정 사양 강화 + 베이스라인 검증 메모리 룰** — 4단 게이트(Cloudflare 100MB / MAX_FILE_SIZE 50MB / quota 500MB / formData OOM) 해소 옵션 매트릭스 5개 비교 → R2 hybrid 옵션 B 채택 (V1=A 단일 PUT 16h / V2=B multipart 3개월 진화). R2 egress $0 + 10GB 무료 = 운영자 사실상 무료. local+R2 50MB 경계. **V1 사전 코드 6 파일(.txt) `spike-032-prepared-code/`**: migration.sql / r2-client.ts(S3Client + presign + tenants/{tenantId}/users/ key builder) / route-r2-presigned.ts (Zod + quota 사전) / route-r2-confirm.ts (HEAD + prefix + size ±10%) / env / deps — ACCEPTED 직후 5분 cp 이동, fallback 시 rm 폐기. **트랙 B 두 건 outdated 함정 회피**: B-1 Phase 15 = 이미 s32-34 완료(commit 58a517b, prod), B-2 baas-foundation Phase 3 = M3 게이트 미통과. 다른 터미널 Claude 정적 분석으로 차단 → 영구 취소. supabase-parity Wave5 roadmap(2026-04-18) status 갱신 부재 + 베이스라인 우선 검증 누락이 원인. **메모리 룰 신규**: `feedback_baseline_check_before_swarm.md` — kdyswarm 발사 전 current.md/next-dev-prompt/최근 handover/실제 코드 4개 사전 점검 강제. **D 트랙**: ADR 보완 4건 모두 이미 세션 30(2026-04-19) 완료 → _SPIKE_CLEARANCE 표기만 갱신. 본 세션 코드 변경 0(docs/+memory만), 영역 분리 OK(다른 터미널 트랙 A src/+prisma/ 진행 중과 충돌 0). **이월** (S72+): 트랙 A ACCEPTED 후속, S71-B docx 비교, S71-C LibreOffice, SP-013/016 실측. |
| 70 | [260501-session70-boot-manual-filebox-diag.md](./260501-session70-boot-manual-filebox-diag.md) | **부팅/종료 매뉴얼 전면 개정 + docx 재생성(v1 인라인 양식 baked-in) + 파일박스 1.4GB 전송 진단** — (1) `docs/guides/server-boot-manual.md` 전체 재작성 — `dashboard→ypserver`, standalone 모드, `wsl-build-deploy.sh`, 종료 시나리오 A~E 신규 추가. (2) docx 변환 — sudo 없이 standalone pandoc 3.1.13 (~/.local/bin/pandoc) + WSL 한글 변수 함정 회피 패턴 / v1 양식 분석 5차 반복 끝에 결정적 발견: v1 양식은 styles.xml 이 아니라 document.xml 인라인 오버라이드 (Heading1=#1A73E8 + 하단 테두리, Heading2=#333333, 코드=#1B5E20, 표 테두리=#CCCCCC, 헤더=#F0F4FA) → pandoc은 인라인 못 만들므로 styles.xml 에 baked-in 해야 함. `scripts/build-pandoc-ref-from-v1.py` 작성 + `_pandoc-ref-v1plus.docx` 생성. (3) 파일박스 1.4GB 전송 진단 — 4단 게이트 모두 차단(Cloudflare 100MB / 50MB MAX_FILE_SIZE / 500MB quota / formData OOM), 코드 변경 0, LAN 직접 권고. **터치 안 함**: 파일박스 코드, Almanac aggregator, 메신저. **이월**(S71+): 파일박스 large-file ADR + spike, docx 사용자 검증 후 추가 패치. |

### 2026-04-26

| 세션 | 파일 | 주요 작업 |
|------|------|-----------|
| 69 | [260426-session69-aggregator-day2.md](./260426-session69-aggregator-day2.md) | **Track A 완성 — Almanac aggregator 잔여 4 endpoint + srv_almanac_* 키 발급 + withAuth fix + 운영 배포** — 세션 66의 미완(Day 4 잔여 4개) 마무리. **(1) 4 endpoint 신설** (각 spec 변환 패턴 동일: `withApiKey([…], { allowAnonymous:true })` → `withTenant`, `prisma` → `prismaWithTenant`): `contents/route.ts` (cursor pagination + 6 필터, 230줄), `sources/route.ts` (active=true + kind/country, 117줄), `today-top/route.ts` (ContentItemMetric 집계 + score 알고리즘, **spec의 `contentItemId` → schema 실 필드 `itemId` 정정**, 200줄), `items/[slug]/route.ts` (composite `tenantId_slug` compound key 활용 — spec의 글로벌 `findUnique({slug})` 보다 안전, 137줄). (2) **운영 스크립트 신설** `scripts/issue-tenant-api-key.ts` (140줄) — 운영 콘솔 UI 도입 전까지 임시 절차, 안전 게이트 3종(tenant.findUnique + status active + owner ADMIN). **srv_almanac_* 키 발급 완료** (prefix=`srv_almanac_4EJMXSLc`, scopes=read:contents/sources/categories/items/today-top, owner=kimdooo@). 평문은 본 세션 stdout 1회 노출 — handover/journal/commit 미기재. (3) **withAuth fix** (`src/lib/api-guard.ts` +24/-2): `pub_/srv_` Bearer 가 JWT 검증으로 빠지던 사전 결함 표면화 — placeholder payload 분기 신설(/api/v1/t/* scope 이외 거부 + K3 검증은 withTenant 가 담당). 명시 라우트 합류로 노출된 ADR-027 §4.2 의도 결합. (4) **WSL 빌드+배포 2회** (1차 endpoint, 2차 fix), PM2 ypserver restart #5→#6 (pid 86517→93211), ELF Linux x86-64 통과. (5) **검증 매트릭스 5/5 PASS + K3/scope 가드 2건**: tsc 0 / eslint 0 / vitest 372 pass + 33 skip / 정상 4개 200 / 404 NOT_FOUND / **K3 cross-tenant 차단** `/t/default/categories` w/ almanac key → 403 / **scope 가드** 글로벌 `/api/v1/api-keys` w/ tenant key → 401. (6) **회귀 시그니처 0건**. **다음 세션 액션**: Almanac Vercel 측 `ALMANAC_TENANT_KEY=srv_almanac_*` env 등록 + `NEXT_PUBLIC_AGGREGATOR_ENABLED=true` → Redeploy → /explore 가시화. **터치 안 함**: aggregator 비즈니스 로직(fetcher/dedupe/classify/promote ~28h) / cron AGGREGATOR 분기 + 6종 등록 / 소스 점진 활성화 / 관리자 UI 4페이지 / Track B(messenger M2-Step1) 미커밋 영역 — 영역 분리 유지. |
| 68 | [260426-session68-messenger-m2-plan.md](./260426-session68-messenger-m2-plan.md) | **메신저 M1 점검 (9/9 PASS) + M2 정밀화 산출물 (655줄, 코드 변경 0)** — 다른 터미널 미커밋 작업 (Almanac aggregator) 진행 중 영역 분리 보장하며 *읽기 전용 점검 + 문서 작성만* 으로 한정. **M1 점검 9/9 PASS**: 9 model + 6 enum + 6 마이그 + RLS 9 테이블 정책 + UNIQUE 5종 + `MessageAttachment.fileId → files(id) ON DELETE RESTRICT` raw SQL ALTER + partial idx + GIN trgm + RLS 단위 테스트 13 it (env-gated) 모두 spec 정확 정합. **M2 핵심 결정 7건**: (1) **라우트 prefix `/api/v1/t/[tenant]/messenger/...` (Phase 1부터)** — Almanac 검증 패턴 답습 + ESLint rule 자동 강제 + Phase 2 plugin 분리 비용 0. (2) 도메인 헬퍼 4개 = 비즈니스 룰 SoT (`src/lib/messenger/{conversations,messages,blocks,reports}.ts`). (3) clientGeneratedId 멱등 = pre-lookup + race UNIQUE catch. (4) `withTenantRole` 채택 (운영자 패널 4종만). (5) vitest + 실제 DB (RLS env 재사용). (6) Audit 5종 발화 머지 게이트. (7) rate-limit 기존 헬퍼 재사용. **산출물 `docs/research/messenger/m2-detailed-plan.md` 655줄** 11 섹션 (M1 점검 / M2 결정 7건 / 도메인 헬퍼 4개 정밀 시그니처 / Zod 스키마 9개 / 라우트 19개 정확 명세 / 통합 테스트 / 일정 분할 M2-Step1/2/3 = 예상 세션 69~71 / 영역 분리 매트릭스 / 함정 5건 / 결정 근거 10건). `_index.md` 진행 상태 + 다음 액션 갱신. **검증**: 코드 변경 0, M1 점검은 spec 정합성 매트릭스 재확인 (S67 5/5 PASS 결과 답습). **터치 안 함**: Almanac 미커밋 영역 (42 modified + 3 untracked) 모두 보존. 6 lock agent worktree. **이월** (S69+): Track A (Almanac 잔여 4 endpoint + API 키 발급, `next-dev-prompt.md` P0) 또는 Track B (M2-Step1 도메인 헬퍼 4개) — 영역 분리로 병렬 가능. |
| 67 | [260426-session67-messenger-m1-data-model.md](./260426-session67-messenger-m1-data-model.md) | **메신저 Phase 1 M1 — 데이터 모델 + RLS + 6 마이그 (~4-5h, 1 commit 예정)** — 세션 64 ADR-030 ACCEPTED + PRD 18섹션 후속의 첫 코드 산출. 사용자 "다음 작업 진행" 자율 채택으로 2 단계 순차 수행. **(1) M0.3 kdyspike #1 (30분 마이크로)** — PG NOTIFY + SSE 정합성 PoC. milestones.md M0.3 정의 그대로 (NOTIFY 8KB / Cloudflare 100초 idle / 다중 채널 LISTEN connection 누수) 3 web search 기반 답안 도출. **메타-only NOTIFY (~200B)** + **30초 keepalive + last-event-id 재연결** + **단일 listener + tenant 단위 channel grouping** 결정. **Phase 2 진입 시 POC 5건 명문화** (NOTIFY→SSE p95<50ms / listener 24h 안정성 / PM2 cluster LISTEN fork 분리 / queue 80% 알람 / PgBouncer session pool). **ADR-031 의도적 보류** (milestones 명시대로 입력 자료 보존, spike §8에 골자 사전 작성). 산출 `docs/research/spikes/spike-006-pg-notify-sse.md` (10 섹션) + `_SPIKE_CLEARANCE.md` 1행. **Conditional Go**. **(2) M1 (W1) 데이터 모델 (압축 4-5h)** — milestones 정의 그대로 prisma **9 model + 6 enum + 9 User backref** (data-model.md §2.1-2.9 정확 복사). **6 마이그 작성·Claude 직접 deploy 적용** (`20260501000000~050000_messenger_phase1_*`): #1 enums / #2 conversations + members / #3 messages + 부속 3 + 보류 FK / #4 safety 3 / #5 partial idx + GIN trgm / #6 RLS+grants+self-check. 일관 패턴: `gen_random_uuid()` id / `COALESCE(current_setting('app.tenant_id', true), default)` tenant_id (20260428100000 fix 패턴, missing_ok 보장) / `now()` timestamp / 마이그 #6 `RAISE EXCEPTION` self-check (RLS 9+정책 9 미충족 시 자동 rollback). **3 trap 발견·즉시 정정**: (a) NotificationPreference 1:1 관계 P1012 → `userId @unique` 추가 (복합 PK만으로 부족, MfaEnrollment 동일 패턴), (b) WSL bash `npx` UNC 변환 실패 → Windows 측 직접 호출(`/mnt/c/Program Files/nodejs/npx` resolve 회피), (c) adminPool `any` 타입에서 `query<T>` generic 인자 미허용 → `as Row[]` 캐스트. **RLS 단위 테스트** `tests/messenger/rls.test.ts` 신설 (cross-tenant-leak.test.ts 패턴 답습, env-gated 13 case: M1 SELECT / M2 UPDATE / M3 INSERT WITH CHECK / M4 미설정 0 row / M5×9 모델 cross-tenant leak 0). **검증 5/5 PASS**: prisma validate ✓ / migrate status up to date(28 마이그) ✓ / generate ✓ / tsc 0 errors ✓ / vitest 13 skipped(env-gated) ✓ / WSL psql RLS 9/9 `t,t` ✓ / 마이그 #6 self-check 통과. **머지 게이트 정합** (M1 정의): 6 마이그 적용+RLS 검증 ✓ / RLS 단위 테스트 ✓ (env-gated skip) / tsc 0 ✓ / eslint M2 시점 게이트(현재 src/lib/messenger 미존재). **CK +1** (48→49): `2026-04-26-prisma-1to1-with-composite-pk.md` (pattern/high — Prisma 1:1은 FK 단일 unique 강제 + multi-tenant에서 글로벌 @unique 정당화 + 두 unique 인덱스는 의도적). **터치 안 함**: PushSubscription/Notification(M6 보류) / Phase 2 reactions+bookmarks / src/lib/messenger 디렉토리(M2) / 19 API 라우트(M2) / 운영 배포(코드 없음, 데이터 layer만 production DB 적용). **이월** (S68+): M2 (W2) API CRUD + 멱등성 + 권한 5-6 작업일 (도메인 헬퍼 4 + Zod + 라우트 19 + 통합 테스트 7 + audit 5종) → M3 SSE 실시간 → ... → M6 출시. T2.5 packages/tenant-almanac 별도 트랙. |
| 65 | [260426-session65-deploy-filebox-standalone.md](./260426-session65-deploy-filebox-standalone.md) | **옵션 A+B 후속 — 운영 배포 + filebox/PG16 일괄 fix + standalone 정합성 (~3h, 6 commits, 50 파일, 마이그레이션 8건 적용, CK +2 → 48)** — 세션 63 옵션 A+B 통합 commit `0d910e8` 후속. 사용자 인가로 마이그레이션+빌드+PM2 reload 직접 수행. **운영 적용 8건** + Drizzle 3건. **2건 P3018 + fix** (`f0d4443`): folders DB drift IF EXISTS 보강 + PG 16 `REVOKE BYPASSRLS` → `ALTER ROLE NOBYPASSRLS` 정정. **role 32바이트 ALTER**, **Almanac 통합 가이드 10 섹션** (`docs/handover/260426-almanac-tenant-integration.md`). **filebox 3-track 디버깅**: MIME markdown(`e61a496`) → 빈 문자열 fallback(`3e6a366`) → **dbgenerated default 결함 COALESCE fix**(`f8ef8a7`, 21 테이블 일괄 ALTER). **CLAUDE.md 정정** (`138edbe`) — `~/ypserver/`가 실제 PM2 실행 위치 명시 + 세션 52 NFT 결정 근거. **standalone/ leftover 134.6MB 정리** (5 부속 파일은 시나리오 C용 보존). **CK +2**: PG 16 BYPASSRLS 키워드 함정 + Prisma dbgenerated current_setting+COALESCE 패턴. **검증**: ESLint 0, TSC 0, Vitest 364, Build OK, 운영 헬스체크 ROOT 307/ALMANAC 308/TENANT 401/신규 에러 0, 사용자 filebox 업로드 정상 확인. |
| 62 | [260426-session62-t14-sweep-p1-followups.md](./260426-session62-t14-sweep-p1-followups.md) | **T1.4-sweep + P1 통합 부채 정리 — kdyswarm parallel 7 commits (~30min)** — 사용자 "다음 권장 진입 점 수행" 자율 채택. **3 agent 워크트리 격리 동시 발사** (sonnet × 3, 파일 소유권 0% 중첩으로 머지 충돌 0): **P1-keys-tenant-include `04ee7cb`**: keys-tenant.ts 2-query → `include: { tenant: true }` 단일 query (T1.5 relation 활용) + TODO 제거 + VerifyResult 6종 reason 코드 100% 보존, DB round-trip 50% 감소. **P1-resolve-tenant `c365597 + ced644d`**: with-request-context.ts.resolveTenantId() stub → path 정규식(`/api/v1/t/[slug]/...`) 추출 → `resolveTenantFromSlug(slug)` 호출. **자율 결정**: suspended/archived tenant 도 `tenant.id` 반환 (관측성 vs 인가 분리, withTenant가 410 책임). 9 신규 테스트 케이스. **T1.4-sweep `c1283a4 + 191ad47 + f753c4f`**: 4 호출 사이트 (login/register/settings·users) `findUnique({ tenantId_email: { tenantId: 'default', email } })` composite 전환 + VaultService SecretItem Tenant-bypass 주석 / prisma/schema.prisma User.email/EdgeFunction.name/CronJob.name @unique 제거 + deviation 주석 정리 + 마이그레이션 `20260427130000_phase1_4_sweep_drop_global_unique` (DROP CONSTRAINT IF EXISTS × 3) / ESLint warn → error 승격 + @typescript-eslint/{parser,eslint-plugin} 설치. EdgeFunction/CronJob name-기반 호출 사이트 0건 확인. **통합 단계 sentinel 보정 `99f02ca`**: agent 주석에서 sentinel 표기 `'_system'` → `'default'` 1줄 수정 (memory `project_tenant_default_sentinel.md` invariant 정합). agent는 컨텍스트 분리로 메모리 미접근 정상 — 메인이 `verification-before-completion` 원칙으로 후처리. **검증 4/5 PASS**: prisma validate ✓ / tsc 0 ✓ / vitest **364/364 PASS** (totp pre-existing 통합 후 자동 해소) / build ✓. **ESLint --max-warnings 0 DEFERRED** — 기존 raw prisma 직접 호출 126건이 sweep 범위 밖, severity error 승격으로 신규 PR 차단 목적 달성. **의사결정 D-1~D-7**: parallel 전략 / 3 agent sonnet / suspended tenant.id 반환 / sentinel 단독 보정 커밋 / DEFERRED 수용 / 운영자 작업 1건 (마이그레이션 deploy + DROP CONSTRAINT 검증). **CK +0** (46 유지) — 본 세션 패턴은 기존 CK(`project_tenant_default_sentinel.md` + `dispatching-parallel-agents` 원칙)에 흡수, 신규 솔루션 가치 부족. **터치하지 않은 영역**: T1.6 Almanac backfill (10h, P0 다음 진입점) / raw-prisma-sweep 126건 (4~8h, 별도 sweep) / ApiKey K3 dbKey.tenant=null 가드 테스트 (30분, P2) / Almanac spec 적용 / 03:00 KST cron / ADR cascade 6위치 / S54·53 6항. **이월** (S63): T1.6 Almanac backfill → M2 게이트 (`/api/v1/t/almanac/health` 200 + audit_logs.tenant_id NULL 0) → raw-prisma-sweep 126 → Phase 2 Plugin 시스템 (T2.1~2.6 ~100h, M3 게이트). |
| 61 | [260426-session61-t1.4-rls-p0-membership.md](./260426-session61-t1.4-rls-p0-membership.md) | **T1.4 RLS Stage 3 + P0-membership 통합 — kdyswarm sequential 완주 (~2h, 8 commits)** — 사용자 "다음작업 수행" + 세션 60 권장 진입점 `/kdyswarm --tasks T1.4,P0-membership --strategy sequential` 채택. **T1.4 agent (opus, worktree, 22분, 5 commits, 9 파일 +1159/-44)**: schema 17 모델 Stage 3 (NOT NULL + dbgenerated `(current_setting('app.tenant_id'))::uuid`) + migration 251줄 (15 테이블 backfill + SET NOT NULL + SET DEFAULT + ENABLE/FORCE RLS + tenant_isolation 정책 + composite unique + cron_jobs/api_keys FK SetNull→Cascade + app_migration BYPASSRLS / app_runtime / app_admin NOLOGIN) + Prisma Client Extension 165줄 + ESLint custom rule 167줄 (severity warn 롤아웃) + RLS e2e 스캐폴드 225줄 (env-gated) + api-guard-tenant re-export 22줄. **agent 자율 이탈 6건 수용**: User.email/EdgeFunction.name/CronJob.name 글로벌 @unique 유지+composite 추가 / audit_logs RLS 제외 (SQLite Drizzle) / WebAuthnChallenge nullable / JwksKey/SecretItem nullable / ESLint warn / app_admin NOLOGIN+grant. **통합 보강 `db76253`**: (1) `node_modules/@prisma/client/` 빈 디렉토리 발견 (pnpm-workspace 호이스팅 잔재) → `npm install` 1회 복구 / (2) prismaWithTenant 모듈-load 시점 $extends 호출 → vi.mock("@/lib/prisma") 환경에서 mock $extends 부재 → lazy Proxy 전환 (기존 prisma.ts 패턴) / (3) vitest.config.ts `@yangpyeon/core/*` wildcard alias 누락 → array+regex 형태로 추가. **P0-membership inline `6af4154`** (30분, 4 파일 +168/-19): TenantMembership Tenant-bypass 모델 (UUID + tenant_id FK CASCADE + user_id FK CASCADE + role TEXT default 'MEMBER' + (tenant_id, user_id) composite unique + Tenant.memberships / User.tenantMemberships backref) + migration (CREATE TABLE + indexes + 'default' 활성 사용자 OWNER 시드 ON CONFLICT DO NOTHING) + membership.ts 본문 교체 (stub null → prisma.tenantMembership.findUnique + role narrow + fail-closed null 보존). **결정**: TenantMembership = Tenant-bypass (멤버십 조회는 tenant context 결정 *전* — RLS USING 절 self-defeating, (tenant, user) composite filter로 cross-tenant 안전). **검증 5/5 PASS**: prisma validate ✓ / tsc 0 ✓ / packages/core tsc 0 ✓ / vitest **355/355 PASS** (15 skipped) ✓ / npm run build ✓ Compiled successfully. **CK +2** (44→**46**): `2026-04-26-prisma-client-empty-pnpm-workspace.md` (bug-fix/medium) + `2026-04-26-prisma-client-extension-lazy-proxy-test.md` (pattern/high). **터치하지 않은 영역**: keys-tenant.ts 2-query → include 단일 query (30분) / with-request-context.ts.resolveTenantId() wiring (1h) / Almanac spec 적용 / 03:00 KST cron / ADR cascade 6위치 / 글로벌 스킬 drift / S54·53 6항. **이월** (S62): 통합 부채 잔여 2건 (~1.5h) → T1.6 Almanac backfill (10h) → M2 게이트 → Phase 2 Plugin 시스템 (~100h). **운영자 작업**: prisma migrate deploy × 2 + role 패스워드 placeholder 교체 + DATABASE_URL `app_runtime` 전환 + tenant_memberships OWNER 시드 검증 |
| 60 | [260426-session60-g1b-parallel-merge.md](./260426-session60-g1b-parallel-merge.md) | **G1b 4 agent 병렬 발사 + DAG 통합 — Phase 1 본진 일괄 완료 (~3h, 9 commits, 285→355 tests +24%)** — 사용자 "다음 작업 진행" + handover의 `/kdyswarm --tasks T1.2,T1.3,T1.5,T1.7 --strategy parallel` 권고. **kdyswarm 7 Phase 완주**: Phase 0 컨텍스트 → Phase 1 가이드라인 정렬(impl-spec 4종 1M 적재) → Phase 2 task 분해 + 소유권 맵 → Phase 2.5 Pre-Flight(기추적 0, lock 없음) → Phase 4 4 agent worktree 격리 + run_in_background 동시 발사 → Phase 5 DAG 순서 머지 → Phase 6 검증. **schema 단독 소유권**: T1.5만 prisma/schema.prisma(TenantCronPolicy 신규 + Tenant relation + circuit breaker 4 cols), T1.7만 drizzle src/lib/db/schema.ts(audit_logs.trace_id + tenant_metrics_history) → 머지 충돌 0. **stub 인터페이스 분리**: T1.2가 keys-tenant.stub.ts(47줄, INVALID_FORMAT 항상 반환) 자체 생성 + 통합 시 일괄 교체. **A1 (T1.2 router)** `cb7e298` 11신규 895줄 + 9 vitest. **A2 (T1.3 K3)** `6645f28` 3신규 614줄 + 13 vitest(7 시나리오 매트릭스). **A3 (T1.5 WorkerPool)** `2bc35da` 13신규/6수정 1,351줄 + 29 vitest(packages/core/src/cron pure logic 16 + src/lib/cron 13). **A4 (T1.7 audit-metrics)** `3714073` 7신규/9수정 988줄 + 28 vitest. **DAG 머지 순서**: T1.5 → T1.7 → T1.3 → T1.2 (4 merge --no-ff). **통합 commit** `6c9f631`: stub 삭제 + import 교체(@/lib/auth/keys-tenant.stub → /keys-tenant) + tsconfig standalone/** exclude(WSL pack 복사본의 stale registry.ts 차단). **검증**: tsc 0 / packages/core tsc 0 / vitest **355/355 PASS** (27 파일, 1.31초, +70 신규) / npm run build Compiled successfully(/api/v1/t/[tenant]/[...path] ƒ 등록) / prisma validate PASS / ADR-021 invariant 11 콜사이트 변경 0 검증. **agent 자율 의사결정 3건**: bcrypt를 revokedAt보다 먼저(timing 보호) / sha256 BIGINT advisory lock 전략(충돌 ~10⁻¹⁵) / sentinel 'default' 보존(impl-spec '_system' 정정, T0.4 invariant 우선 → memory +1 `project_tenant_default_sentinel.md`). **터치하지 않은 영역**: TenantMembership prisma 모델 부재 → membership.ts fail-closed(cookie 경로 항상 403, P0 다음 세션 3-4h) / keys-tenant.ts 2-query → include 단일 query 통합 가능(30분) / with-request-context resolveTenantId() stub(1h) / 4 worktree branch 정리(시스템 자동 또는 수동). **이월** (S61+): TenantMembership 모델 + migration + wiring(P0 4h) → keys-tenant.ts 단일 query 통합 → resolveTenantId wiring → T1.4 RLS 정책 단일 'default' tenant(18h, T0.3+T1.2+T1.3+T1.5 머지 후 가능) → T1.6 Almanac backfill(10h) → M2 게이트 → Phase 2 Plugin 시스템(T2.1~2.6 ~100h, M3 게이트 = 2번째 컨슈머 코드 0줄) / Almanac spec 적용 / 03:00 KST cron / ADR placeholder cascade 6위치 / 글로벌 스킬 drift / S54·53 6항 |
| 59 | [260426-session59-phase0-foundation.md](./260426-session59-phase0-foundation.md) | **Phase 0 Foundation 7 task 완료 + M1 게이트 통과 (~3h, 7 commits)** — 사용자 "phase 0 부터 모두 순차적 진행" 지시. **진입점** `ea4d89c` 세션 58 BaaS Foundation 31파일 17,315 insertions. **T0.1** `619a952` spike-baas-002 부수 fix 3건 (runner.ts:21 env override / runner.ts:72 AbortController + WEBHOOK_FETCH_TIMEOUT_MS / registry.ts:135 silent failure → safeAudit). **T0.5** `66dd62e` Almanac 마이그레이션 계획 노트 247줄. **T0.2** `d24ea37` 모노레포 incremental — pnpm-workspace.yaml + turbo.json + packages/core/ 골격 (4 파일 187줄). 핵심 결정: src/ → apps/web/ 이동 보류 (`pack-standalone.sh` 강결합), 워크스페이스 인프라만 추가. **T0.3** `89ea7e4` Tenant Prisma 모델 (UUID PK + slug TEXT) + 18 모델 tenantId nullable + Stage 1 마이그레이션 (default 시드 고정 UUID `00000000-...000`) 146줄. **T0.4** `5a06c96` ADR-021 §amendment-2 — audit_logs/metrics_history/ip_whitelist tenant_id slug TEXT + 시그니처 불변 + Phase 1.7 자동 주입 보류 (171줄). 결정: PG-SQLite tenant_id 의도적 분리 (UUID vs slug, dashboard 가독성), MAX_BUCKETS=200 cap 유지. **M1 게이트 fix** `d65cfce` tsconfig docs/assets/** exclude — 세션 57 spec 미적용 코드 typecheck 끼임 사전 존재 이슈 회피. **M1 검증 통과**: npm run build ✓ Compiled successfully + 모든 라우트 / prisma validate ✓ valid / prisma generate ✓ 7.7.0 / tsc src/ 0 에러 / vitest audit-metrics 9/9 PASS. **남은 검증** (배포 환경): prisma migrate status / PM2 standalone 헬스 200. **CK +1** (42→**43**): `2026-04-26-incremental-monorepo-staged-extraction.md` (pattern/high). **터치하지 않은 영역**: 사용자가 cs 시점 packages/core/src/tenant/{context,index}.ts + index.ts export + tsconfig types + vitest.config.ts 변경 (Phase 1.1 시작) — 본 cs commit 미반영, 별도 PR. **이월** (S60+): T1.1 검증/머지 + T1.2 withTenant + router (16h) + T1.3 ApiKey K3 (12h, T1.2 병렬) + T1.5 TenantWorkerPool (30h, sketch -8h) + T1.4 RLS 정책 (18h, T0.3+T1.2+T1.3 머지 후) / Almanac spec 적용 / 03:00 KST cron / ADR cascade 6위치 / 글로벌 스킬 drift / S54·53 6항 |
| 58 | [260426-session58-baas-foundation.md](./260426-session58-baas-foundation.md) | **BaaS Foundation 멀티테넌트 BaaS 전환 설계 (~5h, 단일 세션 풀 사이클)** — 사용자 "10~20개 프로젝트 백엔드로 사용, 근본 설계 방향" 요구로 ADR-001 (Multi-tenancy 의도적 제외)의 재검토 트리거 1+3 발동. **4-Round 자율 흐름**: R1 (2 Explore — Wave 1~5 + 7 spike + ADR-001~021 통합 감사 + 코드 ~30 파일 단일테넌트 가정 매핑) → R2 (8 general-purpose 병렬 — ADR-022~029 초안 4,432줄) → R3 (2 spike — Prisma schema-per-tenant + Worker pool, 1,143줄) → R4 (8 sub-agent + 4 직접 편집 — ADR ACCEPTED 적용 + CLAUDE.md 4섹션 갱신 + status/handover/next-dev-prompt). **8 ADR ACCEPTED**: ADR-022 (옵션 A closed multi-tenant) / **ADR-023 옵션 B shared+RLS — spike-baas-001로 권고 변경** (Prisma 7.6도 동적 schema-per-tenant 미지원 + SET search_path silent 충돌 + max_connections 압박 + Almanac plugin 본질적 충돌 + 공식 권장 = RLS) / ADR-024 (옵션 D hybrid plugin) / ADR-025 (옵션 A 단일 인스턴스 Phase 1~3) / ADR-026 (옵션 C TS+DB hybrid manifest) / ADR-027 (옵션 A `/api/v1/t/<tenant>/*` + K3 prefix+FK+검증) / ADR-028 (옵션 D worker_threads → pg-boss) / ADR-029 (M1+L1+T3 SQLite → Phase 4 OTel). **CLAUDE.md 4 섹션 갱신**: 프로젝트 정보 정체성 재정의 + 문서 체계 트리 baas-foundation/ + 핵심 원칙 7원칙 신설 + 운영 규칙 5개. **kdywave 호출 (압축형 4 sub-wave)**: Wave 1~3은 supabase-parity Wave 1~5 + 8 ADR ACCEPTED로 흡수, Wave 4+5만 4 sub-wave (A 5-Plane Overview + 8 ADR 구현 specs 9 agent / B Sprint plan 1 agent / C Migration 1 agent / D Validation 1 agent) × 12 sub-agent 병렬 발사 → **15 파일, 9,761줄 산출**. 5-Plane 4 불변 인터페이스 (withTenant/withTenantTx/dispatchTenantJob/computeEffectiveConfig), 크리티컬 패스 178h(병렬 156h), 5 Stage 마이그레이션, N=20 한계 주 25h+자동화 5종 100% 필수, retrofit ADR 5건 (ADR-001/005/015/021/018), 누적 공수 ~1,237~1,290h (50~70주). **spike-baas-002 부수 발견 3건 즉시 fix 트리거**: runner.ts:21 DEFAULT_ALLOWED_FETCH 정책화 / runner.ts:72 WEBHOOK fetch AbortController + 60s timeout / registry.ts:135 runJob catch structured log (CK-38 패턴). **Almanac spec 처리**: v1.0 그대로 출시 → 출시 후 packages/tenant-almanac/로 마이그레이션 (~5~7일, 충돌 회피). **충돌 회피 검증**: 모든 산출물이 `docs/research/baas-foundation/` 새 디렉토리 + 4개 기존 메타 파일(CLAUDE.md/current.md/handover/next-dev-prompt) 갱신만 — `spec/aggregator-fixes` 브랜치(다른 터미널)의 src/, prisma/, docs/assets/yangpyeon-aggregator-spec/ 무수정. baas-foundation 누적 31 파일, 16,826줄. **CK +1** (41→**42**): `2026-04-26-compressed-kdywave-on-existing-wave.md` (pattern/high — 기존 Wave 산출물 위 압축형 kdywave 4-sub-wave 패턴 / ADR 재검토 트리거 자동 감지 / sub-agent 컨텍스트 파일 우선 패턴 / 4-Round 자율 흐름). **이월** (S59+): Phase 0 즉시 진입 — spike-baas-002 fix 3건 + 모노레포 변환 (pnpm + turborepo) + Tenant Prisma 모델 + ADR-021 amendment-2 / Almanac spec 적용 (S57 이월) / 03:00 KST cron 검증 (S56 이월) / ADR-021 placeholder cascade 6위치 (S56 이월) / S54·53 잔존 6항 |
| 57 | [260426-session57-aggregator-spec-rewrite.md](./260426-session57-aggregator-spec-rewrite.md) | **yangpyeon-aggregator-spec v1.0 → v1.1 정합화 (~3h)** — `docs/assets/yangpyeon-aggregator-spec/` v1.0(2026-04-25 작성) 검토에서 P0 결함(`suggestedCategoryId` vs `suggestedCategorySlug`) 1건 발견 → Approach B(`feat/aggregator-v1`) 적용 시도 시 **81 컴파일 에러 / 21 파일 영향** 폭발. 4 카테고리 분류: (A) 스펙 자체 스키마와 코드 불일치 ~30 (`nameKo` vs `name`, staging에 없는 필드 `qualityFlag`/`reviewedById`/`track`/`thumbnailUrl`/`externalUrl` 사용, `'published'` 미존재 enum, ContentSource.id Int 미인지, ContentSourceKind 소문자) / (B) Prisma 경로 ~5 (`@prisma/client` → `@/generated/prisma/client`) / (C) yangpyeon 어댑터 ~12 (`session.userId` → `session.sub`, `AuditEntry.actor` 미존재, `extractClientIp(req)` → `(headers)`, `ApiKey.expiresAt` 미존재, `Button asChild` 미지원) / (D) 환경 셋업 누락 ~24 (npm 3 + shadcn 9, Step 1에서 예고). 평가: 스펙 v1.0이 type-check 한 번도 안 거친 초안. **백아웃 + 스펙 재작성** (경로 2) 사용자 결정 → R-1 백아웃 (브랜치 삭제 + 추적 4파일 git checkout + 4 디렉터리 rm + prisma generate) → R-2 `spec/aggregator-fixes` 브랜치 → R-3 yangpyeon 7 계약 사실 확인 → R-4~R-7 18 파일 일괄 정합화. **핵심 결정**: (1) 큐레이션 4필드 ContentIngestedItem 추가 (qualityFlag/reviewedById/reviewedAt/reviewNote, FK 비참조), (2) ContentIngestStatus에 'promoted' 추가, (3) asChild 회피 (Link with className / controlled Dialog), (4) ContentSourceKind 대문자 강제, (5) ID 타입 정정 (Source Number / Item·Category String cuid), (6) seed-aggregator.ts → `import { prisma } from "@/lib/prisma"` lazy proxy, (7) promote.ts excerpt/track/publishedAt 필수 폴백 + ContentItem 비참조 필드 제거, (8) AuditEntry shape 정합 + `extractClientIp(request.headers)`, (9) api-guard-publishable expiresAt 로직 제거. **R-8 검증** (scratch 적용 → tsc → 백아웃 패턴): src/ 18 에러 모두 TS2307 missing module — 9 shadcn + 3 npm + 0 외 = **셋업 외 잔존 에러 0개**. 즉 spec 자체는 즉시 적용 가능 상태. README/02-applying-the-patch에 v1.1 changelog + Step 1 분할 (1-1 npm 3 / 1-2 shadcn 9 / 1-3 Prisma 경로) 명시. **CK +1** (40→**41**): `2026-04-26-spec-typecheck-driven-rewrite.md` (pattern/high — 외부 스펙 type-check 검증 의무 / scratch 적용 → tsc → 백아웃 표준 패턴 / yangpyeon 5 컨벤션 명시 / 셋업 갭 vs 실제 결함 분리 / spec schema 보강 vs 코드 다이어트 결정). 코드 변경 0 (spec 자산만 수정, `feat/aggregator-v1`은 백아웃되어 main 영향 0). 본 세션 산출물은 `spec/aggregator-fixes` 브랜치 단일 커밋으로 흡수. **이월** (S58+): spec 적용 세션 — 02-applying-the-patch 그대로 따라가면 0 에러 보장 / S56 이월(03:00 KST cron 검증 + ADR-021 placeholder cascade) / S55·56 이월(다른 글로벌 스킬 audit drift) / S54·53 잔존 6항 |

### 2026-04-25

| 세션 | 파일 | 주요 작업 |
|------|------|-----------|
| 56 | [260425-session56-audit-fail-soft-migration-self-heal.md](./260425-session56-audit-fail-soft-migration-self-heal.md) | **로그인 장애 4층 결함 근본 해결 — ADR-021 Audit fail-soft + 마이그레이션 self-heal (~2.5h)** — 세션 55 정식 배포 직후 12:52 `SqliteError: no such table: audit_logs` 로 인한 로그인 500 → systematic-debugging 4층 분해. **L1 전술**(빈 SQLite DB) / **L2 프로세스**(빌드 게이트 부재 + `pack-standalone.sh` drizzle 마이그레이션 미동봉) / **L3 아키텍처**(`writeAuditLogDb` 동기 throw + login-finalizer:54 미격리) / **L4 메타**(세션 54 silent-failure 패턴이 cleanup-scheduler 1곳에만 적용, 11 도메인 콜사이트 누락). **Step 1 운영 DB 즉시 복구**: drizzle 마이그레이션 직접 적용 → 4 테이블 생성, WAL 모드라 PM2 재시작 불필요. **Step 2 safeAudit chokepoint + 11 콜사이트 sweep**: `safeAudit(entry, context?)` 도입(`writeAuditLogDb` `@internal`) + login-finalizer / 9 v1·admin 라우트 / cleanup-scheduler 일괄 변경(세션 54 수동 try/catch 제거 + context 인자로 진단성 보존). **Step 3 빌드 게이트 + 운영 self-heal 이중 안전망**: 신규 `src/lib/db/migrate.ts` (TS, instrumentation) + `scripts/run-migrations.cjs` + `scripts/verify-schema.cjs` (CJS, 빌드 게이트). `pack-standalone.sh` 가 `<bundle>/db-migrations/` 동봉. `wsl-build-deploy.sh` 6→8 단계 확장 ([6/8] migrate / [7/8] verify, 실패 시 PM2 reload 차단). `instrumentation.ts` 부팅 self-heal (best-effort, warn-only). 탐색 우선순위 — env override > db-migrations/ > src/lib/db/migrations/. **Step 4 ADR-021 정식화**: `docs/research/decisions/ADR-021-audit-cross-cutting-fail-soft.md` 9-필드 포맷, §2.4 Postgres 통합 보류 + §5 트리거 3건 명시 / `next-dev-prompt.md` "세션 56 신규" 블록 / **CK +1** (39→**40**): `2026-04-25-audit-fail-soft-and-migration-self-heal.md` (pattern/high). **빌드 게이트 자체 검증**: `wsl-build-deploy.sh` 1회 실행 → [6/8] `skip 0000_old_maverick (already applied)` 멱등 ✓ / [7/8] `OK — required tables present` ✓ / [8/8] PM2 ↺=4→5 online + ELF Linux 양쪽 ✓. **검증**: tsc 0 errors / vitest 13/13 PASS / 12:52:52 이후 신규 SqliteError 0건. **세션 번호 충돌 정정**: 세션 55 가 이미 점유 (12:47 ypserver 스킬 v2 + 운영 재배포)였음 발견 → ADR-021/CK/next-dev-prompt 모두 "세션 55"→"세션 56" 일괄 정정 후 정식 /cs. **이월** (S57+): 04-26 03:00 KST cron 첫 정상 실행 검증 / audit-failure 카운터 메트릭 / 다른 글로벌 스킬 drift 점검 / S54·S53 잔존 6항 |
| 55 | [260425-session55-ypserver-skill-v2-deploy.md](./260425-session55-ypserver-skill-v2-deploy.md) | **ypserver 스킬 v2 리팩터 + 세션 54 패치 운영 반영 (~45min)** — 세션 54 cleanup-scheduler.ts 패치 운영 반영 시도 중 사용자 직감 인터럽트("지난 세션에서 wsl 관련 문제로...")로 **글로벌 ypserver 스킬이 운영보다 6일 뒤처짐** 발견. v1 가정(`~/dashboard` / `pm2 start npm -- start` / Windows `next build`)은 세션 50→52 운영 진화(`~/ypserver` / standalone / `wsl-build-deploy.sh`)와 분기 — v1 실행 시 포트 3000 충돌 + 듀얼 디플로이 + ELF 회귀 위험. **스킬 v2 전면 리팩터** (`~/.claude/skills/ypserver/SKILL.md`): 경로/PM2명 일괄 갱신 / Phase 1 Windows next build **삭제**(NFT cross-platform 함정 근본 부적합) / Phase 2 수기 6단계 → `wsl-build-deploy.sh` 단일 호출 위임 / `pm2 start npm` → `pm2 start ecosystem.config.cjs` / 인수 신설 `--migrate`/`--quick` / 회귀 시그니처(`ERR_DLOPEN_FAILED`/`PrismaClientInitializationError`/`EADDRINUSE`) / §4 PM2 safeguard 보존 / 변경 이력 섹션. **갱신된 스킬로 정식 배포**: better-sqlite3 gyp 실패→prebuild fallback(설계대로 689 packages 59초) / @node-rs/argon2 + Prisma client 재생성 / NFT 자가치유 멱등 / ELF 검증 BuildID `7fdd4a4a...` ✓ / PM2 PID 179097→183166 ↺=4. **헬스체크**: HTTP 307 × 3회(101/14/12ms 워밍업 곡선) / ERR_DLOPEN_FAILED 신규 0건 / Next.js 16.2.3 Ready 0ms / cleanup-scheduler.ts diff 0(패치 정확 반영) / cloudflared 6일 uptime. **부가 발견**: PM2 로그 04-21~25 매일 03:00 audit failed 5일 연속 — **세션 54 패치가 04-26 03:00 KST cron에서 자기 자신을 검증** 예정. **CK +1** (38→**39**): `2026-04-25-skill-ops-drift-pattern.md` (pattern/high — 글로벌 스킬 vs 운영 진화 분기 / 사용자 직감 트리거 / 갱신-우선 배포 순서 / drift 진단 절차). 코드 변경 0건. **이월** (S56+): 04-26 03:00 audit 결과 / 다른 글로벌 스킬 audit / S54 잔존 6항 |
| 54 | [260425-session54-audit-silent-failure-diagnostic.md](./260425-session54-audit-silent-failure-diagnostic.md) | **audit log silent failure 진단 패치 + 블로킹 사유 정리 (~30min, 자율)** — S53 §보완 후속. `src/lib/cleanup-scheduler.ts` 두 catch 블록(line 89 `runSessionsCleanupWithAudit` / line 151 `writeCleanupAudit`)이 `catch {}` (err 바인딩 없음) → silent failure 정의 그 자체. **격리 의도 유지 + err 구조화 노출** 패치(`{ message, stack }`), 한 줄 변경으로 가설 검증 비용 24h → 즉시. **다른 터미널 ELF 가설 부분 기각**: cleanup이 정수 카운트 정상 출력 후 audit만 실패 → ELF mismatch였다면 better-sqlite3 import 시점 폭발했어야 함. Prisma+PG와 drizzle+SQLite 하이브리드 구조에서 audit_logs만 SQLite 의존 → 진짜 silent failure는 audit 모듈 내부 가능성. **검증**: tsc 0 errors / vitest 13/13 PASS. **부 Turbopack 워닝 보류**(정확한 텍스트 미캡처, wsl-build-deploy.sh에 stderr `tee logs/build-*.log` 선행 필요). **블로킹 5건 사유 영구 기록**: C(DATABASE_URL rotation=파괴) / E(Windows 재부팅=물리 대기) / 6-E(`_test_session` DROP=파괴) / 6-F(MFA biometric=디바이스) / 6-G(SP-013·016=인프라/용량). **CK +1** (37→**38**): `2026-04-25-cleanup-scheduler-audit-silent-failure.md` (pattern/high — `catch {}` 1bit vs `catch (err)+structured log` 다차원 진단성 비대칭 / 격리 패턴 표준 형식 / silent-failure-hunter 적용 영역 / 검증 비용 비대칭 ROI). **단일 커밋**: S53 §보완 산출물 + S54 마감 함께 흡수 (커밋 메시지에 양쪽 명시). **이월** (S55+): 2026-04-26 03:00 KST audit 재발 검증(prod 패치 반영 빌드 선행) / wsl-build-deploy.sh 빌드 로그 캡처 / S54 우선순위 1~2 6항 |
| 53 | [260425-session53-priority-0-2-cascade.md](./260425-session53-priority-0-2-cascade.md) | **S51 이월 일괄 처리 + 우선순위 0-2 자율 실행 (~1.5h)** — 사용자 "순차적으로 모두 진행" 지시. **Task 3 kdywave --feedback**: 24 banner 파일은 commit `de9c962`(병렬 세션)에서 이미 처리 → idempotent no-op. **진짜 신규 가치 = ADR placeholder cascade 5건 정정**: ADR-020 슬롯 점유로 §5 예상 후보를 한 칸씩 밀기 (ADR-021 마이그레이션 롤백 슬롯 유지 / ADR-022 Prisma 8 / ADR-023 Next.js 17 / ADR-024 Capacitor) + §3.1 다음 번호 021→**025** + §Z DQ 매핑 정정 + 16-ux-quality-blueprint §1570-1571 + 02-final-summary 충돌 경고. **Task 4 _CHECKPOINT_KDYWAVE.md §보완 신설**: B-01~B-03 변경 이력 + 누적 변경 요약 표 (Wave 5 본문 무손상). **Task 5 S50 이월**: WSL `pm2 install pm2-logrotate@3.0.0` (10MB/30일/일일 압축/`pm2 save` 영속), postgresql.service `enabled` + `pg_isready 5432 OK` 확인 (부팅 자동 기동 보장). **우선순위 2 자동 처리 4건**: KEK 일치 퍼즐(psql secret_items 1 row + decrypt 에러 0건 → 이미 해결 확인) / MASTER_KEY_PATH 단일 출처(`DEFAULT_MASTER_KEY_PATH` 상수 + `resolveMasterKeyPath()` 헬퍼) / rotateKek 단위 테스트 4건(32-byte 가드 / 전체 row migration / round-trip 신키 OK 구키 reject / 0건 no-op) / SecretItem @@index 중복 제거(schema.prisma 수정 + 신규 마이그레이션 `20260425000000_drop_redundant_secret_items_name_idx`). **부가 발견**: 매일 03:00 "audit log write failed" = better-sqlite3 ELF mismatch 잔재, commit 9a37dfb에서 이미 수정, WSL 재배포 시 해소. **KST 03:00 cleanup tick은 04-19~04-25 6일 연속 정상** → 우선순위 2 "24h+ 관찰" 충족. **검증**: 268/268 vitest PASS / tsc 0 errors / 10 files + 1 새 마이그레이션 / +161/-21. **이월** (S54+, 환경/생체 의존만): `_test_session` drop(파괴적) / MFA biometric QA / SP-013·016 물리 측정 / WSL 재배포 회복 모니터링 / Windows 재부팅 자동 복구 |
| 52 | [260425-session52-wsl-build-pipeline.md](./260425-session52-wsl-build-pipeline.md) | **WSL 빌드 파이프라인 + NFT 네이티브 모듈 정합성 보장 (~2h)** — 세션 50 standalone 재도입 직후부터 로그인 시 `ERR_DLOPEN_FAILED / invalid ELF header` 회귀 근본 해결. `file(1)`로 `.next/node_modules/better-sqlite3-*/build/Release/better_sqlite3.node`가 `PE32+ for MS Windows`임을 확증 — Windows 빌드 NFT가 해시 디렉토리에 Windows DLL을 번들해 Linux dlopen 실패. **L2(build-on-target) + L1(self-heal) defense-in-depth**: **L2** `scripts/wsl-build-deploy.sh` 신규 — Windows 소스 → WSL 네이티브(`~/dev/ypserver-build/` ext4) → `npm ci` → `next build` → `pack-standalone.sh` → rsync 배포 → PM2 재시작 단일 명령, rsync `--exclude` leading `/` 앵커링. **L1** `standalone/install-native-linux.sh`에 `sync_native_to_nft()` 블록 + `-ef` inode 체크(NFT hardlink 대응, L2 happy path에서 no-op). **pack-standalone.sh 3 버그 수정**: helper 5종 save/restore + `--exclude='/standalone/'` nested 차단 + `mkdir -p "$DEST"` 멱등성. **검증**: WSL build 77 pages + 107+ routes, `better_sqlite3.node` = ELF 64-bit Linux BuildID=7fdd4a4a... ✓, PM2 ↺=2 online, 로그인 라우트 403 CSRF(핸들러 도달), 재배포 이후 ELF 에러 **0건**, nested 잔재 **0건**. **S51 이월 3 파일 의도 본 세션 산출물로 확정**. **CK +1** (35→**36**): `2026-04-25-nft-native-binary-platform-mismatch.md` (bug-fix/high — 빌드환경=실행환경 / serverExternalPackages 한계 / rsync 앵커링 / --delete-excluded vs find preserve 충돌 / L1+L2 defense-in-depth). **커밋 2건 push**: 9a37dfb(deploy 3 파일 206/-12) + de9c962(supabase-parity docs 27 파일 +54 누적 편집). **이월** (S53+): `~/ypserver/data/` SQLite 조사 + cleanup-scheduler "audit log write failed" 원인 / 브라우저 E2E CSRF 풀 플로우 / DATABASE_URL 패스워드 rotation / S51 이월 전수 |
| 51 | [260425-session51-kdywave-eval-adr020.md](./260425-session51-kdywave-eval-adr020.md) | **kdywave 문서 이행도 평가(A- 85/100) + ADR-020 신설 + 로드맵 재보정 + Git 태그 3건 소급 (~3h, 자율 실행)** — 사용자 "kdywave 문서 이행도 평가" 요청에 3중 Wave 레이어(platform-evolution-S/supabase-M/2026-04-supabase-parity-L) 진행 vs 실구현 6차원 평가 → 종합 **A- 85/100**. 이행 강점(역방향 피드백 0건/Phase 15 1주 압축) + 결함 3건 식별 (R1 ADR-015 vs 실구현 불일치 / R2 standalone 700-패키지 인플레이션 / R3 계획외 timestamptz 캐스케이드 5세션). 사용자 "즉시 권장 조치 순차적 모두 진행" → "모두 진행" 2차 지시로 **6 작업 자율 완료**. **Task 1 ADR-020 신설**: ADR-019 점유 발견 → 020으로 변경, 5 편집 (§0.4 표/ADR-015 §세션 50 보완 4 재진입 트리거 정량화/ADR-020 신규 본문 4 후속 P0/§3.1 다음 번호 021/05-rollout-strategy.md 헤더 부분 대체 통지). **Task 2 Git 태그 소급 3건**: v0.1.0-alpha.0(b4287b0=세션31)·alpha.1(9d2a61e=세션39 Phase 15 완료)·alpha.2(c4794d4=세션48 Phase 16a Vault) 모두 정확 SHA에 생성 + 원격 push. **Task 3 로드맵 공수 재보정**: §1.2 핵심 수치 870h→916h·50주→25-54주 / §8.1 25% 상향 / §8.1.1 신설 (계획외 +46h 표 + 실측 속도 50h+/주 + 시나리오 A 53-54주 vs B 25주 병기 + 측정 지표 3건) / §13 위험 신호 2건 (하향 이탈 -50%/계획외 15%) / §14.3 거버넌스 (Phase 완료 git 태그 + 자동 측정). **Task 6 kdywave --feedback 부분 전파**: 41 영향 파일 중 5 핵심에 cross-reference (README row 14·01-adr-log §2 그래프·05-operations-blueprint·04-integration/02-cloudflare-deployment). Wave 1 deep-dive 73문서와 _archived/는 역사 보존 미수정. **커밋 2건 + push**: 7a45c7b(ADR/로드맵 3 파일) + 7d68b00(피드백 4 파일) → main 정상. **CK +1** (34→**35**): `2026-04-25-adr-numbering-and-partial-supersession.md` (pattern/high — 신규 ADR 등록 3 위치 검색 + Deprecated/Superseded 전체/부분 3 모드 + 보완 블록 표준 형식). 코드 변경 0건 (docs 한정). **사용자 진행 중 3 파일 보존** (M pack-standalone.sh / M install-native-linux.sh / ?? wsl-build-deploy.sh — 본 대화 외 사용자 작업, 다음 세션 의도 확인 필요). **이월** (S52+): 사용자 작업 3건 의도 확인 / Windows 재부팅 자동복구 실증 (S50 이슈 #2) / kdywave --feedback 정식 모드 (36 잔여 파일 일괄) / _CHECKPOINT_KDYWAVE.md §보완 행 / S50 이월 (pm2-logrotate/postgresql systemd) / S49 이월 (KEK 퍼즐/MASTER_KEY_PATH 통일 등) |

### 2026-04-19

| 세션 | 파일 | 주요 작업 |
|------|------|-----------|
| 50 | [260419-session50-standalone-package.md](./260419-session50-standalone-package.md) | **Next.js standalone 실행 패키지 + WSL 프로덕션 배포 전환 (~2h, 자율 실행, 세션 3 결정 역전)** — `output:"standalone"` 재도입 (세션 3 제거 결정이 Next.js 표준 동작에 대한 운용법 오해임을 공식 문서+실증으로 반증 후 역전). `scripts/pack-standalone.sh` 신규 (5단계 패키징 + NFT 보수성 대응 시크릿 2중 방어 `rm .env/data/scripts`). `standalone/` 기동 헬퍼 5종 (start.sh · ecosystem.config.cjs fork · install-native-linux.sh · .env.production.example · README.md). Windows 스모크 HTTP 200 `/login` in 48ms. **WSL 실측 배포**: rsync → install-native-linux.sh (binding.gyp 부재 → fallback 700 패키지 흡수, NFT 설계 위배이나 기능 정상) → `pm2 delete dashboard` (이전 full 프로젝트) → `pm2 start ecosystem.config.cjs` → `ypserver` id=6 online 189.7MB 재시작 0. **검증**: HTTP/1.1 200 OK + 보안 헤더 6종 / Next.js 16.2.3 Ready in 0ms / BUILD_ID(`WYBvMpH13rjEvlzoA_mm1`) 교차검증 확정. **CK +1** (33→**34**): `nextjs-standalone-output-misunderstanding.md`. **장기 운영 이월 4건**: Windows 재부팅 자동 복구 / PostgreSQL systemd / pm2-logrotate / 디스크 모니터. **Phase 16b Capistrano 필요성 재평가** (standalone+rsync+pm2 reload 실증 후). 코드 변경 0 (본 세션 기능 코드 미변경), 인프라 파일 9건 추가. |
| 49 | [260419-session49-s49b-plan-expansion-phase16a-deploy.md](./260419-session49-s49b-plan-expansion-phase16a-deploy.md) | **S49 플랜 outline→풀 디테일 확장 + Phase 16a 프로덕션 배포 완결 (16b 구현 미실행)** — writing-plans 스킬로 §세션 49 Outline(58줄) → 풀 디테일 848줄 (6 Task × 5-6 Step + bash 전문 + 10 파일 매트릭스 + Gotchas 9종). 총 997→**1,787줄**. 드리프트 방지 3원칙 내재 (SP-018 600ms gap → post-swap 5-retry exponential 커버). subagent-driven-development 스킬 진입 시 **실측으로 진입 조건 미충족 발견** (secrets.env MISSING / ~/dashboard flat / Phase 16a prod 미완료) → 경로 A (우선순위 0 선행) 채택. Step1 사용자 sudo `/etc/luckystyle4u/secrets.env` 0640 root:smart + MASTER_KEY 64hex / Step2 /ypserver prod Phase 2 전구간 (prisma migrate `No pending` / npm install / next build / Drizzle / PM2 restart --update-env ↺=19 pid=52616 / HTTP 307) / `~/dashboard/.env.production` `MASTER_KEY_PATH` append / Step3 migrate-env-to-vault `{"skip":"mfa.master_key","reason":"already migrated"}` (dev/prod 동일 인스턴스 반영) / Step4 phase16-vault-verify `=== PASS ===` (login accessToken_len=260 + mfa_status Vault decrypt 성공). Task 49-1 서브에이전트 디스패치 직전 사용자 `/cs` interrupt — **16b Capistrano 실구현 0건**. **알려진 이슈 6**: ①KEK 일치 퍼즐 (새 MASTER_KEY 로 S48 SecretItem decrypt 성공 — fallback 경로 조사 필요) · ②MASTER_KEY_PATH 이중 상태 (`/etc/luckystyle4u/` vs S48 `/home/smart/.luckystyle4u/`) · ③병렬 세션 unstaged 3건 · ④SecretItem @@index 중복 (이월) · ⑤`_test_session` drift (이월) · ⑥Turbopack warning (사전존재). **커밋 0**: 본 세션 산출물은 plan 확장 1건만 unstaged — 다음 세션에서 handover/logs 와 병합 커밋 예정. **이월** (S50+): KEK 퍼즐 해소 → Task 49-1 서브에이전트 디스패치 → live cut-over → Task 49-2~49-6 순차 |
| 48 | [260419-session48-phase16a-vault.md](./260419-session48-phase16a-vault.md) | **Phase 16a Vault 구현 + 프로덕션 배포 완결 (~1.5h, 플랜 8h 대비 대폭 단축)** — Task 48-1~6 + S49 선행 배포 + verify 스크립트 Bearer 픽스까지 동일 세션 내 전구간 완결. Vault decrypt E2E 실측 `{"pass":true,"decrypted_len":64,"env_len":64}`. — executing-plans 스킬 순차 실행, main 직접. **Task 48-1~48-6 전부 완결 + 4-gate PASS**. SecretItem Prisma 모델(@map snake_case 보정 + @db.Timestamptz(3)) + migration `20260422000000_add_secret_item` (drift 회피 위해 수동 SQL + `db execute` + `migrate resolve --applied` 3단계 우회). MasterKeyLoader (4 tests: ENOENT/권한0644/정상 0640/64hex) + VaultService (6 tests: encrypt row / round-trip / tamper / not-found / IV 유일성 100 / 32-byte 가드) + getVault() 싱글톤 + migrate-env-to-vault.ts + phase16-vault-verify.sh. **mfa/crypto.ts sync→async 전환**: getMasterKey() → getMfaMasterKey() (Vault decrypt + cachedKey 프로세스 싱글톤), encryptSecret/decryptSecret Promise<string> 로 변경 → 4 call site 5곳 await 전환 (mfa/service.ts:53, enroll/route.ts:26, confirm/route.ts:46, totp.test.ts 3 test) + `vi.mock("@/lib/vault")` 테스트 격리. **검증**: tsc 0 / **vitest 264 PASS** (S47 기준 254 → +10, 회귀 0) / npm run build exit 0 (Turbopack 4 warnings = 사전존재 instrumentation.ts Edge runtime, 무관). **커밋 6건**: 1637fe8(48-1 병렬 /cs 흡수) / 9c0fbf8 (48-2) / 907df2e (48-3) / 5812f94 (48-4) / fb63a55 (48-5) / 9942ca4 (48-6). **플랜 편차 6건 보정**: @map snake_case / `@/lib/db`→`@/lib/prisma` / @@index 중복 유지 / Prisma 7 mock 캐스팅 / migrate diff shadow DB 우회 / drift `_test_session` 회피. **드리프트 방지 3원칙 내재화 완료**: ①SP-017/018/019 GO ②Timestamptz(3) ③회귀 가드 스크립트. **이월**: 프로덕션 배포(secrets.env 생성 + migrate-env-to-vault + verify) / S49 Capistrano Outline → 풀 디테일 확장 / rotateKek 단위 테스트 보강 / SecretItem @@index 중복 정리 / `_test_session` drop / MFA biometric QA / SP-013·016 / KST tick |
| 47 | [260419-session47-phase16-spikes.md](./260419-session47-phase16-spikes.md) | **Phase 16 사전 스파이크 3건 PASS (SP-017/018/019) (~1.5h, S47 예상 6h 대비 단축, 자율 실행)** — 세션 46 플랜의 S47 병렬 스파이크 구간 inline 실행. **SP-017 Vault crypto GO**: IV 1M 샘플 충돌 0, GCM tamper throw 확인, 100 DEK rotation **1.18ms** (<500ms 목표의 1/400). `VaultService.rotateKek` 단일 트랜잭션 for-loop 구현 확정. **SP-018 symlink + PM2 reload GO (16b) / 16c 정당화**: `ln -sfn` 1000 reads × 100 swaps fails=0 (OS 수준 원자성 확증, version_a 496 / version_b 504 분산으로 swap 발생 실증). 현재 fork 모드 `pm2 reload dashboard` 내부 239ms + 관측 다운타임 **~600ms (3 timeouts × 200ms)** → 단일 프로세스 shutdown→listen gap 불가피 → Phase 16c cluster:4 전환 정량적 근거 강화. **SP-019 PM2 cluster + better-sqlite3 Conditional GO**: 4 worker × 5ms × 25s = 19,465 insert 중 `SQLITE_BUSY` 0건 (WAL + busy_timeout 5000ms 조합 완벽), 4 worker 가 각자 `globalThis.__spike019Scheduler` 독립 init → **scheduler 류 fork × 1 분리 필수 확정** (세션 50 Task 50-1 3-앱 구조 유지), PM2 v6.0.14 `pm2 delete <name>` 이름 기반은 spike019 namespace 만 제거 `default` 완벽 보호 (세션 30 사고는 이름 기반 아닐 가능성). **Clearance Registry** 17→**20 엔트리** (SP-017 GO / SP-018 GO / SP-019 Conditional GO). **커밋 3건**: `408c782` SP-017 / `3e2d225` SP-018 / `959c487` 세션 46 마감 자료 + SP-019 아티팩트 병합 (병렬 프로세스 흡수). 코드 변경 0, tsc/vitest 회귀 0 (스파이크는 런타임 코드 미변경). **드리프트 방지 3원칙**: ① 사전 스파이크 PASS 로 Clearance 확보 / ② `@db.Timestamptz(3)` 은 S48 Task 48-1 `SecretItem` 모델에서 첫 적용 / ③ 회귀 가드 curl 은 S48 Task 48-6 에서 첫 도입. **S48 Phase 16a Vault 즉시 진입 가능** (플랜 풀 디테일 준비 완료, 8h TDD 20+ tests). **이월**: S48~S52 플랜 실행 / MFA biometric / SP-013·016 측정 / KST tick (uptime 24h+ 조건) |
| 46 | [260419-session46-phase-16-design-plan.md](./260419-session46-phase-16-design-plan.md) | **Phase 16 설계 + 구현 플랜 완결 (브레인스토밍 + 플랜 체인, ~3h, 자율 실행)** — 세션 45 이월 6건 매트릭스 평가. KST tick PM2 uptime 0.44h 조기 이월 / kdygenesis Phase 16 §9 흡수. **brainstorming**: feedback_autonomy 메모리로 "one question at a time" 건너뜀 + Wave 5 `05-roadmap/01-milestones-wbs.md §4` 자기추론. Wave 40h → 세션 33 JWKS core 회수로 **35h 보정** + 16-B → "16e JWKS Vault 통합 (3h)" 재분류. 5 sub-phase (16a Vault 8h / 16b Capistrano 10h / 16c PM2+Canary 10h / 16d UI 4h / 16e JWKS 3h). 드리프트 방지 3원칙 내재 (SP-017/018/019 선행 / `@db.Timestamptz(3)` 강제 / 회귀 가드 필수). 산출물 `docs/superpowers/specs/2026-04-19-phase-16-design.md` **425줄** (ADR-020 초안). **writing-plans**: 점진적 계획 원칙 채택 (S47/S48 풀 디테일 591줄 + S49~S52 outline 406줄). 산출물 `docs/superpowers/plans/2026-04-19-phase-16-plan.md` **997줄**. 코드 변경 0 / tsc 0 / 회귀 0. 커밋 2건 (`089374a` + `92c4b9e`). **CK +1** (32→**33**): progressive-large-scale-plan-just-in-time (pattern/high). **이월**: S47 스파이크 3건 즉시 진입 가능 / MFA biometric / SP-013·016 / KST tick uptime 조건 |
| 45 | [260419-session45-hs256-removal.md](./260419-session45-hs256-removal.md) | **HS256 legacy 제거 + 이월 6건 현실성 분류 (~40min, 자율 실행)** — 사용자 "모든 이월 세션 모두 직접 실행" 요청에 대해 세션 44 이월 6건 AI 단독 실행 가능성 분류. **실행**(1건): HS256. **부분 수행**(1건): cleanup tick baseline. **불가**(4건): MFA biometric/Phase 16 24h/SP-013·016/kdygenesis. **상태 스냅샷**: dashboard uptime 21분(↺=15), JWKS 활성 키 1개(kid=`047353dc...` ES256 CURRENT), 활성 v1 sessions 8건, **WSL2 `.env` `AUTH_SECRET` 이미 미설정** → HS256 fallback 이미 dead code 확인 → 리스크 0 pure cleanup 으로 축소 (원래 1.5h 단독 세션 예측 → 40분). **코드**: `src/lib/auth.ts` `getLegacySecret()` + HS256 분기 삭제 (81→44 라인, -44%) + `src/lib/auth/signing.ts` 전체 삭제 (45 라인, grep import 0건) + `.env.example` AUTH_SECRET → forensic marker 주석. 신규 `scripts/session45-active-sessions.cjs`(진단 재사용 자산 37 라인). **검증**: tsc 0 / vitest **254 PASS**(회귀 0) / `/ypserver prod --skip-win-build` (PM2 ↺=16, Tunnel 9h online) / **E2E**: `POST /api/auth/login` → dashboard_session JWT header = `{"alg":"ES256","kid":"047353dc..."}` (HS256 아님, JWKS CURRENT 일치) / AUTHED `/` HTTP 200 / AUTHED `/settings/users` HTTP 200 / NOAUTH `/` HTTP 307. **CK 신규 0**: AUTH_SECRET 이미 env 제거 상태에서의 pure refactor는 generalize 부족. **이월 (세션 46+)**: KST 03:00 tick (uptime 24h+ 조건) / MFA biometric / Phase 16 분해 / SP-013·016 / /kdygenesis |
| 44 | [260419-session44-zero-row-tables-date-fix.md](./260419-session44-zero-row-tables-date-fix.md) | **0-row 테이블 17 파일 Date 직렬화 선제 일괄 수정 + 공용 헬퍼 도입 (~1.5h, 자율 실행)** — 세션 43 이월 8건 중 본 세션 가능 3건 처리. **P3** (잔존 `::uuid` grep) 0건 즉시 종결. **P2** 공용 헬퍼 `src/lib/date-fields.ts` 신규 (ALLOWED_TABLES Set + COLUMN_RE 정규식 두 겹 화이트리스트 + `fetchDateFieldsText` + `toIsoOrNull`) + 단위 테스트 10건 (boundary + SQL injection + 정상). 12 API 라우트 수정 (cron×2 / webhooks×2 / api-keys×2 / log-drains×2 / functions×3 / mfa/status) — 각 파일 file-local `attachXDates`/`withXDates` 헬퍼 + 핸들러 1줄 호출. **수정 불필요 5 파일** (cron/[id]/run / webhooks/[id]/trigger / log-drains/[id]/test / functions/[id]/run / mfa/webauthn/authenticators/[id]) — Prisma write-only 후 Date 응답 없음 판별. **functions/route.ts nested runs 처리** — runs select 에 id 추가 + fnDateMap + runDateMap 두 헬퍼 호출. **mfa/status enrollment.id select 추가** — 헬퍼 호출 위해. **신규 E2E 스크립트** `scripts/session44-verify.sh` (지속 자산) — 5건 diff_ms=0 검증(webhook POST/GET-single/GET-list, cron POST, log-drain POST). **P1** PM2 cleanup baseline: 0 entries (uptime 67분, 정상, 익일 KST 03:00 자동 tick 발동 예정). **검증**: tsc 0 / vitest **244 → 254 PASS**(+10 헬퍼, 회귀 0) / `/ypserver prod --skip-win-build` (PM2 ↺=15, Tunnel 9h online) / 응답 createdAt === PG `to_char(... AT TIME ZONE 'UTC' ...)` 정확 일치. **CK 갱신 1건** (32 유지): `orm-date-filter-audit-sweep.md` 잔존 과제 §2 해소 + 세션 44 추가 섹션. **이월** (세션 45): KST 03:00 자동 tick 익일 / MFA biometric QA / HS256 legacy / Phase 16 / SP-013·016 / /kdygenesis |
| 43 | [260419-session43-users-date-fix.md](./260419-session43-users-date-fix.md) | **세션 42 이월 3건 순차 처리 (~1.5h, 자율 실행)** — P1(KST 03:00 자동 cleanup tick) 관찰만: uptime 111분 미달로 익일 이월. **P2 CK §2 진전**: `scripts/session43-parsing-repro.ts` 로 Prisma 7 adapter-pg parsing-side +9h 시프트 **실측 재현** (diff=32,400,147ms, 정확히 KST offset). DB 인벤토리상 users 테이블만 데이터 있음 → users 경로 4 API 파일 7 핸들러 수정 (패턴 B/C 확장): `/v1/members/[id]` GET+PUT · `/v1/members` GET · `/v1/auth/me` GET+PUT · `/settings/users` GET+POST+PATCH. 파생 버그 `::uuid` 캐스트 오남용(users.id 는 PG `text`) → 500 발생 → 5곳 `::uuid` 제거 및 `ANY(${ids}::text[])` 로 수정. **P3 CK §3 기각**: `@edge-runtime/cookies/index.js` 소스 추적 (`cookie.expires = new Date(Date.now() + maxAge*1e3).toUTCString()` UTC 정확) + 실측 (서버 Date + Set-Cookie Expires = 정확히 +7d UTC) → 9h 시프트 **재현 불가**, 가설 폐기. **검증**: tsc 0 / vitest 13 files 244 PASS (회귀 0, 2회) / /ypserver prod 2차 재배포 (PM2 ↺=13→14) / E2E 응답 `createdAt="2026-04-06T14:11:17.147Z"` === PG authoritative UTC 완벽 일치. **CK 갱신 2건** (32 유지): orm-date-filter-audit-sweep.md (§2 진전 섹션) + prisma-orm-tz-naive-filter-gotcha.md (§3 기각 표기). **신규 스크립트 2건**(지속 가치): session43-parsing-repro.ts + session43-verify.sh. **이월**: KST 03:00 자동 tick 익일 / 0-row 테이블 17 파일 Date 직렬화 / MFA biometric / HS256 legacy / Phase 16 / SP-013·016 / /kdygenesis |
| 42 | [260419-session42-insert-audit.md](./260419-session42-insert-audit.md) | **Prisma INSERT timestamptz 시프트 검증 + 선제적 방어 (~20min, /cs 자율 실행)** — 세션 41 CK 잔존 과제 §1 (`prisma-orm-tz-naive-filter-gotcha` 의 "INSERT-side binding 시프트" 가설) 실측 검증. 프로덕션 로그인 → PG 직접 SELECT (`EXTRACT(EPOCH FROM expires_at - created_at)`) 으로 **DB 저장 TTL = 604800 sec 정확** 확인 → **INSERT 시프트 없음** 확정, §1 해소. 부수 발견 (A) `prisma.session.create({select:{expiresAt}})` read-back 은 여전히 parsing-side +9h 시프트 → `issueSession`/`rotateSession` 의 select 에서 expiresAt 제거 + JS-side 원본 반환으로 **선제적 방어** (caller 영향 0, 향후 오용 차단). 부수 발견 (B) Set-Cookie `Expires=... GMT` 가 `Max-Age=604800` 로부터 계산될 때 +9h 시프트 — RFC 6265 §5.2.2 Max-Age 우선이라 cosmetic, 실 동작 영향 0 (§3 신규로 이월). **검증**: tsc 0 / vitest 13 files / **244 PASS** (세션 41 대비 회귀 0) / `/ypserver prod` rsync+build+PM2 restart ↺=12 / E2E 재실행 새 세션 `d1bdcc85` TTL=604800 정확. **CK +1 갱신** (32 유지): `prisma-orm-tz-naive-filter-gotcha.md` 에 "세션 42 추가 검증" 섹션 추가 (§1 해소 + 부수 발견 (A)(B) + 잔존 과제 재정렬 §1 해소/§2 유지/§3 신규). 커밋 `42eed04` 푸시 완료. **이월**: KST 03:00 자동 cleanup tick 관찰 / 기타 API route Date 직렬화 전수 리뷰 (§2) / Next.js 16 cookies Expires 근본 조사 (§3) / MFA biometric QA / HS256 legacy 제거 / Phase 16 진입 / SP-013·016 측정 / `/kdygenesis --from-wave` |
| 41 | [260419-session41-orm-date-audit.md](./260419-session41-orm-date-audit.md) | **Wave 충실이행도 평가 + ORM Date 비교 전수 감사 (~1h, 자율 실행)** — 세션 초반 Wave 1~5 (123 문서 / 106,588줄) 대비 실구현 **87/100** 6차원 평가 (기술 채택 98 / 스키마 95 / MVP 72 / ADR 95 / 스파이크 80 / 지식보존 100). 세션 40 권장 7건 중 **#1 (ORM 시간 비교 전수 검토)** 자율 선택. 프로젝트 `src/` 전수 스캔 → **4 파일 8곳** 취약 패턴 발견 (ORM WHERE 4 + JS-side 4). **3 가지 전환 패턴 설계 + 일괄 적용**: A=raw SELECT id + ORM deleteMany (cleanup) · B=전체 raw SELECT with `::text` (목록+display 9h 시프트 동시 해결) · C=보조 boolean 쿼리 (ORM join 유지). 수정 4 파일: `jwks/store.ts` (3 함수) / `mfa/webauthn.ts` (2 함수) / `mfa/service.ts` (verifyMfaSecondFactor + `locked_until::text` 응답 복원) / `sessions/tokens.ts` (findSessionByToken 보조 · listActiveSessions 전체 raw). **검증**: tsc 0 / vitest **244 PASS** (세션 40 대비 회귀 0) / 양 패턴 재스캔 0 match. **CK +1 신규** (31→**32**): `orm-date-filter-audit-sweep.md` (pattern/high, 패턴 A/B/C 가이드 + 재발 방지 체크리스트 + 잔존 과제 2종 인계). 커밋 `90ad952` 푸시 완료. **이월**: INSERT-side 시프트 검증 (신규 최상위) / KST 03:00 자동 tick 관찰 (익일) / MFA biometric QA / HS256 legacy 제거 / Phase 16 진입 / SP-013·016 측정 / `/kdygenesis --from-wave` |
| 40 | [260419-session40-timestamptz-migration.md](./260419-session40-timestamptz-migration.md) | **TIMESTAMPTZ 마이그레이션 + cleanup ORM 복원 시도 (~2h, /cs 자율 실행)** — 세션 39 권장 6건 중 2·3b·5 자율 채택, HS256(잠금)/MFA biometric/SP-013·016 측정/kdygenesis 제외. **17 모델 / 47 DateTime 컬럼**에 `@db.Timestamptz(3)` + `ALTER COLUMN ... TYPE TIMESTAMPTZ(3) USING ... AT TIME ZONE 'Asia/Seoul'` 적용. pg_dump 백업 (5.3MB) → BEGIN/ROLLBACK dry-run 17 ALTER OK → migrate deploy 적용. **cleanup.ts ORM 복원 1차 시도 → 실패** (Prisma 7 adapter-pg binding-side 9h 시프트가 timestamptz 컬럼에서도 별도 존재함이 E2E 재현으로 발견). **정공법**: raw SELECT (PG NOW()-INTERVAL '1 day' 위임) + `expires_at::text` 캐스팅 + ORM `deleteMany({where:{id:{in:ids}}})` 하이브리드. **검증**: tsc 0 / vitest **245→244 PASS** (-1, raw SQL assertion 일부 → ORM call signature) / `/ypserver prod --skip-win-build` 2회 통과(PM2 ↺=10/11) / E2E 12 step 전수 PASS (summary.sessions=2 정확). **CK +1 신규 1건 갱신** (30→31): `timestamp-to-timestamptz-migration-using-clause` (pattern, high) 신규 + `prisma-orm-tz-naive-filter-gotcha` 에 "TIMESTAMPTZ 후 binding-side 시프트 잔존" 세션 40 정정 섹션 추가. **이월**: HS256 (잠금) / MFA biometric / SP-013·016 측정 / kdygenesis / 다른 모듈 ORM 시간 비교 전수 검토 (신규 후속) |
| 39 | [260419-session39-session-expire-admin-revoke.md](./260419-session39-session-expire-admin-revoke.md) | **Phase 15-D 보강 — SESSION_EXPIRE per-row audit + 관리자 forced revoke (~1.5h, /cs 자율 실행)** — 세션 38 위임 5건 중 HS256/MFA biometric/SP013·016/kdygenesis 제외, 2·3a·3b 3건 채택. **TDD**: `cleanup.test.ts` 11 신규 + `tokens.test.ts` admin/logout reason 3 신규 (vitest 231 → **245 PASS**). **cleanup.ts** `$queryRaw + $executeRaw` 2-step 로 재설계 + `buildSessionExpireAuditDetail` 순수 함수. **cleanup-scheduler.ts** `runSessionsCleanupWithAudit` 래퍼에서 각 entry 별 `SESSION_EXPIRE` audit 기록. **tokens.ts** `revokeAllUserSessions(userId, reason?)` 확장. **신규** `DELETE /api/admin/users/[id]/sessions`. **프로덕션 E2E 중 버그 재현·근본 수정**: Prisma 7 adapter-pg + PG `TIMESTAMP(3)` timezone-naive → 9h KST 오프셋으로 ORM `findMany` filter 가 조용히 0 rows. 서버측 `NOW() - INTERVAL '1 day'` + `expires_at::text` 캐스팅으로 국소 회피 (세션 34 CK 재현 케이스). **검증**: tsc 0 / `/ypserver prod --skip-win-build` 통과 / E2E curl 12 step 전수 PASS (revokedCount 일치 · SESSION_EXPIRE audit 5건 · summary.sessions=4). **CK +2** (28→30): `prisma-orm-tz-naive-filter-gotcha` (bug-fix-pattern, high) / `per-row-audit-on-batch-delete` (pattern, high). **이월**: HS256 legacy 제거(단독 세션 권장) · TIMESTAMPTZ 마이그레이션 기술부채 신규 추가 |
| 38 | [260419-session38-phase-15d-touch-throttle-ua-label.md](./260419-session38-phase-15d-touch-throttle-ua-label.md) | **Phase 15-D 보강 — touch throttle + activity fingerprint (~1h, `/kdyguide` 자율 실행)** — 세션 37 위임 우선순위 3 중 **가장 작고 독립적이며 낮은 리스크** 2개 선정. 신규 `src/lib/sessions/activity.ts` pure function 2개 (shouldTouch 60초 디바운스 + parseUserAgent regex 기반 "Chrome 130 · Windows") + `activity.test.ts` 25 테스트(TDD, iOS Safari regex 1회 fail → 수정). `tokens.ts` SessionLookup lastUsedAt + ActiveSessionSummary userAgentLabel 필드 추가. `sessions/route.ts` 무조건 touch → shouldTouch 분기. `/account/security` UI 라벨 표시 + raw UA title 툴팁 보존. **검증**: tsc 0 / vitest **206→231 PASS**(+25, 회귀 0) / `/ypserver prod --skip-win-build` 통과(PM2 ↺=4) / **프로덕션 E2E curl 3 시나리오 전 PASS**(3초 동일·curl 8 label·70초 갱신) / **Playwright MCP 브라우저 자동화** — 로그인→/account/security→"Chrome 147 · Windows" 라벨+툴팁 확인+스크린샷. 커밋 `b454e3c` |
| 37 | [260419-session37-revoked-reason-intent-fix.md](./260419-session37-revoked-reason-intent-fix.md) | **세션 36 위임 ③⑤ 실행 + revoke 의도-혼동 버그 수정 (~2h)** — 위임 5건 중 ①MFA 브라우저/②KST 03:00/④SP-013/016 환경 제약 이월. ③Phase 15-D 보강(병렬 터미널 `ffb3e0f` 가 revokeAllExceptCurrent / POST /revoke-all / touchSessionLastUsed / UI 버튼 / +13 tests 기초 구현) + 본 세션 **E2E 자기파괴 버그 발견·수정**: A 가 revoke-all 후 B 의 stale /refresh 가 defense-in-depth 발동시켜 A 도 revoke. **수정**: `Session.revokedReason` String? + migration `20260419170000_add_session_revoked_reason` + 6경로 reason 태깅 + refresh route `isRotationReuse` 분기. vitest 201 → **206 PASS**. 프로덕션 E2E before/after: SESSION_REUSE_DETECTED → SESSION_REFRESH_REJECTED + A 생존 검증. **CK +3** (누적 24 → 27): login-finalize-helper-centralization / session-revoke-user-intent-vs-defense / revoked-reason-intent-tagging (functional-bug severity) |
| 36 | [260419-session36-phase-15d-refresh-rotation.md](./260419-session36-phase-15d-refresh-rotation.md) | **Phase 15-D Refresh Token Rotation + Cleanup 수동 실행 UI (세션 35 위임 4건 중 P3·P4·P2 완결)** — P1(MFA UI browser)/P5(SP-013/016)는 환경 제약으로 사용자 위임. **P4**: `src/lib/cleanup-scheduler.ts` 에 `CleanupActor` 인터페이스 + `runCleanupsNow(actor?)` 확장(하위 호환). `/api/admin/cleanup/run` POST 엔드포인트 + `(admin)/settings/cleanup` 페이지(4종 대상 설명 + 실행 버튼 + 결과 표). 사이드바 `Cleanup 실행` 엔트리. **P2 조기 검증**: 수동 트리거로 webauthn-challenges 1건 삭제 실측 + CLEANUP_EXECUTED_MANUAL audit 에 actor(userId/email/ip) 기록 확인. KST 03:00 자동 스케줄은 병행 유지. **P3 Phase 15-D**: opaque refresh token (32bytes hex) + SHA-256 hash + Prisma Session DB-backed rotation. 신규 `src/lib/sessions/tokens.ts`(196줄 issueSession/rotateSession/revokeAllUserSessions/listActiveSessions 등) + `login-finalizer.ts`(3개 로그인 종료 경로 공통화) + tokens.test.ts(+8 tests). API 3건 신설(`POST /api/v1/auth/refresh` reuse 탐지 + defense-in-depth / `GET /api/v1/auth/sessions` 목록 with current 플래그 / `DELETE /api/v1/auth/sessions/[id]` self-revoke with userId 매칭). `/api/v1/auth/logout` 서버측 Session revoke + audit. 로그인 3경로(login/challenge/assert-verify) finalizeLoginResponse 사용. `/account/security` 활성 세션 카드 3번째 + 사이드바 logout v1+dashboard 병행. `jwt-v1.ts` 미사용 export 정리(createRefreshToken/verifyRefreshToken/REFRESH_MAX_AGE 제거). 감사 이벤트 4종 신규(SESSION_LOGIN/ROTATE/REVOKE/REUSE_DETECTED). **검증**: tsc 0 / vitest **188→201 PASS**(+13, 회귀 0) / `/ypserver prod --skip-win-build` 통과(HTTP 307, ↺=1, Tunnel OK). **E2E curl 9 시나리오 전 PASS** — login/list/rotate/reuse 탐지/DELETE/logout/post-logout refresh/cleanup 수동/audit 확인 |
| 35 | [260419-session35-cleanup-scheduler-ck-batch.md](./260419-session35-cleanup-scheduler-ck-batch.md) | **세션 34 위임 4건 순차 처리 — Cleanup Scheduler + CK 4건 + MFA QA 가이드** — **우선순위 1**: `docs/guides/mfa-browser-manual-qa.md` 신규 8 시나리오 (WebAuthn 브라우저 인터랙션 필수라 자동화 불가 → 다음 세션 직접 실행 SOP). **우선순위 2**: `src/lib/cleanup-scheduler.ts` 신설 — 4종 cleanup(sessions/rate-limit-buckets/jwks-retired/webauthn-challenges) 매일 KST 03:00 + 1분 tick + dedupe + 독립 try/catch + audit `CLEANUP_EXECUTED`. `cron/registry.ts` 와 분리(UI CRUD vs 시스템 내부). `computeCleanupWindow` 순수 함수(UTC 연산). `runCleanupTasks` 테스트 13건 추가(**vitest 175→188 PASS**). `instrumentation.ts` 통합. `/ypserver prod --skip-win-build` 통과. **우선순위 3**: SP-013/016 Pending 유지 확인만. **우선순위 4**: CK 6건 중 세션 34 unstaged 2건 확인 + 신규 4건 Agent 2대 병렬 작성(otplib-v13 142줄 / simplewebauthn-v10 203줄 / mfa-challenge-token 183줄 / bcrypt-argon2-rehash 175줄, 총 703줄). **CK 누적 12→23건**. |
| 34 | [260419-session34-phase15-ui-and-mfa-status.md](./260419-session34-phase15-ui-and-mfa-status.md) | **Phase 15 UI 통합 (MFA 등록·해제 + 로그인 MFA 분기)** — 세션 33 백엔드(`58a517b`)에 이어 UI 부재만 마감. **신규 API 2건**: `GET /mfa/status`(TOTP+Passkey+recovery 단일 호출) + `DELETE /mfa/webauthn/authenticators/[id]`. **신규 통합 페이지** `/account/security`((admin) 그룹 밖, 모든 사용자) 3-stage TOTP stepper(idle→qr+secret+6자리→recovery 10코드+다운로드) + Passkey Card(목록+삭제+신규 with friendlyName) + TOTP Disable(비밀번호+코드). `@simplewebauthn/browser@^10` 설치. **로그인 페이지 MFA UI**: `MfaState` discriminated union + Method Tabs(passkey/totp/recovery) + 우선 method 자동 선택 + 6자리 자동 비숫자 제거 + Passkey `startAuthentication()` 통합. 사이드바 "내 계정" 그룹 신설(Lock 아이콘). **검증**: tsc 0 / vitest **175 PASS** 회귀 0 / `/ypserver` 4회 / curl smoke(307/401/200). **Step 6 라이브 디버깅 2건**: (1) proxy 인메모리 5 vs handler DB 10 충돌 → `HANDLER_OWNED_RATE_LIMIT_PATHS` Set 양도, (2) PG TIMESTAMP(3) timezone-naive + JS Date 9시간 오프셋(retry-after 32453s 발견) → `EXTRACT(EPOCH...)` PG 직접 계산(60s 정확). login 11회 차단(max=10) 정확. CK 후보 2건 식별 |
| 33 | [260419-session33-phase15-step3-4-5.md](./260419-session33-phase15-step3-4-5.md) | **Phase 15 Auth Advanced Step 3·4·5 (+ 타 터미널 Step 6 병합)** — next-dev-prompt 우선순위 1 연속 수행. **Step 3 JWKS**: Prisma `JwksKey`+`JwksStatus` + migration + `src/lib/jwks/{generate,store}.ts`(ES256 키쌍 + kid 16byte hex + CURRENT|RETIRED grace + rotateKey/cleanup) + `/api/.well-known/jwks.json`(Cache-Control 180s SWR 600s, MIME `application/jwk-set+json`) + `src/lib/auth.ts` ES256 + HS256 legacy fallback(kid 헤더 분기). **Step 4 TOTP MFA (FR-6.1)**: 3 모델(User.mfaEnabled/MfaEnrollment/MfaRecoveryCode) + migration + `otplib@12.0.1`(v13 Noble crypto plugin breaking 회피) + `qrcode` + `src/lib/mfa/{totp,crypto,challenge,service}.ts`(AES-256-GCM + 10 recovery codes XXXXX-XXXXX + timing-safe + 5분 challenge JWT purpose=mfa_challenge) + API 5건(`/api/v1/auth/mfa/{enroll,confirm,disable,challenge}`+admin reset) + login route MFA 분기. MFA_MASTER_KEY 64 hex 생성·WSL `.env` append. **프로덕션 E2E 6건 PASS**(enroll→confirm→mfaRequired→TOTP challenge→recovery challenge→재사용 거부). **Step 5 WebAuthn/Passkey (FR-6.2)**: 2 모델(WebAuthnAuthenticator+WebAuthnChallenge) + migration + `@simplewebauthn/server@10.0.1` + `src/lib/mfa/webauthn.ts`(7 exports: options/verify/persist/consume/cleanup) + API 4건(register-options/verify/assert-options/verify) + login route hasPasskey 분기. **E2E**: register-options 200 + DB challenge 기록(rp.id=stylelucky4u.com, challenge 43 chars). 브라우저 create/get() full round-trip 후속. **타 터미널 Step 6 병합**: `src/lib/mfa/lock-policy.ts`(computeLockedUntil + 7 tests) + `src/lib/rate-limit-guard.ts` + `rate-limit-db.ts`+test(12 PASS) + login/challenge/assert-verify route에 applyRateLimit 주입. **통합 결과**: tsc 0 / vitest 131→**175 PASS**(+44) / 3 migration 적용 / 3회 WSL2 배포·PM2 restart·Tunnel online |
| 32 | [260419-session32-phase15-step1-2.md](./260419-session32-phase15-step1-2.md) | **Phase 15 Auth Advanced Step 1-2 (Prisma Session + argon2id)** — Blueprint §7.2.2/§7.2.3 + SP-015/SP-011 결과 코드 반영. Step 1 Sessions 테이블(복합 인덱스 + cleanup.ts 인프라만) + Step 2 `@node-rs/argon2` + verifyPasswordHash `$2` prefix 분기 + login route 자동 재해시(lastLoginAt update 머지로 round-trip 0개). Vitest 131→139 PASS. E2E: `$2b$12$`→`$argon2id$v=` 프로덕션 자동 마이그레이션 실증 |
| 31 | [260419-session31-cleanup-safeguard-adr-reflect.md](./260419-session31-cleanup-safeguard-adr-reflect.md) | **로컬 정리 + /ypserver safeguard + 세션 30 결과 ADR/DQ 반영** — /kdyclean 로컬 아티팩트 ~11.4MB 정리 (cadb8ad `git commit --only`로 unrelated 스테이징 회피). **/ypserver §4 PM2 safeguard 신설** (78줄 5서브섹션: 금지→실증사고→대안→체크→복구). `/kdyskillaudit` **PASS** (FAIL 0/WARN 0, `docs/security/skill-audit-2026-04-19.md`). L2 ADR/DQ 배치 반영 — 타 터미널 ADR-019 본문 + Auth Advanced Blueprint §7.2 + CK 5건과 분담, 본 터미널은 **DQ-AC-1/AC-2/4.1/12.4 Resolved** + ADR 통계 6지점 동기화(§0.4/§4.1/§4.2 Accepted 18→19/§4.3 Phase 17/§4.4 트리거 48/§5 forecast 재정렬). 89392f7로 외부 세션 31 병합 완료 |
| 30 | [260419-session30-spike-priority-set.md](./260419-session30-spike-priority-set.md) | **우선 스파이크 7건 완결 (SP-010~016, 5 실측 + 2 축약)** — 세션 29 권장 1순위 순차 수행. SP-014 JWKS 캐시 / SP-015 Session 인덱스 / SP-011 argon2id / SP-010 PM2 cluster / SP-012 isolated-vm v6 실측 + SP-013 wal2json / SP-016 SeaweedFS 축약. 총 4.5h 소요(목표 29h 대비 85% 단축). **치명적 발견**: PM2 v6.0.14 `delete all --namespace` 필터 무시 버그 → 프로덕션 dashboard 삭제 사고, pm2 resurrect로 복구. Compound Knowledge 후보 5건 식별 |

### 2026-04-18

| 세션 | 파일 | 주요 작업 |
|------|------|-----------|
| 29 | [260418-session29-supabase-parity-wave-5.md](./260418-session29-supabase-parity-wave-5.md) | **kdywave Wave 5 완료 — 로드맵·스파이크·부록 25 문서 / 20,128줄 (Phase 0-4 전체 완주)** — 2 세션(28-1 선행 5 Agent / 28-2 신규 6 Agent) × 11 Agent 병렬 = 25 문서. **05-roadmap 13 (10,629줄)**: 9 릴리스 코드명(Nocturne→Centurion) + M1~M16 + MVP FR 27건(Phase 15-17 122h) + R-001~035 Top 10 + TD 22건 20% 할당 + KPI 127 + 3년 TCO $950~2,150 절감. **06-prototyping 9 (6,621줄)**: 22 신규 스파이크(SP-010~031, 우선 7건 29h 4주 / 지연 15건 63h) + 실행 프로토콜 + 5 상세 스펙. **07-appendix 3 (2,878줄)**: 용어 230+ / DQ 64건 100% Resolution / kdygenesis 인수인계(85+ 태스크). **Wave 1+2+3+4+5 누적 123 문서 / 106,588줄** (계획 ~105 대비 +17%). 역방향 피드백 0건(전 Wave). `_CHECKPOINT_KDYWAVE.md status=completed`. 다음 권장 `/kdyspike --full` 우선 7건 → Phase 15 Auth Advanced 22h → `/kdygenesis` 연계 |
| 28 | [260418-session28-supabase-parity-wave-4.md](./260418-session28-supabase-parity-wave-4.md) | **kdywave Wave 4 완료 — 아키텍처 청사진 26 문서 / 32,918줄** — `/kdywave --resume` → 11 Agent / 3 Tier 병렬. **Tier 1 A1 opus** (3 문서 / 3,713줄): system-overview 1,298 + adr-log 848(ADR-001~018, 재검토 트리거 45) + erd 1,567(PG 10→29+SQLite 3→6). **Tier 2 B1~B7 sonnet** (14 문서 / 18,251줄): 7 페어링(Auth / Observability·Operations / Compute / Editor / Delivery / DB 관리 / Cross-cutting). **Tier 3 U1+I1+I2 sonnet** (9 문서 / 10,954줄): UI/UX 5(세션 말미 editor-components 스텁 해소) + Integration 4. **평균 1,266줄/문서 (Wave 3 대비 +67%)**. DQ 28건 답변 완료, 역방향 피드백 0건. Wave 1+2+3+4 누적 **98 문서 / 86,460줄**(예상 91 초과). 다음 세션 Wave 5(로드맵+스파이크 10~15) |
| 25-C | [260418-session25c-tunnel-complete-playwright.md](./260418-session25c-tunnel-complete-playwright.md) | **Cloudflare Tunnel 대폭 안정화 + VIEWER UI 점검 + Playwright 라이브** — 세션 25-B 위임 5건 중 #1 sysctl 적용 (#2 systemd는 진단 결과 기 완료 + pm2-smart.service enabled). `/etc/sysctl.d/99-cloudflared.conf`(tcp_keepalive 7200→60, rmem/wmem 212KB→16MB) + `pm2 restart cloudflared`. **curl 28/28 edge 관통 + 측정 프로토콜 v2**(`/login` 200 기준). **Playwright 6 실패(S1 /login 530 cascade)** → 100% 보증 아닌 "확률적 매우 높음"으로 결론 보정. VIEWER: 사이드바 MANAGER_PLUS_PATHS에 /tables 포함으로 USER disclosure 불일치 1건 발견. Compound Knowledge quic-tuning-partial-fix.md "100% 측정의 한계" 섹션 추가 + 남은 위임 #3(multi-instance) 재고 승격 |
| 27 | [260418-session27-supabase-parity-wave-3.md](./260418-session27-supabase-parity-wave-3.md) | **kdywave Wave 3 완료 — 비전+FR/NFR+DQ 재분배 11 문서 / 8,350줄** — 7 Agent 병렬(V1/V2/R1/R2 opus, M1/M2/M3 sonnet). 비전 620 / 스토리 830(7 Epic × 36) / FR 1,477(55 FR) / NFR 500(38 NFR) / CON·ASM 420 / 100점 정의 435 / 운영 페르소나 449 / DQ 매트릭스 1,648(64 DQ 재분배 Wave3=20/W4=28/W5=16) / STRIDE 782 / **ADR-001 Multi-tenancy 621**(재검토 트리거 4) / 카테고리 우선순위 568. **100점 총 공수 1,008h(~50주), 3년 TCO $950~2,150 절감, MVP=Phase 15~17**. `.gitignore` 보강(test-results/ + playwright-report/). Wave 1+2+3 누적 72 문서 / 53,542줄 (전체 91 중 79%) |
| 25-B | [260418-session25b-deploy-tunnel-tuning.md](./260418-session25b-deploy-tunnel-tuning.md) | **세션 24 4건 권장 작업 + 후속 배포·Tunnel 튜닝** — (a) Compound Knowledge 4건(raw-sql-updatedat-bump / nextjs-private-folder-routing / timestamp-precision-optimistic-locking / csrf-api-settings-guard) + (b) Playwright 인프라 셋업(라이브 미통과) + (c) runReadwrite pg pool 모킹 33 케이스(vitest 89→131) + (d) USER-as-VIEWER 확장(table-policy SELECT 분기 + GET 핸들러 + viewer-curl V1~V9). 후속 3단계: git push 1655fce, `/ypserver prod` 배포 + viewer-curl V1~V9 라이브 매트릭스 전 PASS, Cloudflare Tunnel QUIC→HTTP/2 폴백 부분 수정(~30%→~50%, 100% 미달). 결정적 진단: `cloudflared metrics request_errors=0` → KT 회선 edge↔connector 패킷 drop 확정. Compound Knowledge `2026-04-18-cloudflare-tunnel-quic-tuning-partial-fix.md` (67b414d) |
| 26 | [260418-session26-supabase-parity-wave-2.md](./260418-session26-supabase-parity-wave-2.md) | **kdywave Wave 2 완료 — 매트릭스 + 1:1 비교 28 문서 / 18,251줄** — `/kdywave --resume wave-2` → 7 Agent 병렬 발사(A~G, 각 4 문서). 역방향 피드백 0건 (14 카테고리 모두 Wave 1 1위 유지). DQ-12.3 MASTER_KEY 위치 확정. 정량화된 재고 조건 명시(Garage/pg_graphql/Docker/KMS 각 수요 트리거). **Compound Knowledge**: "1:1 비교는 계층 분리를 드러낸다" — wal2json vs realtime-port / isolated-vm vs Deno / splinter vs squawk 모두 "경쟁이 아니라 역할 분담"으로 수렴, Wave 4 청사진의 계층 설계 축. Wave 1+2 누적 61 문서 / 45,192줄 |
| 25 | [260418-session25-supabase-parity-wave-1.md](./260418-session25-supabase-parity-wave-1.md) | **Supabase 100점 동등성 평가 + kdywave Wave 1 완료** — 14 카테고리 평가표(절대 55 / 가중 60) → `/kdywave` 호출 → Phase 0/1 자율 압축 → Round 1+2 분할 5+5 Agent 병렬. **33 deep-dive / 26,941줄 / 9 DQ 잠정 답변 / 64 신규 DQ**. 사전 스파이크 4건(Postgres CDC / isolated-vm v6 / SeaweedFS / WebAuthn) 모두 "조건부 GO". Compound Knowledge 2건(단일 vs 하이브리드 9:5 / PG 확장 vs 자체구현 7건 기준) |
| 24b | [260418-session24-phase-14c-beta.md](./260418-session24-phase-14c-beta.md) | Phase 14c-β 복합 PK 지원 — 신규 `/composite` 엔드포인트(바디 `pk_values` map) + schema `compositePkColumns` + UI 훅 분기. ADR-005. **세션 중 2 근본 수정**: Next.js private folder(`_composite`→`composite`), TIMESTAMP(3) 정밀도 정렬. curl E2E B1~B9 전 PASS |
| 24 | [260418-session24-phase-14c-alpha.md](./260418-session24-phase-14c-alpha.md) | Phase 14c-α 인라인 편집 + 낙관적 잠금 — α/β/γ 분해 후 α 단독. `expected_updated_at` 바디 필드 + 409 CONFLICT + Sonner 3액션 토스트 + EditableCell/useInlineEditMutation/TypedInputControl. 2차 근본 수정: raw UPDATE 자동 `updated_at = NOW()` bump. ADR-004 + curl E2E C1~C6 전 PASS |

### 2026-04-17

| 세션 | 파일 | 주요 작업 |
|------|------|-----------|
| 23 | [260417-session23-phase-14c-updated-at-fix.md](./260417-session23-phase-14c-updated-at-fix.md) | Phase 14c 1순위 — brainstorming→writing-plans→subagent-driven-development 체인 실행. 9 모델(5 병기 + 4 신규) + 단일 migration + B2 백필. `updated_at` 생략 payload E2E 200 확인(세션 22 500 버그 근본 수정). Compound Knowledge 2건(prisma-windows-wsl-gap, curl-e2e-recipe) |
| 22 | [260417-session22-phase-14b-e2e-updatedat-bug.md](./260417-session22-phase-14b-e2e-updatedat-bug.md) | Phase 14b E2E DOD 실수행(S8~S11 전체 통과) + `@updatedAt` DB DEFAULT 부재 버그 발견·문서화. Phase 14c 1순위 수정 대상 확정 |
| 21 | [260417-session21-phase-14b-implementation.md](./260417-session21-phase-14b-implementation.md) | Phase 14b 구현 — `/kdyplanon`으로 세션 20 plan 재개, C1 SQL 롤(luckystyle4u DB) + C2 라이브러리 + C3 API + C4 UI 4 커밋 완료. C5(docs + 배포 + push) 사용자 승인 대기 |

### 2026-04-12

| 세션 | 파일 | 주요 작업 |
|------|------|-----------|
| 20 | [260412-session20-phase-14b-design.md](./260412-session20-phase-14b-design.md) | Phase 14b 설계 체인 — brainstorming(D1~D5 + 추가 3건 합의) → ADR-003 확정 → 실행 계획(12 Task × 5 커밋) 작성. 구현은 다음 세션 이관 |
| 19 | [260412-session19-ops-security-hardening.md](./260412-session19-ops-security-hardening.md) | 세션 18 후속 — auth-guard 감사 로그(AUTH_FAILED/FORBIDDEN) + instrumentation data/ mkdir + Table Editor 프로덕션 E2E + Phase 14b CRUD 프롬프트 + NFT/audit 분류 |
| 18 | [260412-session18-auth-refactor.md](./260412-session18-auth-refactor.md) | 근본 auth 재설계 (middleware→proxy + CVE-2025-29927 방어 + authZ 버그 수정) + 기술부채 정리 (NFT/audit/cron) + Phase 14a Table Editor |
| 17 | [260412-session17-monaco-xyflow.md](./260412-session17-monaco-xyflow.md) | SQL Editor Monaco 치환 + Schema Visualizer xyflow/elkjs 치환 + 12 P0 페이지 Playwright E2E + 기본 쿼리 오류 부수 수정 |
| 16 | [260412-session16-supabase-deploy.md](./260412-session16-supabase-deploy.md) | 세션 15 배포 (마이그레이션 적용 + app_readonly 롤 + UI 패키지) + 레거시 에러 2건 수정 + Cloudflare Tunnel PM2 복구 |
| 15 | [260412-session15-supabase-clone.md](./260412-session15-supabase-clone.md) | Supabase 관리 체계 이식 — Phase A 리서치(23문서) + Phase B 11 P0 모듈 병렬 구현(55 파일) + Prisma +7 모델 |
| 14 | [260412-session14-phase13d-complete.md](./260412-session14-phase13d-complete.md) | 중단 터미널 3개 복구 + Phase 13d 완료 (9개 페이지 스켈레톤 + EmptyState) |

### 2026-04-06

| 세션 | 파일 | 주요 작업 |
|------|------|-----------|
| 1 | [250406-session1-init-security.md](./250406-session1-init-security.md) | 초기화 + 대시보드 v1 + 보안 Wave 1 |
| 2 | [260406-session2-dashboard-improve.md](./260406-session2-dashboard-improve.md) | 대시보드 기능 개선 (그래프, 모달, 검색, 반응형) |
| 3 | [260406-session3-security-wave2.md](./260406-session3-security-wave2.md) | 보안 Wave 2 (Rate Limiting + 감사 로그) |
| 4 | [260406-session4-frontend-design.md](./260406-session4-frontend-design.md) | 프론트엔드 디자인 전면 개선 + ypserver 배포 스킬 |
| 5 | [260406-session5-master-plan.md](./260406-session5-master-plan.md) | kdywave 종합 분석 + 마스터 개발 계획서 |
| 6 | [260406-session6-spike-zod.md](./260406-session6-spike-zod.md) | ypserver 배포 + Zod + SPIKE 3건 기술 검증 |
| 7 | [260406-session7-filebox-v2.md](./260406-session7-filebox-v2.md) | 파일박스 v1→v2 (DB 기반 폴더 관리) |
| 8~12 | [260406-session8-12-massive-feature.md](./260406-session8-12-massive-feature.md) | 토스트+DB감사로그+IP화이트리스트+SSE+인증통합+Cmd+K |
| 13 | _인수인계서 없음 (logs 통합 참조)_ | 회원관리 백엔드 + PostgreSQL + Warm Ivory 테마 |

---

## 인수인계서 미작성 세션

모든 세션에 인수인계서가 작성되지는 않습니다.
간단한 세션은 아카이브 로그로 대체 → [logs/_index.md](../logs/_index.md)

## 인수인계서 관리 전략

프로젝트 특성에 따라 택일:

| 전략 | 설명 | 적합한 경우 |
|------|------|-------------|
| **max 5 순환** | 최대 5개 유지, 초과 시 archive/로 이동 | 단기 프로젝트, 소규모 |
| **영구 보존** | 모든 인수인계서 보존, 이 목록으로 관리 | 장기 프로젝트, 다수 참여 |

## 프로토콜 참조

인수인계서 작성 형식과 세션 프로토콜 → [README.md](./README.md)

---
[← CLAUDE.md](../../CLAUDE.md)
