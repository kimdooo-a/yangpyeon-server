# Journal — 2026-04-19

> 세션 30 기록

---

## 2026-04-19 (세션 30) — 우선 스파이크 7건 순차 실행 (SP-010~016)

**대화 토픽 흐름**: 사용자 "다음세션 권장 모두 순차적 수행" → current/next-dev-prompt/spike-priority-set 확인 → 9개 태스크(SP-014/010/011/015/012/013/016 + Phase 15 + genesis) 생성 → 우선순위 순 `/kdyspike --full` 진입.

### 세션 진행

**SP-014 JWKS 캐시** (소요 1.2h / 목표 3h)
- Phase 0 탐지: 현 시스템 HS256(대칭키), JWKS 엔드포인트 미존재
- 실험: Node v24 + jose@6.2.2 + ES256 키쌍 + mock JWKS endpoint (127.0.0.1:9001)
- 결과 핵심:
  - 캐시 없음: p95 1.340ms
  - cacheMaxAge=180s: **p95 0.189ms, hit rate 99.0%**
  - 키 회전 grace: JWKS가 both 키 서빙해야 성립 (단순 교체 시 ERR_JWKS_NO_MATCHING_KEY)
  - Cloudflare Tunnel RTT p95: 148.7ms (기준 100ms 48% 초과)
- **조건부 Go** — 실효 지연 1.62ms (hit 99%), Workers 캐시 도입 트리거 2건 정의

**SP-015 Session 인덱스** (소요 0.8h / 목표 2h)
- SQLite(better-sqlite3) + PG 16.13 이중 실험
- SQLite 10만 행 insert 536ms, 복합 인덱스 p95 0.053ms
- PG: EXPLAIN Bitmap Index Scan, p95 **48μs**
- **핵심 발견**: `CREATE INDEX ... WHERE expiresAt > NOW()` → ERROR NOW() STABLE → **cleanup job 대안** 채택
- Seq Scan p95 5105μs (인덱스 drop 시) — 106배 저하 → 인덱스 필수성 실증
- 1M extrapolation p95 ≈ 0.065ms (목표 2ms의 30배 여유)
- **Go** · Compound Knowledge 후보: "PG partial index + NOW() 비호환 → cleanup job 전환"

**SP-011 argon2id** (소요 0.6h / 목표 3h)
- `@node-rs/argon2` prebuilt binary 설치 3.3초
- WSL2 Ubuntu 24.04 + Node v24.14.1 확인 (spec 예상 Node 22 초과)
- bcrypt(12) hash/verify p95 ≈ 170ms
- argon2id default p95 19.8ms — **13배** 빠름 (spec 예상 5× 초과)
- 1000 사용자 점진 마이그레이션: 오류 0 / 100% 성공
- **Go** · ADR-022 신규 제안 · 사실관계 수정: 프로젝트 현행은 bcryptjs 아님 (bcrypt@6.0.0 N-API)

**SP-010 PM2 cluster** (소요 1.2h / 목표 4h)
- **치명적 운영 사고**: `pm2 delete all --namespace sp010` 이 namespace 필터를 무시하고 프로덕션 dashboard/cloudflared 삭제 → `pm2 resurrect`로 즉시 복구
- 복구 후 전략 전환: PM2 생략, Node `cluster` 모듈 직접 사용 (내부적으로 동일)
- autocannon 10s × 50 conn:
  - fork: RPS 54,692
  - cluster:4: RPS 76,489 = fork × **1.40** (+39.9%)
- SQLite WAL 4 worker_threads 10s: SQLITE_BUSY **0/1968 = 0.000%**
- advisory lock: PG 공식 보증으로 실증 축약
- **조건부 Go** · `/ypserver` 스킬에 `pm2 delete all` safeguard 보강 권장

**SP-012 isolated-vm v6** (소요 0.7h / 목표 4h)
- isolated-vm@6.1.2 Node v24.14.1 WSL2에서 **1.6초 prebuilt 설치**
- 기본 실행: `1 + 2*3 = 7` ✅
- Cold start 100 iter: p95 0.909ms (목표 50ms의 55배 여유)
- 메모리 격리 32MB: OOM 시 Isolate 자동 dispose, 호스트 안전
- 10s × 1,092 churns/s = 10,927 churns, delta +16MB (정상 부하 환산 0.09MB/10분)
- **Go** · ADR-009 재검토 트리거 1 "v6 Node 24 ABI break" **해소**

**SP-013 wal2json + SP-016 SeaweedFS** (축약 — 물리 측정 별도 세션)
- SP-013: 30분 DML 주입 + 슬롯 손상 recovery + presence_diff 구조 — 모두 물리 측정 필요
- SP-016: weed CLI 미설치 + 50GB 디스크 필요
- 두 건 모두 **설계 체크리스트 + 실측 절차 + proto-verdict "조건부 Go"** 문서화만 완료
- Pending 상태로 `_SPIKE_CLEARANCE.md` 등록, 후속 세션 위임

### 변경 사항

- **신규 문서 9건** (spike result 7건 + 전체 문서화)
  - `docs/research/spikes/spike-010-pm2-cluster-result.md`
  - `docs/research/spikes/spike-011-argon2-result.md`
  - `docs/research/spikes/spike-012-isolated-vm-v6-result.md`
  - `docs/research/spikes/spike-013-wal2json-slot-result.md` (축약)
  - `docs/research/spikes/spike-014-jwks-cache-result.md`
  - `docs/research/spikes/spike-015-session-index-result.md`
  - `docs/research/spikes/spike-016-seaweedfs-50gb-result.md` (축약)
- **실험 스크립트 8건** (재현 가능)
  - `spike-010-pm2-cluster/*.cjs,*.mjs` (8 파일)
  - `spike-011-argon2/bench.mjs`
  - `spike-012-isolated-vm/bench.mjs`
  - `spike-014-jwks-cache/experiment.mjs`
  - `spike-015-session-index/sqlite-bench.mjs, pg-bench.sh`
- **수정 1건**
  - `docs/research/_SPIKE_CLEARANCE.md` — 7 엔트리 추가
- **메타 4건** (세션 마감)
  - `docs/status/current.md` — 세션 30 행
  - `docs/logs/journal-2026-04-19.md` — 본 파일
  - `docs/logs/2026-04.md` — 세션 30 색인
  - `docs/handover/260419-session30-spike-priority-set.md` — 상세 인수인계
  - `docs/handover/next-dev-prompt.md` — 갱신

### 주요 논의

- **우선순위 전환**: 스펙의 실행 순서(Week 1~4)와 별도로 **시간 소요 최소 순** 선택 — SP-014(3h) → SP-015(2h) → SP-011(3h) → SP-010(4h) → SP-012(4h)
- **PM2 namespace 버그 발견**: `pm2 delete all --namespace X`의 필터 무시는 v6.0.14 재현 확인. Compound Knowledge 후보 등재
- **N-API prebuilt 패턴**: argon2(3.3초), isolated-vm(1.6초), better-sqlite3(기존) 모두 native addon이지만 설치 3~5초. 과거 node-gyp 고통 사실상 해소
- **Phase 15/genesis 위임**: 22h + 수시간 규모로 단일 세션 불가 → next-dev-prompt에 명시

### Compound Knowledge 후보 (본 세션 5건)

1. `2026-04-19-pg-partial-index-now-incompatibility.md` (SP-015)
2. `2026-04-19-napi-prebuilt-native-modules.md` (SP-011)
3. `2026-04-19-pm2-delete-all-namespace-bug.md` (SP-010)
4. `2026-04-19-isolated-vm-v6-node24-wsl2-verified.md` (SP-012)
5. `2026-04-19-jwks-grace-endpoint-vs-client-cache.md` (SP-014)

### 커밋 계획

세션 30 단일 커밋:
- 스파이크 결과 7건 + 실험 스크립트 8건
- `_SPIKE_CLEARANCE.md` 7 엔트리
- 메타 갱신 5건 (current / 2026-04 / journal-04-19 / handover / next-dev-prompt)

### 다음 세션 진입

**우선순위 1 (남은 물리 측정)**:
- SP-013 wal2json 실측 (5h, 별도 PG 랩)
- SP-016 SeaweedFS 50GB 실측 (8h, 50GB 디스크 + 별도 환경)

**우선순위 2 (Phase 15 Auth Advanced MVP, 22h)**:
- SP-011 Go 결과 반영 → @node-rs/argon2 도입 (8h 예상 감소)
- SP-015 Go 결과 반영 → Session PG 테이블 + 복합 인덱스 + cleanup job
- SP-014 조건부 Go → JWKS endpoint 구현 + cacheMaxAge + grace 정책
- TOTP + WebAuthn + Rate Limit 22h

**우선순위 3 (/kdygenesis --from-wave)**:
- 07-appendix/03-genesis-handoff.md 기반 태스크 오케스트레이션
- 85+ 태스크 주간 실행 플로우 자동 생성

**우선순위 4 (Compound Knowledge 5건 신규 작성)**

**우선순위 5 (Blueprint 반영)**:
- ADR-006/008/009/010/013/015 §결과 보완
- Auth Advanced Blueprint / DB Ops Blueprint / Edge Functions Blueprint 업데이트
- DQ-AC-1/AC-2/4.1/12.4 **Resolved**
- 06-prototyping/01-spike-portfolio.md 상태 **Completed** ×5, **Pending** ×2

---

## 2026-04-19 (세션 31) — 로컬 정리 + /ypserver safeguard + 세션 30 결과 ADR/DQ 반영

**대화 토픽 흐름**: 코드라인수 질문 → Supabase 코드 존재 여부 확인 → `/kdyclean` 로컬 아티팩트 정리 → 커밋 → `/kdyguide` 실행 → Option A DAG 채택(L0 safeguard → L1 audit → L2 ADR/DQ → L3 CK[타 터미널] → L4 /cs) → 작업 실행 → 타 터미널 종료 확인 → `/cs`

### 세션 진행

**[1] 프로젝트 규모 확인** (정보성 Q&A)
- 사용자: "이 프로젝트 총 코드가 몇줄이야?"
- 집계: **245,699줄 / 585파일** (git tracked). 내역: 문서 211K(86%) + TS/TSX 19,393줄(8%) + JSON 11,701(5%) + 기타 3.2K(1%)
- 보정 인사이트: `git ls-files | xargs wc -l`는 Windows bash에서 배치 쪼개짐으로 마지막 배치만 집계 → `xargs -n 100` + `grep total | awk sum`으로 정확 산출

**[2] 규모 평가** (해석 요청)
- 사용자: "많은거야?"
- 결론: **코드 19K = 초기~중기 Next.js 대시보드 정상 범위**, **문서 211K = 이례적으로 방대**(OSS 수준의 2~4배)
- 원인 추정: 프로젝트의 "역사 삭제 금지" 원칙 + Wave 1~5 리서치 누적(123 문서/106,588줄) + 세션 1~30 로그/저널/인수인계 전량 보존

**[3] Supabase 코드 존재 여부** (사실 확인)
- 사용자: "git에 supabase studio 코드 있어??"
- 결과: **없음**. `supabase` 키워드 매칭 파일 204개는 전부 **리서치 문서**(supabase-scrape 14 + parity 연구 + handover). 코드 파일은 `src/lib/types/supabase-clone.ts` 1개(타입 정의만). Supabase Studio 본 소스 미포함. **"벤치마킹해서 직접 구현" 프로젝트로 확인**.

**[4] `/kdyclean` 실행** (정리)
- 사용자: `/kdyclean`
- 스캔: 안전 삭제 대상 ~11.4MB + 선택 삭제 1.3GB
- 안전 묶음만 진행: `nul` + `_CHECKPOINT_KDYWAVE.md.bak` + `tsconfig.tsbuildinfo` + 빈 폴더 `cp/`·`ls/`·`tmp/` + `.playwright-mcp/`(27 로그) + `test-results/` + 루트 `e2e-*.png` × 15 = 삭제 완료
- `.gitignore`에 `*.bak` 추가
- 발견: `.playwright-mcp/` 일부 파일이 과거 커밋에 tracked된 채 남아있었음 → 이번 삭제로 git "D" 상태
- 커밋 `cadb8ad` — `chore: 로컬 아티팩트 정리 — .playwright-mcp 및 체크포인트 백업 제거`. **주의 — `git commit --only -- <pathspec>`** 사용으로 이미 스테이지된 spike-010~016 파일을 섞지 않고 정리 파일만 단독 커밋

**[5] `/kdyguide` 추천** (라우팅)
- 사용자: `/kdyguide`
- 제약: 우선순위 1/2/5(Phase 15 22h, SP-013/016 13h, kdygenesis 수h) **단일 세션 초과 → 별도 세션 위임**
- 세션 내 가능 3항: `/ypserver` safeguard / Compound Knowledge 5건(2h) / ADR·Blueprint·DQ 배치 반영(2h)
- Option A 채택: L0(`/ypserver`) → L1(`/kdyskillaudit`) → L2(ADR/DQ) → L3(CK) → L4(`/cs`)
- 사용자 추가 지시: "Compound Knowledge 5건은 **다른 터미널에서 진행중이니 제외**" → L3 이 터미널에서 제외

**[6] L0 — `/ypserver` safeguard 보강** (글로벌 스킬 수정)
- 파일: `~/.claude/skills/ypserver/SKILL.md` (git 미추적, 글로벌)
- **§4 운영 safeguard — PM2 파괴적 명령 가드** 신설 (5 서브섹션 / 78줄):
  - §4-1 절대 금지: `pm2 delete all` / `pm2 delete all --namespace X` / `pm2 stop all` / `pm2 kill`
  - §4-2 세션 30 실증 사고: PM2 v6.0.14 `--namespace` 필터 무시 버그 → 프로덕션 dashboard + cloudflared 동시 삭제
  - §4-3 허용 대안: 이름 나열 `pm2 delete sp010-fork sp010-cluster-0`
  - §4-4 에이전트 실행 전 의무 4단계 (`pm2 list` 스냅샷 → dashboard/cloudflared 포함 시 `AskUserQuestion` → all/--namespace 거부)
  - §4-5 복구 절차 3순위 (`pm2 resurrect` → 수동 기동 → `pm2 save`)
- description 앞부분에 `⚠ pm2 delete all 사용 금지` 경고 주입
- 관련 Compound Knowledge `2026-04-19-pm2-delete-all-namespace-bug.md` forward reference

**[7] L1 — `/kdyskillaudit` 검증**
- 대상: `~/.claude/skills/ypserver/SKILL.md` (275줄, scripts/ 없음)
- 5 Phase 스캔: 프롬프트 인젝션 0 / 커맨드 인젝션 0 / 크레덴셜 접근 0 / 권한 상승 0 / 외부 전송 0
- **판정: ✅ PASS** (FAIL 0, WARN 0)
- §4 safeguard의 `pm2 delete`/`pm2 resurrect` 코드 블록은 문서 예시이며 에이전트가 자동 실행하지 않으므로 FAIL 패턴 매칭 회피
- 산출: `docs/security/skill-audit-2026-04-19.md`

**[8] L2 — ADR/Blueprint/DQ 배치 반영**
- 발견: 타 터미널이 동시 작업 중
  - ADR-019 (argon2id) 본문 — 타 터미널 작성 (고품질 완성)
  - Auth Advanced Blueprint §7.2.1~7.2.3 (JWKS/세션/argon2) — 타 터미널 작성
  - ADR-009 재검토 트리거 1 "isolated-vm v6 Node 24 ABI 호환 깨짐" → **RESOLVED 2026-04-19 (SP-012)** — 기존 세션에서 기 반영
  - ADR-006/008/010/013/015 §세션 30 보완 — 기 반영
- 본 터미널 담당 (중복 회피):
  - **DQ-AC-1 Resolved** — argon2 Phase 17 점진 마이그레이션, SP-011 실측 첨부
  - **DQ-AC-2 Resolved** — `(userId, expiresAt)` 복합 인덱스 + cleanup job, PG partial+NOW() 불가 이유 명시
  - **DQ-4.1 Resolved (조건부 Go)** — cluster:4 동시 접속>30 트리거, PM2 safeguard 규칙 인용
  - **DQ-12.4 Resolved (현 시점 불필요)** — hit 99%에서 Workers 캐시 ROI 낮음, 재도입 트리거 2건 정의
  - **ADR §0.4 요약 테이블** — ADR-019 행 추가
  - **ADR §4.1 카테고리별 수** — Auth Core: ADR-006/017 → 006/017/**019**
  - **ADR §4.2 상태별 분포** — Accepted 18 → **19** (ADR-019 추가 근거 명시)
  - **ADR §4.3 Phase 매핑** — Phase 17: ADR-006/008 → 006/008/**019**
  - **ADR §4.4 트리거 수** — 18 × 2.5 ≈ 45 → 19 × 2.5 ≈ 48 (ADR-009 해소 명시)
  - **ADR §5 Wave 5 예상 목록** — 기존 "ADR-019(Prisma)" → "ADR-020(Prisma)" 한 칸 내림, "ADR-022(argon2)" 항목 제거(확정)

**[9] L3 — Compound Knowledge 5건** (타 터미널 담당, 이 세션 제외)
- 산출: 타 터미널이 커밋 `67731da` — SP-010/011/012/014/015 각 1건
- 본 세션에서는 **참조만** 수행 (ypserver SKILL.md의 safeguard, ADR-019/DQ 엔트리에 링크)

**[10] L4 — `/cs` 진입 준비**
- 타 터미널 종료 확인 → working tree clean, 최신 커밋 `89392f7`(ADR/Blueprint/DQ 반영 + 세션 31 병합)
- 본 세션의 `docs/security/skill-audit-2026-04-19.md`도 타 터미널이 병합 커밋에 포함
- `/cs` 프로토콜 6단계 진입

### 변경 사항

**본 세션 본 터미널 직접 작업**:
- `.gitignore` — `*.bak` 패턴 추가
- 루트 임시 파일 8종 삭제(`nul` / `_CHECKPOINT_KDYWAVE.md.bak` / `tsconfig.tsbuildinfo` / `cp,ls,tmp/` / `.playwright-mcp/` / `test-results/` / 루트 `e2e-*.png` × 15)
- `~/.claude/skills/ypserver/SKILL.md` — §4 safeguard 신설(78줄) + description 경고(글로벌, git 미추적)
- `docs/security/skill-audit-2026-04-19.md` — 신규(감사 PASS 보고서)
- `docs/research/2026-04-supabase-parity/00-vision/07-dq-matrix.md` — DQ 4건 Resolved
- `docs/research/2026-04-supabase-parity/02-architecture/01-adr-log.md` — 통계 테이블·§5 예상 목록 재정렬(6 지점)

**타 터미널 작업 (본 세션 기간 중 병합 커밋)**:
- ADR-019 argon2id 본문 (약 50줄)
- Auth Advanced Blueprint §7.2.1~7.2.3 (JWKS/세션/argon2)
- `docs/solutions/2026-04-19-*.md` × 5 (CK 5건)
- `06-prototyping/01-spike-portfolio.md` 상태 업데이트
- `07-appendix/02-dq-final-resolution.md` 반영

### 주요 논의

- **병렬 터미널 충돌 회피**: 타 터미널이 ADR-019 작성 + CK 5건 + Blueprint §7.2 수정을 동시 진행 중임을 중간에 발견. **본 터미널은 "놓친 부분"(통계 테이블 번호 불일치 + DQ Resolved 마킹)에 집중**하여 중복 작업 0 달성
- **git commit --only pathspec**: 스테이지된 unrelated 변경(spike-010~016)과 정리 변경을 분리 커밋. 스테이징 상태 건드리지 않고 특정 경로만 단독 커밋 가능
- **PM2 safeguard의 §4 구조**: 단순 경고 한 줄이 아니라 **금지 → 사고 기록 → 허용 대안 → 실행 전 체크 → 복구 절차** 5단 구조로 에이전트가 실제 판단 가능하도록 설계. `/kdyskillaudit` PASS 유지하기 위해 문서 예시는 실행 코드 아닌 설명 블록으로 감쌈
- **ADR 번호 재정렬 원칙**: §3.1 "현재 019부터" 규칙 기준으로 argon2id를 ADR-019로 확정. §5 forecast 번호들은 한 칸씩 내림. 중복 언급(ADR-006 §보완에 "상세는 ADR-019", §5에 "ADR-022(예상): argon2") 해소

### Compound Knowledge (본 세션 직접 산출 없음)

세션 30의 실측 결과가 타 터미널에서 CK 5건으로 정식화됨. 본 세션은 **CK를 기존 문서에 forward reference로 연결**하는 역할만 수행:
- `/ypserver` §4-5 → `pm2-delete-all-namespace-bug.md` 인용
- ADR-019 참조 → `napi-prebuilt-native-modules.md` 인용
- DQ-12.4 확정 답변 → `jwks-grace-endpoint-vs-client-cache.md` 인용
- DQ-AC-2 확정 답변 → `pg-partial-index-now-incompatibility.md` 인용

### 다음 세션 진입

**우선순위 1 (즉시 착수 가능)**: Phase 15 Auth Advanced MVP 22h — 세션 30 스파이크 결과 반영된 구현 순서는 `next-dev-prompt.md`에 상세 기록. Prisma Session 모델(1h) → argon2id(3h) → JWKS endpoint(4h) → TOTP(8h) → WebAuthn(10h) → Rate Limit(4h)

**우선순위 2 (환경 확보 시)**: SP-013 wal2json 물리 측정(5h), SP-016 SeaweedFS 50GB(8h)

**우선순위 3 (장기)**: `/kdygenesis --from-wave` 연계 — 85+ 태스크 주간 실행 플로우

---

## 2026-04-19 (세션 32) — Phase 15 Auth Advanced Step 1-2 (Prisma Session + argon2id 점진 마이그레이션)

**대화 토픽 흐름**: 사용자 "다른터미널에서 option 1 진행중이니 넌 다음 작업이 뭔지 생각하면됨" → /kdyguide 실행 후 세션 32 진입 명령 도출(Phase 15 Step 1-2) → 사용자 "다른 터미널 세션 종료 문서 마감했음 확인하고 너가 이어서 진행해줘" → working tree clean(`b4287b0`) 확인 후 즉시 진입.

### 세션 진행

**탐색 단계** (Task 1, ~10분)
- `prisma/schema.prisma` 현재 모델 확인 — User 모델(bcrypt passwordHash) + 기타 11개 모델, **Session 모델 없음** (신규 추가 대상)
- Blueprint §7.2.1~7.2.3 사양 정독 — 정확한 컬럼명/인덱스 정책/argon2 분기 코드 예시 확보
- 기존 인증 코드 위치 — `src/lib/password.ts` (bcrypt cost=12), `src/lib/auth.ts` (jose HS256 stateless JWT), `src/app/api/v1/auth/login/route.ts`, register/password/admin-create 4개 호출자
- CK `2026-04-17-prisma-migration-windows-wsl-gap.md` 패턴 확인 — 수동 SQL + `migrate deploy`

**Step 1 — Prisma Session 모델** (~30분)
- `schema.prisma` 편집 — Session 모델 신규 + User에 `sessions Session[] @relation("UserSessions")` 추가
  - 컬럼: id(uuid) / userId(FK CASCADE) / tokenHash(SHA-256, UNIQUE) / ip / userAgent / createdAt / lastUsedAt / expiresAt / revokedAt
  - 인덱스: `@@index([userId, revokedAt, expiresAt])` (SP-015 채택안)
- `prisma/migrations/20260419120000_add_sessions_table/migration.sql` 수동 작성
- `npx prisma format` + `npx prisma validate` PASS
- `npx prisma generate` — 신규 Prisma client 생성
- `src/lib/sessions/cleanup.ts` — `cleanupExpiredSessions()` (DELETE WHERE expires_at < NOW() - INTERVAL '1 day'). 활성화는 후속 단계
- 결과: 인프라만 추가. 활성 사용은 Phase 15-D Refresh Rotation에서

**Step 2 — argon2id 도입** (~1.5시간)
- `npm install @node-rs/argon2` — WSL2 prebuilt binary, npm 경고 0
- `src/lib/password.ts` 수정 — 시그니처 보존 + 내부 분기:
  - `hashPassword(plain)` → argon2id default
  - `verifyPasswordHash(plain, hash)` → `$2` prefix 시 bcrypt.compare, 그 외 argon2.verify
  - `needsRehash(hash)` 신규 export (bcrypt 해시 판정)
- `src/app/api/v1/auth/login/route.ts` — 검증 성공 후 `needsRehash` true면 `hashPassword`로 재해시. lastLoginAt update에 머지하여 round-trip 0개 (Blueprint §7.2.3 예시는 별도 update이지만 본 프로젝트가 이미 lastLoginAt update를 수행 중이라 자연 통합)
- 호출자 영향: register/password change/admin user create 3곳은 `hashPassword` 시그니처 동일하므로 코드 변경 0
- `src/lib/password.test.ts` 신규 — 8 케이스 PASS (argon2id format / argon2 verify 정·오답 / bcrypt 역호환 정·오답 / needsRehash 두 분기)

**검증** (Task 4, ~30분)
- Vitest 전체 회귀: 131 → **139 PASS** (신규 8 / 회귀 0)
- `tsc --noEmit`: 0 에러
- `/ypserver prod --skip-win-build` 배포:
  - Phase 1: 스킵
  - Phase 2-1: PM2 dashboard 존재 → restart 모드
  - Phase 2-2: prisma 복사 + `migrate deploy` — `20260419120000_add_sessions_table` 1건 적용 성공
  - Phase 2-3: src 복사 + npm install + `npm run build` — 빌드 성공 (전체 라우트 + Proxy + Static/Dynamic)
  - Phase 2-4: Drizzle `db:migrate` — applied successfully
  - Phase 2-5: `pm2 restart dashboard` — restart count 1, online
  - Phase 2-6: cloudflared online (uptime 48s)
- Phase 3 헬스체크: `curl localhost:3000` HTTP 307 (proxy 로그인 리다이렉트, 정상)
- E2E (자동 재해시 실증):
  - 배포 직후 DB prefix: `$2b$12$wiApl` (bcrypt)
  - 1차 로그인: HTTP 200 + JWT 발급
  - DB prefix 변화: `$argon2id$v=` ⭐ 자동 마이그레이션 성공
  - 2차 로그인: HTTP 200 (argon2 분기) ⭐ 점진 마이그레이션 후 검증 PASS
  - Sessions 인덱스 EXPLAIN: `Index Scan using sessions_user_id_revoked_at_expires_at_idx` ⭐ SP-015 채택안 작동

### 변경 사항

- `prisma/schema.prisma` — Session 모델 + User.sessions 관계
- `prisma/migrations/20260419120000_add_sessions_table/migration.sql` (신규)
- `src/lib/sessions/cleanup.ts` (신규)
- `src/lib/password.ts` — argon2id default + 분기
- `src/lib/password.test.ts` (신규, 8 PASS)
- `src/app/api/v1/auth/login/route.ts` — 자동 재해시 통합
- `package.json` / `package-lock.json` — `@node-rs/argon2`

### 주요 논의

- **자동 재해시 round-trip 0개 압축** — Blueprint §7.2.3 예시 코드는 검증 직후 별도 prisma.user.update를 호출하지만, 본 프로젝트는 이미 lastLoginAt update를 수행 중이라 `{lastLoginAt, passwordHash?}` 머지로 단일 트랜잭션화. CK 후보로 별도 기록 권장
- **시그니처 보존 전략의 효과** — `verifyPasswordHash(plain, hash)` 시그니처를 그대로 두니 호출자 4개 중 3개가 코드 변경 0. 자동 재해시는 login route 1곳에만 명시. 단일 책임 원칙 + 호출자 영향 최소화 동시 충족
- **Sessions 인프라만 추가, 활성화는 후속** — 현재 인증이 stateless JWT라 Session 테이블 사용처가 없음. Phase 15-D Refresh Rotation 도입 시 활성화 예정. Step 1 범위를 1h로 압축
- **isolatedModules 환경에서 const enum** — `Algorithm.Argon2id` 사용 시 `isolatedModules: true` 환경에서 컴파일 경고 가능 → `const ARGON2ID_ALGORITHM = 2` 상수 캡슐화로 회피
- **kimdooo 계정 1 사용자 case 실증** — SP-011 1000 사용자 마이그레이션 100% 성공의 production 첫 case. prefix 변화가 한 번의 로그인으로 즉시 발생함을 EXPLAIN/curl로 확인

### Compound Knowledge 후보 (1건)

- `2026-04-19-bcrypt-argon2-progressive-rehash-merged-update.md` — Blueprint 예시보다 1 round-trip 절약하는 자동 재해시 패턴 (lastLoginAt update에 조건부 passwordHash 머지)

### 커밋 계획

세션 32 단일 커밋:
- 코드: schema.prisma / migration.sql / sessions/cleanup.ts / password.ts / password.test.ts / login route / package.json+lock
- 메타: current.md (세션 32 행) / logs/2026-04.md (세션 32 항목) / journal-2026-04-19.md (본 세션 섹션) / handover/260419-session32-phase15-step1-2.md (신규) / next-dev-prompt.md (Step 3 진입 갱신)

### 다음 세션 (33 진입)

**우선순위 1**: Phase 15 Step 3 — JWKS endpoint 구현 (4h, SP-014 조건부 Go 반영)
- `/api/.well-known/jwks.json` ES256 키쌍 발급
- `JwksKey` Prisma 모델 (status=CURRENT/RETIRED + retireAt grace)
- jose `cacheMaxAge: 180_000` 클라이언트 캐시
- cron 1시간 retireAt 만료 키 정리
- Blueprint §7.2.1 endpoint grace 운용 정책 그대로 따름

**우선순위 2**: Phase 15 Step 4 — TOTP 통합 (8h, FR-6.1) — `otplib@12.x` + QR 발급 + 백업 코드 + admin 강제 해제

**우선순위 3**: Phase 15 Step 5 — WebAuthn (10h, FR-6.2) — `@simplewebauthn/server@10.x` Passkey 등록·인증

**우선순위 4**: Phase 15 Step 6 — Rate Limit (4h, FR-6.3) — `rate-limiter-flexible@5.x` PG 어댑터

**우선순위 5**: SP-013/016 물리 측정 (별도 환경 확보 시)

---

## 2026-04-19 (세션 33) — Phase 15 Auth Advanced Step 3·4·5 (JWKS + TOTP MFA + WebAuthn)

**대화 토픽 흐름**: 사용자 "journal-2026-04-19.md 읽고, 확인한다음에 모든 우선순위 순차적 진행" → 세션 30~32 저널 정독 → 세션 32 working tree(타 터미널 커밋 대기)에 **세션 33 코드 누적** 방식 선택 → Step 3 JWKS → Step 4 TOTP MFA → Step 5 WebAuthn 연속 진입(각 완결 후 배포·E2E).

### 세션 진행

**탐색 단계** (~15분)
- 세션 32 커밋 확인 (`30ee18d feat(auth): Phase 15 Step 1-2`) → 내 작업은 HEAD 위에 누적
- Blueprint §7.2.1 JWKS grace / §7.2.2 Session 인덱스 / §7.2.3 argon2 정독
- SP-014 결과 문서 재확인 — "3분 grace 는 엔드포인트 측 구·신 키 동시 서빙으로 성립" 재확인

**Step 3 — JWKS endpoint (ES256 + grace)** (소요 ~1h)
- **3A**: `prisma/schema.prisma` `JwksKey` + `JwksStatus` enum 추가 (kid unique / publicJwk Json / privateJwk Json / status CURRENT|RETIRED / createdAt / rotatedAt / retireAt, `@@index([status])` + `@@index([retireAt])`)
- migration `20260419130000_add_jwks_keys` 수동 작성 → `migrate deploy` 성공
- **3B**: `src/lib/jwks/generate.ts` — `generateKeyPair("ES256")` + `exportJWK` + kid=16byte hex + `use/alg/kid` 주입
- `src/lib/jwks/store.ts` — getSigningKey(lazy seed), getActivePublicJwks (CURRENT OR RETIRED AND retireAt>NOW), getPublicKeyByKid, rotateKey(graceSec 기본 24h 4m), cleanupRetiredKeys
- **3C**: `src/app/api/.well-known/jwks.json/route.ts` — GET CURRENT+grace 키 JSON + Cache-Control `public, max-age=180, stale-while-revalidate=600` + MIME `application/jwk-set+json`
- **3D**: `src/lib/auth.ts` ES256 전환 + HS256 legacy fallback (kid 헤더 유무로 분기, decodeProtectedHeader 기반) — 기존 HS256 토큰 자연 만료 허용
- **타입 이슈**: jose v6에서 `KeyLike` export 제거 → `CryptoKey` 로 대체
- **3E**: `src/lib/jwks/generate.test.ts` (4 신규 PASS — JWK shape, kid 유니크, sign/verify round-trip, 다른 키 거부) → vitest 139→**143 PASS**
- `/ypserver prod --skip-win-build` 배포 → 1차 로그인 시 JWKS lazy seed (1 key, kid=047353...) → 쿠키 JWT 헤더 `alg=ES256 kid=047353...` → `/api/auth/me` HTTP 200 (ES256 verifySession 경로 실증)

**Step 4 — TOTP MFA** (소요 ~2h)
- **4A**: schema 3 모델 — `User.mfaEnabled` 추가 / `MfaEnrollment` (userId uniq, secretCiphertext AES-256-GCM, confirmedAt, failedAttempts, lockedUntil) / `MfaRecoveryCode` (codeHash SHA-256, usedAt) + migration `20260419140000_add_mfa_tables`
- **4B**: `otplib@^12.0.1` + `qrcode@^1.5.4` 설치. **otplib v13 초기 설치 시 Noble crypto plugin 필요 breaking** → v12 다운그레이드 (Blueprint 사양 일치)
- `src/lib/mfa/totp.ts` — generateTotpSecret / buildOtpAuthUrl / buildOtpAuthQrDataUrl / verifyTotpCode(window=1) / generateRecoveryCodes(10 × XXXXX-XXXXX base32-32자) / normalizeAndHashRecoveryCode
- **4C**: `src/lib/mfa/crypto.ts` — AES-256-GCM encryptSecret(base64url(nonce12||tag16||ct)) + decryptSecret + hashRecoveryCode(SHA-256 hex) + safeEqualHash(timingSafeEqual buffer)
- 관련 타입 이슈: otplib v12 `HashAlgorithms` export는 `@otplib/core` 서브경로에 있음
- **4D**: `src/lib/mfa/challenge.ts` — 5분 TTL HS256 JWT (`purpose=mfa_challenge` 고정, access/refresh 혼용 방지)
- `src/lib/mfa/service.ts` — `verifyMfaSecondFactor(userId, {code|recoveryCode})` — totp 분기(decrypt→verify) + recovery 분기(hash 조회→usedAt 기록) + failedAttempts 증가/리셋
- API 라우트 5건: `/api/v1/auth/mfa/{enroll,confirm,disable,challenge}` + `/api/admin/users/[id]/mfa/reset` (withRole ADMIN 감사 로그 MFA_ADMIN_RESET)
- **초기 경로 결정 수정**: 처음 `/api/auth/mfa/*`로 구현 → proxy.ts CSRF 체크(`/api/*`만 면제는 `/api/v1/*`뿐)로 인해 Bearer 테스트 403 → `/api/v1/auth/mfa/*`로 이동 (Blueprint와도 일치)
- `/api/v1/auth/login` 확장: password 검증 후 `user.mfaEnabled && enrollment.confirmedAt` 체크 → `mfaRequired + challenge + challengeExpiresIn` 반환
- **4E**: `src/lib/mfa/totp.test.ts` (13 신규 PASS — secret/URL/verify 3개, recovery 2개, AES round-trip 3개, safeEqualHash 3개 등) → **156 PASS**
- `.env` `MFA_MASTER_KEY` 64 hex 생성 + `~/dashboard/.env` append (`pm2 restart --update-env`)
- **프로덕션 E2E 6건**: enroll → confirm (recovery 10개 발급 + DB 확인: mfa_enabled=t, length(secret_ciphertext)=59, recovery count=10) → mfaRequired login (challenge 207 chars, expiresIn 300, accessToken 미포함) → TOTP challenge (mfaMethod=totp, accessToken 발급) → recovery challenge (mfaMethod=recovery, usedAt 1건) → 동일 recovery 재사용 거부 INVALID_CODE. DB 정리로 kimdooo 계정 원상복구 (mfa_enabled=f)

**Step 5 — WebAuthn / Passkey** (소요 ~1.5h)
- **5A**: `WebAuthnAuthenticator` (credentialId uniq / publicKey Bytes / counter BigInt / transports String[] / deviceType single|multi / backedUp / friendlyName) + `WebAuthnChallenge` (challenge uniq / purpose registration|authentication / userId? / expiresAt) + migration `20260419150000_add_webauthn_tables` — migrate deploy 성공
- **5B**: `@simplewebauthn/server@10.0.1` 설치. v10 API 구조 변화 확인 (`registrationInfo.credentialID/credentialPublicKey/counter` 평탄 / `verifyAuthenticationResponse.authenticator`(old 명칭 유지)) — d.ts 실측이 Stack Overflow보다 빠름
- `src/lib/mfa/webauthn.ts` — createRegistrationOptions (excludeCredentials + challenge DB 기록) / verifyRegistration / persistAuthenticator (transports는 response 값 사용) / createAuthenticationOptions (allowCredentials scope) / verifyAuthentication (counter bump + lastUsedAt) / consumeChallenge (OTP-like single-use) / cleanupExpiredChallenges
- RP 설정: production=`stylelucky4u.com` / localhost 자동 분기, `WEBAUTHN_RP_ID/ORIGIN/RP_NAME` 환경변수 오버라이드
- **5C**: API 4건 `/api/v1/auth/mfa/webauthn/{register-options,register-verify,assert-options,assert-verify}` — register-verify 는 clientDataJSON 에서 challenge 복구 후 consumeChallenge + verifyRegistration + persistAuthenticator 원자 연쇄 / assert-verify 는 MFA challenge 토큰 + WebAuthn clientDataJSON challenge 교차 검증 → access+refresh 발급
- `/api/v1/auth/login` 추가 확장: `hasTotp || hasPasskey` (Promise.all `findUnique` + `count`) → `mfaRequired + methods:["totp","recovery","passkey"] + challenge`
- **프로덕션 E2E**: `register-options` POST 200 OK (rp.id=stylelucky4u.com, challenge 43 chars, 3 pubKey algs, residentKey=preferred) + DB webauthn_challenges 레코드 purpose=registration 기록. 브라우저 `navigator.credentials.create/get()` 필요한 full round-trip 은 대시보드 UI 연동 후속 세션.

### 변경 사항

**신규 파일 (18건)**:
- `prisma/migrations/20260419130000_add_jwks_keys/migration.sql` · `20260419140000_add_mfa_tables/migration.sql` · `20260419150000_add_webauthn_tables/migration.sql`
- `src/lib/jwks/generate.ts` · `store.ts` · `generate.test.ts`
- `src/lib/mfa/crypto.ts` · `totp.ts` · `totp.test.ts` · `challenge.ts` · `service.ts` · `webauthn.ts`
- `src/lib/schemas/mfa.ts`
- `src/app/api/.well-known/jwks.json/route.ts`
- `src/app/api/v1/auth/mfa/{enroll,confirm,disable,challenge}/route.ts` (4건)
- `src/app/api/v1/auth/mfa/webauthn/{register-options,register-verify,assert-options,assert-verify}/route.ts` (4건)
- `src/app/api/admin/users/[id]/mfa/reset/route.ts`

**수정 파일 (5건)**:
- `prisma/schema.prisma` — JwksKey + JwksStatus enum / MfaEnrollment + MfaRecoveryCode + User.mfaEnabled / WebAuthnAuthenticator + WebAuthnChallenge + User.authenticators
- `src/lib/auth.ts` — ES256 createSession + verifySession(kid 분기) + HS256 legacy fallback
- `src/app/api/v1/auth/login/route.ts` — MFA 분기 (totp | passkey) + challenge 토큰 반환
- `package.json` / `package-lock.json` — `otplib@12.0.1`, `qrcode@1.5.4`, `@types/qrcode`, `@simplewebauthn/server@10.0.1`

**환경 변수 신규 (WSL2 `~/dashboard/.env`)**:
- `MFA_MASTER_KEY` (64 hex, `crypto.randomBytes(32)`) — AES-256-GCM 마스터키

### 주요 논의

- **otplib v12 vs v13**: v13이 Noble crypto plugin 의무 breaking — Blueprint `otplib@12.x` 명시와 일치하게 다운그레이드. 유사하게 `@simplewebauthn/server@10.x` 는 안정 상태로 v10 유지 (v11 호환 시 후속)
- **MFA 경로 규칙 교훈**: `/api/v1/*` 만 CSRF 면제. Bearer 기반 MFA 관리 엔드포인트는 반드시 `/api/v1/auth/mfa/*` — 이를 통해 proxy.ts CSRF·Referer 체크 우회 + Bearer 인증 경로 통일
- **challenge 기반 2FA 설계**: 1차 password 통과 후 accessToken 직접 발급 대신 short-lived(5분) JWT `purpose=mfa_challenge` 반환 → 2차 factor(totp/recovery/passkey) 검증 시 재교환. `purpose` 필드로 access/refresh 혼용 방지
- **Passkey 자격증명 저장소**: `WebAuthnChallenge` 테이블 선택 — 단일 노드에서는 Redis 없이 PG 충분. Blueprint에도 "다중 노드 시 Redis 이관" 명시
- **JWKS grace 정책**: rotateKey(graceSec 기본값) = `24h(token TTL) + 180s(cacheMaxAge) + 60s(margin)` — SP-014 판정 "endpoint-side grace" 코드화

### Compound Knowledge 후보 (3건)

1. `2026-04-19-otplib-v13-breaking-noble-plugin.md` — otplib v13 major는 `authenticator` export 제거 + `@noble/hashes`/`@scure/base32` crypto plugin 등록 강제. 기존 12.x와 완전 비호환 — 메이저 업그레이드 금지 트리거
2. `2026-04-19-simplewebauthn-v10-api-shape.md` — `registrationInfo.credentialID/credentialPublicKey` 평탄화, `verifyAuthenticationResponse.authenticator` 필드는 여전히 singular, `@simplewebauthn/types` 서브패키지 분리. 타입 import 위치 주의
3. `2026-04-19-mfa-challenge-token-2fa-pattern.md` — 1차(password) → `purpose=mfa_challenge` short-lived JWT → 2차(totp/recovery/passkey) 교환 패턴. accessToken 발급은 반드시 2차 검증 후, WebAuthn 경로는 challenge 이중 검증(MFA challenge JWT + clientDataJSON challenge)

### 커밋 계획

세션 33 단일 커밋:
- 신규 18 + 수정 5 + package-lock
- 메타 5건 (current / 2026-04 / journal-04-19 / handover/세션33 / handover/_index / next-dev-prompt)

### 다음 세션 (34 진입)

**우선순위 1**: Phase 15 Step 6 — Rate Limit 보강 (4h) — `rate-limiter-flexible@5.x` PG 어댑터 + 기존 in-memory + MFA `lockedUntil` 활용. Blueprint §8.3 참조

**우선순위 2**: MFA UI — 설정 페이지(Passkey 등록 브라우저 `navigator.credentials.create`) + 로그인 페이지 2FA 화면 (methods 리스트 기반 분기). 서버측 E2E는 완결, UI 구현 시 full-stack WebAuthn round-trip 검증

**우선순위 3**: SP-013/016 물리 측정 (별도 환경 확보 시)

**우선순위 4**: Compound Knowledge 3건 작성 (otplib breaking / SimpleWebAuthn v10 / MFA 챌린지 패턴)



---

## 세션 34 — Phase 15 UI 통합

### [1] 사용자 요청: 다음 세션 우선순위 모두 순차 진행

> **사용자**: "다음세션 우선 순위 모두 순차적으로 진행 .... ● Step 5 완료 요약 (WebAuthn / Passkey)... 다음 세션 우선순위: 1. Step 6 Rate Limit 보강 (4h) — DB-backed + MFA lockedUntil 활용 / 2. Phase 15 UI 통합 — MFA 등록·로그인 대시보드 페이지 (Passkey 브라우저 검증) / 3. SP-013 wal2json (5h), SP-016 SeaweedFS 50GB (8h) — 별도 환경"

**논의**: 세션 33이 commit `58a517b`로 Step 3·4·5·6 백엔드를 모두 단일 커밋에 통합한 상태로 본 세션 시작. git status는 본 세션 직전 상태에서 일부 untracked만 보였으나 (JWKS migration 등) 알고 보니 이미 커밋되어 있었음 (병렬 터미널 통합 커밋). 본 세션에서는 Step 6 잔여 + UI 통합 + 마감을 처리.

**결론**: TaskCreate로 7 task 분할 — Step 6.1 lockedUntil / 6.2 DB rate limit 라이브러리 / 6.3 라우트 적용 / 6.4 검증 / 5 UI / 6 SP-013/016 deferral / 7 커밋·인수인계.

### [2] Step 6.1 — MFA verify lockedUntil 임계 로직 추가

기존 `verifyMfaSecondFactor`가 lockedUntil 체크는 하고 있었으나 **실패 임계값 도달 시 lockedUntil 설정 로직이 누락**된 것 발견.

**논의**: 락 정책을 어떻게 분리할지 — service.ts에 인라인 vs 순수 함수 분리. 기존 totp.test.ts가 순수 함수만 다루는 패턴이라 분리 채택.

**결론**: `src/lib/mfa/lock-policy.ts` 신규 (computeLockedUntil + getLockPolicy + 환경변수 오버라이드) + 7건 vitest. service.ts의 `registerFailure`가 atomic increment 후 임계 도달 시 lockedUntil 설정. challenge route에 `Retry-After` 헤더 + lockedUntil ISO timestamp 포함.

**파일**: `src/lib/mfa/lock-policy.ts`, `src/lib/mfa/lock-policy.test.ts`, `src/lib/mfa/service.ts`, `src/app/api/v1/auth/mfa/challenge/route.ts`

### [3] Step 6.2/6.3 — DB-backed Rate Limit + 라우트 적용

설계 결정: PG fixed-window UPSERT 단일 쿼리로 atomic 카운터 운용. RateLimitBucket 모델 신규.

**논의**: 인메모리(rate-limit.ts) vs DB-backed(rate-limit-db.ts) — 점진 마이그레이션. handler가 사용할 `applyRateLimit` 가드 헬퍼 추가 (IP + email/user 두 차원 분리).

**결론**:
- `prisma/migrations/20260419160000_add_rate_limit_buckets/` 신규 (TIMESTAMP(3) + INDEX windowStart)
- `src/lib/rate-limit-db.ts` (checkRateLimitDb + cleanupExpiredRateLimitBuckets + buildBucketKey, 12 vitest PASS)
- `src/lib/rate-limit-guard.ts` (applyRateLimit + getClientIp, fail-closed)
- 라우트 3건 적용: login (max=10/min, IP+email) / mfa-challenge (max=20/min, IP) / webauthn-assert-verify (max=20/min, IP)

### [4] Step 6.4 — 라이브 디버깅 #1: proxy 인메모리 vs handler DB 충돌

배포 후 curl 12회 시도에서 6번째에 429 차단됨. max=10인데 5+1에서 차단 → 의외.

**논의**: DB 카운터 직접 조회 결과 hits=5만 기록 → 6~12회 요청은 **DB rate limit code 도달 전에 다른 곳에서 429** 반환된 것. proxy.ts 검사 결과 인메모리 `RATE_LIMITS.v1Login = 5/min`이 동일 경로에 적용 중.

**결론**: defense-in-depth가 의도와 달리 작동 — 두 layer가 같은 정책을 다른 store로 시행하면 더 빡빡한 쪽이 보이는 동작 결정. `HANDLER_OWNED_RATE_LIMIT_PATHS` Set 신설로 v1 auth 경로 3건을 handler에 양도. single source of truth per route. 재배포 후 정확히 11번째에 429 차단 확인.

**파일**: `src/proxy.ts` (HANDLER_OWNED_RATE_LIMIT_PATHS Set 추가)

### [5] Step 6.4 — 라이브 디버깅 #2: PG TIMESTAMP(3) timezone-naive + JS Date 9시간 오프셋

`Retry-After: 32453s` (약 9시간) 발견. windowMs=60000인데 60s가 아닌 9시간이라니 명백한 시간대 버그.

**논의**: PG `TIMESTAMP(3)` (TIMESTAMPTZ 아님) + Prisma의 JS Date 변환 → JS Date가 UTC 값을 KST로 해석하여 9시간(UTC+9 KST) 오프셋 누적. windowStart - Date.now() 계산이 -9시간 → windowMs - elapsed = 60000 + 32400000 = 32460000ms.

**결론**: 클라이언트 elapsed 계산 폐기. PG가 직접 잔여 ms 계산하도록 변경: `EXTRACT(EPOCH FROM (window_start + INTERVAL - NOW())) * 1000`. NUMERIC → string 직렬화이므로 `parseFloat` 필수. 재배포 후 `Retry-After: 60` 정확.

**원칙**: "시간 계산은 가능한 한 DB 한 곳에 위임 — 클라이언트 시간대 변환이 끼어들면 디버깅 지옥"

**파일**: `src/lib/rate-limit-db.ts`, `src/lib/rate-limit-db.test.ts` (모킹 형식 변경)

### [6] Phase 15 UI — 통합 보안 페이지 + 로그인 MFA 분기

**논의**: 위치 결정 — `/settings/mfa` (admin 전용) vs `/account/security` (모든 사용자). MFA는 모든 사용자가 자기 보안 관리해야 하므로 `(admin)` 그룹 **밖**에 배치. `(protected)/layout.tsx`의 `requireSession()` 한 단계만 통과.

**구현**:
- 신규 API 2건: `GET /api/v1/auth/mfa/status` (TOTP+Passkey+recovery 단일 호출), `DELETE /api/v1/auth/mfa/webauthn/authenticators/[id]` (userId 매칭)
- `(protected)/account/security/page.tsx` 통합 페이지 (TOTP 3-stage stepper + Passkey list/enroll/delete + Disable details)
- 로그인 페이지 `MfaState` discriminated union (password|challenge) + Method Tabs + Passkey `startAuthentication()` 통합
- 사이드바 "내 계정" 그룹 신설 (Lock 아이콘) + GROUP_ORDER 추가
- `@simplewebauthn/browser@^10` 설치

**검증**: tsc 0 / vitest 175 PASS / WSL 빌드 4회 / curl smoke (307/401/200).

**결론**: 코드 완결. 브라우저 `navigator.credentials.create/get()` round-trip은 사용자 인터랙션 필요 → 다음 세션 위임.

### [7] SP-013 / SP-016 deferral

**논의**: SP-013은 PG + wal2json 익스텐션 + 30분 DML 부하, SP-016은 50GB 디스크 + B2 오프로드 — 둘 다 별도 환경 필요. 본 세션 환경에서 즉시 진행 불가.

**결론**: deferral 명시. _SPIKE_CLEARANCE.md의 Pending 엔트리 유지. 다음 세션 위임.

### [8] 마감 — 통합 커밋 + push

**논의**: git status 분석 결과 본 세션 산출물(account/security 페이지, status API, authenticators DELETE API, login page, sidebar, package.json) 모두 untracked/M. 세션 33 commit `58a517b`이 이미 Step 3·4·5·6 백엔드 통합한 상태였으니 본 세션은 UI 통합만 단일 커밋.

**결론**: commit `9a6b4ff` (12 files, +1,144/-107) + origin push 완료. handover/260419-session34-phase15-ui-and-mfa-status.md 신규 작성. current.md / logs / next-dev-prompt / handover/_index 모두 동기화.

**파일**: `docs/handover/260419-session34-phase15-ui-and-mfa-status.md` (신규), `docs/handover/_index.md`, `docs/handover/next-dev-prompt.md`, `docs/logs/2026-04.md`, `docs/status/current.md`

---

## 2026-04-19 (세션 35) — 세션 34 위임 4건 순차 처리

**대화 토픽 흐름**: 사용자 "다음 세션 사항 모두 순차적 진행" → next-dev-prompt 우선순위 1~4 확인 → 태스크 5개(MFA UI QA / Cleanup cron / SP-013·16 / CK 6건 / 세션 종료) 생성 → 순차 실행.

### [1] MFA UI QA — 체크리스트 문서화 결정

**논의**: next-dev-prompt 우선순위 1 "MFA UI 브라우저 round-trip 검증" 은 WebAuthn `navigator.credentials.create/get()` 사용자 Touch ID/Windows Hello 입력 필수 → 본 세션(헤드리스) 자동화 불가. 사용자 메모리 "자율 실행 우선, 파괴적 행동만 예외" → 분기 질문 없이 **다음 세션 직접 실행용 SOP 문서화** 로 전환.

**결론**: `docs/guides/mfa-browser-manual-qa.md` 신규 — 8 시나리오(TOTP Enroll / Login MFA TOTP / Passkey Enroll / Passkey Login / Passkey Delete / Recovery 재사용 / Rate Limit / Disable) × (단계 + DOD + 실패 진단 순서 + 관련 CK). `guides/README.md` 엔트리 추가.

### [2] Cleanup Scheduler — cron registry와 분리

**논의**: 후보 2가지 — (A) 기존 `cron/registry.ts` 에 "INTERNAL" kind 추가 / (B) 신규 `cleanup-scheduler.ts` 신설. 선택: B.
- 근거: cron registry 는 DB `CronJob` 레코드 기반 (UI CRUD) SQL/FUNCTION/WEBHOOK 한정. cleanup 은 시스템 내부 유지보수 → 코드 하드코딩 + 함수 호출이 더 단순·안전. 두 개념 혼합 시 UI CRUD에서 내부 task 오작동 유발 가능.

**구현**:
- `computeCleanupWindow` 순수 함수 — UTC 연산만. 시스템 timezone 의존 제거 (세션 34 TIMESTAMP-naive 버그 교훈 반영).
- `runCleanupTasks(tasks)` — 각 task 독립 try/catch, 한 개 실패 (ERROR: msg) 가 뒤 task 블로킹 안 함.
- `ensureCleanupScheduler()` 멱등 — 1분 tick + lastRunKey dedupe + 기동 후 5초 1회 tick (03:00 KST 기동 시 수 초 내 실행 보장).
- 4종 cleanup 함수 (sessions/rate-limit-buckets/jwks-retired/webauthn-challenges) 어댑터로 정규화 → 삭제 행 수 반환.
- Audit 기록 2중 try/catch — audit 실패가 cleanup 차단 안 함.
- `runCleanupsNow()` export — 후속 /api/admin/cleanup/run 용.

**테스트 13 case**: computeCleanupWindow 7 (KST 경계/월경계/커스텀) + runCleanupTasks 6 (summary/실패격리/string throw/빈 배열/순서 보존/상수).

**instrumentation.ts 통합**: `ensureStarted()` (cron registry) 다음 줄에 `ensureCleanupScheduler()` 추가.

**배포**: `/ypserver prod --skip-win-build` — Phase 2-1(PM2 online 확인) → 2-2(Prisma pending 0) → 2-3(WSL 빌드 OK) → 2-4(Drizzle migrations applied) → 2-5(pm2 restart dashboard) → 2-6(Tunnel OK) → Phase 3-1(curl localhost:3000 → 307 로그인 리다이렉트). PM2 로그 Next.js Ready 79ms / 예외 0.

**결론**: vitest 175→188 PASS (+13 회귀 0), tsc 0, 프로덕션 배포 통과. 첫 실행은 KST 03:00 익일.

### [3] SP-013 / SP-016 deferral 유지

**논의**: 물리 측정 환경 미확보 — wal2json 익스텐션 + 30분 DML 부하 / 50GB 디스크 + B2 오프로드. 본 세션도 환경 동일.

**결론**: `_SPIKE_CLEARANCE.md` 마지막 2행 `Pending (축약)` 확인만. 수정 불필요.

### [4] Compound Knowledge — 2건 기작성 확인 + 4건 Agent 병렬 작성

**기작성 확인**: 세션 34 종료 시점에 이미 unstaged 로 존재하던 CK 2건을 본 세션에서 확인:
- `2026-04-19-pg-timestamp-naive-js-date-tz-offset.md` (세션 34 §3.2)
- `2026-04-19-rate-limit-defense-in-depth-conflict.md` (세션 34 §3.1)

두 CK 모두 frontmatter + 증상/원인/해결/재발 방지/관련 구조 갖춤. 본 세션 커밋에 포함.

**신규 4건 Agent 2대 병렬 발사**:
- Agent A (세션 33 외부 라이브러리 함정 2건): otplib-v13-breaking-noble-plugin (142줄) + simplewebauthn-v10-api-shape (203줄)
- Agent B (세션 33+32 설계 패턴 2건): mfa-challenge-token-2fa-pattern (183줄) + bcrypt-argon2-progressive-rehash-merged-update (175줄)

**Agent 완료 후 수정**: Agent B가 추가한 `상위: [solutions/_index.md](./_index.md)` 링크가 broken (해당 파일 부재) → 2건에서 제거. Agent A는 기존 CK 패턴 관찰 후 생략 판단 — 표준화를 Agent A 기준으로.

**결론**: CK 신규 4건 총 703줄. 누적 12→23건.

### [5] 세션 종료

**문서 갱신**: current.md (세션 35 행 추가) + logs/2026-04.md (세션 35 섹션) + journal-2026-04-19.md (본 섹션) + handover/260419-session35-cleanup-scheduler-ck-batch.md (신규) + handover/_index.md (세션 35 엔트리) + handover/next-dev-prompt.md (갱신).

**커밋 + push**: 단일 커밋 후 origin/main push 예정.


---

## 2026-04-19 (세션 36) — Phase 15-D Refresh Token Rotation + Cleanup 수동 실행 UI

**대화 토픽 흐름**: 사용자 "다음 세션 우선 순위 모두 순차적 수행 + 이전 세션들 미실행건도 확인" → P1(MFA UI)/P5(SP-013/016) 환경 제약 명시 후 P4→P2→P3 순차 실행 → 배포 → E2E 9시나리오 → 세션 마감.

### 세션 진행

**P4 — /api/admin/cleanup/run API + Settings UI** (~45분)

- `src/lib/cleanup-scheduler.ts` 확장: `CleanupActor` 인터페이스 + `runCleanupsNow(actor?)` — actor 생략 시 자동 스케줄러 기존 행동 유지(하위 호환). audit detail에 `{ actor, summary }` 병합.
- `src/app/api/admin/cleanup/run/route.ts` — withRole(["ADMIN"]) + `extractClientIp(headers)` + try/catch 500 CLEANUP_FAILED.
- `src/app/(protected)/(admin)/settings/cleanup/page.tsx` — 자동 스케줄 설명 카드 + 즉시 실행 카드(확인 다이얼로그 + busy state + 결과 표). task 이름 한국어 라벨링, 숫자→`N행 삭제`(초록) / 문자열→`ERROR: ...`(빨강 break-all).
- `src/components/layout/sidebar.tsx` — `Trash2` 아이콘 import + 감사·설정 그룹에 엔트리 + ADMIN_ONLY_PATHS 확장.

**P2 — 수동 트리거 조기 검증** (~10분)

배포 후 curl 매트릭스: POST /api/admin/cleanup/run → 200 success + summary `{sessions:0, rate-limit:0, jwks:0, webauthn-challenges:1}`. 세션 33/35 이후 5분 만료된 WebAuthn challenge 1건이 실제로 정리됨. audit log: `CLEANUP_EXECUTED_MANUAL | {"actor":{"userId":"c0c0b305...","email":"kimdooo@stylelucky4u.com","ip":"::1"},"summary":{...}}` 확인.

**P3 — Phase 15-D Refresh Token Rotation** (~2h)

- 설계 결정:
  - 토큰: opaque 32 bytes hex (64 chars) + SHA-256 hash 저장 (JWT stateless 포기, 서버 제어 확보)
  - 쿠키: v1_refresh_token 유지 (이름 호환)
  - 만료: 7일 유지
  - Rotate 전략: refresh 매 회 새 토큰 + 구 토큰 soft revoke (revokedAt)
  - Reuse 탐지: 구 토큰 재사용 시 `revokeAllUserSessions(userId)` (defense-in-depth, 다른 기기까지 차단)
- 신규 파일:
  - `src/lib/sessions/tokens.ts` (196줄) — generateOpaqueToken / hashToken / issueSession / findSessionByToken / rotateSession / revokeSession / revokeAllUserSessions / listActiveSessions / touchSessionLastUsed + 상수
  - `src/lib/sessions/login-finalizer.ts` (77줄) — `finalizeLoginResponse` 로 3 경로(login/challenge/assert-verify) 공통화. 내부에서 accessToken 생성 + Session insert + SESSION_LOGIN audit + 쿠키 세팅.
  - `src/lib/sessions/tokens.test.ts` (+8 tests 회귀 0)
  - `src/app/api/v1/auth/refresh/route.ts` — rate limit 60/min + reuse 탐지 분기 + SESSION_REUSE_DETECTED + rotateSession + SESSION_ROTATE audit
  - `src/app/api/v1/auth/sessions/route.ts` — GET withAuth, current 플래그 계산
  - `src/app/api/v1/auth/sessions/[id]/route.ts` — DELETE withAuth, userId 매칭 강제(404 은폐)
- 수정 파일:
  - login/challenge/assert-verify 3개 → finalizeLoginResponse 한 줄로 대체 (중복 ~30줄씩 제거)
  - logout route → 쿠키 Session 조회 + revoke + SESSION_REVOKE audit (기존 쿠키 제거만에서 업그레이드)
  - `/account/security` 페이지 → 활성 세션 카드 3번째 (IP/User-Agent/시간 정보 + 현재 세션 배지 + 종료 버튼)
  - `sidebar.tsx` 로그아웃 → `Promise.allSettled([v1 logout, dashboard logout])` 병행
  - `jwt-v1.ts` 미사용 export 정리 — createRefreshToken/verifyRefreshToken/RefreshTokenPayload/REFRESH_MAX_AGE/getRefreshSecret 제거
- 감사 이벤트 4종 신규: SESSION_LOGIN / SESSION_ROTATE / SESSION_REVOKE / SESSION_REUSE_DETECTED

**배포 + E2E** (~15분)

- `/ypserver prod --skip-win-build`: Prisma "No pending migrations" / WSL 빌드 성공 / Drizzle migrated / PM2 restart ↺=1 / Tunnel OK / HTTP 307.
- curl E2E 9 시나리오 전 PASS:
  1. LOGIN → opaque 쿠키 64 hex + DB sessions 1행 (ip=::1, user_agent=curl/8.5.0)
  2. GET /sessions → 1건 current:true
  3. POST /refresh → 새 access + 새 쿠키, DB 구 revoked + 신 insert
  4. POST /refresh (구 쿠키) → 401 SESSION_REVOKED + SESSION_REUSE_DETECTED audit + revokeAllUserSessions(1) 실행(신 세션까지 revoke)
  5. 재로그인 + 2개 세션 생성 (UA 구분)
  6. DELETE 타 세션 → 200 + 활성 1건 감소
  7. POST /logout → 200 + 쿠키 제거 + SESSION_REVOKE audit
  8. POST /refresh (logout 후) → 401 SESSION_REVOKED
  9. Audit 누적: SESSION_LOGIN×3 / SESSION_REVOKE×2 / SESSION_REUSE_DETECTED×2 / SESSION_ROTATE×1 / CLEANUP_EXECUTED_MANUAL×1

### 산출물

- 신규 9 파일 (소스 7 + 핸드오버 1 + 스킴아님)
- 수정 8 파일 (소스 6 + 문서 2)
- tsc 0 / vitest 188→201 PASS (+13, 회귀 0) — `/cs` 단계에서 revokeAllExceptCurrent 5 mock 테스트 추가
- 프로덕션 E2E 9 시나리오 전 PASS

### 미실행 (사용자 위임)

- P1 MFA UI 브라우저 round-trip — Touch ID/Hello 생체 인증 필수, Phase 15-D 활성 세션 카드 검증 포함해 다음 세션 1-2h
- P5 SP-013 wal2json / SP-016 SeaweedFS — 환경 미확보, Pending 유지

### 커밋 + push

세션 기능 커밋 `9363409` (22 files, +1504/-200) + origin/main push 완료. `/cs` 실행 시 Compound Knowledge 1건 작성(`2026-04-19-opaque-refresh-rotation-reuse-detection.md`, architecture/high) + 문서 정합성 보강 — **CK 누적 23→24건**.

---

## 2026-04-19 (세션 38) — Phase 15-D 보강 (touch throttle + activity fingerprint)

**대화 토픽 흐름**: 사용자 "/kdyguide" → 4-Phase 스코어링 → "모두 너에게 일임" 자율 실행 → Phase 15-D 보강 5개 중 touch throttle + activity fingerprint 2개 선택 → TDD → 배포 → E2E curl → Playwright MCP 브라우저 자동화 → 마감.

### 세션 진행

**자율 의사결정** (소요 ~3분)
- `/kdyguide` Phase 0~3 실행 완료 — 추천 6개, DAG 제시
- 사용자 "모두 너에게 일임" → brainstorming 축약
- 5개 보강 중 선정 기준: 가장 작고 독립적이며 낮은 리스크 → touch throttle + activity fingerprint 확정
- HS256 제거 / admin forced revoke / SESSION_EXPIRE audit 는 별도 세션 이관

**구현** (소요 ~15분)
- 신규 `src/lib/sessions/activity.ts` — pure function 2개 (shouldTouch 60s 디바운스 + parseUserAgent regex)
- TDD: `activity.test.ts` 25 테스트 먼저 작성
- 1회 fail: iOS Safari UA `Version/17.0 Mobile/15E148 Safari/604.1` 에서 중간 `Mobile/` 토큰 때문에 regex 불매치 → "Safari/ 토큰 존재 + Chrome/Edge 부재" 조건으로 수정 → 25/25 PASS
- `tokens.ts` SessionLookup/ActiveSessionSummary 타입 확장 + listActiveSessions 매핑
- `sessions/route.ts` 무조건 touch → shouldTouch 분기
- `/account/security` 카드 UI — 라벨 표시 + raw UA title 툴팁

**검증** (소요 ~3분)
- tsc --noEmit: 0 에러
- vitest: 206 → **231 PASS** (+25, 회귀 0)

**배포** (소요 ~3분)
- `/ypserver prod --skip-win-build` 통과
- "No pending migrations" (세션 37 revoked_reason 이미 적용)
- PM2 restart ↺=4 / online / HTTP 307

**프로덕션 E2E curl** (소요 ~2분 + 70초 대기)
- 시나리오 1 — throttle: lastUsedAt 3초 후 동일 (01:53:26.159Z → 01:53:26.159Z) ✓
- 시나리오 2 — userAgentLabel: curl/8.5.0 → "curl 8" ✓
- 시나리오 3 — touch: 70초 후 갱신 (01:53:26.159Z → 01:54:42.042Z, diff 76초) ✓

**Playwright MCP 브라우저 자동화** (소요 ~3분)
- browser_navigate /login → browser_fill_form → browser_click "로그인" → 리다이렉트 /
- browser_navigate /account/security
- browser_snapshot → 활성 세션 카드 3건 확인:
  - 현재 세션 (118.33.222.67): "Chrome 147 · Windows" 라벨 + "현재 세션" 배지 ✓
  - curl 세션 2건 (118.33.222.67, ::1): "curl 8" 라벨 ✓
  - raw UA (Mozilla/5.0 Windows NT 10.0 ... Chrome/147 ...) 가 `title` 속성에 보존 ✓
- browser_take_screenshot fullPage → `e2e-session38-account-security.png` (gitignore `e2e-*.png` 패턴)

### 산출물

- 신규 2 파일: `activity.ts` / `activity.test.ts`
- 수정 3 파일: `tokens.ts` / `sessions/route.ts` / `security/page.tsx`
- vitest 206 → 231 PASS (+25 회귀 0)
- 커밋 `b454e3c feat(auth): 세션 38 — Phase 15-D 보강 (touch throttle + activity fingerprint)`

### 미실행 (이월)

- P15-D 보강 남은 3건: SESSION_EXPIRE audit / 관리자 forced revoke / HS256 legacy 제거
- P1 MFA UI biometric 브라우저 수동 QA (세션 37 이월)
- P2 KST 03:00 자동 cleanup 관찰 (익일)
- P4 SP-013/016 물리 측정 (환경 미확보)

### 교훈 / Compound Knowledge 후보

- **pure function 우선 분리**: `shouldTouch` / `parseUserAgent` 를 route handler 안에 섞지 않고 `activity.ts` 단일 모듈로 추출 → 단위 테스트 100% 가능 + 다른 경로에서 재사용 용이. TDD 가 자연스럽게 성립하는 형태.
- **UA 파싱 라이브러리 의존 회피**: ua-parser-js(~20KB) 는 본 프로젝트 식별 범위(주류 브라우저 + curl) 에 과투자. 자체 regex 2 쌍으로 충분. 미탐지 UA 는 "기타 브라우저 · 기타 OS" fallback — 봇 탐지는 범위 밖이라 수용 가능.
- **iOS Safari UA 패턴 함정**: "Version/N ... Safari/N" 사이에 `Mobile/NNN` 같은 토큰이 끼는 경우 regex `Version\/(\d+)[^\s]*\s+Safari` 실패. "Safari 존재 + Chrome/Edge 부재" exclusion 조건이 더 견고.
- **read-only 라벨 변환**: DB 에는 raw UA 만 저장, 응답 시점에 `parseUserAgent` 호출 → 파싱 로직 변경 시 마이그레이션 불필요. 저장/표현 분리 원칙.

### 커밋 + push + CK

코드 커밋 `b454e3c` + 문서 커밋 `d9e68bf` + /cs 단계 CK 추출 커밋 (다음) → origin/main push. **iOS Safari UA regex 함정** 1건만 CK 추출 (bug-fix/high) — `docs/solutions/2026-04-19-ios-safari-ua-regex-trap.md`. pure function + TDD 패턴은 일반론이라 제외. **CK 누적 27 → 28건**.


---

## 2026-04-19 (세션 39) — SESSION_EXPIRE per-row audit + 관리자 forced revoke

**사용자**: "다음 세션 권장 모두 순차적으로 진행 ... 모두 너가 직접 충분한 숙고를 통해 결정하도록"

### [1] 5건 권장 과제 숙고 평가

- **채택 3건**: [2] KST 03:00 자동 cleanup 관찰 · [3a] SESSION_EXPIRE per-row audit · [3b] 관리자 forced revoke
- **이월 2건**: [3c] HS256 legacy 제거 (세션 38 핸드오버가 "단독 세션 권장 — 기존 쿠키 무효화 리스크" 명시 → 잠금 결정 존중) · [1] MFA biometric 브라우저 QA (Touch ID/Windows Hello 필수 → 자율 불가)
- **이월 1건**: [4] SP-013/016 물리 측정 (환경 미확보)
- **이월 1건**: [5] /kdygenesis --from-wave (메타 오케스트레이션 규모 과대)

### [2] Task 1 — 관찰만

audit_logs `timestamp` 컬럼 스키마 혼동 (`created_at` 가정 오류 → `timestamp` INTEGER 초 단위). 자동 `CLEANUP_EXECUTED` 0건 + 수동 `_MANUAL` 1건 (세션 36 분). PM2 uptime 91m 으로 오늘 KST 03:00 tick 미발화 확정. 다음 기회 2026-04-20 03:00.

### [3] Task 2 — cleanup TDD

- 설계: `cleanupExpiredSessions` 반환 타입 `{deleted, expiredEntries}` 로 확장. audit 기록은 scheduler 래퍼에서.
- RED: cleanup.test.ts 10 tests (payload 3 + flow 7) FAIL (`buildSessionExpireAuditDetail` 미존재).
- GREEN: cleanup.ts `prisma.$transaction(findMany → deleteMany)` — PASS.

### [4] Task 3 — admin forced revoke TDD

- `revokeAllUserSessions(userId, reason?)` 시그니처 확장 (default "reuse_detected").
- 신규 route `DELETE /api/admin/users/[id]/sessions` (withRole ADMIN + SESSION_ADMIN_REVOKE_ALL audit).
- RED: tokens.test.ts admin/logout 2 FAIL. GREEN: optional reason 주입 → PASS.

### [5] 배포 + E2E — PG TZ 버그 재현

`/ypserver prod --skip-win-build` 통과. E2E 스크립트 2파일(bash + pg helper) 분리 작성. S1~S7 admin revoke PASS. S10 SESSION_EXPIRE 경로 FAIL — `summary.sessions: 0` 인데 raw SQL 직접은 2 eligible.

디버그 `console.log` 주입 → 재배포 → 확인: Prisma 가 `expires_at` 를 `2026-04-18T11:32:50Z` 로 읽음 (pg 직접 `2026-04-18T02:32:50Z`). **9시간 차이 = KST 오프셋**. 세션 34 CK `pg-timestamp-naive-js-date-tz-offset` 재현.

### [6] 근본 해결 — raw SQL 2-step

```ts
const rows = await prisma.$queryRaw<...>`
  SELECT id, user_id AS "userId", (expires_at::text) AS "expiresAt"
  FROM sessions
  WHERE expires_at < NOW() - INTERVAL '1 day'
`;
const deletedCount = await prisma.$executeRaw`
  DELETE FROM sessions WHERE id = ANY(${ids}::text[])
`;
```

`expires_at::text` 캐스팅으로 adapter 재해석 방지 → UTC 간주 Date 재생성. PG 서버측 NOW()-INTERVAL 로 filter 위임하여 클라이언트 TZ 변환 경로 완전 우회.

### [7] 최종 E2E 통과

rate_limit_buckets 축적 login 차단 → 직접 SQL DELETE 로 초기화 → 재시도 성공:
- S4 admin revoke revokedCount 일치
- S7 SESSION_ADMIN_REVOKE_ALL audit 기록
- S10 summary.sessions=4 (신규 2 + 디버그 누적 2)
- S11 SESSION_EXPIRE audit 5건 표시
- S12 CLEANUP_EXECUTED_MANUAL summary.sessions=4

### 산출물

- 신규 4: `cleanup.test.ts` / `admin/users/[id]/sessions/route.ts` / `scripts/session39-e2e.sh` / `scripts/session39-helper.cjs`
- 수정 4: `cleanup.ts` / `cleanup-scheduler.ts` / `tokens.ts` / `tokens.test.ts`
- vitest 231 → 245 PASS (+14 회귀 0)

### 교훈 / Compound Knowledge 후보

- **PG TIMESTAMP(3) timezone-naive + Prisma 7 adapter-pg = 9h KST 시프트**. filter 는 raw SQL `NOW()-INTERVAL` 서버측 위임으로 안전. 컬럼 TIMESTAMPTZ 마이그레이션이 근본 해결 (기술부채 신규 승격).
- **Per-row audit 패턴**: DB ops 함수는 entries 반환, audit 는 호출자. 집계 + per-row 병행 기록으로 관심사 분리 유지 + 포렌식 능력 추가.
- **optional reason 파라미터 확장**: 기존 호출자 1곳의 동작 유지 + 새 호출자에 공통 로직 재사용. 소프트 확장 원칙.

CK 2건 추출 — `2026-04-19-prisma-orm-tz-naive-filter-gotcha.md` + `2026-04-19-per-row-audit-on-batch-delete.md`. **누적 28 → 30**.

---

## 세션 40 — TIMESTAMPTZ 마이그레이션 + cleanup ORM 복원 시도

### [1] 사용자 지시: 다음 세션 권장 모두 순차 실행

> "다음 세션 권장 모두 순차적으로 실행.. 모두 너가 할 수 있는 한 모두 진행..."

세션 39 next-dev-prompt 6 권장 중 가능 항목 분류:
- ✅ 우선순위 2 (KST 03:00 cleanup 관찰)
- ✅ 우선순위 3b (TIMESTAMPTZ 마이그레이션 — 기술부채 최상위 승격)
- ❓ 우선순위 4 (SP-013/016) — 환경 확인
- ❌ 우선순위 1 MFA biometric (자동화 불가, 사람 필수)
- ❌ 우선순위 3 HS256 제거 (단독 세션 잠금)
- ❓ 우선순위 5 /kdygenesis (스킬 호출)

세션 38의 "단독 세션 잠금" 결정은 글로벌 CLAUDE.md "이전 세션 명시적 정의는 잠금" 규칙으로 보존.

### [2] KST 03:00 cleanup 자동 tick 관찰

dashboard PM2 uptime 2h (KST 13시 시작) → 다음 KST 03:00 tick 익일 미도래. audit_logs `CLEANUP_EXECUTED` (자동) 0건, `CLEANUP_EXECUTED_MANUAL` 다수. 익일 검증 필요.

부수 발견: `audit_logs.timestamp` 컬럼이 `Math.floor(Date.now()/1000)` 즉 sec 단위 저장. UI 코드가 ms 가정으로 표시 시 1970-01-21T... 로 오해. 본 세션 범위 밖, 다음 세션 후속.

### [3] TIMESTAMPTZ 마이그레이션 영향도 분석

- 17개 모델 / ~50개 DateTime 컬럼 영향
- 회피 코드 2곳: `cleanup.ts` (raw SQL + ::text), `rate-limit-db.ts` (EXTRACT EPOCH)
- PG 서버 타임존 = Asia/Seoul (KST). 기존 naive 값 wall-clock 의미가 KST → USING `AT TIME ZONE 'Asia/Seoul'` 정답
- 세션 34 CK 의 옵션 1 (TIMESTAMPTZ 마이그레이션) 채택 시점 — 2회 재현으로 기술부채 최상위 승격

### [4] schema.prisma + migration SQL 작성 + dry-run

- 모든 DateTime 컬럼에 `@db.Timestamptz(3)` 추가 (47개 컬럼)
- `prisma/migrations/20260419180000_use_timestamptz/migration.sql` 작성 (17 ALTER TABLE, USING AT TIME ZONE 'Asia/Seoul')
- pg_dump 백업 (5.3MB) → BEGIN/ROLLBACK dry-run 17 ALTER 모두 OK
- Prisma client 재생성 + tsc 0 + vitest 245 PASS

### [5] cleanup.ts ORM 복원 1차 시도 → 실패

ORM `session.findMany({where:{expiresAt:{lt: jsDate}}})` 로 복원. 단위 테스트 9 PASS. 배포 후 E2E:
- helper INSERT 만료 세션 2건 (past = -25h)
- cleanup 호출 → `summary.sessions: 0` 회귀!
- PG 직접 비교는 expired_1d=t (정상) → ORM binding-side 시프트 가설

### [6] 정공법 발견: raw SELECT + ::text + ORM DELETE

- prisma 7 adapter-pg 가 timestamptz 컬럼에서도 binding/parsing 양방향 9h 시프트 발생함이 E2E 재증명
- **정공법**: cutoff 는 PG 측 NOW()-INTERVAL 위임, `expires_at::text` 캐스팅으로 PG 가 정확한 ISO+offset 문자열 반환, JS new Date() 가 정확 파싱. DELETE 는 ORM `deleteMany({where:{id:{in:ids}}})` (race-safe)
- vitest 244 PASS / tsc 0 / 재배포 (PM2 ↺=11) / E2E summary.sessions=2 정확

### [7] SP-013/016 환경 확인

- wal2json: 미설치 (apt 가능)
- weed: 미설치 (다운로드 가능, 디스크 950G 가용 충분)
- 실측 13h 작업 → 본 세션 범위 외, 다음 세션 권장 유지

### [8] CK 후보 추출

- `prisma-orm-tz-naive-filter-gotcha` 갱신 — TIMESTAMPTZ 만으로는 해소 안 됨, binding-side 시프트 별도. 정공법 = raw SELECT + ::text + PG NOW()-INTERVAL
- `timestamp-to-timestamptz-migration-using-clause` (pattern, high) — USING AT TIME ZONE '<server_tz>' 패턴 정리

### 산출물

- 신규 1: `prisma/migrations/20260419180000_use_timestamptz/migration.sql`
- 수정 4: `prisma/schema.prisma` (47 컬럼 @db.Timestamptz(3)) / `cleanup.ts` (raw SELECT + ::text + ORM DELETE) / `cleanup.test.ts` (10 tests, ORM 복원 검증) / `rate-limit-db.ts` (주석 갱신)
- vitest 245 → 244 PASS (-1, raw SQL assertion 일부 → ORM call signature assertion 으로 대체)
- 배포 2회 (PM2 ↺=10, ↺=11), HTTP 307, Tunnel OK
- E2E 12 step 전수 PASS (summary.sessions=2 정확)
- pg_dump 백업 보존: `/tmp/luckystyle4u-pre-tz-migration-20260419-150722.dump`

### 교훈

- **TIMESTAMPTZ 마이그레이션은 컬럼 측 절반의 해결**: Prisma 7 adapter-pg 의 binding-side TZ 시프트는 별도. 정공법 = SELECT cutoff PG 위임 + ::text 캐스팅
- **마이그레이션 USING 절**: PG 서버 timezone 의 wall-clock 의미를 보존하려면 `AT TIME ZONE '<server_tz>'` 명시. 잘못된 USING 은 데이터 영구 시프트
- **dry-run 의 가치**: BEGIN + 마이그레이션 + ROLLBACK 으로 17 ALTER 사전 검증. 실제 적용 시 안심

---

## 세션 41 — Wave 충실이행도 평가 + ORM Date 비교 전수 감사

### [1] 사용자 지시: wave 조사자료 충실이행도 평가

> "wave 조사자료의 충실이행도 평가."

kdywave Wave 1~5 (123 문서 / 106,588줄) 대비 실제 구현 충실도를 6차원으로 평가:
- 기술 채택 충실도 98/100 (otplib/@simplewebauthn/jose/argon2/prisma/xyflow 모두 Wave 결정 대로 설치)
- DB 스키마 충실도 95/100 (Session/WebAuthn/JwksKey/RateLimitBucket 7개 마이그레이션)
- MVP 로드맵 준수도 72/100 (Phase 15 ~90% / Phase 16 20% / Phase 17 25% — Phase 15-D 보강 범위 팽창)
- ADR 준수 95/100 (ADR 18→19 증가, ADR-008 MinIO 배제 / ADR-001 Multi-tenancy 제외 준수)
- 스파이크 집행도 80/100 (7건 완결, SP-013/016 Pending)
- 지식 보존 100/100 (CK 30건 누적, 역방향 피드백 0건)

**종합 87/100** — "설계-구현 정합성은 A, 진도는 C+".

### [2] 사용자 지시: 다음 세션 권장 종합하여 다음작업 추진

> 세션 40 CK Insight + 최종 결과 + 자율 판단 요약 + 다음 세션 권장 7건을 종합 제시하며 추진 요청.

권장 7건 중 **#1 (다른 모듈 ORM 시간 비교 전수 검토)** 선택 — 즉시 착수 가능 + 1-2h 스코프 + TIMESTAMPTZ 마이그레이션 후속 + 비생체/비환경 작업.

### [3] 전수 스캔으로 8곳 취약 패턴 발견

두 종류의 TZ 시프트 취약 구문:
1. **ORM WHERE Date 비교** (binding-side 시프트): `{lt|gt|lte|gte: jsDate}`
2. **JS-side Date 비교** (parsing-side 시프트): `row.<dateField> <|> new Date()`

| 파일 | 라인 | 함수 | 종류 |
|---|---|---|---|
| jwks/store.ts | 55 | getActivePublicJwks | ORM WHERE |
| jwks/store.ts | 71 | getPublicKeyByKid | JS-side |
| jwks/store.ts | 125 | cleanupRetiredKeys | ORM WHERE |
| mfa/webauthn.ts | 203 | consumeChallenge | JS-side |
| mfa/webauthn.ts | 213 | cleanupExpiredChallenges | ORM WHERE |
| mfa/service.ts | 30 | verifyMfaSecondFactor (lockedUntil) | JS-side |
| sessions/tokens.ts | 117 | findSessionByToken | JS-side |
| sessions/tokens.ts | 234 | listActiveSessions | ORM WHERE |

### [4] 3가지 전환 패턴 설계

- **패턴 A** (cleanup/purge): `raw SELECT id + ORM deleteMany(id)` — jwks cleanup, webauthn cleanup
- **패턴 B** (목록 + display): 전체 raw SELECT with `::text` 캐스팅 + JS new Date() 재구성 — listActiveSessions
- **패턴 C** (기존 ORM join 유지): 보조 `SELECT (expr) AS is_X` boolean — findSessionByToken, consumeChallenge, verifyMfaSecondFactor
  - verifyMfaSecondFactor 는 `locked_until::text` 도 함께 반환하여 응답 payload 의 9h 시프트까지 해결

### [5] 구현 + 검증

- 수정 4 파일: `jwks/store.ts` (3 함수) / `mfa/webauthn.ts` (2 함수) / `mfa/service.ts` (1 함수) / `sessions/tokens.ts` (2 함수)
- tsc 0 에러
- vitest: 13 files / **244 PASS** (세션 40 대비 회귀 0)
- 재스캔: ORM WHERE 0 match / JS-side 0 match

### [6] CK 추출 + 커밋

- CK 신규 1건: `2026-04-19-orm-date-filter-audit-sweep.md` (pattern, high)
  - 3가지 전환 패턴 A/B/C 선택 가이드
  - 재발 방지 체크리스트 4종
  - 잔존 과제 2종 (INSERT-side 시프트 / 기타 API route Date 직렬화) — 다음 세션 인계
- CK 누적 31 → **32**
- 커밋: `90ad952 refactor(orm): 세션 41 — Prisma Date 비교 전수 감사 (4파일 8곳 raw SQL 전환)`
- 푸시: `25e908d → 90ad952` (origin/main)

### 산출물

- 수정 4: `src/lib/jwks/store.ts` / `src/lib/mfa/webauthn.ts` / `src/lib/mfa/service.ts` / `src/lib/sessions/tokens.ts`
- 신규 1: `docs/solutions/2026-04-19-orm-date-filter-audit-sweep.md`
- vitest 244 PASS (회귀 0)

### 교훈 / Compound Knowledge 요약

- **한 건의 취약점 발견은 전수 감사의 신호**. 세션 39 `cleanup.ts` 단일 수정에서 멈추지 않고 세션 40(컬럼) → 세션 41(ORM 전수) 로 확장하여 기술부채 복리 방지.
- **ORM WHERE 와 JS-side 비교는 같은 뿌리의 두 증상**. 함께 감사해야 재발 방지.
- **TIMESTAMPTZ 컬럼도 "Prisma ORM WHERE 에 Date 를 바인딩하지 말라"는 규칙은 유효**. adapter 경계의 TZ 재해석이 근본 원인이라 컬럼 타입과 무관.
- **감사 스캔 쿼리 2종 재사용 가능**: `\{\s*(lt|lte|gt|gte)\s*:\s*(new Date|now)` + `(row|record|enrollment|session|rec)\.\w+(At|Until)\s*[<>=!]=?\s*(new Date|now)`. 향후 회귀 감사 템플릿.
