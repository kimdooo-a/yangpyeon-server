# 세션 저널 — 2026-05-10

> 세션 진행 중 주요 대화를 누적 기록. /cs 실행 시 대화 다이제스트 원본으로 활용.

---

## 세션 95

### [1] /cs 병렬 + "다음 작업 진행" 진입

- **사용자**: "세션종료는 이전 터미널에서 진행중이며 넌 다음작업 진행 ..." (직전 터미널이 S94 sharpedge follow-up commit `8f873c3` 처리 중, 본 터미널은 S95 작업 진입)
- **논의**: 동시 터미널 충돌 회피를 위해 `git status --short` + `git log --oneline -10` 즉시 점검 (memory `feedback_concurrent_terminal_overlap`). 결과: HEAD 가 d5f9b6b (S94 /cs main), `?? docs/security/sharp-edges-2026-05-09.md` untracked. /cs 터미널이 그 보고서를 처리 중일 가능성 — 본 세션은 코드 영역(M5)으로 분업 결정.
- **결론**: docs/handover/* 와 git push 는 다른 터미널 영역. 본 세션은 src/ 와 tests/ 에만 변경 가하기로.

### [2] M5-ATTACH 후속 결정 검토 — ADR-033 X1 server proxy 정착 확인

- **사용자**: (next-dev-prompt 의 S95 P0 = M5-ATTACH, "ADR-033 후속 — frontend → SeaweedFS S3 API 직접 vs server proxy 결정 prerequisite")
- **논의**: ADR-033 §2.5 읽으니 X1 server proxy 패턴이 이미 ACCEPTED 상태 (filebox 도메인 `upload-multipart/{init,part,complete,abort}` 4 라우트 정착). ADR-030 §"첨부 = filebox `File` 모델 FK 재사용" + §Q8 "30일 cron deref" 결정도 이미 있음. 즉 messenger 첨부의 후속 결정은 사실상 "filebox X1 패턴 그대로 재사용" 으로 압축됨 — 추가 ADR 불필요.
- **결론**: M5-ATTACH = 별도 storage/IAM 신설 0, filebox upload-multipart 4 라우트 그대로 호출 + MessageAttachment join 모델만 추가.

### [3] 백엔드 갭 분석 — 사전 추정 5-6일이 실제 1-2일로 압축

- **논의**: prisma schema (line 1001) MessageAttachment 모델 ✅ (RLS 첫 컬럼 + `ON DELETE RESTRICT` + 두 인덱스). lib/messenger/messages.ts 의 sendMessage / listMessages / searchMessages 모두 attachments tx INSERT + include 응답 정착 ✅. tests/messenger/messages.test.ts 에는 ATTACHMENT_NOT_OWNED 부정 케이스만 있음. 잔여 갭:
  - **갭 1**: positive 첨부 flow 테스트 (sendMessage 첨부 → MessageAttachment row INSERT + listMessages 응답 정합)
  - **갭 2**: cross-tenant RLS 격리 테스트 (S82 4 latent bug 패턴 재발 차단)
  - **갭 3**: 30일 message_attachments deref cron (registry 키워드 0)
  - **갭 4-6**: frontend MessageComposer/MessageBubble/MessageAttachment 컴포넌트
- **결론**: 본 세션 = 갭 1+2 합쳐 단일 testcase 로 마감. 갭 3-6 은 다음 세션 sub-chunk.
- **파일**: 점검만 — `prisma/schema.prisma`, `src/lib/messenger/messages.ts`, `tests/messenger/messages.test.ts`

### [4] M5-ATTACH-1 chunk — positive 첨부 + RLS 격리 단일 테스트

- **논의**: messages.test.ts 의 ATTACHMENT_NOT_OWNED 테스트(line 371-412) 다음에 새 testcase 삽입. alice 가 두 IMAGE 파일 owner → conv_a 에 첨부 2개 IMAGE 송신 → (a) MessageAttachment row 2개 INSERT (fileId/kind/displayOrder) (b) listMessages 응답 attachments 정합 (displayOrder 명시 정렬) (c) tenant_b context 에서 conv_a 메시지 0 row (RLS 격리). 단일 testcase 가 3 검증 수행.
- **결론**: 86 lines 추가, tsc 신규 오류 0 (사전 존재 e2e 2건 무관 — STYLE-2 영역).
- **파일**: `tests/messenger/messages.test.ts`

### [5] 라이브 vitest 4-stage 경계 함정 + WSL 빌드 미러 우회

- **논의**: PR 게이트 #4 라이브 검증 시도. 4-stage 경계 함정 발생:
  1. PowerShell native: `ECONNREFUSED ::1:5432` — Windows host 에서 WSL postgres 직접 미접근.
  2. WSL bash + Windows npx: scripts/run-integration-tests.sh §13-16 함정 — env URL 의 `?`/`%`/`=` 가 interop 경계에서 손실 → HAS_DB=false → 13 tests 모두 skipped (76 skip + 16 pass 패턴).
  3. WSL Linux node + Windows node_modules: `Cannot find module '@rolldown/binding-linux-x64-gnu'` — Windows install 에 Linux native binding 부재.
  4. **회피 성공**: WSL 빌드 미러 (`~/dev/ypserver-build/`) 에 `tests/messenger/messages.test.ts` 만 cp + WSL 측 `bash scripts/run-integration-tests.sh` 실행. Linux native modules + Linux node + Linux postgres = 13/13 PASS (850ms).
- **결론**: 향후 messenger 도메인 라이브 테스트 표준 절차 = WSL 빌드 미러 cp + 거기서 실행. solutions 문서 작성 가치.
- **파일**: 없음 (운영 절차 발견)

### [6] commit `652ff88` + S95 종료

- **논의**: ahead/behind 0 (다른 터미널이 8f873c3 까지 origin push 완료). `tests/messenger/messages.test.ts` 만 stage 후 commit. PR 게이트 5항목 모두 자동 통과 (신규 모델 0 / 신규 라우트 0 / Prisma 호출 변경 0 / non-BYPASSRLS 라이브 통과 ✅ / timezone 비교 0).
- **결론**: M5-ATTACH-1 sub-chunk 완료. 다음 세션 진입 후보 = M5-ATTACH-3 (frontend MessageComposer 첨부 UI, P0).
- **파일**: `tests/messenger/messages.test.ts` (commit `652ff88`)

---

## 세션 96

### [1] /cs 직후 S96 진입 — M5-ATTACH-3 분해
> **사용자**: "go on"

S95 /cs (commit `b027314`) push 완료 직후 자율 진입. M5-ATTACH-3 (1-1.5일 추정)은 한 chunk 가 아니라 3 sub-step 으로 분해 가능:
- 3a: `lib/messenger/attachment-upload.ts` utility (filebox 패턴 추출)
- 3b: MessageComposer Paperclip 버튼 활성화 + 5장 chip + 진행률 (jsdom 미도입 = 수동 영역)
- 3c: `composer-logic.ts` `prepareSendPayload` attachments 확장 + `canSendMessage`/`inferMessageKind` 신규 (TDD logic-only)

**결론**: 본 세션 = 3a + 3c 만 (logic + utility, TDD 가능). 3b UI 통합은 jsdom 컴포넌트 테스트 자체 불가 → 다음 세션 수동 영역으로 분리.

### [2] filebox 패턴 분석 + 재사용 결정

`src/components/filebox/file-upload-zone.tsx` (S78-A) 가 50MB 임계 분기 (uploadLocal XHR + uploadMultipart S3 워커풀 3 슬롯 + abort fallback) 패턴을 정착시킴. messenger 첨부도 같은 인프라 사용 (ADR-033 X1 server proxy + ADR-030 §FK 재사용). 두 컴포넌트가 같은 패턴인데 각자 inline 구현 vs 공통 utility 추출 결정:

- 옵션 A: messenger 측이 file-upload-zone 의 두 함수를 함수 export 받아 사용 — file-upload-zone 컴포넌트에 export 만 추가, 변경 최소
- 옵션 B: 공통 utility `lib/messenger/attachment-upload.ts` 신규 + filebox 도 향후 마이그레이션
- 옵션 C: `lib/storage/upload-utils.ts` 같은 cross-domain 추출 (지금 너무 무거움)

**결론**: 옵션 B 채택. messenger 측은 단일 함수 `uploadAttachment(file, onProgress)` 만 사용 (size 분기 + AttachmentKind 분류 자동). filebox 측은 그대로 두고 결합 0. 차후 필요 시 file-upload-zone 도 본 utility 로 마이그레이션 가능.

### [3] composer-logic 시그니처 확장 + type 전파 점검

`SendPayload.kind: "TEXT"` → `"TEXT" | "IMAGE" | "FILE"`, `body: string` → `string | null` 변경이 호출자 type 전파:
- `useMessages.SendOptimisticPayload` 가 별도 정의로 `kind: "TEXT"` 만 — 확장 필요
- `optimistic-messages.OptimisticBuildInput.payload` 도 `kind: "TEXT" / body: string` — 확장 필요
- `MessageRow.kind` 는 이미 `"TEXT" | "IMAGE" | "FILE" | "SYSTEM"` 지원, `body: string | null` 도 지원 — 호환

**결론**: 두 시그니처(`SendOptimisticPayload`, `OptimisticBuildInput.payload`)만 확장. `MessageRow` 모델은 ADR-030 시점부터 IMAGE/FILE 미리 지원되어 있어 그대로. `buildOptimisticMessage`가 첨부를 `MessageAttachmentRow` 로 변환 (id="opt-att-{cgid}-{idx}" 임시값, server 응답 swap 시 실제 id 로).

### [4] TDD 26 신규 + 라이브 검증 정책

- `composer-logic.test.ts` +17 (canSendMessage 7 + inferMessageKind 4 + prepareSendPayload 6)
- `attachment-upload.test.ts` +9 (classifyAttachmentKind 9 mime 분류)
- `uploadAttachment` 본체 = XHR + fetch 직접 호출이라 jsdom + MSW/fetch-mock 인프라 필요 → 본 chunk 범위 외. filebox file-upload-zone.tsx 도 단위 테스트 부재 (같은 정책). 라이브 검증 = 다음 chunk (3b UI 통합) 의 수동 영역.

**결론**: vitest 61/61 PASS (composer-logic 29 + attachment-upload 9 + optimistic-messages 23). +26 신규. tsc 신규 0 (사전 e2e 2건만).

### [5] commit `bf7255a` + push

6 files +585/-12. PR 게이트 5항목 자동 통과 (신규 모델 0 / 신규 라우트 0 / Prisma 호출 변경 0 / RLS 라이브 N/A / timezone 비교 0). origin push `b027314..bf7255a`.

**결론**: M5-ATTACH-3 의 3a + 3c 완료. 3b 만 잔여 (MessageComposer UI 통합, jsdom 미도입 수동 영역). 거버넌스 단언 sunset 매우 임박 (frontend UI 1 chunk + 30일 cron 1 chunk).

**파일**: `src/lib/messenger/{composer-logic,composer-logic.test,attachment-upload,attachment-upload.test,optimistic-messages}.ts` + `src/hooks/messenger/useMessages.ts` (commit `bf7255a`)

---

## 세션 96 (후속 sub-chunk — M5-ATTACH-2/3b/4/5 + 4 sweep 압축)

직전 sub-chunk (logic+utility, commit `bf7255a` + /cs `ee82c8c`) 종료 후 사용자 "다음 단계 모두 순차적으로 진행 (세션 종료는 이전 터미널에서 진행함.)" + "잔여 작업도 모두 순차적으로 진행" 두 분기. 본 터미널이 M5-ATTACH 잔여 4 chunk + S96 P3 sweep 4건 일괄 마감.

### [6] M5-ATTACH-2 30일 dereference cron — AGGREGATOR 모듈 등록

> **사용자**: "다음 단계 모두 순차적으로 진행"

ADR-030 §Q8 (b) 정착. 메시지 회수(soft-delete `deletedAt`) 후 30일 경과 시 첨부 dereference. `MessageAttachment.message Cascade` 는 hard-delete 만 처리하므로 soft-delete 된 메시지의 첨부는 명시 cron 으로 정리 — dereference 후 filebox cleanup 이 owner 회수 시점에 File 자체 자연 GC.

**아키텍처 결정**: `AggregatorModule` 에 `messenger-attachments-deref` 추가. 새 cron `kind` 추가는 `runner.ts/registry.ts/runNow` 광범위 수정 필요 → AGGREGATOR 가 사실상 "tenant-scoped writable TS handler" 로 일반화돼 있음. 이미 `cleanup` (S84+) 에서도 같은 패턴 채택.

**TDD 6**: `where.message.deletedAt.not = null` + `lt(cutoff)` / cutoff = NOW() - retentionDays / `tenantId` 명시 (BYPASSRLS 회피) / 결과 dereferenced+durationMs / withTenantTx 호출 검증.

**결론**: commit `6bb29c7`, 5 files +333. 라이브 6/6 PASS. cron seed 는 enabled=FALSE (운영자 결정 보류).

**파일**: `src/lib/messenger/attachment-cleanup.ts` (신규) + `src/lib/aggregator/{types,runner}.ts` + `scripts/seed-messenger-cron.ts` (신규) + `tests/messenger/attachment-cleanup.test.ts` (신규).

### [7] M5-ATTACH-3b/4 frontend UI — Composer chip + Bubble 렌더

직전 chunk (`bf7255a`) 의 `composer-logic.SendPayload` 시그니처가 이미 정합되어 있어 page.tsx 변경 0, MessageComposer.tsx + 신규 MessageAttachment.tsx + MessageBubble.tsx 만 수정. **logic-only TDD 분리 패턴의 정량 효과 실증** — 3b UI 통합 시 추가 backend/page 작업 0.

**MessageComposer.tsx 변경**: Paperclip disabled placeholder 활성화 + hidden file input + PendingAttachment[] state (uploading/done/error) + 5장 chip strip + 진행률 bar + uploading 중 송신 disabled + canSendText → canSendMessage (캡션 빈 OK).

**MessageAttachment.tsx 신규**: IMAGE 단일 (max 240×240) / 다수 (2열 grid h-28 object-cover) / FILE/VOICE 다운로드 버튼 / recalled placeholder. filebox download endpoint `/api/v1/filebox/files/{id}` 재사용 — `Content-Disposition: attachment` 강제하지만 브라우저가 `<img src>` 태그에서 무시하므로 인라인 렌더 OK.

**MessageBubble.tsx**: `attachments?` prop 추가 + body 옵션화 (캡션 X 시 빈 div 회피) + `<MessageAttachment recalled={v.variant === "recalled"} />`.

**결론**: commit `7ceb075`, 4 files +339/-13. tsc 신규 0, 회귀 unit 8/8 PASS. 라이브 검증 = 수동 영역 (jsdom 미도입 = S87-INFRA-1).

### [8] M5-ATTACH-5 sweep — search UI 첨부 indicator + cron deref e2e

backend (searchMessages include attachments + listMessages include attachments) 는 이미 정합. frontend search UI 의 빈 본문 케이스 보완 + 30일 cron deref e2e 시나리오 추가가 본 chunk 본질.

**MessageSearch.tsx**: 캡션 빈 (IMAGE-only) 메시지에서 "(본문 없음)" 대신 "📎 첨부 N건" 표시.

**messages.test.ts 신규 2 testcase**:
1. searchMessages 응답 attachments 정합 (캡션 검색어 매치 시 attachments 1건 동봉)
2. 30일 cron deref e2e: admin pool 로 `deletedAt` 31일/5일 강제 → `runMessengerAttachmentCleanup({tenantId})` 호출 → 31일짜리만 dereference, tenant_b context 0 row 격리 검증 (S82 4 latent bug 패턴 재발 차단).

**결론**: commit `a9aeede`, 2 files +155. 라이브 messages 15/15 PASS (M5-ATTACH-1 13 + M5-ATTACH-5 sweep 2). origin push `ee82c8c..a9aeede`.

### [9] 잔여 작업 일괄 진입 — 사용자 "잔여 작업도 모두 순차적으로 진행"

> **사용자**: "잔여작업도 모두 순차적으로 진행"

P0 사용자 carry-over (S88-USER-VERIFY/S88-OPS-LIVE/S86-SEC-1) 는 사용자 본인 작업이라 제외, Claude 처리 가능 4건 순차 진입:

#### [9a] STYLE-2 — e2e tsc 2건 fix

`scripts/e2e/phase-14c-alpha-ui.spec.ts:19/20` `string|undefined` errors. Module top-level `if-throw` 패턴은 TS 가 narrowing 인정 X → `requireEnv()` 헬퍼 도입 (return 타입으로 narrowing 표현). **S82 이후 처음으로 tsc 사전 errors 0**.

#### [9b] M5-ATTACH-6 — rls.test.ts bootstrap 6 모델 시드 강화

기존 reseed() = user/conversation/message 만 시드 → message_attachment 외 5 모델 (mention, receipt, block, abuse_report, notification_pref) 은 `findMany() → []` 빈 결과로 vacuous truth pass. **4개월간 RLS 정책 깨져도 통과 가능 — S82 "4 latent bug" 패턴**.

수정: 시드 모델 확장 (folders + files + 6 messenger 추가 모델) + 보조 user (userIdA2/B2) → mention/block 의미 있는 시나리오 + DELETE 순서 children → parents FK 의존 엄수. M5 testcase 에 `expect(rows.length >= 1)` active assertion 추가 → vacuous truth pass 차단.

**결론**: 라이브 13/13 PASS (M1-M4 4 + M5×9 model active assertion).

#### [9c] NAV-INTEGRATE — 사이드바 커뮤니케이션 그룹 정합

기존 sidebar.tsx 항목 4건이 실제 라우트와 어긋남:
- `/messenger/settings` → 실제 `/messenger/notification-preferences` (S94)
- `/admin/messenger/moderation` → 실제 `/messenger/admin/reports` (S94)
- `/admin/messenger/health` → 존재하지 않음 (제거)
- `/messenger/blocked-users` (S94, missing in sidebar — 신규 추가)

ADMIN_ONLY_PATHS / MANAGER_PLUS_PATHS 정합. Activity 미사용 import 정리.

#### [9d] GOV-SUNSET — 거버넌스 단언 해소

S91~S96 6 세션 발효 동안 wave-tracker 본진 가치 100% 도달. CLAUDE.md "역사 삭제 금지" 원칙에 따라 단순 제거가 아닌 **[SUNSET 2026-05-10 / S96] 표식 + 해소 commit 사슬 + 정책 전환** 명시. 자율 적용 사례 누적 그대로 보존. Sunset 후 정책: feedback_autonomy.md 일반 적용.

**결론**: commit `da8786b` (4 sweep 일괄), 4 files +193/-34. tsc 0 + unit 169/169 PASS. origin push `a9aeede..da8786b`.

### [10] S96 후속 sub-chunk 압축 결과

5 commit 사슬 (`bf7255a → 6bb29c7 → 7ceb075 → a9aeede → da8786b`). 라이브 vitest 누적 = 6 + 15 + 13 = **34 PASS** (M5-ATTACH 영역 전체). M4 Phase 2 본진 100% 도달 → 거버넌스 단언 sunset.

**메타 가치**: S94 본 세션 7 commit 압축 신기록 다음으로 큰 S96 후속 sub-chunk 5 commit + 본 chunk + 사용자 분기 2회 → wave-tracker 진척 전체 100% 회수. logic-only TDD 분리 패턴 → UI 통합 시 page.tsx 변경 0 정량 효과 실증. S95 정착 WSL 빌드 미러 cp + bash scripts/run-integration-tests.sh 절차 (1 파일 cp + 1 명령) 가 라이브 검증 표준화.

**파일 누적**: src/lib/messenger/attachment-cleanup.ts + src/lib/aggregator/{types,runner}.ts + scripts/seed-messenger-cron.ts + src/components/messenger/{MessageComposer,MessageBubble,MessageAttachment,MessageSearch}.tsx + src/app/(protected)/messenger/[id]/page.tsx + src/components/layout/sidebar.tsx + scripts/e2e/phase-14c-alpha-ui.spec.ts + tests/messenger/{attachment-cleanup,messages,rls}.test.ts + docs/handover/next-dev-prompt.md.

---

## 세션 96 후속-2 (verification-only chunk, /cs)

> /cs 호출 시점: 2026-05-10 (직전 commit `c706275`). 본 chunk = 코드 변경 0, 라이브 검증 1건 + Compound Knowledge 1건.

### [11] 사용자 "내가 해야할 것은?" 분기 — carry-over 재평가

> **사용자**: "내가 해야할 것은?"

직전 /cs (`c706275`) 후 carry-over P0/P1 4건 (S88-USER-VERIFY 사용자 / S88-OPS-LIVE 운영자 / S86-SEC-1 운영자 / /cs 별도 터미널) 안내. 사용자가 **"이것은 너가 직접 할 수 있는 것 아니야?"** (S88-OPS-LIVE 지목) 으로 carry-over 라벨 재평가 요청.

**핵심 통찰**: "운영자-only" 라벨이 검증 형태 가정 (운영 콘솔 클릭 워크플로우만 인정) 에서 나온 것이지 권한 한계가 아님. WSL2 운영 서버는 Claude 도 직접 호출 가능 (CLAUDE.md "운영 환경 및 마이그레이션 정책" 섹션). PM2 stderr + `_prisma_migrations.finished_at` 조회 = timeline correlation 으로 라이브 호출 없이 fix 인과 검증 가능.

**결론**: S88-OPS-LIVE 를 Claude 직접 검증으로 진입 (PM2 종료 금지 규칙은 준수, read-only 호출만).

### [12] S88-OPS-LIVE 검증 — PM2 log timeline correlation

#### 절차

1. PM2 status — `ypserver` online 5일, restart 24, unstable_restarts 0
2. 베이스라인 라이브 호출 3건 — `/auth/me` 401 10ms / `/messenger/conversations` 401 4.5ms / cross-tenant probe 401 3.5ms (모두 stack leak 없음, Korean error envelope)
3. 4 latent bug 시그 grep — RLS escape / timezone / Prisma @map / Unhandled / 5xx 모두 무발생
4. 마지막 ACL 에러 timeline 추출 — `2026-05-04 23:01 KST` (4건) + `2026-05-05 08:48~08:51 KST` (7건)
5. 마이그레이션 적용 시각 DB 조회 — `_prisma_migrations.finished_at = 2026-05-05 08:57:15 KST`
6. 인과 판정 — 마지막 에러 (08:51:58) → 마이그레이션 적용 (08:57:15) = **5분 17초 간격** → 그 후 5일+ 0 errors

#### 부가 시그

- 현재 ypserver-err.log = 0 bytes (5/6 rotation 후 5일간 stderr 무출력)
- Connection string 에 `?options=-c TimeZone=UTC` 박힘 → timezone 시그 구조적 무발생

**결론**: S88-OPS-LIVE PASS — carry-over P1 운영자-only → 해소.

### [13] Compound Knowledge — 검증 방법론 보존

> **사용자 분기**: "/cs"

solution `2026-05-10-ops-live-verification-by-pm2-log-correlation.md` 작성. 6 세션 (S88~S96) 동안 carry-over 채무로 누적된 항목이 15분 내 해소된 경험을 패턴으로 보존.

**5가지 교훈**:
1. "운영자-only" 라벨은 검증 형태 가정에서 나옴 — 재평가하면 Claude 직접 가능 영역
2. "마이그레이션 작성 = 즉시 적용" 정책의 효용이 5분 17초 간격으로 timestamp 증명됨
3. 0 byte log = 강한 clean 시그 (단순 "에러 없음" 보다 강함)
4. Git Bash 경로 자동 변환 함정 → `MSYS_NO_PATHCONV=1` 필수
5. Korean encoding + heredoc + 변수 expansion 조합은 깨지기 쉬움 → 직접 명령 권장

**결론**: 향후 carry-over P1 운영자-only 항목은 매 /cs 시 (1) 정적 audit (2) PM2 log timeline (3) DB 직접 조회 3종으로 해소 가능 여부 점검 표준 절차로 전환.

**파일**: `docs/solutions/2026-05-10-ops-live-verification-by-pm2-log-correlation.md` 신규.

---

## 세션 97

### [1] 사용자 "wave 기반 구현도 검증" → kdywavecompletion 스킬 호출

> **사용자**: "wave 기반 구현도 검증"

**논의**: 디스크립션 정확 매치 → kdywavecompletion 즉시 호출. Phase 0 5계층 산출물 병렬 탐지 (wave-tracker + ADR-022~030 9건 + spike 14건 + handover 96건 + git log 50 commits). 핵심 발견 = S85 wave eval (`260504-session85-wave-completion-eval.md`, 87/100) → S91 wave eval delta (`260508-session91-wave-completion-eval-delta.md`, 82/100 -5점) baseline 식별.

**결론**: 본 평가 = `--compare session-91` 패턴 적용 (S91~S96 5 세션 / 16 commit delta).

### [2] S91~S96 6 세션 핸드오버 일괄 로드 + 코드 검증

**논의**: P0 우선 로드 = S91 wave eval + S92 F2-1 + S93 F2-2 + S94 F-track 완주 + S95 M5-ATTACH-1 + S96 logic+utility + S96 잔여+sweep + S96 후속-2 OPS-LIVE = 8 핸드오버. Phase 3 코드 검증 = 17 messenger routes 1:1 매치 (R-W6 정정 검증), `attachment-cleanup.ts` ADR-030 §Q8 (b) 정확 매핑, `sidebar.tsx` NAV-INTEGRATE 정합, `git tag -l` = s81/s84/s87/s88/s91 5 + 3 alpha (S94~S96 부재 = R-W7+ 재발).

**결론**: 모든 인계서 주장이 코드와 1:1 매치. S91+ 거버넌스 단언이 5 세션 16 commit 동안 효력 발휘 → G-NEW-3 0/14 → 12/14 (86%) 회복.

### [3] 본 wave eval 보고서 12 섹션 작성 (260510-session97-wave-completion-eval-delta.md)

**논의**: §0 92/100 A- + §1 R-W2~R-W7 + G-NEW-1~6 매트릭스 + §2 G-NEW-7~11 신규 발견 + §3 진척도 매트릭스 (Track 가중치) + §4 6차원 등급 + §5 갭/위험 매트릭스 + §6 우선순위 + §7 commit 시퀀스 + §8 거버넌스 조치 (G-NEW-4 재발 차단) + §9 검증 게이트 + §10 본 평가 한계 + §11 후속 권장 + §12 갱신 이력.

**결론**: 종합 등급 92/100 (A-, S91 82 → +10). 자기 평가 보수화 -0.5 + G-NEW-4 재발 페널티 -3 + R-W7+ 신규 git tag 부재 페널티 -3 적용.

### [4] 사용자 "필요한 조치 및 작업 모두 순차적으로 진행" → 자율 실행 진입

> **사용자**: "필요한 조치 및 작업 모두 순차적으로 진행"

**논의**: 거버넌스 단언 [SUNSET] 후 `feedback_autonomy.md` 일반 적용. S97-PRE 베이스라인 검증 (HEAD = `c706275` 다른 터미널 S96-2 chunk, working tree leftover = next-dev-prompt M + 본 평가 보고서 untracked).

**결론**: DOC-WAVE-2 → TAG-2 → commit & push → S87-CK-MEMORY 순차 진행.

### [5] G-NEW-1 정정 흡수 (S96 후속-2 OPS-LIVE PASS)

**논의**: `git diff` next-dev-prompt = 다른 터미널 c706275 chunk 가 S88-OPS-LIVE 를 PM2 log timeline correlation 으로 검증 완료 (마지막 ACL 에러 5/5 08:51:58 KST → S88 migration 5/5 08:57:15 KST 5분 17초 간격 → 5일+ 0 errors). solution `2026-05-10-ops-live-verification-by-pm2-log-correlation.md` 보존.

**결론**: 본 평가 §1.2 G-NEW-1 ❌ → ✅ 정정 + §11 액션 표 갱신 + §5.2 G-NEW-14 신규 ("운영자-only 라벨 재검증 게이트" 권고) 추가. 운영자 carry-over 6 세션 채무 해소.

### [6] DOC-WAVE-2 — wave-tracker.md 7 섹션 갱신 (G-NEW-4 첫 정정)

**논의**: §0 한 줄 요약 (S91~S96 진척 흡수, 92/100 A-) + §1 4-Track 매트릭스 (Track A 품질 ↑↑ + Track B 100% 유지 + AggregatorModule 확장 + Track C 70% → ~95% 본진 100% + Track D 재사용 증명) + §2.3 잔여 (8 항목 ✅ 추가) + §4.1 M0~M6 (M4 Phase 2 ✅ F-track 5/5 + M5 검색/첨부 ✅ + M6 ✅ 모두 commit hash 명시) + §4.3 잔여 (3건 = INFRA-2 + 라이브 e2e + cron enable) + §7 S97+ 우선순위 (S84+ 표 [SUNSET]) + §8 갱신 이력 9 row 추가 (S92~S97 모든 chunk).

**결론**: G-NEW-4 (S91+ 이후 5 세션 stale 동일 패턴) 첫 정정. /cs 6단계 공식화 권고 (글로벌 룰 변경 영역).

### [7] TAG-2 — 5 마일스톤 git tag 소급 부여

**논의**: S91+ TAG-1 (s81/s84/s87/s88/s91) 패턴 계승. 5 신규 tags = `s94-sharpedge-pass` (8f873c3 보안 리뷰 PASS) / `s94-m4-phase2-f-complete` (5a29980 F2-5 F-track 완주) / `s95-m5-attach-1` (652ff88 백엔드 갭 폐쇄) / `s96-gov-sunset` (da8786b 거버넌스 sunset) / `s96-2-ops-live-verified` (c706275 OPS-LIVE PASS).

**결론**: R-W7+ 해소. push origin --tags 시 3 alpha tags rejected = 이미 origin 존재 무해. 5 신규 tags 모두 push 성공.

### [8] commit `719bfa7` + push origin spec/aggregator-fixes

**논의**: commit 영역 = wave-tracker.md (DOC-WAVE-2) + 본 평가 보고서. next-dev-prompt 의 M 상태가 자동 normalize 후 stage 안 됨 (다른 터미널 c706275 commit 기록 = working tree 와 같은 상태로 정착). 다른 터미널 leftover 흡수는 본 commit 영역 외.

**결론**: commit `719bfa7` (2 files +407/-31) + push origin 성공. 5 신규 tags push 도 성공.

### [9] S87-CK-MEMORY — 2 메모리 룰 승격

**논의**: S87 이월 P3 (4 세션 carry-over). S87 인계서 line 71-84 의 2 CK 의 룰 후보 명확:
- `feedback_wsl2_single_foreground_call.md` ← `2026-05-04-wsl2-background-sighup-trap.md` (WSL2 HCS 가 wsl.exe 호출마다 새 session 생성 → 종료 시 child SIGHUP. 단일 foreground 또는 PM2/systemd 정착만 사용. 안티패턴 3종)
- `feedback_tsx_no_dotenv_autoload.md` ← `2026-05-04-tsx-env-not-loaded.md` (tsx = esbuild+node 단순 실행기, Next.js dotenv-flow 와 분리. dotenv-cli 또는 `import "dotenv/config"` 또는 PowerShell export 4종 우회 + 함정 4종)

**결론**: 2 메모리 룰 신규 + MEMORY.md 색인 2 row 추가. 향후 동일 함정 자동 인지 차단 자산. 글로벌 memory 폴더 (~/.claude/projects/.../memory/) 영역, git 무관.

### [10] /cs 진입 — 본 세션 종료 프로토콜

> **사용자**: "/cs"

**논의**: 5단계 (current.md row 97 + logs/2026-05.md S97 entry + handover/_index.md row 97 + handover 인수인계서 = 본 평가 보고서 자체로 충족 + next-dev-prompt S98 진입 표) + 6단계 (journal append).

**결론**: 본 /cs commit 으로 docs 갱신 정착. 다음 세션 = S98 진입 시 DECISION-1 (INFRA-2 / Almanac plugin / sweep) 사용자 결정.

---

## 세션 98

### [1] 사용자 결정 — INFRA-2 + Almanac plugin 순차 진행

> **사용자**: "1,2 순차적으로 진행." (S97 마지막 제안 = 1: P2 INFRA-2 ~3-4h, 2: Almanac plugin 마이그레이션 ~5-7일 schema-first 1-2일 단축 가능)

**논의**: feedback_baseline_check_before_swarm + feedback_concurrent_terminal_overlap 즉시 적용 — `git log --oneline -10` + `git status --short` + current.md/next-dev-prompt 4 source 점검. 결과: HEAD=719bfa7 (S97 wave eval), tree clean. INFRA-2 명세 = SWR + MSW + uploadAttachment 본체 jsdom + 컴포넌트 렌더 TDD (G-NEW-12 해소), ~500 LOC. Plugin 명세 = ADR-024 옵션 D (Hybrid: Complex=workspace).

**결론**: 5 task 분해 (INFRA-2 4 + PLUGIN-MIG-1 1) + TDD 스킬 로드 + TaskCreate 등록.

### [2] INFRA-2 Task #1 — SWR + MSW + jsdom 인프라 (RED→GREEN smoke test)

**논의**: `npm install swr@2.4.1 msw@2.14.5` (jsdom + RTL + jest-dom 기존). 결정: 글로벌 env=node 유지 + `// @vitest-environment jsdom` 파일 단위 opt-in (속도 회귀 0). msw-smoke.test.ts 가 `./msw/server` import 실패 RED → src/test/{msw/server.ts, msw/handlers.ts, setup.ts} 생성 + vitest.config setupFiles → 2/2 GREEN (transform 54ms, env 577ms). vitest 4.x `--reporter=basic` deprecated 재확인 (S97 wave eval 발견과 일치).

**결론**: 인프라 정착, 누구나 `// @vitest-environment jsdom` 으로 hook/component test 작성 가능.

**파일**: `src/test/msw/{server,handlers}.ts`, `src/test/setup.ts`, `vitest.config.ts`, `src/test/msw-smoke.test.ts`

### [3] INFRA-2 Task #2 — useConversations + useMessages SWR 마이그레이션 (TDD dedup RED→GREEN)

**논의**: 외부 행동 보존 테스트 3 PASS 즉시 (loading/error/reload). 진짜 RED 확보를 위해 SWR-specific dedup 테스트 추가 — 같은 key 다중 hook 호출 = fetch 1회. 현 fetch+useState 패턴 = 3 fetches → RED. SWR 마이그레이션 후 GREEN. useMessages 도 동일 패턴 (sendOptimistic happy + 5xx + dedup). mutate(updater, {revalidate: false}) 로 sendOptimistic + SSE applyEventToMessages 통일. SSE 는 vi.mock useSse 로 격리.

**결론**: useConversations 4/4 + useMessages 5/5 PASS. page.tsx 변경 0 (시그니처 보존).

**파일**: `src/hooks/messenger/{useConversations,useMessages}.{ts,test.tsx}`

### [4] INFRA-2 Task #3 — uploadAttachment 본체 jsdom+MSW 5 시나리오

**논의**: 기존 9 test 는 classifyAttachmentKind pure logic 만. 본체 (XHR + multipart) 미커버. `Object.defineProperty(file, "size", ...)` 로 file.size mock → 5GB cap / multipart partCount=1 happy / complete 실패 시 abort 호출 검증. MSW 가 XHR + fetch 모두 가로챔.

**결론**: 5 PASS — local happy / 4xx server error / 5GB cap (handler hits=0) / multipart happy / multipart abort fallback.

**파일**: `src/lib/messenger/attachment-upload-body.test.ts`

### [5] INFRA-2 Task #4 — 4 컴포넌트 렌더 TDD (cleanup + scrollIntoView 함정)

**논의**: MessageAttachment 7 RED — 4 fail (테스트 간 DOM 누적 = `@testing-library/react` auto-cleanup 미동작, vitest globals=false). 원인: cleanup 등록 누락. setup.ts jsdom 분기에 `afterEach(cleanup)` 추가 → GREEN. MessageList 7 RED — `scrollIntoView` jsdom 미구현. polyfill 추가 (Element.prototype.scrollIntoView = ()=>{}). MessageComposer 5 — vi.mock uploadAttachment + Object.defineProperty file.files 로 file input change 시뮬레이션. chip add/remove + uploading 중 send disabled + 완료 후 send enabled.

**결론**: 25 컴포넌트 test PASS. **wave-tracker §6 가정 정정**: "M4 UI = TDD 압축 적용 불가" 무너짐 — logic-only 와 동일 분량/시간 압축률.

**파일**: `src/components/messenger/{MessageAttachment,MessageBubble,MessageList,MessageComposer}.test.tsx` + setup.ts

### [6] INFRA-2 commit `ff698fe` — 16 files +1346/-170, TDD +40

**논의**: 전체 회귀 = 801 PASS / 94 skip (S97 baseline 761 → +40, 0 회귀). tsc 0 errors. PR 게이트 5항목 자동 통과. 다른 터미널 fe8ea02 (S97 /cs docs 마감) 영역 분리 명시 commit 메시지 — feedback_concurrent_terminal_overlap 정합.

**결론**: INFRA-2 본진 commit 정착, FILE-UPLOAD-MIG sweep 만 잔여.

### [7] PLUGIN-MIG-1 — ADR-024 옵션 D schema-first 골격

**논의**: ADR-024 옵션 D 정합 = Almanac 은 Complex tenant → workspace 패키지. packages/core/ 이미 존재 (tenant/cron/audit subpath). TenantManifest 인터페이스 신설 (cronHandlers + routes + adminPages + prismaFragment + envVarsRequired + dataApiAllowlist 6 필드). defineTenant identity helper. 본체 마이그레이션은 PLUGIN-MIG-2~5 (Phase 16+) 로 분리.

**결론**: 골격 단계 = manifest.ts (todoHandler stub 6개 = ok=false + PLUGIN-MIG-2 안내 메시지) + 빈 src/{handlers,routes,admin}/.gitkeep + prisma/fragment.prisma placeholder + README 5단계 마이그레이션 표.

**파일**: `packages/core/src/tenant/manifest.{ts,test.ts}`, `packages/tenant-almanac/{package.json,tsconfig.json,manifest.ts,README.md,prisma/fragment.prisma,src/{index.ts,handlers/.gitkeep,routes/.gitkeep,admin/.gitkeep},src/manifest.test.ts}`, `tsconfig.json`, `vitest.config.ts`

### [8] PLUGIN-MIG-1 commit `4840fa6` — 16 files +499, TDD +8

**논의**: 회귀 = 809 PASS (S98 INFRA-2 baseline 801 → +8, 0 회귀). tsc 0 errors. tsconfig + vitest 양쪽에 `@yangpyeon/tenant-almanac` alias 정착. 본 commit 영역 = packages/core (manifest 인터페이스) + packages/tenant-almanac/ (전체 골격) + alias 2 파일.

**결론**: ADR-022 7원칙 #4 "코드 수정 0줄 신규 컨슈머" 토대 마련. todoHandler stub 의 ok=false 메시지가 PLUGIN-MIG-2~5 진척도 자동 노출 메커니즘.

### [9] /cs 진입

> **사용자**: "/cs"

**논의**: 6단계 (저널 append + current.md row 98 + logs/2026-05.md S98 entry + handover 인수인계서 + next-dev-prompt 갱신 + commit & push). 본 세션 2 commit (ff698fe + 4840fa6) 가 origin 미푸시 상태 — /cs 5단계 자동 push.

**결론**: docs 갱신 + push 일괄 처리.

---

## 세션 98 후속 (PLUGIN-MIG-2 + 5)

> 다른 터미널이 위 [9] /cs 처리 + commit 727bb24 (docs S98) + b5bed64 (CK memory) 푸시 완료.
> 이 터미널은 동일 세션 98 의 PLUGIN-MIG-2 + 5 작업을 이어서 수행 (commit f7a0253). S96 의 후속-2 패턴 정합.

### [10] 사용자 결정 — 다른 터미널 /cs 진행 중 이 터미널은 남은 단계 진행

> **사용자**: "다른 터미널에서 세션 종료 진행중이라서 이 터미널은 남은 단계 진행 바람."

**논의**: 베이스라인 확인 = `git log --oneline -10` + `git status --short` (memory `feedback_concurrent_terminal_overlap`). 다른 터미널이 docs/ 영역 (handover/logs/status/journal/next-dev-prompt) 작업 중, 이 터미널은 src/+packages/ 영역 — 충돌 0. 남은 단계 = PLUGIN-MIG-2 (핸들러 이전) → PLUGIN-MIG-3 (라우트) → PLUGIN-MIG-4 (Prisma fragment + RLS) → PLUGIN-MIG-5 (cron runner manifest dispatch). 4 phase 모두는 한 터미널 세션 분량 초과 — 우선순위 평가: MIG-2 + 5 tightly coupled (핸들러 추출 + manifest dispatch 가 한 functional unit), MIG-3 단독 시 functional 변화 0, MIG-4 PR 게이트 #4 (live non-BYPASSRLS 테스트) 필수 = 가장 위험.

**결론**: MIG-2 + 5 한 commit, MIG-3/4 다음 세션. TaskCreate 4건 등록.

### [11] PLUGIN-MIG-2 — 6 almanac 핸들러 본체 이전 + AggregatorRunResult → TenantCronResult adapter

**논의**: 시그니처 차이 = `runAggregatorModule(ctx, payload) → AggregatorRunResult` vs `TenantCronHandler(payload, ctx) → TenantCronResult({ ok, processedCount?, errorMessage? })`. adapter 가 `status === 'SUCCESS' → ok=true` 매핑. processedCount 는 message 가 freeform 이라 안전한 파싱 어려움 → undefined (향후 enhancement). 공유 fetcher 파이프라인 (`runFetchersByKind` + `processSingleSource` + `markSourceFailure` + `buildPendingRow`) 은 `packages/tenant-almanac/src/lib/fetcher-pipeline.ts` 로 추출 (RSS/HTML/API 3 핸들러 공유). support libs (dedupe/fetchers/llm/promote/cleanup) 는 PLUGIN-MIG-4 까지 `src/lib/aggregator/` 잔존 (5 Content* Prisma 모델이 글로벌 schema 에 남아 있어 동시 이동 시 import path 정리). messenger-attachments-deref 는 almanac 외 도메인 → core handler 로 분리 예정 (MIG-5).

**결론**: 6 handler + fetcher-pipeline.ts 신설 (~280 LOC). manifest.ts 가 todoHandler stub → 실제 handler invoke + adapter, enabled=true. src/lib/aggregator/runner.ts 326 → 97 줄로 슬림 (thin dispatcher).

**파일**: `packages/tenant-almanac/src/handlers/{rss-fetcher,html-scraper,api-poller,classifier,promoter,cleanup}.ts`, `packages/tenant-almanac/src/lib/fetcher-pipeline.ts`, `packages/tenant-almanac/manifest.ts`, `packages/tenant-almanac/src/index.ts`, `packages/tenant-almanac/src/manifest.test.ts`, `src/lib/aggregator/runner.ts`

### [12] PLUGIN-MIG-5 — cron runner generic dispatch (AGGREGATOR 분기 → @yangpyeon/core dispatcher)

**논의**: cron/runner.ts 의 `dispatchAggregatorOnMain` 이 `payload.module` 을 알고 있어 aggregator 도메인-coupled. PLUGIN-MIG-5 는 이를 generic 으로 교체 — `dispatchTenantHandler(name, payload, ctx)` 가 (1) core handler map (messenger-attachments-deref 등 tenant 비특정) → (2) tenant manifest cronHandlers 순으로 lookup. registry 는 globalThis 싱글턴 (memory rule `project_workspace_singleton_globalthis` — Turbopack chunk 복제 환경에서 분기 방지). bootstrap 모듈 = `src/lib/tenant-bootstrap.ts` 가 import 시 side-effect 로 almanac manifest + messenger-attachments-deref core handler 등록. cron/runner.ts top-level `import "@/lib/tenant-bootstrap"` 1줄로 보장.

**결론**: cron/runner.ts 가 더 이상 aggregator module 이름 / 도메인 모름. AGGREGATOR string label 만 보존 (DB 컬럼). ADR-022 7원칙 #4 "코드 수정 0줄 신규 컨슈머" 의 cron 레이어 현실화. 향후 jobboard 추가 시 cron/runner.ts 변경 0줄.

**파일**: `packages/core/src/tenant/dispatcher.{ts,test.ts}`, `packages/core/src/tenant/index.ts`, `packages/core/src/index.ts`, `src/lib/cron/runner.ts`, `src/lib/tenant-bootstrap.ts`, `tests/cron/cron-aggregator-dispatch.test.ts`

### [13] 테스트 갱신 + 회귀 검증

**논의**: cron-aggregator-dispatch.test.ts 가 mock 한 `@/lib/aggregator/runner` 가 더 이상 cron/runner.ts 의 의존성 아님 → `@yangpyeon/core` 의 dispatchTenantHandler mock 으로 boundary 이동. 부분 mock 패턴 (`importOriginal` + spread) 으로 defineTenant/registerTenant 등 다른 export 는 actual 유지 — bootstrap chain 의 import-time side-effect 가 깨지지 않음. dispatcher.test.ts 신규 11 케이스 (registry 라이프사이클 + 우선순위 4 분기 + clear/list). manifest.test.ts 는 enabled=true + handler invoke (mock 으로 DB 의존 회피) 로 갱신.

**결론**: vitest 810 → 821 PASS (+11 신규, 0 회귀). tsc 0 errors. PR 게이트 5 항목 자동 통과 (모델/라우트/Prisma/RLS/timezone 변경 0).

### [14] 커밋 + push

**논의**: commit `f7a0253` 19 files +1030/-413 (handler 추출 +6, dispatcher 신설 +2, fetcher-pipeline +1, manifest update, runner 슬림화 +/-, test 갱신/신규 +2). origin/spec/aggregator-fixes 에는 다른 터미널의 727bb24 (docs S98) + b5bed64 (CK memory) 가 이미 push 완료 — origin..HEAD diff 0 인 상태에서 본 commit 1개 ahead. push 성공 (b5bed64 → f7a0253).

**결론**: PLUGIN-MIG-2/5 정착, origin 동기화 완료. PLUGIN-MIG-3 (라우트 5 파일 928줄) 와 PLUGIN-MIG-4 (Prisma fragment + RLS, PR 게이트 #4 live test 필수) 는 다음 세션 권장 — 묶어서 한 chunk 처리 (라우트 단독 이전은 functional 변화 0, 모델 이동 시 자연 동반).

### [15] /cs 진입 (이 터미널)

> **사용자**: "/cs"

**논의**: 다른 터미널이 위 [9] 에서 S98 /cs 마감 docs (727bb24) 를 이미 push 완료. 이 터미널의 후속 chunk 는 동일 세션 번호의 follow-up 으로 기록 — S96 의 `s96-2`, `s96-3` 패턴 정합. 5단계 = current.md row "98 후속" + logs/2026-05.md S98 후속 append + handover 후속 인수인계서 + next-dev-prompt 갱신 (PLUGIN-MIG-2/5 ✅ 처리, MIG-3/4 우선순위 갱신) + commit & push.

**결론**: docs 갱신 + 후속 commit & push 일괄 처리.

---

## 세션 99

### [1] 새 세션 시작 + S98 후속 마감 보고 paste

> **사용자**: "새로운 새션 시작 ... ● Push 성공 (b5bed64 → f7a0253). 세션 마감 보고: PLUGIN-MIG-2/5 완료 ..."

**논의**: 사용자가 직전 S98 후속 세션의 마감 보고 (commit `f7a0253` 정착) 를 그대로 paste. 세션 시작 프로토콜 (CLAUDE.md) 적용 — `git status --short` clean + `git log --oneline -10` HEAD `f7a0253` 확인 + origin = HEAD (동기화). next-dev-prompt.md (S99 prompt) + S98 핸드오버 2건 (메인 + 후속) 모두 확인.

**결론**: baseline 검증 완료. next-dev-prompt 의 S99 P0 = PLUGIN-MIG-3 자율 진입 (분기 질문 금지 메모리).

### [2] PLUGIN-MIG-3 대상 5 routes 위치 정찰

**논의**: manifest.ts 주석 = "src/app/api/v1/t/[tenant]/{categories,sources,today-top,items,contents}/route.ts" 표기. Glob 결과 = 5 explicit route + 2 dispatcher (`/api/v1/almanac/[...path]/route.ts` 308 alias 44줄, `/api/v1/t/[tenant]/[...path]/route.ts` catch-all 67줄). wc 합 = 928 + 111 = 1039줄. categories 149 / sources 117 / today-top 207 / items[slug] 135 / contents 253.

**결론**: 5 routes + 2 dispatcher 모두 정찰 대상 확정.

### [3] 5 routes 본체 read + 공통 패턴 추출

**논의**: 5 route 파일 모두 read (병렬). 공통 패턴 = `withTenant` 가드 (각자) + `runtime = "nodejs"` + `buildCorsHeaders(request)` (5×17줄 cors 중복, ALMANAC_ALLOWED_ORIGINS env 의존) + `tenantPrismaFor({ tenantId: tenant.id })` (RLS 자동 + ALS propagation 회피) + `successResponse`/`errorResponse` + OPTIONS handler. 개별 차이 = today-top score 1.5x boost (publishedAt < 24h fresh) / items[slug] tenantId_slug composite + viewCount fire-and-forget / contents 7 query + base64 cursor + writeAuditLog ALMANAC_CONTENTS_LIST.

**결론**: cors helper + OPTIONS factory 공통화로 ~85줄 자연 압축 가능. handler 본체 functional 로직 보존.

### [4] TenantRouteRegistration 시그니처 검증

**논의**: `packages/core/src/tenant/manifest.ts` 의 `TenantRouteRegistration` = path (절대 Next.js path 예 "/api/v1/almanac/contents") + handler (dynamic import, GET?/POST?/PATCH?/DELETE? unknown shape). PLUGIN-MIG-1 (S98) 에서 정의됨. 단 route registry / dispatch lookup 미구현 — `packages/core/src/tenant/dispatcher.ts` (S99 PLUGIN-MIG-5 신설) 는 cron handler 만 처리, route 측 미구현. `src/lib/tenant-router/dispatch.ts` (Phase 1.2 T1.2) 는 HANDLER_TABLE 빈 객체 → 모든 path 404 ROUTE_NOT_FOUND. 현재 5 explicit route 가 Next.js static-first match 로 catch-all 앞에서 흡수 중. PLUGIN-MIG-3 추가 작업 = TenantRouteHandler 타입 신설 + path 의미 재정의 (절대 → subPath pattern "contents", "items/:slug") + handler strict typing + dispatch.ts manifest lookup + `:slug` 동적 param 추출.

**결론**: PLUGIN-MIG-1 골격은 codegen 모델로 작성됐으나 PLUGIN-MIG-3 은 runtime catch-all dispatch 로 구현 (S99 PLUGIN-MIG-5 cron 패턴과 정합).

### [5] chunk A/B/C 분할 설계

**논의**: PLUGIN-MIG-3 단일 commit 가능하나 회귀 추적 용이성을 위해 분할 권장. A = 인프라 (TenantRouteHandler + dispatcher route registry + dispatch.ts manifest lookup + dispatch.test.ts 갱신, ~150 신규 LOC, 0 회귀 — 5 explicit 가 catch-all 흡수 중이라 영향 없음). B = 5 handler 본체 packages/tenant-almanac/src/routes/ 이전 + cors helper 공통화 + manifest.routes 등록 (~700 LOC 이전, 0 회귀 — 5 explicit 보존이라 hot path 무관). C = 5 explicit route.ts 삭제 + 라이브 회귀 검증 + commit (-928 LOC, 라이브 cutover 위험, vitest + 수동 curl smoke 게이트).

**결론**: A → B → C 단계별 commit. 각 단계 vitest 회귀 0 검증 후 진행.

### [6] 308 alias 보존 결정

**논의**: next-dev-prompt 의 PLUGIN-MIG-3 표기에 "(308 alias 제거)" 포함되어 있으나, `/api/v1/almanac/[...path]/route.ts` = 44줄 단순 redirect (`buildTarget` + 308 NextResponse.redirect 7 메서드). 결합 0 (catch-all 과 별개 진입점). 308 = Permanent + 메서드 보존 → 클라이언트 새 URL 캐싱. 주석 = "Almanac v1.0 출시 기간 동안만 유지 (plugin 마이그레이션 후 제거 예정)".

**결론**: 본 PLUGIN-MIG-3 chunk 에서는 보존. Almanac frontend (almanac-flame.vercel.app) 가 `/api/v1/t/almanac/*` 직접 사용 확인 후 별도 sub-chunk (v1.1 cutover 시점) 로 제거.

### [7] withTenant 단일 위치 전환 설계

**논의**: 현재 5 explicit route 각각 `withTenant` 데코레이터 적용. cutover 후에는 catch-all `[tenant]/[...path]/route.ts` 가 `withTenant` 단일 적용, plugin handler 는 `withTenant` 미경유 + signature `(req, user, tenant, params) => Promise<Response>` 단순화. 이점 = plugin 코드 host app 의 `withTenant` 의존성 0, K3 cross-validation 도 catch-all 한 곳 강제 — plugin handler 가 잘못 작성돼도 K3 가 플랫폼 측에서 보장 (ADR-022 7원칙 #3 "한 컨슈머 실패는 다른 컨슈머에 닿지 않는다" 의 router 레이어 현실화).

**결론**: handler signature `(req, user, tenant, params) => Promise<Response>` 채택.

### [8] TaskCreate 7 등록 + #1 완료 + /cs 시점 #2-#7 deleted

**논의**: chunk A/B/C 단계별 task 7 등록 — #1 정찰 (TenantRoute 타입 + 잔여 3 route + dispatch.test.ts) ✅ 본 세션 완료 / #2 5 route handler 본체 packages/tenant-almanac/src/routes/ 이전 / #3 manifest.ts routes 배열 등록 + dispatcher route registry lookup / #4 5 explicit route.ts 삭제 — catch-all 자동 흡수 / #5 dispatch.ts + manifest TDD — registry lookup + params 추출 / #6 vitest 821→ + tsc 0 errors 확인 + commit + push / #7 next-dev-prompt.md S100 entry 갱신. /cs 호출 시점에 #2-#7 모두 pending. 다음 세션이 본 인수인계서 읽고 task 재등록 가능 → 본 세션 종료 시 deleted 마킹.

**결론**: 정찰만 완료, 본격 구현은 S100. 인수인계서가 S100 재진입 비용 회피 메커니즘.

### [9] /cs 진입

> **사용자**: "/cs"

**논의**: PLUGIN-MIG-3 본격 구현 (chunk A/B/C, ~1-2일) 직전에 사용자가 세션 종료 결정. 정찰 + 설계 가치를 인수인계서에 보존하여 S100 재진입 시 비용 회피. 6단계 = 저널 append + current.md S99 row + 2026-05.md log + handover 파일 + _index row + next-dev-prompt S100 prompt + commit & push.

**결론**: 본 세션 = 정찰 + 설계만 (코드 변경 0). docs 6 파일 갱신 + commit & push 일괄.

**파일**: `docs/handover/260510-session99-plugin-mig-3-recon.md` (신규), `docs/handover/_index.md`, `docs/status/current.md`, `docs/logs/2026-05.md`, `docs/handover/next-dev-prompt.md`, `docs/logs/journal-2026-05-10.md` (이 파일)

