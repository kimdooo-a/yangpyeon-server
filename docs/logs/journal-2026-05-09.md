# 세션 저널 — 2026-05-09

> 세션 진행 중 주요 대화를 누적 기록. /cs 실행 시 대화 다이제스트 원본으로 활용.

---

## 세션 94

### [1] 베이스라인 점검 + S93 인계서 정독

> **사용자**: "모두 순차적으로 진행.(이전 터미널은 지금 세션종료 중이고)"

다른 터미널이 65cf152 (S93 /cs F2-2 단독 chunk 종료) 로 next-dev-prompt 를 S94 로 갱신한 직후 본 세션 진입. 영역 분리 (`feedback_concurrent_terminal_overlap`) 적용 — 본 세션 = S94 첫 multi-chunk.

베이스라인 4종 병렬 확인 — git log clean / S92 인계서 정독 → 거버넌스 단언 적용 중 + F2-3~F2-5 → M5 → M6 순차 권고 확인.

### [2] F2-3 답장 인용 + 멘션 popover (logic-only TDD)

backend (POST /messages 가 이미 replyToId/mentions: string[] 처리 + sendMessage 의 cross-conv 검증 + 차단 필터 + 자기-멘션 제외) 6개월 선행 확인 → frontend-only chunk.

신규 logic 모듈:
- `mention-search.ts` (TDD 18): @ 트리거 감지 + 후보 필터링 + 선택 적용
- `reply-quote.ts` (TDD 13): 80자 컷 + kind 별 변형 (recalled/system/image/file/text)
- `composer-logic.ts` 확장 (+6): replyToId/mentions opts + dedup + 빈 배열 정규화

UI 확장: MessageComposer (cmdk popover + reply banner) + MessageBubble (quote preview + hover 답장 버튼) + MessageList (id-indexed lookup + senderMap) + page.tsx (replyTo state + members fetch).

backend 변경 1줄: conv detail GET 의 members include 에 user.email/name (additive).

**결론**: commit `8903e1d` 13 files +985/-118, vitest 619 → 656 (+37). PR 게이트 5항목 모두 N/A 또는 자동 통과.

### [3] F2-4 + INFRA-1 (jsdom + use-sse) — 본 세션 병행 진입 결정

> **사용자**: "본 세션 병행 진입 (Recommended)" (F2-4 + INFRA-1 동반)

INFRA-1 핵심 = jsdom + @testing-library/react + @testing-library/dom + jest-dom 4 devDep install (SWR 도입은 별도 chunk 보류).

신규 모듈:
- `sse-events.ts` (TDD 13, node env): SSE event name + raw JSON → typed RealtimeEvent + applyEventToMessages reducer. clientGeneratedId 우선 dedupe (optimistic swap), parse 실패 시 unknown (스트림 중단 회피).
- `use-sse.ts` (TDD 7, jsdom env): EventSource 래퍼 hook. url 변경 시 close+재생성, unmount cleanup, ready 이벤트로 connected 갱신, onEvent ref 패턴으로 stale closure 회피.

확장: useMessages 안에 useSse 호출 + handleSseEvent 콜백 + sseConnected 노출. page.tsx 헤더에 emerald-500/gray-400 점.

vitest config 변경 없음 — file-level `// @vitest-environment jsdom` 주석으로 환경 분리.

**결론**: commit `088f623` 8 files +1347/-8, vitest 656 → 676 (+20).

### [4] F2-5 DIRECT peer 이름 lookup

ConversationList 의 file-local `derivePeerLabel` 분리 + DIRECT 분기 8 + GROUP/CHANNEL 분기 3 = 10 case 단위 테스트.

신규 모듈 `peer-label.ts` (TDD 10): user.name → user.email → userId 8자 prefix → "DM" fallback 우선순위. user.name 공백만 trim 후 빈 문자열 처리, members undefined → "DM", currentUserId undefined → 첫 멤버를 peer.

backend conversations GET include 1줄 (additive members.user.email/name) + ConversationRow type 확장 + ConversationList import 변경 + page.tsx 헤더 conversationId.slice(0,8) → derivePeerLabel + subtitleForKind.

**결론**: commit `5a29980` 6 files +231/-31, vitest 676 → 686 (+10). M4 Phase 2 F 트랙 5/5 chunk 완주.

### [5] 다음 단계 결정 — 옵션 2-4 순차 진행

> **사용자**: "2-4 작업 모두 순차적 진행."

거버넌스 단언이 M5+M6 완료 시 sunset 이라 진척 가속 가치. 본 세션 토큰 잔량 의식.

### [6] M5 검색 UI

backend GET /messages/search (이미 완비, GIN trgm 인덱스) wiring. 사용자 명시 trigger 패턴 (Enter/검색 버튼, debounce 별도 chunk).

신규 모듈 `search-query.ts` (TDD 16): normalizeQuery / validateQuery (zod 1~100자 정합) / canSearch / highlightMatches (segments 단위, 정규식 메타문자 이스케이프, 대소문자 무시).

신규: `useMessageSearch` hook + `MessageSearch.tsx` 컴포넌트 + /messenger 페이지 우측 영역 통합 (대화 미선택 시 검색 진입점).

**결론**: commit `112c8be` 5 files +414/-11, vitest 686 → 702 (+16).

### [7] M6 운영자 신고 패널

backend GET /admin/reports + POST /admin/reports/[id]/resolve 활용. 운영자 영역 신규 page `/messenger/admin/reports`.

신규 모듈 `report-actions.ts` (TDD 9): formatResolveAction (한국어 라벨) + describeResolveImpact (영향 범위 설명, dialog 본문) + formatReportStatus + ALL_RESOLVE_ACTIONS / ALL_REPORT_STATUSES.

신규: `useReportQueue` hook + page (status 탭 OPEN/RESOLVED/DISMISSED + 신고 row + dialog action select + note + 영향 설명).

권한: Frontend useCurrentUser role !== ADMIN/OWNER → 403 메시지, Backend withTenantRole 가드 자동 검증.

**결론**: commit `2f9125a` 4 files +493, vitest 702 → 711 (+9).

### [8] Sweep — GCM 룰 메모리 승격

S87 이월 P3 항목. S91 인계서의 GCM credential reject 표준 우회 패턴을 메모리로 정착.

`memory/reference_gcm_credential_reject.md` 신규 + MEMORY.md 인덱스 1행 추가. 한국어 Windows + Bash cmdkey 인코딩 함정 + Git 직접 명령 cross-platform 표준 우회 + GCM auto-fallback 메커니즘.

### [9] 본 세션 진척 보고 + 다음 결정

> **사용자**: "2-4번 모두 순차적 진행."

옵션 2 (M6 차단/알림 UI) + 옵션 3 (M5 첨부) + 옵션 4 (kdysharpedge 보안 리뷰) 중 옵션 2 본 세션 진입 가능, 옵션 3 (SeaweedFS multipart 통합 = 큰 chunk) + 옵션 4 (별도 스킬 호출) 는 별도 세션 권고.

### [10] M6 user-blocks 차단 관리 UI

backend GET/POST /user-blocks + DELETE /user-blocks/[id] 활용. 신규 page `/messenger/blocked-users` + `useUserBlocks` hook.

목록 row + 해제 버튼. 차단 사유 표시, createdAt 한국어 포맷, 신규 차단 추가는 별도 chunk (대화 화면 hover 진입).

### [11] M6 notification-preferences UI

backend GET/PATCH /notification-preferences 활용. 신규 page `/messenger/notification-preferences` + `notification-prefs.ts` pure logic (TDD 16).

`notification-prefs.ts`: isValidHHMM (zod HHMM_RE 정합) + normalizeHHMM + isInDndWindow (야간 wrap 지원, dndStart>dndEnd 시 자정 cross 처리, 0폭 윈도 false).

UI: pushEnabled 토글 + mentionsOnly 토글 + dndStart/dndEnd HHMM 입력 + "현재 활성" 인디케이터 + 양쪽 모두 비우거나 모두 채우기 검증 + 사전 검증 에러 표시.

**결론**: commit `5f5253c` 5 files, vitest 711 → 727 (+16).

### [12] 본 세션 토큰 한계 인지 — M5 첨부 + kdysharpedge 별도 세션 위임

옵션 3 (M5 첨부 = SeaweedFS multipart 통합) 단독 큰 chunk + 옵션 4 (kdysharpedge) 별도 스킬 호출 정석 → 본 세션 미진입 결정. /cs 진입 권고.

S94 본 세션 7 commit 누적 (압축 실행 신기록, 평소 1 chunk = 1 commit 패턴 대비 7x 압축, S81 의 5x 초과). 거버넌스 단언 sunset 가까워짐 (M5 검색 ✅ + M6 운영자/차단/알림 ✅, M5 첨부 + 보안 리뷰 잔여).
