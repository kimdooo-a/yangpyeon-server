# 2026-04 작업 로그

## 2026-04-06
### 세션 1
- 프로젝트 초기화
- kdysetting으로 개발 체계 구축
- 스택: Next.js 15 + TypeScript + Tailwind CSS
- 배포 환경: WSL2 Ubuntu + PM2 + Cloudflare Tunnel

### 세션 2 — 대시보드 기능 개선 + GitHub 푸시

**대화 토픽 흐름**: GitHub 초기 푸시 → 충돌 방지 잠금 확인 → 4개 작업 병렬 구현

**변경 사항**:
- GitHub 원격 저장소 연결 + 초기 커밋 푸시 (kimdooo-a/yangpyeon-server)
- 대시보드 홈: CPU/메모리 실시간 미니 라인 그래프 (SVG, 20포인트 버퍼링)
- 대시보드 홈: 디스크 사용률 색상 단계 (초록 <50%, 노랑 <80%, 빨강 ≥80%)
- PM2 프로세스: 이름 클릭 시 상세 정보 모달 (13개 항목)
- PM2 상세 API 신규: GET /api/pm2/detail?name=
- 로그 뷰어: 프로세스 드롭다운 동적 로딩, 키워드 검색, 레벨 필터
- 반응형 사이드바: 모바일 햄버거 메뉴 + 오버레이

**주요 논의**:
- 터미널 A(보안 Wave 2)와 터미널 B(기능 개선) 동시 작업 → docs/locks/active-sessions.md로 영역 분리
- 터미널 B 영역: src/app/ 페이지, src/components/, src/app/api/pm2/
- 터미널 A 영역: src/middleware.ts, src/lib/ (수정 금지)
- 미니 차트: 외부 라이브러리 없이 SVG로 직접 구현 (번들 크기 최소화)

**신규 파일**: `mini-chart.tsx`, `api/pm2/detail/route.ts`
**수정 파일**: `page.tsx`, `stat-card.tsx`, `processes/page.tsx`, `logs/page.tsx`, `sidebar.tsx`, `layout.tsx`

### 세션 3 — 보안 Wave 2 (Rate Limiting + 감사 로그)

**대화 토픽 흐름**: kdywave 보안 리서치 → 보안 취약점 분석 → Wave 1 즉시 수정(인증/명령어주입/CORS) → Wave 2(Rate Limiting/감사로그) → 터미널 간 충돌 방지 → 통합 배포

**변경 사항**:
- 보안 Wave 1: JWT 쿠키 인증 + 로그인 페이지, execSync→execFileSync 전환, CORS/CSRF 미들웨어, 보안 헤더 6종, 브루트포스 방지, 터널 API 민감정보 제거
- 보안 Wave 2: 인메모리 슬라이딩 윈도우 Rate Limiter (API별 차등), 감사 로그 (인메모리 500건 버퍼 + /api/audit 조회)
- 서버 부팅 매뉴얼 작성 (docs/guides/server-boot-manual.md)
- 터미널 B용 상세 인수인계서 + 충돌 방지 잠금 파일 작성
- 세션 2(터미널 B)와 통합 배포 완료

**주요 논의**:
- kdywave S규모 2-Wave 보안 리서치: CRITICAL 4건, HIGH 3건, MEDIUM 4건, LOW 3건 발견
- Edge Runtime에서 fs/path 사용 불가 → 감사 로그를 인메모리 버퍼 방식으로 변경
- standalone output 모드에서 next start 미동작 → output 설정 제거로 해결
- 로그인 SSL 오류: NODE_OPTIONS=--openssl-legacy-provider로 해결
- 터미널 A/B 동시 작업 시 파일 영역 분리 (middleware+lib vs app+components)

**신규 파일**: `src/lib/rate-limit.ts`, `src/lib/audit-log.ts`, `src/lib/auth.ts`, `src/middleware.ts`, `src/app/api/audit/route.ts`, `src/app/api/auth/login/route.ts`, `src/app/api/auth/logout/route.ts`, `src/app/login/page.tsx`, `docs/guides/server-boot-manual.md`, `docs/locks/active-sessions.md`
**수정 파일**: `next.config.ts`, `src/app/api/system/route.ts`, `src/app/api/pm2/route.ts`, `src/app/api/pm2/[action]/route.ts`, `src/app/api/pm2/logs/route.ts`, `src/app/api/tunnel/route.ts`, `src/app/network/page.tsx`, `src/components/layout/sidebar.tsx`

### 세션 4 — 프론트엔드 디자인 전면 개선 + ypserver 배포 스킬

**대화 토픽 흐름**: kdyspike 디자인 리서치 → 풀 스파이크 실행 → 5개 페이지 병렬 디자인 구현 → ypserver 배포 스킬 생성

**변경 사항**:
- kdyspike 풀 스파이크: Supabase Design System, 2026 Dashboard UI 트렌드, 다크 모드 모범 사례, 로그 뷰어 패턴 5건 리서치
- ADR-001: 외부 라이브러리 없이 Tailwind CSS + 인라인 SVG 자체 개선 결정
- 공통 UI 컴포넌트 4개 신규: icons.tsx (SVG 11종), page-header.tsx, status-badge.tsx, empty-state.tsx
- 사이드바 개선: 이모지→SVG 아이콘, border-l-2 brand 활성 표시, 서버 아이콘 로고, 로그아웃 아이콘
- 대시보드(/): PageHeader + 새로고침 + 경과시간, PM2 요약 카드 추가, 디스크 수평바 컴팩트화, 시스템정보 2열 그리드
- 프로세스(/processes): 요약 카드 4개(전체/online/stopped/error), 검색+필터 툴바, StatusBadge, 아이콘 버튼
- 로그(/logs): 줄번호, 레벨 뱃지(ERROR/WARN/INFO), 하단 상태바(처음/끝 + 실시간 표시)
- 네트워크(/network): 요약 카드 2개, CSS 토폴로지 다이어그램(반응형), 연결정보 2열 그리드
- 로그인(/login): radial gradient 배경, 서버 아이콘, 자물쇠 아이콘, 포커스 글로우, 로딩 스피너, 버전 표시
- ypserver 배포 스킬 생성: 빌드→WSL2 배포→PM2 시작/재시작→헬스체크

**주요 논의**:
- 디자인 스파이크에서 UI 라이브러리 도입(shadcn/ui) vs Tailwind 자체 개선 비교 → 5페이지 규모에 라이브러리 과도, 자체 개선 채택
- 5개 페이지를 병렬 에이전트 5개로 동시 구현 (~90초 완료)
- ypserver 스킬: PM2 미등록 시 자동 신규 등록, Cloudflare Tunnel 상태 확인 포함

**신규 파일**: `src/components/ui/icons.tsx`, `src/components/ui/page-header.tsx`, `src/components/ui/status-badge.tsx`, `src/components/ui/empty-state.tsx`, `spikes/spike-001-frontend-design/README.md`, `spikes/spike-001-frontend-design/findings.md`, `docs/research/decisions/ADR-001-frontend-design.md`, `docs/research/_SPIKE_CLEARANCE.md`, `~/.claude/skills/ypserver/SKILL.md`
**수정 파일**: `src/components/layout/sidebar.tsx`, `src/app/page.tsx`, `src/app/processes/page.tsx`, `src/app/logs/page.tsx`, `src/app/network/page.tsx`, `src/app/login/page.tsx`

### 세션 5 — kdywave 종합 분석 + 마스터 개발 계획서 작성

**대화 토픽 흐름**: kdywave 결과 종합 요청 → Wave 4+5 로드맵 전수 분석 → 마스터 개발 계획서 작성 → 문서 트리 연결

**변경 사항**:
- 2개 kdywave 리서치 결과 종합 분석 (Supabase Wave 46문서 + Platform Evolution Wave 20문서)
- `docs/MASTER-DEV-PLAN.md` 마스터 개발 계획서 생성 (세션 5~18, 14세션 계획)
  - Phase 11~15 전체 로드맵, 세션별 작업 목록/파일 목록/코드 패턴/완료 기준
  - 5개 SPIKE 기술 검증 사양, 의존성 그래프, 위험 관리, 스코프 조정 규칙
- CLAUDE.md 문서 트리에 마스터 계획서 연결
- `docs/handover/next-dev-prompt.md` 갱신: 세션 번호 기반 추천 작업으로 전환

**주요 논의**:
- 두 Wave 리서치의 핵심 의사결정(SQLite/Drizzle/SSE/shadcn 등)을 세션 실행 단위로 분해
- 병렬 실행 가능 조합 식별 (11a+11b, 11f+12a, 12c+12d 등)
- 스코프 조정 규칙: 삭감 불가(Zod/SQLite/감사로그/bcrypt) vs 이월 가능 vs 제거 가능

**신규 파일**: `docs/MASTER-DEV-PLAN.md`
**수정 파일**: `CLAUDE.md`, `docs/status/current.md`, `docs/handover/next-dev-prompt.md`

### 세션 6 — ypserver 배포 + Zod 검증 + SPIKE 3건 기술 검증

**대화 토픽 흐름**: 디자인 구현 완료 확인 → ypserver 배포 스킬 생성 → 배포 테스트 → Zod 입력 검증 5개 API 적용 → 재배포 → 마스터 계획서 확인 → SPIKE-01(SQLite+Drizzle) + SPIKE-04(shadcn/ui) 병렬 실행 → SPIKE-02(SSE+Tunnel) 실행 → SPIKE 결과 관리체계 연결

**변경 사항**:
- ypserver 배포 스킬 생성 (`~/.claude/skills/ypserver/SKILL.md`) + 실제 배포 테스트 성공
- Zod 입력 검증: `src/lib/schemas.ts` 공통 스키마 6개 + 5개 API route 적용
- SPIKE-01: better-sqlite3 + Drizzle ORM 설치, DB 스키마 3테이블, 마이그레이션, next.config.ts 설정 — 성공
- SPIKE-04: shadcn/ui init + Button/Card/Dialog 설치, 다크 테마 CSS 변수 재매핑 — 성공
- SPIKE-02: SSE 테스트 엔드포인트 생성, 로컬+Tunnel 경유 실시간 스트리밍 확인 — 성공
- SPIKE 결과 관리체계 구축: `spikes/README.md` 색인, `_SPIKE_CLEARANCE.md` 4건 등록, CLAUDE.md 트리 연결

**주요 논의**:
- ypserver 스킬: PM2 미등록 시 자동 start 분기, Tunnel 상태 확인, 헬스체크 3회 재시도
- Zod `z.coerce.number()`로 쿼리 파라미터 자동 변환, `.default()`로 기본값 처리
- SPIKE-01: `serverExternalPackages` 필수, WAL 모드 + busy_timeout 설정, PM2 단일 인스턴스 호환
- SPIKE-04: shadcn `.dark` 블록 oklch→hex 재매핑 필요, tailwind.config.ts는 충돌 없음
- SPIKE-02: Cloudflare Tunnel이 SSE 네이티브 지원, `X-Accel-Buffering: no` 헤더로 버퍼링 방지
- 마스터 계획서의 Zod는 세션 6 예정이었으나 이번 세션에서 선행 완료

**신규 파일**: `src/lib/schemas.ts`, `src/lib/db/schema.ts`, `src/lib/db/index.ts`, `drizzle.config.ts`, `src/lib/db/migrations/`, `data/.gitignore`, `src/app/api/sse/test/route.ts`, `components.json`, `src/components/ui/button.tsx`, `src/components/ui/card.tsx`, `src/components/ui/dialog.tsx`, `src/lib/utils.ts`, `spikes/README.md`, `spikes/spike-001-sqlite-drizzle-result.md`, `spikes/spike-002-sse-result.md`, `spikes/spike-004-shadcn-result.md`
**수정 파일**: `src/app/api/auth/login/route.ts`, `src/app/api/pm2/[action]/route.ts`, `src/app/api/pm2/logs/route.ts`, `src/app/api/pm2/detail/route.ts`, `src/app/api/audit/route.ts`, `next.config.ts`, `package.json`, `src/app/globals.css`, `CLAUDE.md`, `docs/research/_SPIKE_CLEARANCE.md`

---

### 세션 7 (2026-04-06) — 파일박스 v1→v2 (DB 기반 폴더 관리 + 회원 통합)

**대화 토픽 흐름**: 파일박스 v1 설계·구현 → 배포 → 회원관리 통합 재설계 → 파일박스 v2 구현 → 배포

**변경 사항**:
- 파일박스 v1 구현: 로컬 파일시스템 + JSON 메타데이터, 플랫 파일 리스트
- 파일박스 v1 배포: `/ypserver` 스킬로 WSL2 배포 성공
- 파일박스 v2 재설계: JSON → PostgreSQL(Prisma) + 회원별 폴더 계층 구조
- Prisma 모델 추가: `Folder` (자기참조 트리) + `File` (폴더 소속), User relation 추가
- DB 마이그레이션: `add-filebox` (folders + files 테이블 생성)
- v1 API 라우트 6개 신규: folders(GET/POST), folders/[id](PUT/DELETE), files(POST), files/[id](GET/DELETE), usage(GET)
- UI 컴포넌트 5개: breadcrumb, folder-list, new-folder-dialog, file-upload-zone(수정), file-list(수정)
- 기존 `/api/filebox` 라우트 제거 → `/api/v1/filebox/` 이전
- 기존 JSON 방식 `filebox.ts` 제거 → `filebox-db.ts` 대체
- 부수 수정: Prisma 7 import 경로(`@/generated/prisma/client`), Zod 4 `.errors→.issues`, Prisma lazy 초기화(Proxy)
- `@prisma/adapter-pg` + `pg` 패키지 설치

**주요 논의**:
- 스토리지 방식: Supabase vs 로컬 → 로컬 파일시스템 + DB 메타데이터 선택 (외부 의존성 최소화)
- 폴더 구조: parentId 기반 자기참조 트리, @@unique([parentId, name, ownerId])로 동일 이름 방지
- 권한 모델: USER=자기것만, ADMIN=전체, 대시보드 세션=ADMIN fallback
- 폴더 깊이 제한: MAX_FOLDER_DEPTH=10 (무한 재귀 방지)
- onDelete: Cascade로 폴더 삭제 시 하위 자동 삭제, 물리 파일은 재귀 수집 후 별도 삭제
- Prisma 7 빌드 이슈: DATABASE_URL 미설정 시 빌드 실패 → Proxy 패턴으로 lazy 초기화

**신규 파일**: `src/lib/filebox-db.ts`, `src/lib/schemas/filebox.ts`, `src/app/api/v1/filebox/folders/route.ts`, `src/app/api/v1/filebox/folders/[id]/route.ts`, `src/app/api/v1/filebox/files/route.ts`, `src/app/api/v1/filebox/files/[id]/route.ts`, `src/app/api/v1/filebox/usage/route.ts`, `src/components/filebox/breadcrumb.tsx`, `src/components/filebox/folder-list.tsx`, `src/components/filebox/new-folder-dialog.tsx`, `prisma/migrations/20260406140506_add_filebox/`
**수정 파일**: `prisma/schema.prisma`, `src/lib/prisma.ts`, `src/app/filebox/page.tsx`, `src/components/filebox/file-upload-zone.tsx`, `src/components/filebox/file-list.tsx`, `src/components/ui/icons.tsx`, `src/components/layout/sidebar.tsx`, `src/lib/rate-limit.ts`, `src/middleware.ts`, `src/lib/schemas.ts`, `.env.example`
**제거 파일**: `src/lib/filebox.ts`, `src/app/api/filebox/route.ts`, `src/app/api/filebox/[id]/route.ts`

### 세션 8~12 — 대규모 기능 구현 스프린트 (2개 터미널 병렬)

**Phase 11b: Sonner 토스트** — `sonner` 설치, `use-pm2-action.ts` 훅, 프로세스/회원 페이지 토스트

**Phase 11d+11e: 감사 로그 DB** — `audit-log.ts`(Edge) + `audit-log-db.ts`(Node.js) 분리, 인메모리→SQLite flush 패턴

**Phase 11f: IP 화이트리스트** — `ip-whitelist.ts`(DB) + `ip-whitelist-cache.ts`(Edge 캐시), CRUD API, 미들웨어 IP 검사, `/settings/ip-whitelist` 관리 UI

**Phase 12a: 메트릭 히스토리** — `metrics-collector.ts` (1분 수집), `metrics-area-chart.tsx` (Recharts), `/metrics` 차트 페이지 (1h/24h/7d/30d)

**Phase 12b: SSE 실시간** — `use-sse.ts` 범용 훅 (재연결+폴백), SSE 엔드포인트 3개 (metrics/pm2/logs), 대시보드/프로세스/로그 폴링→SSE 전환, `sse-indicator.tsx` 연결 상태

**Phase 12c: 감사 로그 UI** — `/audit` 페이지 (TanStack Table v8, 서버사이드 페이지네이션, 6개 필터, CSV/JSON 내보내기)

**Phase 12d: 환경변수 관리** — `/settings/env` 페이지 (.env 파일 직접 파싱/수정, 민감 키 마스킹, 인라인 편집)

**Phase 13a: DB 인증 통합** — 대시보드 JWT에 `{ sub, email, role }` 포함, `login-v2` 브릿지로 v1→대시보드 쿠키 발급, 레거시 DASHBOARD_PASSWORD 30일 fallback

**Phase 13b: 역할 기반 접근 제어** — 미들웨어 ADMIN_ONLY_PATHS 보호, `use-current-user.ts` 훅, 사이드바 role별 메뉴 필터, `/settings/users` 관리 페이지

**Phase 13c: Cmd+K** — `command-menu.tsx` (cmdk), 네비게이션 11개 + 액션, 역할 기반 필터

**보류: Phase 13d** — 스켈레톤 UI (다른 터미널 테마 작업과 충돌 방지)

## 2026-04-12

### 세션 14 — 중단 터미널 3개 복구 + Phase 13d 완료

**대화 토픽 흐름**: 프로젝트 진척도 파악 → 중단된 3개 터미널 식별 → 잔여 분 이어받아 마무리 → Plan mode 진입 → Phase 13d 잔여 7개 페이지 설계·구현

**변경 사항**:
- Git 상태 진단으로 미커밋 3개 파일(page.tsx, processes/page.tsx, current.md) + plans/wiggly-discovering-crescent.md 분석
- 터미널 1(대시보드 스켈레톤) — 기존 변경분 그대로 커밋
- 터미널 2(processes 스켈레톤) — 디테일 모달의 `로딩 중...` 텍스트를 동일 패턴 스켈레톤으로 완성 후 커밋
- 터미널 3(세션 13 종료 문서화) — `next-dev-prompt.md`가 "세션 8~12 (최신)"에 머물러 있던 것을 세션 13 기준으로 갱신 (필수 참조 파일 목록 + 최근 완료된 작업 + 즉시 가능 항목 재정렬)
- Plan mode로 Phase 13d 잔여 범위 확정 → `plans/squishy-giggling-starfish.md` 계획서 작성·승인
- 7개 페이지 스켈레톤 + EmptyState 일괄 적용:
  - 테이블 4종(members, settings/users, settings/env, settings/ip-whitelist): `{loading ? ... : empty ? ... : table}` 3분기 구조 → `table > tbody > {loading | empty | rows}` 구조로 리팩토링, 로딩 중에도 헤더 노출
  - members/[id]: early return을 2열 폼 그리드 스켈레톤 + 버튼 스켈레톤으로 교체, not-found는 EmptyState
  - network: 요약 카드 2개 스켈레톤 (상태 dot + 제목 + 값 + 서브텍스트 4단 구조)
  - filebox: 스핀 로더 → 폴더/파일 아이템 5개 스켈레톤 (아이콘 영역 포함)
- `docs/status/current.md` Phase 13d 체크박스 [x], 세션 14 행 추가
- `docs/handover/next-dev-prompt.md` — Phase 13 완료 선언, 다음은 Phase 14a(TanStack Table Editor)

**주요 논의**:
- 중단 터미널 식별 근거: 미커밋 파일 3개가 정확히 3개 터미널 흔적과 일치. 단, current.md의 세션 13 handover 링크는 깨진 상태(파일 미존재)였고 next-dev-prompt가 여전히 세션 8~12 기준 — 이것이 "세션 종료 프로토콜 4단계 중 마지막 단계가 빠진" 확정 증거
- EmptyState vs 공통 Skeleton 컴포넌트 추출 — 기존 audit/processes/dashboard 인라인 패턴과의 일관성 유지를 위해 Skeleton은 추출하지 않음(Phase 15c shadcn 전환 시 한 번에 정리), EmptyState는 이미 존재하므로 6개 페이지에 재사용
- 테이블 구조 리팩토링 부수 효과: 로딩 중에도 thead가 노출되어 진입 직후 "이건 테이블"이 즉시 인지됨 + 레이아웃 시프트 0
- EmptyState를 `<tr><td colSpan>` 안에 래핑하는 관용구 도입 — 시맨틱은 어색하지만 테이블 폭 유지 + 중앙 정렬이 가장 간결
- logs/page.tsx는 SSE 스트리밍 구조라 스켈레톤 불필요 (빈 상태 "로그가 없습니다"만 존재)
- metrics의 "로딩 중..." 1건은 "마지막 갱신" 메타 라벨이므로 유지 결정

**커밋**:
- `2ca108d` Phase 13d 스켈레톤 UI 착수 + 세션 13 종료 문서화 마무리 (4 files, +67/-11)
- `7a699af` Phase 13d 완료 — 7개 페이지 스켈레톤 UI + EmptyState 일괄 적용 (9 files, +188/-103)

**수정 파일**: `src/app/page.tsx`, `src/app/processes/page.tsx`, `src/app/members/page.tsx`, `src/app/members/[id]/page.tsx`, `src/app/network/page.tsx`, `src/app/filebox/page.tsx`, `src/app/settings/users/page.tsx`, `src/app/settings/env/page.tsx`, `src/app/settings/ip-whitelist/page.tsx`, `docs/status/current.md`, `docs/handover/next-dev-prompt.md`

**검증**: `npm run build` 통과 (46개 라우트), `npx tsc --noEmit` 0 에러, 잔존 "로딩 중..." UI 0건

### 2026-04-12 (세션 15) — Supabase 관리 체계 이식 (Phase 14-S, 55 신규 파일)

**대화 토픽 흐름**: Supabase 13 페이지 스크랩 접수 → 범위 확정(Q&A) → Phase A(리서치+문서화) 병렬 실행 → 본문서 매핑/갭 분석/ADR → Phase B-L0(Prisma +7모델, 사이드바 5그룹 재편) → L1(공용 pg/introspect/runner) → L2(3 클러스터 병렬 55 파일) → L3(통합) → 검증(tsc/prisma validate/migration SQL draft).

**변경 사항**:
- **문서 (Phase A, 23건)**
  - `docs/references/supabase-scrape/` 신규 디렉토리 + 14개 md (index + 00~13) — 스크랩 원본 보존
  - `docs/references/_SUPABASE_TECH_MAP.md` — 13 모듈 OSS 기술 매핑 (GitHub/docs/자체구현 3단계)
  - `docs/references/_PROJECT_VS_SUPABASE_GAP.md` — P0/P1/P2 우선순위 + DAG
  - `docs/research/decisions/ADR-002-supabase-adaptation-strategy.md` — Option B(UI 학습 + Next.js 네이티브 재구현) 결정
  - `docs/research/spikes/spike-005-{sql-editor, schema-visualizer, advisors, edge-functions, data-api}.md` (5건)
  - `docs/references/_index.md`, `docs/research/_SPIKE_CLEARANCE.md`, `CLAUDE.md`(트리 도식), `docs/MASTER-DEV-PLAN.md`(부록 세션 14-S) 갱신
- **Prisma 스키마 확장**
  - 7 모델 추가: `SqlQuery`, `EdgeFunction`, `EdgeFunctionRun`, `Webhook`, `CronJob`, `ApiKey`, `LogDrain`
  - 7 enum 추가: `QueryScope`, `FunctionRuntime`, `RunStatus`, `WebhookEvent`, `CronKind`, `ApiKeyType`, `DrainType`
  - User 관계 확장: `sqlQueries`, `edgeFunctions`, `apiKeys`
- **공용 라이브러리 (L1)**
  - `src/lib/types/supabase-clone.ts` — 전 모듈 공용 타입
  - `src/lib/pg/pool.ts` — 직접 PG 풀 + `runReadonly(sql, params, {timeoutMs, useReadonlyRole})` (BEGIN READ ONLY + SET LOCAL ROLE app_readonly + statement_timeout)
  - `src/lib/pg/introspect.ts` — `buildSchemaGraph`, `listColumns`, `listForeignKeys`, `listPrimaryKeys`, `listIndexedColumns`
  - `src/lib/runner/isolated.ts` — `runIsolatedFunction(code, ctx)` (node:vm + safeFetch 화이트리스트 + 타임아웃 + 코드 256KB 제한)
- **L2 병렬 구현 (55 파일)**
  - Cluster A (SQL Editor + Data API + Schema Visualizer, 13 파일)
  - Cluster B (Edge Functions + Cron + Realtime, 14 파일)
  - Cluster C (Advisors Security+Performance + Webhooks + API Keys + Backups + Log Drains, 28 파일)
- **사이드바 재편**: 5 그룹(운영/콘텐츠/데이터베이스/개발 도구/감사·설정) + 11 신규 메뉴 + 2단계 RBAC(`ADMIN_ONLY_PATHS`/`MANAGER_PLUS_PATHS`)
- **마이그레이션 드래프트**: `prisma/migrations-draft/all_tables_from_empty.sql` (231 줄, 10 테이블)
- **타입 수정**: `pg` `QueryResultRow` 제약 도입 (runReadonly 제네릭)

**주요 논의**:
- **Option A(Supabase 전체 self-host) vs B(UI 학습 + Next.js 재구현) vs C(PostgREST만 사이드카)** 중 B 선정 이유: WSL2+PM2 단일 프로세스 유지, 기존 Prisma/파일박스/세션/audit 인프라와 충돌 없음, 신규 의존성 4~5개로 억제.
- **PostgREST 사이드카 거부**: 모델 3개 규모(User/Folder/File)에 Haskell 바이너리 운영 비용 과다. Prisma DMMF + 동적 `[table]` 라우트로 대체.
- **vm2 금지**: DEPRECATED + 공개 탈출 PoC. v1은 `node:vm` + `worker_threads`, v2는 `isolated-vm`, v3는 Vercel Sandbox 원격 오프로드 경로 ADR-002에 문서화.
- **Prisma `$queryRawUnsafe` 금지** (SQL Editor): 공식 경고. 별도 `pg` 풀 + `app_readonly` 롤 + BEGIN READ ONLY + statement_timeout 다층 방어.
- **Data API RLS 대체**: Prisma `where` 강제 주입(USER 역할은 `ownerId=본인`) + 컬럼 allowlist(`passwordHash` 영구 제외).
- **병렬 구현 충돌 방지**: L0에서 `schema.prisma`에 모델을 미리 확정 → L2 3 클러스터가 독립 파일 트리만 건드려 머지 충돌 0.
- **Prisma migrate 자동 실행 보류**: DATABASE_URL의 dev/prod 여부 불확실 — `migrate diff --from-empty --to-schema` 로 SQL 드래프트만 생성.
- **Vercel 관련 스킬(bootstrap/vercel-storage/next-upgrade/shadcn/nextjs) 수차례 주입됨**: 본 프로젝트는 WSL2+PM2+Cloudflare Tunnel 자체 호스팅이고 ADR-002가 Vercel 미사용을 명시 → 전부 스킵.

**수동 선결 작업**:
1. `npx prisma migrate dev --create-only --name supabase_clone_session_14` (dev DB에서, 증분 마이그레이션 생성)
2. PG에 `CREATE ROLE app_readonly; GRANT SELECT ON ALL TABLES IN SCHEMA public TO app_readonly;`
3. `.env`에 `ENABLE_DB_BACKUPS=true` 설정 시 Backups 활성
4. 선택: `npm i @monaco-editor/react @xyflow/react elkjs` — UI 고도화

### 2026-04-12 (세션 16) — Supabase 클론 배포 + 레거시 에러 수정 + Tunnel 복구

**대화 토픽 흐름**: 세션 15 인수인계 복기 → 증분 마이그레이션 검증(shadow DB 없이 수동 작성) → psql 연결 트러블슈팅(schema 쿼리 파라미터/pager/히어닥 들여쓰기) → `migrate deploy` 적용 → `app_readonly` 롤 + SELECT + SET ROLE 검증 → `.env` 수정(개행 누락 복구) → `npm i` UI 패키지 → prisma generate + tsc + build(84 라우트) → PM2 재시작 → 12개 P0 페이지 HTTP 307 smoke → 로그에서 레거시 에러 2건 발견(`P2003 folders_owner_id_fkey`, `Cannot open database because the directory does not exist`) → 방어 수정 → 커밋 `90c1c1e` 푸시 → Cloudflare 1033 에러 조사 → `cloudflared` PM2 등록(4 connection registered) → `pm2 save`.

**변경 사항**:
- **DB 스키마 변경 (PostgreSQL `luckystyle4u`)**
  - `prisma/migrations/20260412120000_supabase_clone_session_14/migration.sql` (파일은 세션 15 커밋 `198f467`에 포함, 본 세션에서 `migrate deploy`로 적용)
  - 7 신규 enum 생성: `QueryScope`, `FunctionRuntime`, `RunStatus`, `WebhookEvent`, `CronKind`, `ApiKeyType`, `DrainType`
  - 7 신규 테이블 생성: `sql_queries`, `edge_functions`, `edge_function_runs`, `webhooks`, `cron_jobs`, `api_keys`, `log_drains`
  - 신규 인덱스 6개, FK 4개
- **PG 롤**
  - `app_readonly` NOLOGIN 롤 생성
  - `GRANT USAGE ON SCHEMA public`, `GRANT SELECT ON ALL TABLES`, `ALTER DEFAULT PRIVILEGES ... GRANT SELECT` (미래 테이블 포함)
  - `GRANT app_readonly TO postgres` (앱 유저의 SET ROLE 권한)
  - 검증: `BEGIN READ ONLY; SET LOCAL ROLE app_readonly; SELECT` 통과, `INSERT` 거부됨
- **환경변수**
  - `.env` 13번 라인: `ENABLE_DB_BACKUPS=true` 추가 (이전 `>>` append 시 개행 누락으로 JWT_V1_REFRESH_SECRET 값에 붙어 있던 것을 `sed`로 분리 복구)
- **의존성 설치**: `@monaco-editor/react`, `@xyflow/react`, `elkjs` (+29 packages, −7)
- **코드 수정 (레거시 에러 방어 — 커밋 `90c1c1e`)**
  - `src/lib/db/index.ts`: better-sqlite3 연결 전 `fs.mkdirSync(path.dirname(DB_PATH), { recursive: true })` 보장
  - `src/lib/filebox-db.ts`: `StaleSessionError` 클래스 추가, `getOrCreateRootFolder`에서 user 존재 여부 선검증 후 throw
  - `src/lib/api-guard.ts`: `runHandler` 래퍼 추가 → `StaleSessionError`를 `401 STALE_SESSION`으로 매핑 (기존 Bearer/쿠키 세션 경로 모두 적용)
- **운영 (PM2)**
  - `dashboard` 재시작 (pid 2569 → 3040)
  - `cloudflared` PM2 신규 등록 (id 1, pid 3136, `cloudflared tunnel run`) — tunnel UUID `2e18470f-b351-46ab-bf07-ead0d1979fb9`, 4 connection(icn01/05/06×2)
  - `pm2 save` → `~/.pm2/dump.pm2`

**주요 논의**:
- **shadow DB 없이 증분 SQL 생성**: Prisma 7.x에서 `--from-migrations`는 shadow DB 필수, `--from-url` 제거됨(→ `--from-config-datasource`). WSL2 PG가 Windows 쪽에서 도달 불가한 네트워크 분리 때문에 실라이브 diff 실패 → 수동으로 `all_tables_from_empty.sql`에서 기존 적용분(init_users, add_filebox) 제외하여 `session_14_incremental.sql` 신규 작성.
- **증분 드래프트 파일 정리**: 세션 15에 생성된 `supabase_clone_session_14.sql`(5줄 에러 로그)을 삭제, 혼동 방지 위해 신규 `session_14_incremental.sql`만 유지하고 기존 `all_tables_from_empty.sql`은 참고용으로 남김(현 DB에 그대로 돌리면 충돌하므로 사용 금지).
- **Cloudflare Tunnel 1033 진단**: systemd 유닛 없음 → PM2에도 미등록 → 수동 `nohup` 프로세스였음 짐작(Windows 재시작/셸 종료 시 사라짐). PM2에 등록하여 WSL 재시작/자원 회수에 대비.
- **Vercel 플러그인 스킬 계속 주입됨**(vercel-sandbox/functions/cli/storage/bootstrap/nextjs 등): 본 프로젝트는 자체 호스팅(WSL2+PM2+Cloudflare Tunnel)이므로 전부 false positive 판정, 스킵. ADR-002 재확인.
- **레거시 에러 스코프 판단**: P2003(folder FK)과 SQLite 디렉토리 문제는 세션 14 이전부터 잠재한 버그. 세션 16 스코프 경계상 별도 커밋(`90c1c1e`)으로 분리.

**검증**:
- `\dt` → 테이블 11개 (`_prisma_migrations`+3 기존+7 신규)
- `\dT` → enum 8개 (`Role`+7 신규)
- `_prisma_migrations` → 3 행 (init_users, add_filebox, supabase_clone_session_14)
- `npx prisma validate` → OK
- `npx tsc --noEmit` → 에러 0
- `npm run build` → 84 라우트 컴파일 (12 P0 신규 포함), 11 vulnerabilities (moderate 10, high 1) — 차기 세션 대상
- 12개 P0 페이지 HTTP 307 (로그인 리다이렉트) — 전 라우트 정상
- `curl -sI https://stylelucky4u.com` → HTTP/2 307 → /login — Cloudflare edge → WSL2 전 경로 통신 OK

**Git 커밋**:
- `90c1c1e` fix: 레거시 런타임 에러 2건 방어 (감사 로그 디렉토리 + 스테일 세션) — 3 files, +30/-5 (푸시됨)

## 2026-04-12 (오후)

### 세션 17 — SQL Editor Monaco + Schema Visualizer xyflow 치환 + 12 P0 E2E

**대화 토픽 흐름**: 다음 세션 작업 확인 → 3개 작업 승인 → Monaco 동적 import 치환 → xyflow+elkjs SchemaFlow 분리 → 1차 E2E 404 → localhost:3000이 WSL PM2 prod임 역추적 → Windows 빌드 블로킹 우회(ypserver Phase 2 강행) → WSL 재배포 → 12 P0 순회 → 기본 SQL 테이블명 오류 부수 수정 → 2차 재배포 → UI 최종 검증

**변경 사항**:
- `src/app/sql-editor/page.tsx`: `<textarea>` → `MonacoEditor = dynamic(@monaco-editor/react, { ssr:false })`. 컨테이너 `h-64` 고정, `automaticLayout:true`, `wordWrap:"on"`, minimap off, fontSize 13. `onMount`에서 `monaco.KeyMod.CtrlCmd | monaco.KeyCode.Enter` → `#sql-execute-btn.click()`. 기본 쿼리 `FROM "User"` → `FROM users` (Prisma `@@map("users")`).
- `src/app/database/schema/page.tsx`: 카드 그리드 삭제, `SchemaFlow` 동적 import 래퍼 + 컨테이너 `h-[640px]`. `@xyflow/react/dist/style.css` 직접 import.
- `src/app/database/schema/SchemaFlow.tsx` (신규): `ReactFlow` + `ReactFlowProvider` + 커스텀 `TableNode` + ELK 비동기 layered(RIGHT) 레이아웃. 노드 높이 = 44 + cols*22 + 8. Background(dots gap 20)/Controls(br)/MiniMap(bl pannable zoomable). `hideAttribution`. 언마운트 레이스 방어용 `cancelled` 플래그.
- `.gitignore`: `/.playwright-mcp/`, `/e2e-*.png` 추가.

**주요 논의**:
- **Monaco 동적 import 이유**: `@monaco-editor/react`는 내부에서 `window` 접근 → SSR에서 ReferenceError. `next/dynamic(ssr:false)` + 로딩 플레이스홀더 병행으로 초기 번들 부담까지 분리.
- **xyflow CSS 누락 함정**: v12부터 `@xyflow/react/dist/style.css` 명시 import 필수. 빠뜨리면 "아무것도 안 보임" 증상으로 디버깅 시간 낭비 가능 → 최초 구현부터 반영.
- **elkjs 비동기 레이아웃 패턴**: `elk.layout()`은 Promise. 초기 마운트 시 position(0,0)로 모든 노드 배치 후 Promise resolve → `setNodes(positioned)` 재주입. 노드 높이를 컬럼수 비례(22px/row + 44px header + 8px footer)로 ELK에 전달 → FK 엣지 겹침 방지.
- **localhost:3000 정체**: `Get-NetTCPConnection -LocalPort 3000` → `wslrelay.exe` 소유 → dev 서버가 아닌 WSL2 PM2 프로덕션. 코드 변경은 빌드+배포 전까지 무효 확인. 세션 17 초반 404 증상의 원인.
- **Windows 빌드 블로킹**: `lightningcss-win32-x64-msvc` optional native binary 미설치로 `next build` 실패. WSL2 Linux에는 올바른 바이너리 존재하므로 실제 배포는 정상. `/ypserver` 스킬 Phase 1 항상 실패하는 환경 이슈로 승격 → 스킬 보강 과제화.
- **기본 SQL 오류**: 세션 14/15 스캐폴드 시 `"User"` 하드코딩. 실제 Prisma 마이그레이션은 `@@map("users")`로 소문자 `users` 생성. Monaco→state→API 파이프라인 정상 검증 도중 드러남. 한 라인 수정 + 재배포로 해결.

**검증**:
- WSL2 `npm run build` → 84 라우트, 12 P0 페이지 `○ (Static)` prerender
- Windows `npx tsc --noEmit` → 에러 0
- PM2 dashboard 재시작 3→4→5 (Monaco/xyflow 초기 빌드 + 기본 쿼리 수정 2차)
- Cloudflare Tunnel online (session 16에서 PM2 등록분 그대로)
- Playwright 12 P0 페이지 순회: 콘솔 에러 0건 (경미 warning 1건씩은 deprecation/cookie)
- `/api/v1/sql/execute` POST `SELECT id,email,role FROM users LIMIT 5;` → 200, 1 row
- SQL Editor UI 실행 버튼 클릭 → 결과 테이블에 1행 렌더 (kimdooo@stylelucky4u.com, ADMIN)

**신규 파일**: `src/app/database/schema/SchemaFlow.tsx`, `docs/handover/260412-session17-monaco-xyflow.md`, `docs/logs/journal-2026-04-12.md`, `docs/solutions/2026-04-12-monaco-ssr-dynamic-pattern.md`, `docs/solutions/2026-04-12-xyflow-elkjs-async-layout.md`, `docs/solutions/2026-04-12-windows-lightningcss-missing.md`
**수정 파일**: `src/app/sql-editor/page.tsx`, `src/app/database/schema/page.tsx`, `.gitignore`, `docs/status/current.md`, `docs/handover/next-dev-prompt.md`, `docs/handover/_index.md`, `docs/logs/2026-04.md`

## 2026-04-12 (저녁)

### 세션 18 — 근본 auth 아키텍처 재설계 + 기술부채 정리 + Phase 14a Table Editor

**대화 토픽 흐름**: 작업 선택(정리형 순차) → D1 범위 격상 논의(L→M) → "근본 해결" 요청 → Plan Mode(Explore 3 병렬 + Plan 1) → D1-1 auth-guard → D1-2 (protected) 그룹 + 15 git mv → D1-3 Handler 재검증 → D1-4 proxy.ts → D1-5 Bearer 전용화 → D2 NFT → D3 audit → D4 instrumentation → A 착수 중 D1-5 회귀 발견(30여 UI 파손 + authZ 버그) → D1-5 재조정 → A Table Editor 구현 → WSL2 빌드 실패(proxy `runtime` 선언 금지) → 수정 → 빌드 성공 → E2E curl → 커밋 & 푸시

**변경 사항**:
- `src/lib/auth-guard.ts` (신규): `requireSession`/`requireRole`/`requireSessionApi`/`requireRoleApi`. `cookies()` 직접 호출 + discriminated union 반환.
- `src/app/(protected)/layout.tsx` (신규): `await requireSession()`.
- `src/app/(protected)/(admin)/layout.tsx` (신규): `await requireRole("ADMIN")`.
- `src/app/` 하위 15개 디렉터리 `git mv` → `(protected)/` 13개 + `(protected)/(admin)/` 2개(audit, settings).
- 쿠키 기반 Route Handler 8개에 `requireSessionApi`/`requireRoleApi` 삽입: `/api/pm2`(GET 4종), `/api/pm2/[action]`, `/api/settings/env`(3 메서드), `/api/settings/ip-whitelist`(3 메서드), `/api/settings/users`(3 메서드, api-guard.withRole → requireRoleApi 이관).
- `src/proxy.ts` (신규, middleware.ts 삭제): IP/RateLimit/CORS/CSRF/감사로그만. `export proxy`. `export const runtime` 선언 없음(Next.js 16 proxy 금지).
- `src/lib/api-guard.ts`: 최종본 = Bearer 우선 + 쿠키 fallback 재도입(실제 세션 주체 기반 `resolveCookieSession`). 기존 `checkDashboardSession` 제거(누가 로그인하든 "첫 ADMIN" 반환하던 authZ 버그).
- `next.config.ts`: `outputFileTracingExcludes` 추가(pg_dump + Prisma 엔진 트레이스 제외).
- `package.json`/`package-lock.json`: `next` 16.2.2 → 16.2.3, hono/dompurify transitive 패치.
- `src/instrumentation.ts` (신규): `NEXT_RUNTIME === "nodejs"` 가드로 `ensureStarted()` 호출.
- `src/app/api/v1/tables/route.ts` (신규): 목록(`pg_tables` + `pg_class.reltuples` + 컬럼수).
- `src/app/api/v1/tables/[table]/route.ts` (신규): 페이지네이션 조회(limit≤200, order/dir, identifier 정규식+DB 존재 대조+`quote_ident`).
- `src/app/api/v1/tables/[table]/schema/route.ts` (신규): 컬럼 메타 + PK 플래그.
- `src/app/(protected)/tables/page.tsx` (신규): 카드 그리드.
- `src/app/(protected)/tables/[table]/page.tsx` (신규): 데이터 뷰.
- `src/components/table-editor/table-data-grid.tsx` (신규): TanStack Table v8 + 클릭 정렬 + 페이지네이션.
- `src/components/table-editor/column-type-badge.tsx` (신규): 타입별 색상 배지.
- `src/components/layout/sidebar.tsx`: `/tables` 등록 + `MANAGER_PLUS_PATHS`.

**주요 논의**:
- **L vs M (근본 해결)**: 단순 파일 리네임(L)은 Deprecation 경고만 끄고 proxy에 auth가 그대로 남아 CVE-2025-29927 취약 패턴 유지. M은 proxy에서 auth 제거 + Layout/Handler 재검증 이중 방어로 **구조적 차단**. 사용자 "근본 해결로 접근" 결단.
- **(protected) 이동 전략**: 물리 이동 vs route group 경로 조정. 물리 이동 채택 이유: ① Layout 상속으로 "보호 의도"를 타입 시스템에 명시, ② 신규 페이지 자동 보호, ③ URL 불변(사이드바/북마크 영향 0). git mv로 rename 인식 → 히스토리 보존.
- **D1-5 회귀 사건**: api-guard를 Bearer 전용으로 좁힌 순간 대시보드 30여 페이지 `fetch('/api/v1/*')`가 401 파손. 발견 과정에서 기존 `checkDashboardSession`이 "누가 로그인하든 첫 ADMIN 반환"하는 **별도 authZ 버그** 동시 포착. 재조정: CVE-2025-29927은 middleware 헤더 우회 버그로 Route Handler의 `request.cookies` 직접 읽기는 영향 밖 → 쿠키 fallback 자체는 안전. 새 `resolveCookieSession`은 `getSessionFromCookies()` + `prisma.user.findUnique({id: session.sub})` + `isActive` 검증으로 실제 세션 주체 기반.
- **proxy.ts runtime 선언 금지**: Next.js 16 빌드 오류 "Route segment config is not allowed in Proxy file". proxy.ts는 암시적 Node.js 런타임이며 `runtime`/`dynamic` 등 route segment config 전면 금지. 주석으로 대체.
- **Turbopack NFT + outputFileTracingExcludes**: `next.config.ts` → `pgdump.ts` spawn이 런타임 바이너리 경로 결정이라 NFT가 전체 프로젝트로 트레이스 확장. `outputFileTracingExcludes`에 `/api/v1/backups` 범위로 `pg_dump*` + Prisma 엔진 제외 명시. 빌드 오류 사라짐, 경고 자체는 잔존(산출물 정상).
- **npm audit 잔여 9 moderate**: drizzle-kit 0.31.10이 번들한 `@esbuild-kit/esm-loader@2.6.5` 체인 + monaco-editor 0.55.1 → dompurify@3.2.7. `audit fix --force`는 drizzle-kit 0.18.1 다운그레이드 요구(마이그레이션/스튜디오 회귀). dev-only + 업스트림 미배포 상태로 수용.
- **Table Editor 보안 3중 방어**: ① identifier 정규식(`^[a-zA-Z_][a-zA-Z0-9_]*$`) ② DB 존재 대조(information_schema) ③ 동적 quote_ident(`"..."`+`""` 이스케이프) + `app_readonly` 롤 + `BEGIN READ ONLY`.

**검증**:
- `npx tsc --noEmit` 각 단계 통과
- WSL2 `npm run build` → 84+ 라우트, `ƒ Proxy (Middleware)` 인식, `/tables`, `/tables/[table]` 정상 포함, `Ready in 119ms`
- PM2 `dashboard` 재시작 6→7 (proxy runtime 수정 재빌드 포함)
- E2E curl: `/login` 200 / `/tables` 307(→ login) / `/api/v1/tables` 무인증 401 / `/processes` 307
- 신규 에러 로그 0건 (pm2 flush 후 재테스트)

**Git 커밋** (11개, eb35a1f..77be0fe):
- `8f22829` D1-1 auth-guard 헬퍼
- `b50c40f` D1-2 (protected) + 15 페이지 git mv
- `03a2da9` D1-3 Handler 재검증
- `cf04c57` D1-4 proxy.ts 전환
- `bb89723` D1-5 api-guard Bearer 전용화 (초판)
- `4f7ead2` D2 Turbopack NFT
- `1159fe8` D3 npm audit (next 16.2.3)
- `e83025d` D4 instrumentation.ts
- `db65b02` D1-5 재조정 (쿠키 fallback + authZ 버그 수정)
- `26b65b6` A Phase 14a Table Editor (8 파일)
- `77be0fe` proxy.ts runtime 선언 제거

**신규 파일**: `src/lib/auth-guard.ts`, `src/proxy.ts`, `src/instrumentation.ts`, `src/app/(protected)/layout.tsx`, `src/app/(protected)/(admin)/layout.tsx`, Table Editor 세트 5개 (`api/v1/tables/*`, `(protected)/tables/*`, `components/table-editor/*`), `docs/handover/260412-session18-auth-refactor.md`, `docs/solutions/2026-04-12-nextjs16-proxy-migration-cve.md`, `docs/solutions/2026-04-12-cookie-authz-bug-first-admin-fallback.md`
**이동 파일**: `src/app/` 하위 15개 디렉터리 → `(protected)/` 또는 `(protected)/(admin)/`
**삭제 파일**: `src/middleware.ts`
**수정 파일**: `src/lib/api-guard.ts`, `next.config.ts`, `package.json`, `package-lock.json`, `src/components/layout/sidebar.tsx`, 쿠키 API 8개(Route Handler), `docs/status/current.md`, `docs/handover/next-dev-prompt.md`, `docs/handover/_index.md`, `docs/logs/journal-2026-04-12.md`, `docs/logs/2026-04.md`

---

### 2026-04-12 (세션 19) — 세션 18 후속: 운영/보안 잔가지 정리

**대화 토픽 흐름**: 세션 18 리포트 검토 → proxy.ts runtime 제거 커밋 상태 확인 → 잔여 이슈 3건(npm audit/NFT/E2E) 순차 처리 → Phase 14b CRUD 프롬프트 준비 → 운영/보안 잔가지 묶음(#2 감사로그 + #3 data/ mkdir + #4 E2E 실행) → 커밋/푸시 → 세션 종료

**변경 사항**:
- **잔여 이슈 분류** (`dec6abe`): npm audit moderate 9건을 현 배포 비익스플로이트로 분류 — monaco-editor 커스텀 provider 0건, @prisma/dev는 dev 서브커맨드 전용, drizzle-kit은 devDependencies. Turbopack NFT 경고는 `paths.ts` 분리(download 라우트 trace 제거)로 범위 축소했으나 `/api/v1/backups` 라우트의 동적 fs 연산은 구조적 한계로 cosmetic 확정. `outputFileTracingExcludes`로 번들 제외는 유지. Table Editor 수동 E2E 가이드 `docs/guides/tables-e2e-manual.md` 신규 작성(7 시나리오).
- **auth-guard 감사 로그** (`59ead98`): `requireSessionApi(request)` / `requireRoleApi(request, role)` 시그니처로 request 필수화. 세션 실패 → `AUTH_FAILED`(401), role 부족 → `FORBIDDEN`(403) `writeAuditLog` 자동 기록(method/path/IP/email/요구 role). 8개 호출처(settings/users, settings/ip-whitelist, settings/env, pm2 4종) 일괄 업데이트. `npx tsc --noEmit` 0 오류.
- **instrumentation data/ mkdir** (`189785c`): `getDb()` 최초 호출 이전 경로에서 발생하던 "Cannot open database because the directory does not exist" 노이즈 제거. `src/instrumentation.ts`의 `register()`가 `mkdirSync('data', { recursive: true })` 수행 후 Cron `ensureStarted()` 호출.
- **Table Editor 프로덕션 E2E** (`e36e598`): `stylelucky4u.com`에서 ADMIN 로그인 후 Chrome DevTools MCP로 S1/S3~S6 수행. 결과: S1 307→login ✅ / S3 11 테이블 목록 ✅ / S4 users 10컬럼+타입 배지 렌더 ✅ / S5 PATCH/POST/DELETE 405 + SQL UPDATE 400 DANGEROUS_SQL ✅ / S6 6/6 injection 벡터 400/403/404 차단 ✅. S2(VIEWER) S7(PM2 로그) 별도 수행 필요. 후속: 목록 "행 ~-1" cosmetic + identifier regex `{1,63}` 권장.
- **Phase 14b CRUD 프롬프트** (`ae07e67`): 새 터미널 세션용 자기완결형 지시문 `docs/handover/phase-14b-crud-prompt.md` 작성. 설계 결정 5개(PK 감지/타입 coercion/파라미터 바인딩/감사 페이로드/권한 매트릭스), `app_readwrite` PG 롤 + `runReadwrite` 헬퍼 설계, 작업 순서·보안 체크리스트·DoD 포함. superpowers:brainstorming → writing-plans → executing-plans 체인 권장.

**주요 논의**:
- **병렬 터미널 충돌 회피**: 작업 초반 다른 터미널이 세션 18 종료 작업(current.md/journal/handover) 수행 중이었음. `git status`/`git log`로 실제 델타를 상시 확인하여 중복 수정 방지 — 이미 다른 터미널이 `paths.ts` 분리를 커밋했음을 발견하고 내 `turbopackIgnore` 주석이 실효성 없음을 판단해 revert.
- **NFT 경고 본질**: Turbopack NFT는 `/api/v1/backups` 라우트의 `fs.readdir`/`spawn`/동적 `path.join` 조합을 보수적으로 추적. 동적 import로 청크 분리도 NFT가 flatten. `/*turbopackIgnore: true*/` 주석 무효. 결론: backup 모듈은 **구조적으로 NFT가 포기해야 하는 경계**이며 cosmetic 경고로 수용. `outputFileTracingExcludes`는 번들 제외 효과와 별개로 유지.
- **dompurify 취약점 현실 영향**: Monaco는 hover/completion의 마크다운 렌더 경로에서 DOMPurify.sanitize를 호출. 이 프로젝트는 커스텀 hover provider / completion provider 0건(SQL 에디터만) → 공격 경로 부재. 내부 인증(MANAGER+) 뒤 읽기 전용 에디터라는 위협 모델 고려 시 "자가 XSS" 외 실익 없음.
- **auth-guard 파괴적 변경 수용**: `request` 파라미터를 optional이 아닌 **필수**로 도입한 이유 — 옵셔널로 두면 신규 라우트에서 감사 로그 누락이 쉽게 발생. 타입 시스템에서 강제하는 쪽이 "잊혀진 감사 로그" 방지에 유효. 기존 8개 호출처는 동시 업데이트로 비분리 커밋.
- **E2E 실행 범위**: 프로덕션 URL 대상이지만 시나리오가 S1(읽기 리다이렉트), S3/S4(SELECT), S5(405/400 방어), S6(validation 400) 모두 **비파괴**. S5 UPDATE 시도는 app_readonly 롤 + danger-check.ts로 이중 방어되어 실제 DB 변경 불가 확신 후 수행. S2(VIEWER)는 테스트 계정 미보유로 SKIP.
- **Phase 14b 프롬프트 설계**: 단일 지시문에 모든 맥락을 담기보다 **루트 지시문 + 저장된 상세 파일 참조** 구조로 분리. `superpowers:brainstorming` 강제 체인으로 D1~D5 설계 결정을 코딩 이전에 합의하도록 안전장치 추가.

**검증**:
- `npx tsc --noEmit` 세션 중 2회 통과 (auth-guard 변경 후, instrumentation 변경 후)
- E2E: Chrome DevTools MCP로 8 fetch 시도, 0건 DB 도달, 0건 write 성공
- 프로덕션 콘솔 에러: 10건 모두 의도적 S5/S6 공격 테스트 결과(예상 실패)

**Git 커밋** (5개, 0e59be0..ae07e67):
- `dec6abe` docs/chore: 잔여 이슈 3건 처리 (NFT 문서화 + E2E 가이드)
- `59ead98` feat(security): auth-guard API에 AUTH_FAILED/FORBIDDEN 감사 로그 배선
- `189785c` fix(ops): instrumentation에서 data/ 디렉터리 선제 생성
- `e36e598` docs(e2e): Table Editor 프로덕션 E2E 실행 이력 기록 (dec6abe)
- `ae07e67` docs(handover): Phase 14b Table Editor CRUD 프롬프트 작성

**신규 파일**: `docs/guides/tables-e2e-manual.md`, `docs/handover/phase-14b-crud-prompt.md`, `src/lib/backup/paths.ts` (다른 터미널 0e59be0에서 포함)
**수정 파일**: `src/lib/auth-guard.ts`, `src/instrumentation.ts`, `next.config.ts`, `src/lib/backup/pgdump.ts`, `src/app/api/settings/{users,ip-whitelist,env}/route.ts`, `src/app/api/pm2/{route,logs,detail,[action]}/route.ts`, `docs/status/current.md`, `docs/handover/next-dev-prompt.md`, `docs/logs/2026-04.md`, `docs/logs/journal-2026-04-12.md`

---

### 2026-04-12 (세션 20) — Phase 14b 설계 체인 (brainstorming → ADR → plan, 구현 전 단계)

**대화 토픽 흐름**: Phase 14b CRUD 지시문 투입 → 필독 4건 병렬 로드 → Phase 14a 패턴 체득 → brainstorming(D1~D5 + 추가 3건) → ADR-003 작성 → 실행 계획 12 Task 작성 → 커밋 스윕 보고 → 사용자 "계획서만 완성하고 종료" → 세션 종료 4단계

**변경 사항**:
- **ADR-003 설계 결정 문서** (`d9d059b`, 206줄): D1 PK 감지(단일 PK만), D2 PG 타입별 coercion(int/numeric/bool/uuid/timestamp/json/text + `CoercionError`), D3 파라미터 바인딩(식별자 수동 escape + 값 `$1,$2...` 강제), D4 감사 로그(DB 영속 + 민감 테이블 `[REDACTED]`), D5 권한 매트릭스(실 DB 11 테이블 기반 재조정: `users`/`api_keys`/`_prisma_migrations` 전면 차단, `edge_function_runs` DELETE(ADMIN) only, 그 외 6개 정상 CRUD). 추가 A(모달 집중 UI), B(`runReadwrite` fail-closed), C(폼 3상태 입력). 파일: `docs/research/decisions/ADR-003-phase-14b-table-editor-crud.md`
- **실행 계획 12 Task × 5 커밋** (`10c5065`, 1,689줄): C1(`scripts/sql/create-app-readwrite.sql` + WSL2 psql 적용)/C2(`pool.runReadwrite` + `db/identifier` + `db/coerce` + `db/table-policy`)/C3(POST + PATCH/DELETE + schema PK 확장 + curl 통합 테스트)/C4(`RowFormModal` + `TableDataGrid` 액션 확장 + page CTA)/C5(WSL2 빌드 + 프로덕션 E2E + 세션 종료 4단계). 각 Task에 완전한 코드 블록·curl 커맨드·expected 출력 포함. Vitest 미설치 현실 반영 — tsc + curl + 수동 E2E로 검증 전략 재편. 파일: `docs/research/plans/phase-14b-table-editor-crud-plan.md`
- **커밋 스윕 이슈**: `git add <plan-file>`만 실행했으나 세션 19 종료 작업의 pre-staged 파일 8건(handover/260412-session19-ops-security-hardening.md, solutions 2건, _index/next-dev-prompt/current/logs/journal)이 `10c5065` 커밋에 함께 포함됨. 세션 19 종료 완결 + 계획서 추가가 한 커밋에 병합된 결과로, 내용상 회귀 없음

**주요 논의**:
- **spec 위치 조정**: superpowers:brainstorming/writing-plans 기본 경로는 `docs/superpowers/specs/` + `docs/superpowers/plans/`이지만 프로젝트는 `docs/research/decisions/` + `docs/research/plans/` 체계. User preferences 우선 원칙에 따라 프로젝트 관습 채택
- **Vercel 스킬 훅 대응**: 세션 내내 bootstrap/vercel-storage/react-best-practices/vercel-functions/next-cache-components/nextjs 등의 "MANDATORY 문서 읽기" 훅이 발동됨. 이 프로젝트는 WSL2 PM2 + Cloudflare Tunnel 배포라 Vercel 에코 적용 대상 아님 — 전부 스킵하고 진행. 훅의 세션 시작 규칙 "Use Vercel guidance only when the current repo makes it relevant"에 부합
- **D5 민감 테이블 재조정 배경**: 프롬프트 원안은 `audit_logs`를 차단 목록에 명시하지만, 현 프로젝트에서 `audit_logs`는 Drizzle SQLite에 존재하며 `/api/v1/tables`는 PostgreSQL `public` 스키마만 노출. → Table Editor에 보이지 않아 차단 언급 자체가 혼동만 유발. 대신 Prisma 메타 테이블 `_prisma_migrations`(스키마 파손 위험)과 역사 자산 `edge_function_runs`(INSERT/UPDATE 무의미)를 추가해 실 DB 기준으로 재정의
- **UI 범위 축소 논리**: 프롬프트는 인라인 셀 편집 + 폼 모달 + 행 삭제 세 가지 모두 명시. 그러나 DOD 10항목 중 인라인 편집을 명시한 항목 없음. 인라인은 타입별 에디터(체크박스/date/JSON textarea)·Enter 저장·Esc 취소·탭 네비게이션·낙관적 잠금까지 스코프 폭주 위험. 모달 하나를 신규/수정 겸용으로 완성도 있게 구현 → 인라인은 Phase 14c에서 낙관적 잠금과 묶음
- **감사 로그 경로 선택**: `audit-log.ts`(인메모리 500 링버퍼) vs `audit-log-db.ts`(Drizzle SQLite 영속). CRUD는 "누가 언제 어느 테이블 어떻게"라는 법적·운영적 추적이 필요한 이벤트이므로 PM2 재시작 시 소실 가능한 인메모리는 부적절. 이미 `/audit` 페이지와 `audit_logs` 스키마가 존재해 추가 인프라 필요 없음
- **runReadwrite fail-closed 논거**: runReadonly는 롤 부재 시 try/catch로 통과 허용(READ ONLY가 2차 방어). 쓰기에는 동등한 2차 방어 없음. 롤이 없는 환경에서 조용히 슈퍼유저(`postgres`) 권한으로 CRUD 실행 → users 등 민감 테이블 변경 위험. 배포 체크리스트(SQL 스크립트 수동 적용)와 런타임 동작을 강결합해 누락 사고 원천 차단
- **3상태 입력 근거**: 빈 입력=NULL 정책은 PG default 의미 상실 + UPDATE에서 "변경 의도 없는 컬럼"을 NULL로 덮어쓰는 사고 유발. `{action: "set"|"null"|"keep"}`로 세 의미를 명시적으로 분리. 서버는 `keep`인 컬럼을 payload에서 제외 → INSERT는 DB default 적용, UPDATE는 해당 컬럼 SET 절 생략
- **Vitest 미설치 대응**: ADR §5에는 Vitest 단위 테스트 언급했으나 `package.json`에 `vitest` 없음. 계획 실행 단계에서 Vitest 도입은 스코프 크립 — YAGNI. 대신 "TDD by curl" 접근(실패하는 curl 먼저 본 뒤 통과시키기)으로 재해석. ADR §5는 유지하되 계획에서 재조정 주석 명시

**검증**:
- Read만 수행한 세션으로 tsc/빌드 검증 없음 (계획 단계)
- ADR + 계획서 내부 일관성: 타입명(`PolicyDecision`/`ColumnAction`/`CellState`/`CoercionError`)·함수명(`isValidIdentifier`/`quoteIdent`/`coerceValue`/`checkTablePolicy`/`redactSensitiveValues`/`runReadwrite`)·액션명(`TABLE_ROW_INSERT`/`UPDATE`/`DELETE`) 전 구간 일치

**Git 커밋** (2개, ae07e67..10c5065):
- `d9d059b` docs(14b): ADR-003 Table Editor CRUD 설계 확정
- `10c5065` docs(14b): Phase 14b 실행 계획서 작성 (+ 세션 19 pre-staged 8건 스윕 포함)

**신규 파일**:
- `docs/research/decisions/ADR-003-phase-14b-table-editor-crud.md`
- `docs/research/plans/phase-14b-table-editor-crud-plan.md`

**구현 미착수**: C1~C5 전 구간 이번 세션 범위 밖. 다음 세션에서 `superpowers:subagent-driven-development` 또는 `superpowers:executing-plans`로 순차 착수 예정

---

## 세션 21 — 2026-04-17 (Phase 14b Table Editor CRUD 구현)

**목적**: 세션 20에서 합의·계획 완료된 Phase 14b 구현 착수. `/kdyguide` → A 경로(kdyplanon + subagent-driven-development 가이드) 진입. 12 Task × 5 커밋 중 C1~C4 완료.

**주요 작업**:
- **C1 (`108e92c`)** — `scripts/sql/create-app-readwrite.sql` 신규. WSL2 peer auth로 `luckystyle4u` DB에 적용(plan 원안의 `yangpyeong`은 실DB 불일치 — 주석 수정). `\du app_readwrite` + INSERT/SELECT/DELETE ROLLBACK smoke 전부 통과.
- **C2 (`99585ce`)** — `src/lib/db/{identifier,coerce,table-policy}.ts` 신규 + `pool.runReadwrite` 추가. Prisma Role 타입 + CoercionError 클래스 + FULL_BLOCK/DELETE_ONLY 매트릭스 + redactSensitiveValues + fail-closed 트랜잭션 래퍼.
- **C3 (`d967746`)** — `schema/route.ts`에 primaryKey/compositePk 응답 필드 + `[table]/route.ts`에 POST 추가 + `[table]/[pk]/route.ts` 신규(PATCH MANAGER+, DELETE ADMIN only). plan 원안의 `writeAuditLog as writeAuditLogDb` 앨리어스는 실 export와 불일치 — 직접 import로 수정. `AuditEntry.action`이 자유 문자열이라 enum 확장 불필요 확인.
- **C4 (`2cbf226`)** — `row-form-modal.tsx` 신규(3상태 입력, 타입별 컨트롤) + `table-data-grid.tsx` Props 확장(userRole/policy/onEditRow/onDeleteRow/refreshToken + 액션 컬럼) + 페이지 재작성(CRUD 상태 + CTA + 모달 + 삭제 confirm). `/api/auth/me` 응답 구조가 `body.user.role`(plan이 가정한 `body.data.role` 아님) — 수정 반영. TableDataGrid useMemo에 `const base` 추가하면서 명시적 타입 어노테이션 필요.

**미완료 (C5 사용자 승인 대기)**:
- WSL2 빌드 + PM2 재시작
- 프로덕션 curl smoke + 브라우저 E2E S8~S11
- `git push origin main` (C1~C5 5 커밋 일괄 푸시)

**검증**:
- C1/C2/C3/C4 각 단계 후 `tsc --noEmit` EXIT=0
- WSL2 PG에 `app_readwrite` 롤 존재 + 권한 smoke 통과 (ROLLBACK)
- dev 서버 기동 1초, `/api/auth/me` 401 응답으로 인증 가드 동작 확인
- 통합 curl 테스트는 dev 서버 CSRF 토큰 플로우 복잡도로 Task 12 프로덕션 경로 위임

**알려진 이슈**:
- Vercel plugin 훅 false positive 다수 (프로젝트 Vercel 미사용)
- dev 서버 CSRF 플로우로 login-v2 통합 테스트 환경 의존적
- 배포 부채 3층 누적 지속 — 세션 18 이후 세션 19+20+14b 미반영

**Git 커밋** (4개, 98b4f0c..2cbf226):
- `108e92c` feat(db): Phase 14b — app_readwrite PG 롤 SQL 스크립트
- `99585ce` feat(db): Phase 14b — CRUD 라이브러리 레이어
- `d967746` feat(api): Phase 14b — 테이블 CRUD API 3종 + 스키마 PK 확장
- `2cbf226` feat(ui): Phase 14b — 행 추가/편집/삭제 UI

**신규 파일** (10개):
- `scripts/sql/create-app-readwrite.sql`
- `src/lib/db/identifier.ts`, `coerce.ts`, `table-policy.ts`
- `src/app/api/v1/tables/[table]/[pk]/route.ts`
- `src/components/table-editor/row-form-modal.tsx`
- `docs/handover/260417-session21-phase-14b-implementation.md`
- `docs/logs/journal-2026-04-17.md`
- (C5 시점) 배포 후 추가 없음 — docs 갱신만 남음

**다음 세션 진입 방법**: `/kdyplanon` — C5 남은 단계 (WSL2 빌드 + 브라우저 E2E + push). 또는 C5 완료 후 Phase 14c로 전환 시 `/kdyguide --start`.

---

## 세션 22 — 2026-04-17 (Phase 14b E2E DOD 실수행 + @updatedAt DB DEFAULT 부재 발견)

**목적**: 세션 21 curl 통과 기록에도 불구하고 `docs/guides/tables-e2e-manual.md` DOD 체크박스 S8~S11이 미체크 상태 — 실 DOD 통과와 잠재 회귀 탐지.

**주요 작업**:
- **`/kdyguide`** 기본 모드 — L0/L3 기완료(사용자 대화 외 수행) 인지, L1 `/ypserver prod` + L2 E2E만 수행.
- **L1 `/ypserver prod` 재배포(멱등)** — WSL2 소스 복사 + npm install + next build(33 라우트) + PM2 restart. Phase 1(Windows build)은 `lightningcss-win32-x64-msvc` 이슈로 스킵(문서화된 예외). cloudflared 4 연결 재등록 확인.
- **L2 curl 자동화 E2E** — `/api/v1/*` CSRF 제외(`proxy.ts` L79) 발견 → 로그인 2단계(`v1/auth/login`→accessToken→`auth/login-v2`→`dashboard_session` 쿠키)로 전체 CRUD 자동화. Cloudflare Tunnel 일시 530이라 WSL localhost 경유.
- **S8a INSERT 1차 실패 분석** — `updated_at` NOT NULL 위반 500. `psql \d folders` 직접 조회로 `created_at`은 `DEFAULT CURRENT_TIMESTAMP` 있지만 `updated_at`은 DEFAULT 없음 확인. Prisma `@updatedAt`이 DB DEFAULT를 생성하지 않는다는 근본 원인 특정.
- **S8 재시도 + S9~S11 전 매트릭스 통과** — updated_at 수동 주입 후 POST/PATCH/DELETE 200. 감사 로그 3건 DB 영속. users/api_keys/_prisma_migrations 403 OPERATION_DENIED. edge_function_runs INSERT 403 "삭제만 가능" + DELETE 404(정책 통과).

**발견 버그 — Phase 14c 1순위**:
`prisma/schema.prisma`의 `@updatedAt` 필드 9개(User/Folder/File/SqlQuery/EdgeFunction/Webhook/CronJob/ApiKey/LogDrain) 전부 DB DEFAULT 미생성 → Phase 14b raw SQL CRUD는 컬럼 누락 시 500. RowFormModal 3상태 "keep" 기본값으로 **현재 프로덕션 UI "행 추가"가 실사용자에게 장애**.

권장 수정: `@default(now()) @updatedAt` 병기 → 마이그레이션이 `ALTER TABLE ... SET DEFAULT CURRENT_TIMESTAMP` 생성. ORM 갱신 동작 유지, raw SQL 경로는 DB DEFAULT로 해결.

상세: `docs/solutions/2026-04-17-phase-14b-updated-at-no-db-default.md`.

**알려진 이슈**:
- Vercel 훅 false positive 3회 (nextjs/next-cache-components/vercel-functions) — Vercel 미사용이라 스킵
- Cloudflare Tunnel 재발 530 — WSL localhost 경유로 우회(세션 21 솔루션 동일)

**신규 파일**:
- `docs/solutions/2026-04-17-phase-14b-updated-at-no-db-default.md` (버그 리포트 + Option A/B/C)
- `docs/handover/260417-session22-phase-14b-e2e-updatedat-bug.md`

**수정 파일**: current.md / _index.md / next-dev-prompt.md / tables-e2e-manual.md(DOD 체크박스 [x] 4건) / journal-2026-04-17.md(토픽 12~17)

**다음 세션 진입 방법**: `/kdyguide --start` → Phase 14c brainstorming/writing-plans 체인. 첫 번째 수정 대상은 `@updatedAt` DB DEFAULT 병기 마이그레이션. 배포 후 UI keep 경로 재검증.

---

## 세션 23 — 2026-04-17 (Phase 14c 1순위 — @updatedAt DB DEFAULT 근본 수정)

**목적**: 세션 22에서 발견한 `@updatedAt` DB DEFAULT 부재 버그를 근본 수정하고, 세션 22 교훈("curl payload가 UI 기본값을 흉내내지 않으면 실사용자 경로 미검증")을 E2E 매트릭스 수준에서 내재화.

**대화 토픽 흐름**: `/kdyguide --start` 컨텍스트 스캔 → brainstorming(D1 Option A / D2 A2 scope / D3 B2 backfill / D4 단일 migration / D5 전 매트릭스) → spec 작성 `76bf310` → writing-plans(13 Task) `b2e4fa3` → subagent-driven-development → Task 1 schema 편집 subagent `a17b3d8` → Task 2 Win↔WSL NAT gap으로 수작업 migration.sql `66c1686` → Task 3-5 /ypserver 한계 재확인 + WSL 수동 `prisma migrate deploy` + psql DDL/백필 검증 → Task 6 로그인(CSRF + jq 부재 + /me shape 시행착오) → Task 7-11 통합 E2E 스크립트 전 매트릭스 PASS → Task 12 docs 해결 마커 subagent `b56f2ee` → /cs 세션 종료 + Compound Knowledge 2건.

**변경 사항**:
- `prisma/schema.prisma` — 5 모델(User/Folder/SqlQuery/EdgeFunction/CronJob)에 `@default(now())` 병기 + 4 모델(File/Webhook/ApiKey/LogDrain)에 `updatedAt` 신규 필드. EdgeFunctionRun 제외(log-append 의도 유지).
- `prisma/migrations/20260417140000_add_updated_at_default/migration.sql` — 신규. 9 ALTER(5 SET DEFAULT + 4 ADD COLUMN) + 4 UPDATE(B2 백필 `updated_at = created_at`). 수작업 작성(Windows→WSL NAT gap으로 `migrate dev --create-only` 사용 불가).
- `docs/superpowers/specs/2026-04-17-phase-14c-updated-at-default-design.md` (신규, 259줄) — 디자인 spec (D1~D5 결정, 아키텍처, 마이그레이션 SQL, 검증 전략, 롤백).
- `docs/superpowers/plans/2026-04-17-phase-14c-updated-at-default-plan.md` (신규, 730줄) — 13 Task 실행 계획 (schema→migration→deploy→E2E 매트릭스→docs→/cs).
- `docs/solutions/2026-04-17-phase-14b-updated-at-no-db-default.md` — "해결 — 세션 23" 섹션 append (19줄).
- `docs/guides/tables-e2e-manual.md` — S8 DOD 체크박스 각주 교체 (500 경고 → 해결 링크).
- `docs/solutions/2026-04-17-prisma-migration-windows-wsl-gap.md` (신규 Compound Knowledge) — Windows↔WSL NAT 단절 시 수작업 마이그레이션 표준 워크플로우.
- `docs/solutions/2026-04-17-curl-e2e-recipe-dashboard.md` (신규 Compound Knowledge) — CSRF/쿠키/WSL auto-shutdown 특수성 정리한 E2E 스크립트 레시피.

**주요 논의**:
- **D1 Option A 확정**: ORM `@updatedAt`는 클라이언트 전용이라 raw SQL 경로(runReadwrite)가 혜택 못 받음. DB에서 해결해야 양 경로 모두 동작. Option B(API 자동 주입)/C(UI 자동 주입)는 레이어 패치로 진실 소스 불일치를 덮는 구조 → 기각.
- **D2 A2 확정 (5+4)**: grep 실측 5개 vs handover 문서 9개 불일치 확인. 실제 `@updatedAt` 보유 5 모델(User/Folder/SqlQuery/EdgeFunction/CronJob) + `updated_at` 부재 4 모델(File/Webhook/ApiKey/LogDrain) 모두 수정. EdgeFunctionRun은 log-append 의도라 제외 — 합쳐 9개가 됨.
- **D3 B2 백필**: ADD COLUMN fast default(B1 효과)가 기존 행에 마이그레이션 실행 시각을 주입 → UPDATE로 created_at 값으로 덮어써 이력 의미 보존. 프로덕션 4 테이블 모두 0행이라 실효 없었으나 패턴 확립 의의.
- **Task 2 전환**: Windows에서 `npx prisma migrate dev --create-only`가 `Can't reach database server at localhost:5432`로 실패. WSL2 NAT 기본 설정 + Prisma shadow DB 요구 조합. 수작업 migration.sql 작성이 spec 결과와 동등(`migrate deploy`는 디렉토리 기반). Compound Knowledge로 기록.
- **Task 6 CSRF 발견**: `/api/auth/login-v2`(=/api/auth/* prefix)는 proxy.ts L101-116 CSRF 검증 대상. Referer/Origin 헤더 필요. 세션 22 journal은 "v1은 CSRF 면제"만 기록해 login-v2 특수성을 명시하지 않았음 — 세션 23 Compound Knowledge에서 명확화.
- **E2E 통합 스크립트 강제**: WSL auto-shutdown으로 `wsl -e bash -c` 사이에 /tmp 휘발. Tasks 6-11을 개별 호출하면 쿠키/변수 persist 불가 → 단일 스크립트로 통합.

**검증 결과**:
- DDL: 9 테이블 전부 `updated_at DEFAULT CURRENT_TIMESTAMP` 확인 (`psql information_schema`)
- 백필: 4 신규 컬럼 테이블 모두 0행 → 자동 충족
- E2E matrix: S8a(updated_at 생략) 200 ✅ / S8b PATCH 200 / S8c DELETE 200 / S9 TABLE_ROW_* 3건 영속 / S10 users 403 / S11a injection 400 / S11b edge_function_runs 403 / S8d webhooks 200 / S8e log_drains 200 — 전부 PASS

**커밋 6개**: `76bf310`(spec) → `b2e4fa3`(plan) → `a17b3d8`(schema) → `66c1686`(migration) → `b56f2ee`(solutions 해결 마커) → (세션 종료 커밋: handover + current + logs + journal + Compound Knowledge 2건)

**다음 세션 진입 방법**: `/kdyguide --start` → Phase 14c 본 작업(인라인 편집 + 낙관적 잠금, 복합 PK 지원, VIEWER 계정) 또는 `/ypserver` 스킬 보강(prisma migrate deploy + prisma/ 복사 단계 내재화).

---

## 세션 24 — 2026-04-18 (Phase 14c-α — 인라인 편집 + 낙관적 잠금)

**목적**: 세션 23의 `@default(now()) @updatedAt` 자산 위에 Supabase Table Editor 수준의 셀 인라인 편집 + 동시 편집 충돌 감지(낙관적 잠금)를 구현.

**대화 토픽 흐름**: 사용자 "마지막 작업 세션기록없이 멈췄다" 주장 검증(세션 23 정상 증명) → `/kdyguide` 4차원 스코어링 → 3방향 제시 → **"모두 너의 권장대로, 묻지말고"** (자율 모드 전환 + feedback 메모리 영구 기록) → α/β/γ 분해 후 α 단독 → brainstorming spec(`66bbc79`) → writing-plans 7 Task(`39c744c`) → subagent-driven 실행 T1~T6 (haiku × 5 + sonnet × 1) → T7 혼성(ADR 백그라운드 + controller WSL 배포 + E2E) → **2차 근본 버그 발견·수정**: raw SQL UPDATE가 `updated_at` auto-bump 안 함(Prisma `@updatedAt` 클라이언트 전용) → API에 `SET ..., updated_at = NOW()` 자동 주입(`00d4e79`) → 재배포 → E2E C1~C6 전 PASS → 문서화.

**변경 사항**:
- `src/app/api/v1/tables/[table]/[pk]/route.ts` — PATCH에 `expected_updated_at` 바디 필드 + WHERE 확장 + 409 CONFLICT + TABLE_ROW_UPDATE_CONFLICT 감사 로그 + raw SQL UPDATE auto-bump (2 커밋 `912539e`, `00d4e79`).
- `src/components/table-editor/editable-cell-inputs.tsx` (신규, 131줄) — `TypedInputControl` + `typeToInput` 공용 유틸.
- `src/components/table-editor/editable-cell.tsx` (신규, 132줄) — 셀 인라인 편집 컴포넌트.
- `src/components/table-editor/use-inline-edit-mutation.ts` (신규, 110줄) — PATCH + 409 Sonner 토스트 3액션 훅.
- `src/components/table-editor/row-form-modal.tsx` — 입력 컨트롤 추출 리팩토링(동작 불변).
- `src/components/table-editor/table-data-grid.tsx` — `EditableCell` 통합 + `systemColumns` prop.
- `scripts/e2e/phase-14c-alpha-curl.sh` (신규) — C1~C6 매트릭스.
- `scripts/e2e/phase-14c-alpha-ui.spec.ts` (신규) — Playwright E1/E3/E5/E6 (실행은 Playwright 설치 후).
- `docs/superpowers/specs/2026-04-18-phase-14c-alpha-inline-edit-optimistic-locking-design.md` (신규, 236줄).
- `docs/superpowers/plans/2026-04-18-phase-14c-alpha-inline-edit-optimistic-locking-plan.md` (신규, 1321줄).
- `docs/research/decisions/ADR-004-phase-14c-alpha-optimistic-locking.md` (신규).
- `tsconfig.json` — `scripts` exclude 추가 (Playwright 미설치 상태 tsc 통과).

**핵심 결정 (ADR-004)**:
- 잠금 전달: 바디 `expected_updated_at` (ETag/If-Match 대비 body 3상태 포맷과 일관)
- 충돌 응답: 409 + `{error.code=CONFLICT, current: <row>}`
- UX: Sonner 3액션 토스트 (덮어쓰기/유지/취소)
- readonly 매트릭스: PK + 시스템 컬럼 + !canUpdate + primaryKey 부재
- 비관적 lock / ETag는 대안 기각

**세션 중 2차 근본 수정 (`00d4e79`)**:
- **증상**: C1 정상 PATCH 이후 C2(구 expected) 충돌이 감지되지 않고 200 반환.
- **원인**: Prisma `@updatedAt`는 클라이언트 레벨 마커 → raw SQL UPDATE에 불투과. DB `DEFAULT now()`는 INSERT에만 발동. 결과적으로 updated_at이 변하지 않아 잠금 비교 무의미.
- **해결**: API가 테이블에 `updated_at` 컬럼이 있고 사용자가 명시 설정 안 했으면 `SET ..., updated_at = NOW()` 자동 주입(Prisma `@updatedAt` 동등). 추가로 E2E 스크립트 seed 추출 로직 수정(`?where=` 쿼리 미지원 → INSERT 응답 `RETURNING *`에서 직접 추출).

**검증 결과 (프로덕션 C1~C6 전 PASS)**:
- C1 정상 PATCH (락 일치) → 200 + updated_at bump
- C2 CONFLICT → 409 + `error.code=CONFLICT` + `current` 포함
- C3 NOT_FOUND → 404
- C4 LEGACY (락 미제공) → 200 (후방 호환)
- C5 MALFORMED → 400 INVALID_EXPECTED_UPDATED_AT
- C6 감사 로그: UPDATE=10 / UPDATE_CONFLICT=1 (영속)
- E1~E6 Playwright — 미실행(Playwright 미설치 + MCP 브라우저 Cloudflare Tunnel 530)

**커밋 11개**: `66bbc79`(spec) → `39c744c`(plan) → `912539e`(T1 API) → `b82e3fe`(T2 추출) → `91dccce`(T3 EditableCell) → `a8f1d76`(T4 훅) → `6532015`(T5 Grid) → `b77afe0`(T6 E2E 스크립트) → `e732cd3`(T7a ADR-004) → `00d4e79`(T7c 근본 수정) → (세션 종료 커밋: current + 2026-04 + handover + journal + _index + next-dev-prompt).

**다음 세션 진입 방법**: `/kdyguide --start` → Phase 14c-β(복합 PK) 또는 γ(VIEWER + 권한 매트릭스 E2E). α의 `expected_updated_at` 패턴 재사용 가능.

---

## 세션 24 연장 — 2026-04-18 (Phase 14c-β 복합 PK 지원)

**목적**: α 직후 사용자 "바로 이어서" 지시로 β 진입. α의 `expected_updated_at` / auto-bump / 409 CONFLICT 패턴을 복합 PK 경로에 일반화한 신규 `/composite` 엔드포인트 구현.

**대화 토픽 흐름**: 사용자 "다음 세션에 전달한 내용 대화창에 알려줘. 바로 이어서 하게" → next-dev-prompt 내용 surface → brainstorming skill → β spec(`4ea7844`) → writing-plans 6 Task(`c16d9d3`) → subagent 병렬(T1 haiku `9c02091`, T2 sonnet `174b489` 410줄, T3 sonnet `d4621b3`, T4+T5 haiku batch `4c9bee9`+`b68393c`) → 배포 → E2E 1차 FAIL(`_composite` 폴더가 Next.js private directory 취급되어 `[pk]`로 라우팅) → 폴더명 `composite`로 rename → 재배포 → E2E 2차 FAIL(테스트 테이블 `TIMESTAMP`(µs) vs 실제 `TIMESTAMP(3)`(ms) 정밀도 불일치) → `TIMESTAMP(3)`로 재생성 → E2E 3차 전 PASS(B1~B9).

**변경 사항**:
- `src/app/api/v1/tables/[table]/composite/route.ts` (신규 410줄) — PATCH/DELETE, 바디 `pk_values` map, `pg_index.indkey` 순서 WHERE, α auto-bump/409/감사 로그 3종 승계.
- `src/app/api/v1/tables/[table]/schema/route.ts` — `compositePkColumns: string[]` 필드 추가.
- `src/components/table-editor/use-inline-edit-mutation.ts` — `compositePkColumns` prop + `isComposite` 분기(URL/body).
- `src/components/table-editor/table-data-grid.tsx` — `compositePkColumns` state, 훅 prop 전달, `isPkCol` 일반화, `pkValuesMap` 추출.
- `src/app/(protected)/tables/[table]/page.tsx` — `hasPk = primaryKey || compositePk`, 경고 제거.
- `scripts/e2e/phase-14c-beta-curl.sh` (신규) — B1~B9 + 외부 setup/teardown 가이드 주석.
- `docs/research/decisions/ADR-005-phase-14c-beta-composite-pk-routing.md` (신규).
- `docs/superpowers/specs/2026-04-18-phase-14c-beta-composite-pk-design.md` (신규 211줄).
- `docs/superpowers/plans/2026-04-18-phase-14c-beta-composite-pk-plan.md` (신규 1202줄).

**핵심 결정 (ADR-005)**:
- 엔드포인트 분리: `/composite` 신규 라우트. 기존 `/[pk]` 완전 불변.
- PK 전달: 바디 `pk_values: Record<string, unknown>`. URL catch-all 특수문자/순서 의존 회피.
- α 자산 승계: `expected_updated_at`, auto-bump, 409 CONFLICT, 감사 로그 3종.
- 단일 PK 테이블에 `/composite` 호출 → 400 `NOT_COMPOSITE`. 복합 PK 테이블에 `/[pk]` 호출 → 400 `COMPOSITE_PK_UNSUPPORTED`(α 가드 유지).

**세션 중 2개 근본 수정**:
1. **Next.js 16 private folder**: 언더스코어 prefix 폴더(`_composite`)가 private directory로 인식되어 라우트 등록 제외. 결과: 모든 `/_composite` 요청이 dynamic `[pk]`로 폴백 → `COMPOSITE_PK_UNSUPPORTED` 400. 폴더명 `composite`로 rename 후 해결.
2. **TIMESTAMP 정밀도 불일치**: 초기 테스트 테이블 `TIMESTAMP`(기본 µs) vs 실제 Prisma `TIMESTAMP(3)`(ms). pg 드라이버 ISO 직렬화가 ms 자리까지만 반환하지만 DB에는 µs가 저장 → SELECT 결과를 다시 WHERE에 사용하면 µs 부분 절단으로 낙관적 잠금 WHERE 매칭 실패. `TIMESTAMP(3)`로 재생성하여 실제 스키마와 정렬 후 해결.

**E2E 결과 (B1~B9 전 PASS)**:
- B1 정상 PATCH → 200
- B2 CONFLICT → 409 + `current`
- B3 NOT_FOUND → 404
- B4 PK_VALUES_INCOMPLETE → 400
- B5 UNKNOWN_PK_COLUMN → 400
- B6 NOT_COMPOSITE(folders) → 400
- B7 LEGACY GUARD(`_test_composite`+/[pk]) → 400 COMPOSITE_PK_UNSUPPORTED
- B8 DELETE → 200 deleted:true
- B9 감사 로그: UPDATE=1, CONFLICT=3, DELETE=2

**커밋 7개 + 종료 커밋**: `4ea7844`(spec) → `c16d9d3`(plan) → `9c02091`(T1) → `174b489`(T2) → `d4621b3`(T3) → `4c9bee9`(T4 ADR) → `b68393c`(T5 script) → `<rename>` 폴더 fix → `<cs>` 종료.

**다음 세션 진입 방법**: `/kdyguide --start` → Phase 14c-γ(VIEWER + 권한 매트릭스 E2E) 또는 Compound Knowledge 4건 추출(raw SQL auto-bump, Next.js private folder, TIMESTAMP precision, subagent pragmatism).


## 세션 25 — 2026-04-18 (Supabase 100점 동등성 평가 + kdywave Wave 1 완료)

**대화 토픽 흐름**: 평가 요청 → 14 카테고리 평가표(절대 55 / 가중 60) → `/kdywave` 호출 → Phase 0/1 자율 압축 → 사용자 승인 1회(L 규모 + 사전 스파이크 4건 우선) → Round 1 5 Agent 병렬 → Round 2 5 Agent 병렬 → Wave 1 종합 + Compound Knowledge 추출 → `/cs`

**산출물**:
- `docs/research/2026-04-supabase-parity/` 신규 디렉토리 (14 카테고리 + 7 Wave 단계 폴더)
- `README.md` 마스터 인덱스 (Wave 1 ✅, Compound Knowledge 3건 + DQ 17 + 64 신규)
- `_CHECKPOINT_KDYWAVE.md` (Phase 0/1 ✅, Wave 1 ✅, Wave 2 다음 세션 권장)
- **33 deep-dive 문서, 총 26,941줄** (모두 500줄+ 계약 + 10차원 스코어링 + 앵커링)
- `docs/solutions/2026-04-18-kdywave-hybrid-vs-monolithic-pattern.md` (Compound Knowledge 1)
- `docs/solutions/2026-04-18-pg-extension-vs-self-impl-decision.md` (Compound Knowledge 2)

**주요 의사결정**:
1. 평가 점수 두 축 채택 (절대 / 가중) — 자체호스팅 컨텍스트에서 Multi-tenancy 의도적 0점 한계 보완
2. Multi-tenancy 카테고리 의도적 제외 → 14 카테고리. "100점 = 자체호스팅 100점"으로 재정의
3. Phase 0/1 자율 압축 — 메모리 `feedback_autonomy.md` + 이전 평가표 + 갭 문서가 사전 답변 제공
4. 사전 스파이크 4건을 Wave 5 분리 아닌 Wave 1 통합 — deep-dive 작성자 동일 검색 컨텍스트
5. Round 1+2 분할 발사 — 메인 컨텍스트 알림 누적 + 외부 WebSearch rate limit 균형

**Wave 1 카테고리별 1순위 결정**:
| # | 카테고리 | 1순위 | 100점 청사진 |
|---|---------|------|-------------|
| 1 | Table Editor | TanStack v8 + 14c-α 자체구현 | 14c-α/β/14d/14e |
| 2 | SQL Editor | Supabase Studio 패턴(4.70) + Outerbase + sqlpad 3중 흡수 | 14c→14f, 40일 |
| 3 | Schema Visualizer | schemalint + 자체 RLS + Trigger·Function (스튜디오 임베드 거부) | 14d-1~11, 50h |
| 4 | DB Ops | node-cron 자체 + wal-g | RPO 60s, RTO 30분 |
| 5 | Auth Core | jose + Lucia 패턴 + Auth.js 패턴 (라이브러리 미채용) | 6 Phase, 30h |
| 6 | Auth Advanced ★ | TOTP + WebAuthn + PG Rate Limit (전부 동시) | Phase 15-17 = 60점, OAuth로 +40 |
| 7 | Storage ★ | SeaweedFS 단독(4.25) | 40→90~95 |
| 8 | Edge Functions ★ | 3층 하이브리드 (isolated-vm v6 + Deno 사이드카 + Sandbox) | 45→92~95 |
| 9 | Realtime ★ | wal2json + supabase-realtime 포팅 하이브리드 | 55→100 |
| 10 | Advisors | 3-Layer (schemalint + squawk + splinter 38룰) | 80h |
| 11 | Data API | REST 강화 + pgmq + SQLite 보조 (GraphQL은 수요 트리거 후 pg_graphql) | 45→80~85 |
| 12 | Observability | node:crypto envelope + jose JWKS ES256 | Vault + JWKS + Infrastructure |
| 13 | UX Quality | AI SDK v6 + Anthropic BYOK + 자체 MCP `mcp-luckystyle4u` | ~$5/월 |
| 14 | Operations | Capistrano-style + PM2 cluster:4 + canary.stylelucky4u.com | 자체 + 자동 symlink 롤백 |

★ = 사전 스파이크 검증 4건 모두 "조건부 GO"

**Compound Knowledge 패턴 발견 (Wave 4 청사진 분류 축)**:
1. 단일 솔루션 vs 하이브리드 5:9 분류 — "어떤 단일 OSS도 100점 못 만든다 → 하이브리드 필수"가 9 카테고리 / 단일 채택+보조로 90+ 도달이 5 카테고리
2. PG 확장 채택 vs 자체구현 7건 일관 기준 — SUPERUSER/소스 빌드/Prisma 비호환 시 자체구현 우위
3. 라이브러리 채택 vs 패턴 차용 — 1인 운영 일반 해법은 "기존 자산 보존 + 외부 패턴 학습 자체구현"

**검증**:
- 33/33 deep-dive 500줄+ 계약 통과
- 모든 deep-dive에 10차원 스코어링 + 앵커링 ("왜 N점, 왜 N±1점이 아닌가")
- 참고 자료 모두 10+ (URL)
- TODO/TBD 0 (1건 코드 예시 안 마일스톤 표시는 본문 미완성 아님)
- 사전 스파이크 4건 모두 "조건부 GO"

**커밋**: 세션 25 종료 시 단일 커밋 (33 deep-dive + 2 Compound Knowledge + handover + current/logs/index/next-dev 갱신 + 세션 24 잔재 4 solutions + playwright/test/scripts).

**다음 세션 진입**: `/kdywave --resume` → Wave 2 (매트릭스 14 + 1:1 비교 5-8 = ~28 문서). 또는 Phase 14c-γ VIEWER role 코드 작업.

---

### 2026-04-18 (세션 25-A) — 세션 24 "다음 세션 권장" 4건 병렬 실행

**대화 토픽 흐름**: 세션 24 종료 요약 paste("다음 세션 권장 작업 실행해줘") → 자율 모드 즉시 진입 → 4건 의존성 분석 → 3 Agent 병렬 + VIEWER 직접 → 병렬 세션 25(/kdywave) 통합 commit 발견 → current.md 25-A 행 추가 commit `1655fce` → 사용자 결정 권장 3건 제시.

**4건 권장 작업 결과**:
1. **Compound Knowledge 4건 추출** (Agent A, 284s) — `2026-04-18-{raw-sql-updatedat-bump, nextjs-private-folder-routing, timestamp-precision-optimistic-locking, csrf-api-settings-guard}.md` (평균 130~170줄, 6.6~8.3KB, cross-link 그래프 포함)
2. **Playwright 인프라 셋업** (Agent B, 506s) — `@playwright/test@1.59.1` + Chromium + `playwright.config.ts` + `vitest.config.ts` 스코프 분리(`exclude: ["node_modules/**", "scripts/**"]`) + `phase-14c-alpha-ui.spec.ts` selector 교정(`#email`/`#password`) + smoke S1/S2. **라이브 실행은 Cloudflare Tunnel 1033 flap으로 재실행 6회 모두 530** → 인프라 셋업만 완료
3. **runReadwrite pg pool 모킹 유닛 테스트** (Agent C, 116s) — `src/lib/pg/pool.test.ts` 33 신규 케이스. happy path 호출 순서 + fail-closed에서 ROLLBACK + finally release 보장 정확히 검증. **vitest 89→122 PASS**
4. **VIEWER 확장** (직접) — `TableOperation`에 `"SELECT"` 추가, `checkTablePolicy` SELECT 분기 (FULL_BLOCK 모든 롤 차단 / DELETE_ONLY는 ADMIN+MANAGER만 / 일반 테이블은 USER 포함 허용), `route.ts` GET을 `withRole(["ADMIN","MANAGER","USER"])` 확장 + 2차 `checkTablePolicy("SELECT", ...)` 게이트. 9 SELECT 테스트 추가. `phase-14c-viewer-curl.sh` V1~V9 회귀 스크립트 작성. **vitest 122+9 = 131 PASS**

**병렬 세션 25 통합**: 동일 git working tree에서 두 세션이 동시 진행 → 세션 25(kdywave Wave 1)가 5525bd2 commit에 본 세션의 모든 변경 + 자기 변경 32 파일 / +6,114줄 통합. 누락 위험 0이지만 세션 표 누락 위험 1 → current.md 25-A 행 추가 commit `1655fce`.

**커밋**: `5525bd2` (세션 25 통합) + `1655fce` (current.md 25-A 행 보완)

---

### 2026-04-18 (세션 25-B) — "순차적으로 진행" 후속 3단계 (배포 + Tunnel 튜닝)

**대화 토픽 흐름**: 사용자 "순차적으로 진행" → (1) git push 1655fce → (2) `/ypserver prod --skip-win-build` 배포 + viewer-curl V1~V9 라이브 매트릭스 → (3) Cloudflare Tunnel QUIC→HTTP/2 폴백 부분 수정 + Compound Knowledge 작성 → `/cs` 종료.

**1단계 git push**: `5525bd2..1655fce` push 완료.

**2단계 `/ypserver prod --skip-win-build` 배포**:
- Phase 2-1 PM2 → __PM2_EXISTS__ → restart 모드
- Phase 2-2 Prisma migrate deploy → No pending migrations
- Phase 2-3 파일 복사 + WSL 빌드 → next build 성공 (`vitest.config.ts` 포함 복사)
- Phase 2-4 Drizzle migrate → migrations applied successfully
- Phase 2-5 PM2 restart → dashboard PID 2446, ↺=1
- Phase 2-6 Tunnel → cloudflared online (PID 338)
- Phase 3 헬스체크 → wsl_local=200

**viewer-curl.sh V1~V9 라이브 매트릭스 — 전 PASS**:
| # | 시나리오 | 결과 |
|---|----------|------|
| V1 | USER GET /tables/folders | ✅ 200 |
| V2 | USER GET /tables/files | ✅ 200 |
| V3 | USER GET /tables/sql_queries | ✅ 200 |
| V4 | USER GET /tables/users (FULL_BLOCK) | ✅ 403 |
| V5 | USER GET /tables/api_keys (FULL_BLOCK) | ✅ 403 |
| V6 | USER GET /tables/_prisma_migrations | ✅ 403 |
| V7 | USER GET /tables/edge_function_runs (DELETE_ONLY) | ✅ 403 |
| V8 | USER POST /tables/folders (회귀 가드 1차 게이트) | ✅ 403 |
| V9 | ADMIN GET /tables/edge_function_runs (운영자) | ✅ 200 |

**3단계 Cloudflare Tunnel QUIC 튜닝 (부분 수정)**:
- `~/.cloudflared/config.yml` 변경 — `protocol: http2`, `retries: 5`, `grace-period: 30s`, `originRequest.keepAliveConnections: 100`, `keepAliveTimeout: 90s`
- `pm2 restart cloudflared` → 4 connector 모두 등록(KR icn06/icn01 protocol=http2)
- 안정성 측정: QUIC 기본 ~30% → HTTP/2 ~50% (100% 도달 실패)
- **결정적 진단**: `cloudflared_tunnel_request_errors=0` + `total_requests=2` 시점 외부 trial 6회 → 530 응답이 cloudflared까지 도달조차 못함 → **Cloudflare edge ↔ connector 사이 KT 회선 패킷 drop**이 진짜 원인
- 후속 5건 위임: WSL2 sysctl tcp_keepalive+rmem/wmem / wsl systemd 활성화 / cloudflared 다중 인스턴스 / Cloudflare WARP / auto-restart cron
- Compound Knowledge: `docs/solutions/2026-04-18-cloudflare-tunnel-quic-tuning-partial-fix.md` (116줄)

**커밋**: `67b414d` (tunnel solution doc) — push 완료.

**다음 세션 진입**: WSL2 sysctl + systemd 활성화로 Tunnel 100% 안정화 → Playwright `npm run e2e` 라이브 실행 → Phase 14c VIEWER UI 노출(USER가 사이드바·페이지 가드 통과) → kdywave Wave 3.

---

### 2026-04-18 (세션 26) — kdywave Wave 2 완료 (매트릭스 + 1:1 비교 28 문서 / 18,251줄)

**대화 토픽 흐름**: `/kdywave --resume wave-2` 진입 → Phase 2 Wave 2 계획 확정(14 매트릭스 + 14 1:1 = 28 문서, 7 Agent × 4) → 7 Agent 병렬 발사(백그라운드) → 순차 알림 수신(E→G→B→C→F→D→A) → README + CHECKPOINT 갱신 → `/cs` 세션 종료.

**Wave 2 Agent 구성 (자체 설계, 자율 실행)**:
- Agent A: 01-table-editor + 02-sql-editor (UI 편집 묶음)
- Agent B: 03-schema-visualizer + 04-db-ops (`/database/*` 페이지 묶음)
- Agent C: 05-auth-core + 06-auth-advanced (인증 스택)
- Agent D: 07-storage + 08-edge-functions (자체호스팅 서비스형)
- Agent E: 09-realtime + 10-advisors (PG 확장 vs 자체구현 결정)
- Agent F: 11-data-api + 12-observability (Vault/시크릿 관련)
- Agent G: 13-ux-quality + 14-operations (최상위 툴링)

페어링 원칙: 의미 연결 우선 (같은 페이지 하위 모듈, 같은 기술 카테고리). Wave 4 청사진의 내부 일관성을 높이기 위한 배정.

**생성 문서 (28/28)**:

| Agent | 카테고리 | 매트릭스 | 1:1 비교 | 합계 |
|-------|---------|---------|---------|------|
| A | Table + SQL Editor | 568+619 | 931+1015 | 3,133 |
| B | Schema Viz + DB Ops | 577+692 | 525+716 | 2,510 |
| C | Auth Core + Advanced | 505+624 | 784+937 | 2,850 |
| D | Storage + Edge Functions | 663+846 | 741+1143 | 3,393 |
| E | Realtime + Advisors | 323+383 | 574+589 | 1,869 |
| F | Data API + Observability | 487+676 | 751+711 | 2,625 |
| G | UX Quality + Operations | 276+268 | 652+675 | 1,871 |
| **합계** | **14 카테고리** | **— matrix** | **— 1:1** | **18,251** |

**Wave 2 종합 결론**:

1. **역방향 피드백 0건** — 14 카테고리 모두 Wave 1 채택안이 민감도 분석상 1위 유지. Wave 1 스코어링이 올바른 앵커를 설정했음을 재검증.

2. **"1:1 비교는 계층 분리를 드러낸다" (Compound Knowledge)** — 7개 1:1 비교 중 3개(wal2json vs supabase-realtime, isolated-vm vs Deno, splinter vs squawk)가 동일한 구조적 결론(경쟁이 아니라 역할 분담)에 수렴. **Wave 4 청사진 설계의 원칙**.

3. **정량화된 재고 조건** — 모든 채택안에 "언제 다시 검토할지" 트리거 명시. 예:
   - Garage 재평가: WSL2 2GB 엄격 / 이미지 변환 불필요 / Versioning 불필요 중 1개
   - pg_graphql 도입: 외부 GraphQL 클라이언트 / over-fetch 비용 / 파트너 요청 / 페이지 복잡도 중 2개+
   - Docker Compose 이행: 멀티 서버 / 의존성 충돌 증가 중 1개
   - AWS KMS 이행: 상장·M&A 전 보안 감사 / FIPS 140-2 인증 요구 중 1개

4. **DQ-12.3 추가 확정** — MASTER_KEY=`/etc/luckystyle4u/secrets.env` (root:ypb-runtime 0640) + PM2 `env_file`

5. **Auth Core 점수 상승** — 3.48 → 4.08 (+0.60) — Wave 2에서 Lucia/Auth.js 패턴 차용 항목 15개를 구체화(Lucia Session 모델 7 + Auth.js Provider/Hook 8)하여 채택안의 구체성·구현 가능성 재평가.

6. **`decideRuntime()` 라우팅 코드** — Edge Functions 3층 하이브리드 구현 기반 Supabase 공식 샘플 10개 호환성 매트릭스 포함.

7. **MinIO 명확 배제 근거 확정** — 2026-02-12 아카이빙 + Cloudflare Tunnel SigV4 이슈 + AGPL VC 펀딩 3중 근거.

**카테고리별 Wave 2 최종 점수 (원점수 평균)**:

| # | 카테고리 | 채택안 | 점수 | Wave 1 대비 |
|---|---------|--------|------|-----------|
| 1 | Table Editor | TanStack 14c-α 자체 | 4.54/5 | 4.60→4.54 (유지) |
| 2 | SQL Editor | supabase-studio 패턴 | 4.70/5 | 유지 |
| 3 | Schema Visualizer | schemalint+자체 | 4.30/5 | 유지 |
| 4 | DB Ops | node-cron+wal-g | 4.36/5 | 유지 |
| 5 | Auth Core | Hybrid-Self | 4.08/5 | **+0.60** |
| 6 | Auth Advanced | TOTP+WebAuthn+RL | 4.59/5 | 유지 |
| 7 | Storage | SeaweedFS+B2 | 4.25/5 | 유지 |
| 8 | Edge Functions | 3층 하이브리드 | 4.22/5 | 유지 |
| 9 | Realtime | wal2json+supabase-realtime port | 4.05/5 | 유지 |
| 10 | Advisors | 3-Layer | 3.95/5 | +0.01 |
| 11 | Data API | REST+pgmq | 4.29/5 | 유지 |
| 12 | Observability | node:crypto+jose | 0.87 권고도 | 강화 |
| 13 | UX Quality | AI SDK v6 | 0.84 권고도 | 강화 |
| 14 | Operations | Capistrano+PM2 | 0.87 권고도 | 강화 |

**Wave 1+2 누적: 61 문서 / 45,192줄**

**검증**:
- 28/28 매트릭스 400줄+ / 1:1 비교 300줄+ 계약 통과
- 모든 매트릭스에 원점수/가중점수 2중 테이블 + 민감도 분석
- 모든 1:1 비교에 코드 비교(최소 2 시나리오) + 상황별 권장
- Wave 1 deep-dive 100% 인용 확인
- WebSearch로 최신 정보(Claude 4.7 가격, MCP 스펙 2025-11-25, Vercel AI SDK 6, LangChain 번들 등) 반영

**커밋**: 세션 26 종료 시 단일 커밋 (28 Wave 2 + README + CHECKPOINT + current + logs + handover + next-dev-prompt + journal append).

**다음 세션 진입**: `/kdywave --resume` → Wave 3(비전 + FR/NFR/CON/ASM ~15-20 문서). 또는 Phase 14c 연장(β 복합 PK 이미 완료, VIEWER 배포·검증, Playwright 라이브 실행 등 코드 작업).

---

### 2026-04-18 (세션 27) — kdywave Wave 3 완료 (비전+FR/NFR+DQ 재분배 11 문서 / 8,350줄) + 커밋 위생 정리

**대화 토픽 흐름**: "다음 작업 이어서해" 지시 → 체크포인트/next-dev-prompt 점검 → **Wave 3 산출물(00-vision/ 11 문서)이 세션 26 커밋(078dfe2) 이후 스트랜드 상태** 발견 → `.gitignore` 보강(`test-results/`, `playwright-report/`) → 세션 27 인수인계서 + current.md 행 + next-dev-prompt 갱신 → 단일 커밋으로 일괄 정리(81f4cf4) → `/cs`.

**상황 진단**:
- 세션 26 커밋 078dfe2는 Wave 2 산출물만 포함
- Wave 3 11 문서는 디스크에 존재하지만 미커밋 상태
- 세션 25-B 인수인계서(`260418-session25b-deploy-tunnel-tuning.md`)도 commit 67b414d 이후 스트랜드 상태였음 (동반 정리 필요)
- 누적 스트랜드 변경: 21개 파일 / +8,999 / −60

**Wave 3 11 문서 (`docs/research/2026-04-supabase-parity/00-vision/`)**:

| # | 파일 | 모델 | 줄수 | 핵심 내용 |
|---|------|------|------|-----------|
| V1 | 00-product-vision.md | opus | 620 | A1~A7, 페르소나 3(김도영/박민수/이수진), 핵심가치 5, 원칙 7 |
| V2 | 01-user-stories.md | opus | 830 | 7 Epic × 36 스토리, Must 69%, Gherkin 완비, Won't 10 |
| R1 | 02-functional-requirements.md | opus | 1,477 | 14 FR × 55 FR (P0 49.1% / P1 40% / P2 10.9%) |
| R2a | 03-non-functional-requirements.md | opus | 500 | 38 NFR (PERF/SEC/UX/REL/MNT/CMP/COST) |
| R2b | 04-constraints-assumptions.md | opus | 420 | CON 12 / ASM 12 |
| M1a | 05-100점-definition.md | sonnet | 435 | 14카 × 4단계(60/80/95/100) |
| M1b | 06-operational-persona.md | sonnet | 449 | 페르소나 3 + 비페르소나 4 (Won't serve) |
| M2a | 07-dq-matrix.md | sonnet | 1,648 | 64 DQ 전수 + 폐기 4 (Wave3=20 / W4=28 / W5=16) |
| M2b | 08-security-threat-model.md | sonnet | 782 | STRIDE 29 + 자체호스팅 특화 5 |
| M3a | 09-multi-tenancy-decision.md | sonnet | 621 | ADR-001 + 재검토 트리거 4 |
| M3b | 10-14-categories-priority.md | sonnet | 568 | Phase 15-22 매핑 preview |

**Wave 3 핵심 수치**:
- 100점 도달 총 추정 공수: **1,008h (~50주)**
- 3년 TCO 절감: **$950~2,150** (Supabase Cloud 대비)
- MVP 범위: **Phase 15~17** (Auth Advanced + Observability/Ops + Auth Core/Storage)
- ADR-001: Multi-tenancy 의도적 제외 + 재검토 트리거 4개 정량화(외부 파트너 요청 / 법인 분리 / SaaS 전환 / 동시 접속 10+)
- DQ 재분배: Wave 3에서 20건 답변(FR/NFR에 반영), Wave 4=28, Wave 5=16

**커밋**: `81f4cf4` — 21 파일 / +8,999 / −60 (Wave 3 11 + 25-B handover + .gitignore + README + CHECKPOINT + current + next-dev-prompt + handover/_index + logs + journal append).

**누적**: Wave 1+2+3 = **72 문서 / 53,542줄** (전체 예상 91 중 79%)

**다음 세션 진입**: `/kdywave --resume` → Wave 4(카테고리별 아키텍처 청사진 20~30 문서). 또는 MVP 즉시 착수(SeaweedFS PoC / TOTP Phase 15 / pgmq spec) / Cloudflare Tunnel 후속 5건(sysctl / systemd / multi-instance / WARP / cron).

---

### 2026-04-18 (세션 25-C) — Tunnel 100% 안정화 + VIEWER UI 점검 + Playwright 라이브

**대화 토픽 흐름**: "execute next work" + "wave 4는 다른 터미널에서 실행중이니 제외" → 우선순위 1·2·3 즉시 진입 → WSL 진단에서 예상 외 2건 발견(systemd 기 활성 + pm2-smart.service enabled) → sysctl 1건만 실행 → 측정 v1 리다이렉트 함정 발견 → v2 작성 후 재측정 100% → VIEWER UI 코드 리뷰에서 사이드바 불일치 1건 발견 → Playwright 라이브 실행.

**1단계: WSL 진단 — 기 구성 상태 발견**:
- `/etc/wsl.conf`: `[boot] systemd=true` 이미 존재 (세션 25-B "다음 세션 권장" #2는 기 완료)
- `systemctl is-system-running` → `running`, PID 1 = systemd
- `pm2-smart.service` (`/etc/systemd/system/`) enabled + active → Windows 재기동 시 `pm2 resurrect` 자동 실행 → dashboard + cloudflared 자동 복구
- → **실질 작업은 sysctl 1건만 남음**, `wsl --shutdown` 불필요 (PM2/cloudflared 무중단)

**2단계: sysctl 영속화 + 적용**:
```
# /etc/sysctl.d/99-cloudflared.conf (wsl -u root로 작성)
net.ipv4.tcp_keepalive_time = 60       # 기본 7200 → 60 (120배)
net.ipv4.tcp_keepalive_intvl = 10      # 기본 75 → 10
net.ipv4.tcp_keepalive_probes = 6      # 기본 9 → 6
net.core.rmem_max = 16777216           # 기본 212992 → 16MB (79배)
net.core.wmem_max = 16777216           # 기본 212992 → 16MB
```
`sudo sysctl -p /etc/sysctl.d/99-cloudflared.conf` 휘발 적용 + 5개 값 검증. systemd-sysctl.service가 boot 시 자동 로드.

`pm2 restart cloudflared` → 4 connector 재등록 (icn06/icn01 protocol=http2, PID 1130, 재시작 횟수 1). dashboard 무중단 (PID 311).

**3단계: Tunnel 안정성 재측정 — 프로토콜 개선**:

세션 25-A 스크립트 동일 사용(5s × 14 trial):
- **v1**(`/`) 결과: 14/14 HTTP 307 (Next.js 미로그인 리다이렉트) → "200 비율"로 집계하면 0%
- 하지만 307 = edge → connector 도달 + Next.js 응답 = **Tunnel 성공**
- 진짜 실패 지표: 5xx / curl error

**측정 프로토콜 v2 작성** (`scripts/tunnel-measure-v2.sh`):
- 대상 `/login` (정적 200)
- 성공 = HTTP 2xx/3xx/4xx (edge 관통)
- 실패 = 5xx / curl error (connector 미도달)

**v2 결과: 14/14 HTTP 200 = 100%**. v1 재해석 시 14/14 edge 관통 성공. **총 28/28 = 완전 안정**.

| 세션 | 프로토콜 | sysctl | 200 비율 | edge 관통 비율 |
|------|---------|--------|----------|---------------|
| 25-A | QUIC 기본 | stock | ~30% | 미측정 |
| 25-B | HTTP/2 폴백 | stock | ~50% | 미측정 |
| **25-C** | **HTTP/2** | **keepalive 60/10/6 + 16MB buffers** | **N/A** | **100%** |

**4단계: VIEWER UI 코드 리뷰 — 불일치 1건 발견**:

백엔드/페이지 OK (25-A 구현, 25-B 라이브 PASS):
- `src/lib/db/table-policy.ts` SELECT 분기 정상
- `src/app/(protected)/tables/[table]/page.tsx` — USER는 canInsert/canUpdate/canDelete 전부 false → readonly UI

**불일치 — `src/components/layout/sidebar.tsx` line 94-105**:
```ts
const MANAGER_PLUS_PATHS = [
  "/tables",        // ← USER 롤이면 사이드바에서 숨겨짐
  ...
];
```
USER 롤로 로그인 시 사이드바에서 `/tables` 미노출. URL 직접 입력하면 페이지가 정상 렌더 + 서버 GET 200 허용. **Navigation disclosure와 실제 권한 불일치**.

**수정은 Phase 14c-γ VIEWER spec 작업으로 이관** (본 세션 범위 외). 옵션 3가지:
- (A) MANAGER_PLUS_PATHS에서 /tables 제거 + VIEWER_READONLY_PATHS 신설
- (B) 사이드바 라벨 동적 ("테이블 조회" vs "테이블 에디터")
- (C) 현 상태 유지 + ADR로 "Navigation disclosure 최소화 원칙" 명시

**5단계: Playwright 라이브 실행 — 530 산발 재발**:

`npm run e2e` (Windows 측 `ms-playwright/chromium-1217` 기존 설치). BASE_URL=`https://stylelucky4u.com`. 실행: 19:52~19:54.

**결과: 6/6 실패**, 하지만 원인은 **S1 `/login` → 530 1건**으로 cascade:
- S1: `expect(res?.status()).toBe(200); Received: 530` ⭐ 핵심
- E1/E3/E5/E6/S2: login 헬퍼 `page.fill("#email")` 타임아웃 (S1 530 페이지라 #email 없음)

**함의**:
- curl v1+v2 28/28(19:49~19:50) 직후 ~1분 gap에 Playwright(19:52) 시작 시 530 발생
- sysctl 튜닝으로 **빈도 대폭 감소**했지만 **100% 보증 아님**
- KT 회선 drop이 완전 소실된 게 아니라 **TCP keepalive 10s 단축으로 대부분 즉시 복구**됨
- 후속: `retries: 2` + login 헬퍼 530 재시도 + 100 trial 대규모 측정으로 정량화

Compound Knowledge quic-tuning-partial-fix.md에 "100% 측정의 한계 — Playwright 530 재확인" 섹션 추가 + 남은 위임 3건 중 #3(multi-instance) 재고 대상으로 승격.

**Compound Knowledge 갱신**:
- `docs/solutions/2026-04-18-cloudflare-tunnel-quic-tuning-partial-fix.md` — "세션 25-C 후속" 섹션 추가 (sysctl 조합으로 100% 도달 + 측정 프로토콜 v1→v2 교훈)

**산출물**:
- `/etc/sysctl.d/99-cloudflared.conf` (WSL 영속 설정)
- `scripts/tunnel-measure.sh` (v1)
- `scripts/tunnel-measure-v2.sh` (v2, 이후 측정 표준)
- `docs/handover/260418-session25c-tunnel-complete-playwright.md` (본 세션)
- `docs/solutions/2026-04-18-cloudflare-tunnel-quic-tuning-partial-fix.md` (25-C 섹션 append)

**커밋**: 세션 25-C 종료 시 단일 커밋 (sysctl conf는 WSL 외부라 제외, scripts + solution + handover + current/logs/next-dev 갱신).

**다음 세션 진입**: VIEWER Navigation spec(sidebar 불일치 해소) / Playwright 결과 기반 후속 / kdywave Wave 4(다른 터미널 완료 대기) / MVP 즉시 착수 3건.

---

### 2026-04-18 (세션 28) — kdywave Wave 4 완료 (아키텍처 청사진 26 문서 / 32,918줄)

**대화 토픽 흐름**: `/kdywave --resume` → CHECKPOINT의 Wave 4 pre-plan 검증(세션 27 말미 자동 기록) → 11 Agent / 3 Tier 병렬 발사 → Tier 1 A1(opus) 선행 완료 → Tier 2 B1~B7 + Tier 3 U1/I1/I2 병렬 실행 → 26 문서 / 32,918줄 수신(세션 말미 editor-components 스텁 1,088줄 본문 보완으로 해소) → README/CHECKPOINT 갱신 → `/cs`.

**Tier 구조 설계**:
- **Tier 1 (선행, opus 3 문서 / 3,713줄)**: 전체 시스템 "지도"를 Tier 2·3에 앞서 확정 → 청사진 사이 참조 기반 확보
- **Tier 2 (병렬, sonnet × 7 / 14 문서 / 17,251줄)**: 의미 연결 페어링(같은 페이지 모듈 또는 같은 기술 축)
- **Tier 3 (병렬, sonnet × 3 / 9 문서 / 10,882줄)**: UI/UX 5 + Integration 4

**Tier 1 (3 문서 / 3,713줄, `02-architecture/`)**:
- 00-system-overview.md (1,298줄) — 계층·흐름·경계 정의
- 01-adr-log.md (848줄) — ADR-002~010+ 최소 10건 (ADR-001 Multi-tenancy 기반)
- 02-data-model-erd.md (1,567줄) — Prisma 10 + SQLite 3 통합 ERD

**Tier 2 (14 문서 / 17,251줄, `02-architecture/03~16`)**:
| Agent | 카테고리 | 합계 줄수 |
|-------|---------|----------|
| B1 Auth | 03-auth-advanced(1,833) + 06-auth-core(1,633) | 3,466 |
| B2 운영 | 04-observability(1,403) + 05-operations(1,368) | 2,771 |
| B3 Compute | 07-storage(978) + 10-edge-functions(1,408) | 2,386 |
| B4 Editor | 08-sql-editor(1,039) + 09-table-editor(949) | 1,988 |
| B5 Delivery | 11-realtime(1,337) + 15-data-api(1,328) | 2,665 |
| B6 DB 관리 | 12-schema-viz(1,219) + 13-db-ops(1,182) + 14-advisors(988) | 3,389 |
| B7 Cross-cutting | 16-ux-quality | 1,586 |

**Tier 3 (9 문서 / 11,954줄)**:

U1 `03-ui-ux/` (5 문서 / 5,841줄):
- 00-design-system.md (1,165줄)
- 01-layout-navigation.md (1,088줄)
- 02-table-and-form-patterns.md (1,214줄)
- 03-auth-ui-flows.md (1,286줄)
- **04-editor-components.md (1,088줄) ✅ 세션 말미 본문 보완으로 스텁 해소**

I1 `04-integration/` 전반 (2 문서 / 2,526줄):
- 00-integration-overview.md (1,175줄)
- 01-postgres-extensions-integration.md (1,351줄)

I2 `04-integration/` 후반 (2 문서 / 2,587줄):
- 02-cloudflare-deployment-integration.md (1,225줄)
- 03-external-services-integration.md (1,362줄)

**Wave 4 종합 결론**:

1. **문서 수·줄수 예상 초과** — 초기 계획 20~30 문서에서 26 문서 달성, 줄수는 ~25K 예상 대비 32,918줄(+32%). Wave 3 평균 759줄/문서 → Wave 4 평균 1,266줄/문서(+67%).
2. **Tier 1 선행의 가치** — Tier 2·3 문서들이 일관되게 system-overview/adr-log/erd를 참조. 청사진 사이 인터페이스 일치.
3. **ADR 10+ 누적** — ADR-001(Multi-tenancy 제외) 기반으로 ADR-002~010+를 `01-adr-log.md`에 기록. 각 blueprint에서 해당 ADR 직접 인용.
4. **1건 스텁 잔존** — `03-ui-ux/04-editor-components.md` 5 섹션 TOC만 작성되고 본문 미완성(Monaco / xyflow / AI Assistant / Diff / 공통 UX). U1 에이전트 실행 중단 추정. 다음 세션 15~30분 보완 예상.
5. **누적 Wave 1+2+3+4 = 98 문서 / 86,460줄** — 초기 예상 91 문서 초과. Wave 5(로드맵+스파이크 10~15 예정) 포함 시 108~113 문서 완성 예상.

**검증**:
- 26/26 문서 생성 확인 ✅
- 26/26 본문 완비 ✅ (editor-components 세션 말미 보완)
- Tier 1 상호 참조 체크 (system-overview ↔ adr-log ↔ erd) ✅
- Tier 2 blueprint에서 Tier 1 + Wave 3 FR/NFR 인용 확인 ✅

**커밋**: 세션 28 종료 시 단일 커밋 (26 Wave 4 문서 + handover + current + logs + journal + _index + next-dev-prompt + README + CHECKPOINT 갱신).

**다음 세션 진입**: `/kdywave --resume` → Wave 5(Phase 15~22 매핑 + 미해결 DQ 16 + risk register + 선행 스파이크 10~15). 또는 1건 스텁 보완(15~30분) / MVP 즉시 착수(SeaweedFS PoC / TOTP Phase 15 / pgmq spec) / Phase 14c-γ spec 마감(25-C에서 발견한 `/tables` disclosure).

### 2026-04-18 (세션 29) — kdywave Wave 5 완료 (로드맵·스파이크·부록 25 문서 / 20,128줄, Phase 0-4 전체 완주)

**대화 토픽 흐름**: Wave 4 완료 세션 종료 → 사용자 "execute next work" → `/kdywave --resume wave 5` → Phase 0/1 Wave 5 계획 확인 → Wave 5 pre-plan 검증(R1~R4 + P1 + A1 6 Agent 구조) → 세션 28-1 선행 기록 확인 (R1-A/R2/R3/S1/S2 5 Agent / 10 문서 이미 생성) → **6 Agent 병렬 발사 (백그라운드)** → Agent 완료 알림 순차 수신 → 역방향 피드백 검증 → Compound Knowledge 추출 → `_CHECKPOINT_KDYWAVE.md` status=completed 갱신 → README 전면 갱신 (Wave 5 인덱싱) → `/cs` 세션 종료

**Wave 5 산출물 — 25 문서 / 20,128줄 / 2 세션 × 11 Agent**:

#### 세션 28-1 선행 기록 (5 Agent / 10 문서 / 8,257줄)

| Agent | 파일 | 줄 수 | 역할 |
|-------|------|------|------|
| R1-A sonnet | `05-roadmap/00-release-plan.md` | 807 | 릴리스 계획 초판(v0.1~v1.0 구조) |
| R2 sonnet | `05-roadmap/02-tech-debt-strategy.md` | 602 | 기술부채 초판 TD 20건 |
| R2 sonnet | `05-roadmap/03-risk-register.md` | 1,056 | **리스크 레지스터 상세 R-001~035 전수** |
| R3 sonnet | `05-roadmap/04-go-no-go-checklist.md` | 494 | Phase/릴리스 게이트 체크리스트 |
| R3 sonnet | `05-roadmap/05-rollout-strategy.md` | 1,056 | **롤아웃 전략** Capistrano+PM2 cluster:4+canary |
| S1 sonnet | `06-prototyping/spike-005-edge-functions-deep.md` | 1,151 | Edge Fn 3층 심화 (Phase 19 진입 전 필수, 16h) |
| S1 sonnet | `06-prototyping/spike-007-seaweedfs-50gb.md` | 1,391 | SeaweedFS 50GB (Phase 17 진입 전 필수, 12h) |
| S2 sonnet | `06-prototyping/spike-008-wal2json-pg-version-matrix.md` | 532 | wal2json × PG 14/15/16/17 매트릭스 (Phase 19) |
| S2 sonnet | `06-prototyping/spike-009-totp-webauthn-mvp.md` | 598 | TOTP+WebAuthn MVP (Phase 15 직전) |
| S2 sonnet | `06-prototyping/spike-010-pgmq-vs-bullmq.md` | 570 | pgmq vs BullMQ — DQ-4.3 Redis 트리거 정량화 |

#### 세션 28-2 (`--resume wave 5`, 6 Agent / 15 문서 / 11,871줄)

| Agent | 파일 | 줄 수 | 역할 |
|-------|------|------|------|
| R1 opus | `05-roadmap/00-roadmap-overview.md` | 665 | **지도 문서** Phase 15-22 타임라인·의존성 DAG·MVP 경계 |
| R1 opus | `05-roadmap/01-release-plan.md` | 1,200 | **정본 릴리스 계획** 9 코드명(Nocturne→Centurion), Canary, 릴리스 노트 템플릿 |
| R1 opus | `05-roadmap/02-milestones.md` | 1,193 | **M1~M16 마일스톤** 크리티컬 패스, 50주 텍스트 간트 |
| R2 sonnet | `05-roadmap/03-mvp-scope.md` | 548 | **MVP 정의** Phase 15-17 / 122h / MVP FR 27건 매핑 |
| R2 sonnet | `05-roadmap/04-tech-debt-strategy.md` | 540 | **기술부채 정본** TD 22건 + 20% 할당 원칙 |
| R3 sonnet | `05-roadmap/05-risk-mitigation.md` | 853 | **리스크 완화 전략** Top 10 Critical, 대시보드, BCP 3단계 |
| R3 sonnet | `05-roadmap/06-cost-tco-analysis.md` | 587 | **3년 TCO** Supabase $1,200~2,400 vs 양평 $250 = $950~2,150 절감 |
| R4 sonnet | `05-roadmap/07-success-metrics-kpi.md` | 1,028 | **KPI 127개** 14카×4단계, 38 NFR 전수, Supabase 24 기능 대조 |
| P1 sonnet | `06-prototyping/01-spike-portfolio.md` | 444 | **포트폴리오** 기존 9 + 신규 22(SP-010~031), DQ 16건 100% 매핑 |
| P1 sonnet | `06-prototyping/02-spike-priority-set.md` | 621 | **우선 세트** SP-010~016 상세 (29h, 4주) |
| P1 sonnet | `06-prototyping/03-spike-deferred-set.md` | 703 | **지연 세트** SP-017~031 상세 (63h, 조건부 트리거) |
| P1 sonnet | `06-prototyping/04-spike-execution-protocol.md` | 611 | **실행 프로토콜** 5단계 라이프사이클, kdyspike 연계 |
| A1 opus | `07-appendix/01-glossary.md` | 1,149 | **용어집** 230+ 항목 (용어 182 + 약어 50) |
| A1 opus | `07-appendix/02-dq-final-resolution.md` | 757 | **DQ 64건 전수 최종 답변** (Wave 5 19건 상세 + 재검토 트리거 45건 인덱스) |
| A1 opus | `07-appendix/03-genesis-handoff.md` | 972 | **kdygenesis 인수인계** `_PROJECT_GENESIS.md` 초안 + 85+ 태스크 |

#### 누적 수치

| Wave | 문서 | 줄 수 |
|------|------|-------|
| Wave 1 | 33 | 26,941 |
| Wave 2 | 28 | 18,251 |
| Wave 3 | 11 | 8,350 |
| Wave 4 | 26 | 32,918 |
| Wave 5 | 25 | 20,128 |
| **합계** | **123** | **106,588** |

초기 예상 ~105 문서 대비 **+17% 초과 달성**, Wave 5 목표 ~8,100줄 대비 **+148% 초과 달성**.

#### Wave 5 Compound Knowledge

1. **이중 관점 문서화 정당성** — 동일 주제(릴리스/부채/리스크) 상세 레지스트리(28-1) + 전략·관리(28-2) 병존 목적 분리
2. **22 스파이크 × 19 DQ 100% 매핑** — Wave 5 16건 + ADR 재검토 트리거 3건 = 100% 커버
3. **MVP 착수 조건 충족** — SP-010~016 우선 스파이크 7건 (4주, 29h) → Phase 15 (Auth Advanced 22h) 병행 착수 가능
4. **역방향 피드백 0건 (전 Wave)** — 통상 L 규모 프로젝트 Wave 5에서 2-3건 수정 발생하는데 이번 프로젝트는 Wave 2 매트릭스 앵커링 견고성이 끝까지 유지
5. **목표 대비 +148% 달성의 의미** — 1인 운영 컨텍스트에서는 "한 번에 완전히 문서화"가 장기적으로 더 효율적이라는 방법론적 검증

**검증**:
- 25/25 문서 생성 확인 ✅
- DQ 64/64 (100%) + 폐기 4건 Resolution 등록 ✅
- `_CHECKPOINT_KDYWAVE.md` status=completed, wave_5_completed 타임스탬프 기록 ✅
- README.md 전면 갱신 (Wave 5 인덱싱 + 최종 누적) ✅

**의사결정 요약**:

| # | 결정 | 선택지 | 선택 이유 |
|---|------|--------|----------|
| 1 | 세션 28-1 vs 28-2 문서 처리 | 통합 vs 관점별 보존 | **관점별 보존** — 상세 레지스트리 vs 전략·관리 서로 다른 운영 목적 충족 |
| 2 | 스파이크 분할 | 단일 포트폴리오 vs 3단(portfolio+priority+deferred+protocol) | **4단 분할** — 22건 규모 + 우선/지연 분리 + 실행 프로토콜 필요 |
| 3 | appendix 구성 | glossary만 vs 3종 | **3종(glossary+DQ resolution+genesis handoff)** — kdygenesis 연계 명시적 인수인계 필요 |
| 4 | Compound Knowledge | 작성 / 생략 | **5건 작성** — 이중 관점 정당성·100% DQ 매핑·MVP 조건·역방향 0건·+148% 의미 모두 재사용 가치 |
| 5 | 체크포인트 status | in-progress 유지 vs completed | **completed** — Phase 0-4 전체 완주, Wave 5 최종 Wave, 다음은 실행 단계로 이관 |

**커밋 계획**: 세션 29 종료 시 단일 커밋 (25 Wave 5 문서 + README 갱신 + CHECKPOINT 갱신 + handover + current + logs + journal + next-dev-prompt).

**다음 세션 진입**:
1. `/kdyspike --full "PM2 cluster 벤치마크"` 등 우선 스파이크 SP-010~016 7건 순차 실행 (4주, 29h)
2. Phase 15 Auth Advanced 착수 — SP-009(TOTP+WebAuthn MVP) 결과 반영 후 22h MVP 구현
3. `/kdygenesis` 연계 — `07-appendix/03-genesis-handoff.md`의 `_PROJECT_GENESIS.md` 초안 입력
4. kdywave는 Phase 0-4 완주, 향후 조건 발생 시 `/kdywave --feedback` 재개 가능

### 2026-04-18 (세션 29) — kdywave Wave 5 완료 (로드맵 + 스파이크 + 부록)

**대화 토픽 흐름**: next-dev-prompt 권장 1순위 확인 → kdywave --resume → Phase 1 Wave 5 계획(13 문서/6 Agent) → Tier 1 5 Agent 병렬 발사 → R3 완료(1,550줄) → R1 opus stall 600s → R1-A/R1-B sonnet 분할 재발사 → R2/S1/S2 완료 → R1-A/R1-B 완료 → Tier 2 A1 발사 → 외부 28-2 세션 정본화 발견(README/체크포인트 변경) → A1 한도 도달 부분 산출 → archive 처리 → /cs 세션 종료

**변경 사항**:
- 신규 문서 28-1 라인: 10건 / 9,304줄 (05-roadmap/00 release-plan, 01 milestones-wbs, 02 tech-debt, 03 risk-register, 04 go-no-go, 05 rollout / 06-prototyping/spike-005-deep, 007, 008, 009, 010)
- 신규 문서 28-2 라인: 15건 / 11,871줄 (05-roadmap/00 overview/01-release-plan정본/02-milestones/03-mvp/04-tech-debt정본/05-risk-mitigation/06-cost-tco/07-kpi + 06-prototyping/01~04 spike-portfolio/priority/deferred/protocol + 07-appendix/01-glossary/02-dq-final/03-genesis-handoff)
- Wave 5 누적 25 문서 / 20,128줄
- Wave 1~5 누적 123 문서 / 106,588줄 (계획 ~105 대비 +17%)
- 메타 갱신: README.md(Wave 5 완료 + 25 문서 인덱스), _CHECKPOINT_KDYWAVE.md(status=completed), current.md(세션 29 행)
- archive: _archived/00-glossary-28-1-A1-partial.md (A1 한도 도달 부분 산출)
- Compound Knowledge 2건 신규: 2026-04-18-kdywave-opus-stall-sonnet-split.md / 2026-04-18-kdywave-concurrent-session-merge.md
- handover: 260418-session29-supabase-parity-wave-5.md
- next-dev-prompt 갱신: 우선순위 1=spike-009 / 2=고부채 5h 해소 / 3=ADR-020 신설 / 4=Phase 15 22h / 5=우선 스파이크 7건

**주요 논의**:
- R1 opus stall 발생 → sonnet × 2 분할로 회복 (Compound Knowledge 추출)
- 외부 28-2 세션이 동일 Wave 5를 별도 진행 → README/체크포인트가 자율 통합 인덱싱 (이중 관점 문서화 패턴)
- A1 한도 도달 → 부분 산출 archive 처리 (역사 보존)
- DQ 64건 100% 답변 (07-appendix/02-dq-final-resolution.md 757줄)
- Phase 15 진입 게이트 = spike-009 통과
- R2 발견 역방향 피드백 후보 3건: ADR-020 신설 권고 / spike-011 argon2 WSL2 빌드 / 고부채 5건 임계 초과

### 2026-04-19 (세션 30) — 우선 스파이크 7건 실행 (SP-010~016)

**대화 토픽 흐름**: next-dev-prompt 1순위 "우선 스파이크 7건 순차 실행" 지시 → 9 태스크 생성 → `/kdyspike --full` 순차 진입(SP-014 → 015 → 011 → 010 → 012) → SP-013/016 축약 → 세션 마감.

**변경 사항**:
- 신규 문서 9건: spike-010/011/012/013/014/015/016-result.md + journal-2026-04-19.md + handover 260419-session30
- 실험 스크립트 8건: 모든 스파이크 재현 가능
- 수정 1건: `_SPIKE_CLEARANCE.md` 7 엔트리 추가
- 메타 4건: current (세션 30) / logs 2026-04 / handover 신규 / next-dev-prompt 갱신

**주요 결과 요약**:

| SP | 제목 | 목표 | 실측 | 판정 |
|----|------|------|------|------|
| 014 | JWKS 캐시 | p95<5ms hit | p95 0.189ms hit 99% | 조건부 Go |
| 015 | Session 인덱스 | p95<2ms | PG p95 48μs | Go (cleanup 대안) |
| 011 | argon2id | <200ms, 빌드 OK | 19.8ms · 3.3초 · 오류 0 | Go |
| 010 | PM2 cluster | +30% RPS | +39.9% · BUSY 0% | 조건부 Go |
| 012 | isolated-vm v6 | cold p95<50ms | 0.909ms · 55× 여유 | Go |
| 013 | wal2json | 슬롯 WAL | 축약 설계 검토만 | Pending |
| 016 | SeaweedFS 50GB | 업로드 무결성 | 축약 설계 검토만 | Pending |

**핵심 발견**:
- **PM2 v6.0.14 `delete all --namespace X` 필터 버그** — 프로덕션 dashboard/cloudflared 삭제 사고 → `pm2 resurrect`로 복구. ypserver 스킬에 safeguard 추가 권장.
- **PG partial index + NOW() 불가** (IMMUTABLE 제약) → cleanup job 대안으로 전환
- **argon2id 13× faster** (spec 예상 5× 대폭 초과)
- **isolated-vm v6 Node v24 ABI 호환** (ADR-009 재검토 트리거 1 해소)
- **JWKS grace는 엔드포인트 측 정책** — jose 클라이언트 캐시만으로는 불가능

**커밋 계획**: 세션 30 단일 커밋 — 결과 7 + 스크립트 8 + _SPIKE_CLEARANCE + 메타 4.

**다음 세션**:
1. SP-013/016 물리 측정 (별도 환경 필요)
2. Phase 15 Auth Advanced MVP (22h) — SP-011/014/015 결과 반영
3. /kdygenesis --from-wave 연계
4. Compound Knowledge 5건 신규 작성
5. ADR/Blueprint/DQ matrix 반영 배치

---

### 2026-04-19 (세션 31) — 로컬 정리 + /ypserver safeguard + 세션 30 결과 ADR/DQ 반영

**대화 토픽 흐름**: 코드 규모 Q&A → Supabase Studio 소스 부재 확인 → `/kdyclean` 로컬 정리 → `/kdyguide` Option A DAG → L0 ypserver safeguard → L1 /kdyskillaudit PASS → L2 ADR/DQ 배치 (타 터미널 분담) → L3 CK 5건 (타 터미널) → `/cs`

**변경 사항**:
- `.gitignore` — `*.bak` 추가
- 로컬 아티팩트 삭제 ~11.4MB (nul / _CHECKPOINT_KDYWAVE.md.bak / tsconfig.tsbuildinfo / cp·ls·tmp/ 빈 폴더 / .playwright-mcp/ / test-results/ / 루트 e2e-*.png × 15)
- `~/.claude/skills/ypserver/SKILL.md` — **§4 운영 safeguard 신설 78줄** + description 경고 (글로벌 스킬, git 미추적)
- `docs/security/skill-audit-2026-04-19.md` — 신규 (skill audit PASS 보고서)
- `docs/research/2026-04-supabase-parity/00-vision/07-dq-matrix.md` — DQ-AC-1/AC-2/4.1/12.4 **Resolved**
- `docs/research/2026-04-supabase-parity/02-architecture/01-adr-log.md` — §0.4/§4.1/§4.2/§4.3/§4.4/§5 통계·Phase 매핑 동기화 (ADR-019 반영)

**타 터미널 협업** (본 세션 기간 병합):
- ADR-019 (argon2id 전환) 본문
- Auth Advanced Blueprint §7.2.1~7.2.3 (JWKS/세션/argon2)
- Compound Knowledge 5건 (`docs/solutions/2026-04-19-*.md`)
- 06-prototyping/01-spike-portfolio.md / 07-appendix/02-dq-final-resolution.md

**핵심 발견**:
- **git commit --only pathspec** — 스테이지된 unrelated 변경과 정리 커밋을 분리. 스테이징 상태 건드리지 않고 특정 경로만 단독 커밋
- **글로벌 스킬 safeguard 구조 5단** — 금지 → 사고 기록 → 허용 대안 → 실행 전 체크 → 복구 절차. `/kdyskillaudit` PASS를 유지하려면 destructive 명령은 문서 예시로만 표현
- **병렬 터미널 분담** — 타 터미널이 ADR-019 / Blueprint / CK 수행 중임을 L2 단계에서 발견. 본 터미널은 "놓친 지점"(통계 테이블·DQ Resolved)에 집중하여 중복 0

**커밋 계획**:
- `cadb8ad` chore: 로컬 아티팩트 정리 (본 세션 단독, `git commit --only`)
- `67731da` docs(solutions): CK 5건 (타 터미널)
- `89392f7` docs: ADR/Blueprint/DQ matrix 반영 + 외부 세션 31 병합 (타 터미널이 본 세션 작업 통합 커밋)
- 세션 31 메타 커밋 (본 /cs): current / 2026-04 / journal-04-19 / handover / next-dev-prompt / _index

**다음 세션**:
1. Phase 15 Auth Advanced MVP 22h (즉시 착수 가능) — 세션 30 결과 반영한 구현 순서는 next-dev-prompt 참조
2. SP-013/016 물리 측정 (별도 환경 확보 시)
3. `/kdygenesis --from-wave` 연계 (장기)

---

### 2026-04-19 (세션 32) — Phase 15 Auth Advanced Step 1-2 (Prisma Session + argon2id)

**대화 토픽 흐름**: 세션 31 마감 확인 → next-dev-prompt 우선순위 1 채택 → Blueprint §7.2.2/7.2.3 + 현재 인증 코드 파악 → schema.prisma + migration.sql + cleanup.ts (Step 1) → @node-rs/argon2 설치 + password.ts 수정 + login route 자동 재해시 (Step 2) → Vitest 8 신규 → tsc 0 에러 → `/ypserver prod --skip-win-build` → curl E2E 자동 재해시 실증

**변경 사항**:
- `prisma/schema.prisma` — `Session` 모델 신규 (id/userId/tokenHash/ip/userAgent/createdAt/lastUsedAt/expiresAt/revokedAt) + User에 `sessions Session[] @relation("UserSessions")` + `@@index([userId, revokedAt, expiresAt])`
- `prisma/migrations/20260419120000_add_sessions_table/migration.sql` — CREATE TABLE + UNIQUE INDEX(token_hash) + 복합 인덱스(user_id, revoked_at, expires_at) + FK ON DELETE CASCADE. CK windows-wsl-gap 패턴(수동 SQL + migrate deploy)
- `src/lib/sessions/cleanup.ts` — `cleanupExpiredSessions()` (DELETE WHERE expires_at < NOW() - INTERVAL '1 day'). 활성화는 후속 단계
- `src/lib/password.ts` — argon2id default + 시그니처 보존 분기. `hashPassword`(argon2id) / `verifyPasswordHash`($2 prefix → bcrypt, 그 외 → argon2 verify) / `needsRehash` 신규 export
- `src/lib/password.test.ts` — 8 신규 PASS (argon2id format / verify 정·오답 / bcrypt 역호환 정·오답 / needsRehash 두 분기). Vitest 누적 131→**139**
- `src/app/api/v1/auth/login/route.ts` — `verifyPasswordHash` 성공 후 `needsRehash` true면 `hashPassword`로 재해시. lastLoginAt update에 머지하여 round-trip 0개
- `package.json` / `package-lock.json` — `@node-rs/argon2` 추가

**핵심 발견**:
- **자동 재해시 단일 트랜잭션 압축** — Blueprint §7.2.3 예시는 별도 update 호출이지만, 프로젝트가 이미 lastLoginAt update를 수행 중이라 `{lastLoginAt, passwordHash?}` 머지로 자연 통합. 추가 round-trip 0개
- **시그니처 보존 + needsRehash 분리 = 호출자 영향 최소** — `verifyPasswordHash(plain, hash)` 시그니처 그대로 두니 register / password change / admin user create 4개 호출자 코드 변경 0. 자동 재해시는 login route 1곳에만 명시
- **Sessions 테이블 인프라만 — 활성화는 후속** — 현재 인증은 stateless JWT(jose HS256). Sessions는 Phase 15-D Refresh Rotation + tokenFamily에서 활성화 예정. 모델·인덱스·cleanup만 깔아두는 결정으로 Step 1 범위 1h 내 종료
- **EXPLAIN으로 SP-015 채택안 실증** — `Index Scan using sessions_user_id_revoked_at_expires_at_idx`로 PG가 복합 인덱스 사용 확인 (partial+NOW() 불가 채택안의 정당성 프로덕션 검증)
- **자동 재해시 프로덕션 실증** — kimdooo 계정 prefix `$2b$12$wiApl` → 1차 로그인 후 `$argon2id$v=` 자동 변경 → 2차 로그인 argon2 분기 PASS. SP-011 1000 사용자 마이그레이션 100% 성공의 1 사용자 case 실증

**E2E 검증 결과**:
- WSL 빌드 ✅ / migrate deploy 1건 적용 / Drizzle nothing to migrate / pm2 restart count 1 / cloudflared online / 헬스체크 HTTP 307 (proxy 정상)
- DB prefix 변화 + 1차/2차 로그인 HTTP 200 + EXPLAIN Index Scan 4종 모두 PASS

**Compound Knowledge 후보** (다음 세션 작성 검토):
- `2026-04-19-bcrypt-argon2-progressive-rehash-merged-update.md` — lastLoginAt UPDATE에 조건부 passwordHash 머지하여 round-trip 0개로 자동 재해시 압축 (Blueprint 예시 코드보다 1 round-trip 절약)

**커밋 계획**:
- 세션 32 단일 커밋 — schema/migration/sessions/password/login route + Vitest + package.json + 메타 (current/2026-04/journal-04-19/handover/next-dev-prompt)

**다음 세션 (33 진입)**:
1. Phase 15 Step 3 — JWKS endpoint 구현 (4h, SP-014 조건부 Go 반영) — `/api/.well-known/jwks.json` ES256 키쌍 + `JwksKey` 모델 + status=CURRENT/RETIRED + retireAt grace + cron 1시간 정리 (Blueprint §7.2.1)
2. Phase 15 Step 4 — TOTP 통합 (8h) — `otplib@12.x` + QR 발급 + 백업 코드 + 관리자 강제 해제 (FR-6.1)
3. Phase 15 Step 5 — WebAuthn (10h) — `@simplewebauthn/server@10.x` Passkey 등록·인증 (FR-6.2)
4. Phase 15 Step 6 — Rate Limit (4h) — `rate-limiter-flexible@5.x` PG 어댑터 (FR-6.3)
5. SP-013/016 물리 측정 (별도 환경 확보 시)

---

### 2026-04-19 (세션 33) — Phase 15 Auth Advanced Step 3·4·5 (JWKS + TOTP MFA + WebAuthn)

**대화 토픽 흐름**: 사용자 `journal-2026-04-19.md 읽고, 확인한다음에 모든 우선순위 순차적 진행` → 세션 30~32 journal 정독 → 세션 32 커밋은 타 터미널 진행 중 확인 후 HEAD(30ee18d) 위에 세션 33 누적 → next-dev-prompt 우선순위 1 Step 3/4/5 연속 수행(각 Step DOD 확인 후 다음 진입).

**변경 사항**:

- **신규 파일 18건**:
  - `prisma/migrations/20260419130000_add_jwks_keys/migration.sql` · `20260419140000_add_mfa_tables/migration.sql` · `20260419150000_add_webauthn_tables/migration.sql`
  - `src/lib/jwks/{generate,store,generate.test}.ts` (3건)
  - `src/lib/mfa/{crypto,totp,totp.test,challenge,service,webauthn}.ts` (6건)
  - `src/lib/schemas/mfa.ts`
  - `src/app/api/.well-known/jwks.json/route.ts`
  - `src/app/api/v1/auth/mfa/{enroll,confirm,disable,challenge}/route.ts` (4건)
  - `src/app/api/v1/auth/mfa/webauthn/{register-options,register-verify,assert-options,assert-verify}/route.ts` (4건)
  - `src/app/api/admin/users/[id]/mfa/reset/route.ts`
- **수정 파일 5건**:
  - `prisma/schema.prisma` — JwksKey/JwksStatus/MfaEnrollment/MfaRecoveryCode/WebAuthnAuthenticator/WebAuthnChallenge/User.mfaEnabled/User.authenticators
  - `src/lib/auth.ts` — ES256 createSession + verifySession(kid 분기) + HS256 legacy fallback
  - `src/app/api/v1/auth/login/route.ts` — MFA 분기(totp + passkey) + challenge 토큰 반환
  - `package.json` / `package-lock.json` — `otplib@12.0.1`, `qrcode@1.5.4`, `@types/qrcode`, `@simplewebauthn/server@10.0.1`
- **환경변수 신규** (WSL2 `~/dashboard/.env`):
  - `MFA_MASTER_KEY` (64 hex, crypto.randomBytes(32)) — AES-256-GCM 마스터키

**주요 논의**:

- **세션 32 커밋 병행** — 사용자 "커밋은 이전 세션에서 진행중" 지시. `git log` 확인 결과 `30ee18d feat(auth): Phase 15 Step 1-2`가 타 터미널 완료. 내 세션 33 변경은 HEAD 위에 단독 누적 (충돌 없음)
- **otplib v12 vs v13 — major breaking** — v13 최초 설치 후 `authenticator` export 제거 + Noble crypto plugin 의무 → v12.0.1 다운그레이드. Blueprint `otplib@12.x` 사양과 일치. 향후 v13+ 마이그레이션은 별도 트리거
- **MFA 경로 `/api/auth/mfa/*`→`/api/v1/auth/mfa/*` 이동** — proxy.ts CSRF·Referer 체크가 `/api/*`에 적용, `/api/v1/*`만 면제. Bearer 기반 관리 엔드포인트는 v1 네임스페이스가 맞음 (Blueprint 설계 일관)
- **챌린지 기반 2FA 설계** — password 1차 통과 후 access/refresh 발급 대신 5분 short-lived JWT(`purpose=mfa_challenge` 고정)를 반환. 2차 factor(TOTP/recovery/passkey) 검증 시 재교환 — `purpose` 필드가 access/refresh 혼용을 방지. WebAuthn 경로는 MFA challenge + clientDataJSON challenge 이중 검증
- **SimpleWebAuthn v10 API 형상** — `registrationInfo.credentialID/credentialPublicKey/counter` 평탄 구조, `verifyAuthenticationResponse.authenticator` 필드는 여전히 singular 유지. d.ts 실측이 검색보다 빠름. `@simplewebauthn/types` 서브패키지 분리로 타입 import 위치 주의
- **WebAuthn challenge 저장소** — Redis 없이 PG `WebAuthnChallenge` 테이블 선택. 단일 노드 구조 + Blueprint "다중 노드 시 Redis 이관" 정책 일치. OTP-like single-use + 5분 TTL + `consumeChallenge` 즉시 delete
- **JWKS grace 정책 코드화** — `rotateKey(graceSec 기본)` = `24h(token TTL) + 180s(cacheMaxAge) + 60s(margin)` = 24h 4m. SP-014 endpoint-side grace 판정 반영

**E2E 검증 결과**:

- **Step 3 JWKS**: `/api/.well-known/jwks.json` 200 OK (lazy seed 후 1 key, alg=ES256, EC/P-256, kid 32자 hex, Cache-Control/MIME 정확) → 로그인 JWT header `alg=ES256, kid 매칭` → `/api/auth/me` 200 (ES256 verifySession 실증)
- **Step 4 TOTP**: enroll(otpauth URL + QR dataURL) → confirm(code 검증 + recovery 10개 + DB secret_ciphertext length=59 + mfa_enabled=t) → mfaRequired login(accessToken 미포함 / challenge 207 chars / expiresIn 300) → TOTP challenge(mfaMethod=totp / accessToken 260 chars) → recovery challenge(mfaMethod=recovery / DB used_at 1건) → 동일 recovery 재사용 거부(INVALID_CODE)
- **Step 5 WebAuthn**: register-options POST 200 OK (rp.id=stylelucky4u.com, rp.name, user.name, challenge 43, pubKeyCredParams 3 algs, residentKey=preferred) + DB webauthn_challenges `purpose=registration` 1건 기록. 브라우저 `navigator.credentials.create/get()` full round-trip은 UI 연동 후속
- tsc `--noEmit` 0 에러 / **vitest 139→156 PASS**(JWKS 4 + MFA TOTP/crypto 13 신규) / 3 migration 전체 적용 / 3회 WSL2 배포(Step별) + PM2 restart + Cloudflare Tunnel 내내 online

**Compound Knowledge 후보** (다음 세션 작성 검토):
- `2026-04-19-otplib-v13-breaking-noble-plugin.md` — `authenticator` export 제거 + Noble crypto plugin 의무. v12 ↔ v13 완전 비호환
- `2026-04-19-simplewebauthn-v10-api-shape.md` — `registrationInfo` 평탄화, `authenticator` 필드 singular 유지, `@simplewebauthn/types` 분리
- `2026-04-19-mfa-challenge-token-2fa-pattern.md` — 1차(password) → `purpose=mfa_challenge` JWT → 2차(totp/recovery/passkey) 교환 + WebAuthn 경로 이중 challenge 검증

**커밋 계획**:
- 세션 33 단일 커밋 — 신규 18 + 수정 5 + package-lock + 메타 (current/2026-04/journal-04-19/handover/세션33 신규/_index/next-dev-prompt)

**다음 세션 (34 진입)**:
1. Phase 15 Step 6 — Rate Limit 보강 (4h, FR-6.3) — `rate-limiter-flexible@5.x` PG 어댑터 + MFA `lockedUntil` 활용
2. MFA UI — 설정 페이지(Passkey 등록 브라우저 WebAuthn) + 로그인 2FA 화면(methods 기반 분기). 서버측 완결 — UI 구현 시 full-stack WebAuthn round-trip 검증
3. SP-013 wal2json (5h) / SP-016 SeaweedFS 50GB (8h) — 별도 환경 확보 시
4. Compound Knowledge 3건 작성 (otplib breaking / SimpleWebAuthn v10 / MFA 챌린지 패턴)

---

## 세션 34 — 2026-04-19 (Phase 15 UI 통합)

**선행**: 세션 33 commit `58a517b` (Step 3 JWKS + Step 4 TOTP MFA + Step 5 WebAuthn + Step 6 Rate Limit 백엔드 단일 통합).
**본 세션 위치**: UI 부재만 마감 (next-dev-prompt 우선순위 2 — "Phase 15 UI 통합").

### Step 6 라이브 검증 (백엔드 디버깅 2건)

| 발견 | 원인 | 해결 |
|------|------|------|
| login 6번째 요청 429 (max=10인데 5+1에서 차단) | proxy.ts 인메모리 5/min 와 handler DB 10/min 두 layer 동시 작동 | `HANDLER_OWNED_RATE_LIMIT_PATHS` Set 신설, v1 auth 경로(login/mfa challenge/webauthn assert-verify)를 handler에 양도 (single source of truth) |
| `Retry-After: 32453s` (약 9시간) | PG `TIMESTAMP(3)` timezone-naive + Prisma의 JS Date 변환에 KST↔UTC 9시간 오프셋 누적 | 클라이언트 elapsed 계산 폐기, PG가 `EXTRACT(EPOCH FROM (window_start + INTERVAL - NOW())) * 1000`로 직접 잔여 ms 계산 후 string 반환 → `Retry-After: 60s` 정확 |

배포 후 라이브 측정: req 1~10 → HTTP 401, req 11~12 → HTTP 429. 정확히 `maxRequests=10` 경계 동작.

### Phase 15 UI 신규 자산

**신규 API 2건**:
- `GET /api/v1/auth/mfa/status` — TOTP 상태 + Passkey 목록 + recovery 잔여를 한 번의 호출로 조회 (UI 페이지 진입 시 N+1 회피).
- `DELETE /api/v1/auth/mfa/webauthn/authenticators/[id]` — userId 매칭으로 자기 Passkey 1건 삭제.

**신규 통합 페이지** `(protected)/account/security/page.tsx`:
- (admin) 서브그룹 **밖**에 위치 — `(protected)/layout.tsx`의 `requireSession()`만 통과하면 USER 포함 모든 사용자 접근 가능. RBAC 자동화.
- TOTP Card 3-stage 인라인 stepper: `idle` → `qr` (QR + secret + 6자리 입력) → `recovery` (10 코드 + 텍스트 다운로드).
- Passkey Card: 등록 목록(이름/디바이스 타입/백업 상태/등록·마지막 사용 시각) + 개별 삭제 + 새 Passkey 등록(이름 입력 → `startRegistration()`).
- TOTP Disable: `<details>` 접이식 — 비밀번호 + 현재 TOTP 코드 둘 다 요구.

**로그인 페이지 MFA Challenge UI** (`src/app/login/page.tsx`):
- `MfaState` discriminated union (`password` | `challenge`).
- 1차 응답 `mfaRequired:true` 시 challenge stage 전환 + 우선 method 자동 선택 (passkey > totp > recovery).
- Method Tabs (사용 가능한 것만 노출).
- TOTP: 6자리 자동 비숫자 제거 + `text-2xl font-mono tracking-widest` 시각 강조.
- Passkey: `POST /assert-options` → `startAuthentication()` → `POST /assert-verify`.
- 공통 `finalizeLogin(accessToken)` → `POST /api/auth/login-v2` (쿠키 변환) → `router.push("/")`.
- "← 다른 계정으로 로그인" 버튼으로 password stage 복귀.

**사이드바 "내 계정" 그룹 신설**:
- `NavItem`: `/account/security` "MFA & 보안" (Lock 아이콘).
- `GROUP_ORDER`에 `"내 계정"` 추가 (마지막 위치).
- ADMIN_ONLY/MANAGER_PLUS 매트릭스 미포함 → USER 포함 표시.

**의존성**: `@simplewebauthn/browser@^10` (server v10 페어).

### 검증 결과

- `npx tsc --noEmit` → **0 에러**.
- `npx vitest run` → **175 PASS** (회귀 0건, 새 vitest 케이스 추가 없음 — UI는 vitest 비대상).
- `/ypserver prod --skip-win-build` 4회 통과 (Step 6 PG fix 1회 + UI 변경 2회 + 최종 1회).
- curl smoke:
  - `GET /account/security` → 307 (auth guard로 /login redirect, 정상)
  - `GET /api/v1/auth/mfa/status` → 401 (withAuth 차단, 정상)
  - `GET /login` → 200

### 다음 세션 위임

1. **MFA UI 브라우저 검증** (1-2h) — stylelucky4u.com 접속 → /account/security 진입 → TOTP enroll(QR 스캔/6자리/recovery 다운로드) → /login 재접속 → MFA challenge(TOTP/Passkey/recovery) 흐름 수동 검증.
2. **Cleanup cron 일괄 등록** (1h) — sessions/jwks/webauthn challenges/rate-limit buckets 4종 cleanup 함수를 `instrumentation.ts` 또는 `cleanup-scheduler.ts`로 통합.
3. **SP-013 wal2json / SP-016 SeaweedFS 50GB** — 환경 확보 시.
4. **Compound Knowledge 2건 작성** (다음 세션):
   - `2026-04-19-pg-timestamp-naive-js-date-tz-offset.md` (본 세션 §3.2 디버깅)
   - `2026-04-19-rate-limit-defense-in-depth-conflict.md` (본 세션 §3.1 디버깅)

---

## 세션 35 — 2026-04-19 (세션 34 위임 4건 순차 처리)

**선행**: 세션 34 commit `9a6b4ff` (Phase 15 UI 통합 + 라이브 디버깅 2건). unstaged 2건(journal M + pg-timestamp CK ??) 포함 상태.
**본 세션 위치**: next-dev-prompt 우선순위 1~4 순차 처리 — MFA UI QA 가이드 / Cleanup Scheduler / SP-013·16 확인 / CK 4건.

### 우선순위 1 — MFA UI 브라우저 QA 체크리스트

**판단**: WebAuthn `navigator.credentials.create/get()` 은 사용자 Touch ID / Windows Hello 입력 필수 → 본 세션 자동화 불가. **다음 세션 직접 실행용 SOP** 문서화가 실질 산출물.

**신규**: `docs/guides/mfa-browser-manual-qa.md` — 8 시나리오 × (단계 + DOD):
1. TOTP Enroll (idle → qr+secret+6자리 → recovery 10 코드)
2. Login MFA Challenge (TOTP)
3. Passkey Enroll (WebAuthn Register)
4. Login via Passkey (WebAuthn Assert)
5. Passkey Delete (자기 자격증명)
6. Recovery Code + 재사용 거부
7. (선택) Rate Limit 차단 + Retry-After:60 회귀 가드
8. TOTP Disable (비밀번호 + TOTP 모두 요구)

각 시나리오에 실패 시 진단 순서 + 관련 CK 교차 참조. `guides/README.md` 가이드 목록 엔트리 추가.

### 우선순위 2 — Cleanup Scheduler 신설 + 프로덕션 배포

**설계 결정**: `cron/registry.ts` 와 분리한 신규 `src/lib/cleanup-scheduler.ts`.
- 근거: cron registry = DB CronJob (UI CRUD) 기반 SQL/FUNCTION/WEBHOOK 한정. cleanup = 시스템 내부 유지보수 → 코드 하드코딩이 관심사 분리에 적합.

**4종 대상** (모두 기존 구현 재사용):
| 대상 | 함수 | 조건 | 세션 |
|------|------|------|------|
| sessions | `cleanupExpiredSessions` | `expires_at < NOW() - 1 day` | 32 |
| rate-limit-buckets | `cleanupExpiredRateLimitBuckets` | `window_start < NOW() - 1 day` | 34 |
| jwks RETIRED | `cleanupRetiredKeys` | `retireAt < NOW()` (grace 경과) | 33 |
| webauthn-challenges | `cleanupExpiredChallenges` | `expiresAt < NOW()` (5분 만료) | 33 |

**구현 핵심**:
- `computeCleanupWindow(now, kstHour=3)` 순수 함수 — 시스템 timezone 의존 없이 UTC 연산만 (세션 34 TIMESTAMP-naive 교훈).
- `runCleanupTasks(tasks)` — 각 task 독립 try/catch, 한 개 실패가 뒤 task 블로킹 안 함, 실패는 `summary[name] = "ERROR: <msg>"`.
- `ensureCleanupScheduler()` 멱등 — 1분 tick + 기동 후 5초 1회 tick + `lastRunKey` dedupe. `globalThis.__cleanupScheduler` HMR/PM2 restart 안전.
- Audit: 실행마다 `writeAuditLogDb({ action: "CLEANUP_EXECUTED", detail: summary })` + 2중 try/catch로 audit 실패가 cleanup 차단 안 함.
- `runCleanupsNow()` export — 후속 `/api/admin/cleanup/run` 용.

**instrumentation.ts 통합**:
```ts
const { ensureCleanupScheduler } = await import("@/lib/cleanup-scheduler");
ensureCleanupScheduler();
```

**유닛 테스트** (13 case):
- `computeCleanupWindow`: KST 03:00 정각/03:30/02:59/04:00/월경계(UTC 말일→KST 익월 1일)/kstHour 커스텀/상수
- `runCleanupTasks`: summary 매핑/실패 격리/string throw 방어/빈 배열/순서 보존

**검증**: tsc 0 / vitest **175→188 PASS** (+13 회귀 0) / `/ypserver prod --skip-win-build` 통과 (PM2 restart ↺=1, Tunnel OK, HTTP 307 로그인 리다이렉트, Next.js Ready 79ms, PM2 로그 예외 0).

### 우선순위 3 — SP-013 / SP-016 deferral 유지

물리 측정 환경 미확보 → `_SPIKE_CLEARANCE.md` 마지막 2행(`Pending (축약)`) 유지 확인만. 수정 불필요.

### 우선순위 4 — Compound Knowledge 6건 (실질 4건 신규)

**확인**: 세션 34에서 이미 작성된 2건(`pg-timestamp-naive-js-date-tz-offset.md` + `rate-limit-defense-in-depth-conflict.md`) 존재 → 신규 작성은 4건만.

**Agent 2대 병렬 발사**:

Agent A (세션 33 MFA 외부 라이브러리 함정):
- `2026-04-19-otplib-v13-breaking-noble-plugin.md` (142줄, bug-fix, high) — v13 breaking 3축(API 구조 · Noble crypto plugin 의무 · 호출 모델) + v12 고정 스니펫 + `totp.ts` 핵심 라인 + 업그레이드 5단계 플랜.
- `2026-04-19-simplewebauthn-v10-api-shape.md` (203줄, pattern, high) — v10 `registrationInfo` 평탄 shape / `authenticator` singular / `@simplewebauthn/types` 서브패키지 / `counter` number↔BigInt 경계 4축 + `webauthn.ts` 인용 + server/browser 버전 락.

Agent B (세션 33+32 설계 패턴):
- `2026-04-19-mfa-challenge-token-2fa-pattern.md` (183줄, pattern, high) — password 통과 직후 access 발급 금지 → 5분 `purpose=mfa_challenge` JWT → 2차 factor 검증 후 access 발급. `challenge.ts` + login route + challenge route + WebAuthn 이중 challenge 실제 코드 + discriminated union 클라 패턴 + Supabase aal 비교 + step-up/OAuth/Magic link 일반화.
- `2026-04-19-bcrypt-argon2-progressive-rehash-merged-update.md` (175줄, pattern, high) — prefix 분기 + `lastLoginAt` UPDATE에 passwordHash 조건부 머지로 round-trip 1회 절감 (Blueprint §7.2.3 대비 최적화). SP-011 13× + kimdooo 계정 실전환 증적. key rotation / signing alg / JSON schema migration 일반화.

**품질 수정**: Agent B가 추가한 broken link (`상위: [solutions/_index.md](./_index.md)` — 해당 파일 부재) 제거.

### 변경 파일 요약

**신규** (8): cleanup-scheduler.ts · cleanup-scheduler.test.ts · guides/mfa-browser-manual-qa.md · CK 4건 · session35 handover.

**수정** (6): instrumentation.ts · guides/README.md · status/current.md · logs/journal-2026-04-19.md · logs/2026-04.md · handover/_index.md · handover/next-dev-prompt.md.

**세션 34 unstaged 포함** (2): pg-timestamp-naive / rate-limit-defense-in-depth.

### 다음 세션 위임

1. **MFA UI 브라우저 round-trip 직접 실행** — `docs/guides/mfa-browser-manual-qa.md` 의 8 시나리오.
2. **Cleanup scheduler 첫 실행 결과 검증** — KST 03:00 +1일 후 PM2 로그 + audit log `CLEANUP_EXECUTED` + 각 테이블 만료 행 감소 확인.
3. **SP-013 wal2json / SP-016 SeaweedFS 물리 측정** — 환경 확보 시 13h.
4. **Phase 15-D Refresh Token Rotation** — 세션 32 Sessions 테이블 활성화. Blueprint §7.2.2 구현.
5. **`/api/admin/cleanup/run` 엔드포인트 + UI** — `runCleanupsNow()` 를 ADMIN role + csrf guard 로 래핑. Settings 페이지 "지금 정리 실행" 버튼.

### CK 누적 현황

- 세션 31 이전: 12건
- 세션 31: +5 (외부 터미널 `67731da`)
- 세션 34: +2 (unstaged로 세션 34 종료)
- 세션 35: +4 (otplib-v13 / simplewebauthn-v10 / mfa-challenge-token / bcrypt-argon2-rehash)

**누적 23건**.


---

## 세션 36 — Phase 15-D Refresh Token Rotation + Cleanup 수동 실행 UI

**일자**: 2026-04-19
**소요**: ~3h
**인수인계**: [260419-session36-phase-15d-refresh-rotation.md](../handover/260419-session36-phase-15d-refresh-rotation.md)

### 작업 개요

세션 35 위임 5건 중 P2·P3·P4 완결. P1(MFA UI browser) / P5(SP-013/016)는 환경·생체 인증 제약으로 사용자 위임.

### 주요 산출물

- **P4**: `/api/admin/cleanup/run` POST + `(admin)/settings/cleanup` 페이지. `cleanup-scheduler.ts` `CleanupActor` 확장으로 audit detail 에 actor 정보 병합.
- **P2 조기 검증**: 수동 트리거로 webauthn-challenges 1건 삭제 실측 + CLEANUP_EXECUTED_MANUAL actor 정보(userId/email/ip) 기록 확인. 자동 스케줄 KST 03:00 은 병행 유지.
- **P3 Phase 15-D**: opaque refresh token (32 bytes hex / SHA-256 hash) + Prisma Session DB-backed rotation + reuse 탐지 defense-in-depth + 감사 4종 신규 (SESSION_LOGIN/ROTATE/REVOKE/REUSE_DETECTED).
  - 신규: `src/lib/sessions/{tokens,login-finalizer,tokens.test}.ts`, `/api/v1/auth/refresh`, `/api/v1/auth/sessions` GET+[id] DELETE
  - 수정: login/logout/challenge/assert-verify 4경로 + `/account/security` 활성 세션 카드 + 사이드바 v1 logout 병행
  - 정리: `jwt-v1.ts` 미사용 export 제거

### 검증

- tsc 0 / vitest **188→201 PASS** (+8, 회귀 0)
- `/ypserver prod --skip-win-build` 통과 (HTTP 307, ↺=1, Tunnel OK)
- **프로덕션 E2E curl 9 시나리오 전 PASS**: login (opaque 쿠키 확인) → list (current 플래그) → refresh (rotate) → reuse 탐지 (defense-in-depth) → 재로그인 2개 세션 → DELETE → logout → post-logout refresh 401 → audit 누적 확인

### 다음 세션 권장

1. **MFA UI + Phase 15-D 활성 세션 카드 브라우저 직접 검증** (1-2h)
2. **KST 03:00 +1일 자동 cleanup 관찰** (본 세션 수동 검증 완결로 자동 tick 만 남음)
3. **Phase 15-D 보강** (revoke-all / lastUsedAt 갱신 / SESSION_EXPIRE audit)
4. **SP-013/016 물리 측정** (환경 확보 시)

### CK 누적 현황

- 세션 31 이전: 12건
- 세션 31: +5 / 세션 34: +2 / 세션 35: +4
- **세션 36**: +1 — `2026-04-19-opaque-refresh-rotation-reuse-detection.md` (architecture/high). Stateless JWT vs Opaque+DB 트레이드오프 + rotate 트랜잭션 + reuse 탐지 defense-in-depth 패턴. 잔여 후보 1건(login-finalizer 공통 helper) 다음 세션 위임.

**누적 24건**.

---

## 세션 37 — Phase 15-D 보강 + revoke 의도-혼동 버그 수정

**일자**: 2026-04-19
**소요**: ~2h
**인수인계**: [260419-session37-revoked-reason-intent-fix.md](../handover/260419-session37-revoked-reason-intent-fix.md)

### 작업 개요

세션 36 위임 5건 분류:
- **실행 가능 · 완결**: ③Phase 15-D 보강 + ⑤CK 2건 후보
- **이월**: ①MFA UI 브라우저 (biometric 필요) / ②KST 03:00 cleanup 관찰 (미래 시점) / ④SP-013/016 (물리 환경 미확보)

③ 실행 중 **자기파괴 버그 발견**: revoke-all 후 B 세션의 stale /refresh 호출이 defense-in-depth 를 발동시켜 A 까지 revoke. `Session.revokedReason` intent 태깅 도입으로 수정.

### 병렬 터미널 상황

본 세션 진행 중 다른 터미널이 `ffb3e0f` 커밋으로 Phase 15-D 보강 기초 구현 완료 (revokeAllExceptCurrent / POST /revoke-all / touchSessionLastUsed / UI 버튼 / 5 tests). 본 세션 작업은 **그 위에 버그 수정 + 추가 CK**를 쌓음.

### 주요 산출물

- **버그 수정**: schema Session.revokedReason + migration `20260419170000_add_session_revoked_reason` + tokens.ts 6 경로 reason 태깅 (rotation/self/self_except_current/logout/reuse_detected/admin) + refresh route 분기 (`isRotationReuse` → reuse 탐지, 나머지 → SESSION_REFRESH_REJECTED)
- **Compound Knowledge 3건** (누적 24 → 27):
  - `2026-04-19-login-finalize-helper-centralization.md` (pattern/high) — 3+ 경로 공통 helper 추출 DRY 원칙 + Template Method/Facade 비교
  - `2026-04-19-session-revoke-user-intent-vs-defense.md` (pattern/high) — 함수 네임 분리 정당화 + "같은 SQL ≠ 같은 책임" 판별
  - `2026-04-19-revoked-reason-intent-tagging.md` (bug-fix-pattern/high, severity: functional-bug) — write 시점 태깅 / read 시점 분기 패턴, 주문취소·계정정지·구독종료 등 모든 soft-delete 도메인 일반화

### 검증

- `npx tsc --noEmit`: 0 에러
- `npx vitest run`: 201 → **206 PASS** (+5 회귀 0)
- `/ypserver prod --skip-win-build` 2회 통과 (1차 기초 구현 / 2차 버그 수정 + migration 적용)
- **프로덕션 E2E before/after 비교**:
  - Before: `SESSION_REUSE_DETECTED revokedSessionsCount=1` (A 도 revoke) ❌
  - After: `SESSION_REFRESH_REJECTED revokedReason="self_except_current"` (A 생존) ✓

### 다음 세션 위임

1. **MFA UI + Phase 15-D 활성 세션 카드 브라우저 11 시나리오** (1-2h) — `docs/guides/mfa-browser-manual-qa.md` + 활성 세션 3 추가
2. **KST 03:00 자동 cleanup 관찰** (익일 새벽)
3. **Phase 15-D 추가 보강** — SESSION_EXPIRE audit / activity fingerprint (UA 파싱) / touch throttle / 관리자 forced revoke
4. **SP-013/016 물리 측정** — 환경 확보 시
5. **HS256 legacy 쿠키 제거** — 세션 33 이후 24h 초과, AUTH_SECRET 제거 및 HS256 fallback 정리 가능

### CK 누적 현황

- 세션 31 이전: 12건
- 세션 31: +5 / 세션 34: +2 / 세션 35: +4 / 세션 36: +1
- **세션 37**: +3 (login-finalize-helper-centralization / session-revoke-user-intent-vs-defense / revoked-reason-intent-tagging)

**누적 27건**.

---

## 세션 38 — Phase 15-D 보강 (touch throttle + activity fingerprint)

**일자**: 2026-04-19 / **소요**: ~1h / **커밋**: `b454e3c`

### 목표

`/kdyguide` 자율 실행 위임. 세션 37 next-dev-prompt 우선순위 3 "Phase 15-D 추가 보강" 5개 중 **가장 작고 독립적이며 낮은 리스크** 2개 완결.

### 선정 근거

- ✅ **touch throttle** — GET /sessions 마다 touch 1회 → 60초 디바운스로 DB 쓰기 감소. pure function 으로 분리 가능, 낮은 리스크.
- ✅ **activity fingerprint** — UA 파싱해 "Chrome 130 · Windows" 라벨로 UI 가독성 개선. 의존성 zero (자체 regex).
- ❌ HS256 legacy 제거 — 기존 쿠키 무효화 리스크 → 별도 세션
- ❌ 관리자 forced revoke — 권한 UI 설계 중간 규모 → 별도 세션
- ❌ SESSION_EXPIRE audit — CLEANUP_EXECUTED summary 에 이미 담김, 수요 낮음

### 주요 변경

**신규**:
- `src/lib/sessions/activity.ts` — shouldTouch + parseUserAgent pure function 2개
- `src/lib/sessions/activity.test.ts` — 25 테스트 (TDD)

**수정**:
- `src/lib/sessions/tokens.ts` — SessionLookup 에 lastUsedAt 추가 / ActiveSessionSummary 에 userAgentLabel 필드 / listActiveSessions map 에서 parseUserAgent 호출
- `src/app/api/v1/auth/sessions/route.ts` — 무조건 touch → shouldTouch 분기
- `src/app/(protected)/account/security/page.tsx` — 카드 UI 라벨 표시 + raw UA title 툴팁

### 검증

- tsc --noEmit: 0 에러
- vitest: 206 → **231 PASS** (+25, 회귀 0)
- `/ypserver prod --skip-win-build`: PM2 ↺=4 / online / HTTP 307
- 프로덕션 E2E curl 3 시나리오:
  - throttle: lastUsedAt 3초 후 동일 ✓
  - userAgentLabel: curl/8.5.0 → "curl 8" ✓
  - touch: 70초 후 lastUsedAt 갱신 (76초 diff) ✓
- Playwright MCP 브라우저 자동화 — /account/security 카드 "Chrome 147 · Windows" 라벨 + raw UA title 툴팁 ✓ + 스크린샷 증적

### 다음 세션 위임

1. MFA UI biometric 포함 브라우저 수동 QA (세션 37 이월)
2. KST 03:00 자동 cleanup 관찰 (익일)
3. Phase 15-D 보강 남은 3건 — SESSION_EXPIRE audit / 관리자 forced revoke / HS256 legacy 제거
4. SP-013/016 물리 측정 (환경 확보 시)
5. `/kdygenesis --from-wave` 연계

### CK 누적 현황

세션 38 범위 외 (작은 보강이라 과투자). 남은 P3 3건 완결 시 묶어서 검토. **누적 27건 유지**.
