# 세션 저널 — 2026-05-02

> 세션 진행 중 주요 대화를 누적 기록. /cs 실행 시 대화 다이제스트 원본으로 활용.

---

## 세션 80

> wave 기반 진행도 분석 + 세션 80+ 작업 계획 수립 → 베이스라인 검증으로 plan 정정 → B-pre + B1 + B2 commit 5건. Track B (Almanac aggregator P0 본진, ~28h) 첫 진입.

### [1] wave 기반 진행도 분석 + 세션 80+ 작업 계획 수립

> **사용자**: "wave 기반 구현 진행도와 현재까지의 세션 진행 현황을 분석해서 앞으로 진행해야될 작업에 대해서 계획 수립."

Plan mode 진입. 다중 wave (`docs/research/baas-foundation/04-architecture-wave/` + `docs/research/messenger/`) + ADR 30건 + spike 16건 + master plan + 79 handover 의 5계층 산출물 위에서 진행 중인 프로젝트. 단일 진실 소스 부재로 "다음 작업" 결정마다 4계층 동시 검증 필요 (메모리 룰 `feedback_baseline_check_before_swarm.md` 등재 사례).

**Phase 1**: 핵심 상태 파일 3종 동시 read (current.md / next-dev-prompt.md / MASTER-DEV-PLAN.md). current.md 가 92K 토큰 초과로 부분 read.

**Phase 2**: 3 Explore 에이전트 병렬 발사 — Track A (BaaS Phase 0~4 진행도) / Track B (Aggregator T1~T9 코드 실태) / Track C (Messenger M0~M6 코드 실태). 베이스라인 검증 메모리 룰 적용.

**Explore 결과**:
- Track A: Phase 1 ~85% (T1.1/1.2/1.3/1.4 ✅, T1.5/1.6/1.7 partial)
- Track B: 0% — `src/lib/aggregator/` 디렉토리 자체 부재. 단 5 endpoint (S66/S69) + content_* RLS 마이그레이션 (T1.6) 모두 적용
- Track C: M1 완료 + M2 도메인 헬퍼 + Zod 스키마 완료, M2 19 라우트 미작성

**Phase 3**: Plan 에이전트 1개로 4 트랙 시퀀싱 설계 → 결과 = Track B 우선, Track C 병행은 세션 86부터.

**Phase 4**: `C:\Users\smart\.claude\plans\wave-wiggly-axolotl.md` 작성 (10K+ 토큰, Context + 진행도 표 + 시퀀스 + critical files + 위험 매트릭스 + 검증 게이트 + 7원칙 게이트 매트릭스).

**결론**: ExitPlanMode 승인. 세션 80~84 = Track B B-pre~B7 직진, 85 = 24h 관찰, 86~ = Track C M2 19 라우트.

### [2] B-pre 진입 — 베이스라인 검증의 두 번째 가치 입증

Plan 의 가정 검증을 위해 `audit-metrics.ts` + `prisma/seeds/almanac-aggregator-categories.sql` + `src/lib/db/prisma-tenant-client.ts` 를 직접 read.

**중요한 발견 2건**:

1. **T1.7 audit-metrics tenant 차원 이미 구현됨** — Phase 1.7 byTenant Map (MAX_TENANTS=50 / MAX_BUCKETS_PER_TENANT=100 FIFO evict) + AuditMetricsTenant 타입 + getAuditMetrics() byTenant 정렬 + 6 단위 테스트 (default 폴백 / per-tenant 분리 / 정렬 / FIFO evict 2종 / byBucket 회귀). 초기 Explore 의 grep 한계로 missed (검색어 `audit.*metric` 패턴은 매치했으나 파일 내용 확인 누락).

2. **R1 withTenantTx 존재 확인** — `src/lib/db/prisma-tenant-client.ts:188` 에 정의 + 사용 가능. plan 의 "R1 = 정의 위치 미확인" 잘못된 정보.

3. **R2 슬러그 매핑 — "3개 부족"이 아닌 "완전한 컨벤션 차이"**:
   - DB 시드 37 슬러그 (`almanac-aggregator-categories.sql`) vs classify spec 40 슬러그
   - 정확 매치 단 8개 (hustle 3 + work 0 + build 3 + invest 1 + learn 0 + community 1)
   - 단/복수 차이 (tutorials↔tutorial), 명시 정도 차이 (ai-funding↔funding), 한국 특화 (korean-tech, korean-community), 신규 항목 (DB: data-science/system-design / spec: model-releases/fine-tuning/ai-safety/benchmarks)
   - **결정**: DB 시드 = source of truth (T1.6 마이그 + RLS + dbgenerated 활성). spec 슬러그 강행 시 promote.ts FK violation → cron consecutiveFailures 임계 → 자동 비활성화.
   - 매핑표 `docs/research/baas-foundation/05-aggregator-migration/slug-mapping-db-vs-spec.md` 신규 작성.

`★ Insight ─────────────────────────────────────`
- **베이스라인 검증의 ROI 실증**: plan agent 가 "T1.7 미완료 / R1 미확인 / R2 3개 부족" 으로 보고했으나 실제 코드는 "T1.7 완료 / R1 존재 / R2 컨벤션 완전 차이". 메모리 룰 `feedback_baseline_check_before_swarm.md` 적용으로 단일 sweep 단계에서 미실행 작업 1건 절약 + 미발견 위험 1건 (T4 진입 시 FK violation으로 cron 폭주) 사전 차단.
- **DB-source-of-truth 결정 근거**: T1.6 마이그(`20260427140000`) 가 RLS + dbgenerated default + composite unique 모두 활성화한 상태라 spec slug 강행 시 즉시 cron 폭주.
`─────────────────────────────────────────────────`

### [3] s79 leftover 회복 + B-pre commit (3 commits)

git status 점검 시 추가 발견: `docs/logs/journal-2026-05-01.md` (188줄) + `docs/logs/2026-05.md` (55줄) 가 ff05a07 commit 시점에 staging 누락된 채 unCommit 으로 잔존. 다른 터미널이 동시에 `0647b14` (s79 /cs handover + _index + CK) 를 push 하여 본 commit 과 race 발생했으나 폴더 충돌 0.

**3 commits 분리 진행** (CLAUDE.md "기능 단위 commit" 정신):
1. `046fce8 docs(s79-recovery): journal-2026-05-01 세션 79 entries 누락분 commit` (188줄)
2. `6a8a9eb docs(s79-recovery-2): logs/2026-05.md 세션 79 entry 누락분 commit` (55줄)
3. `c20d90d docs(s80-bpre): aggregator T1~T9 plan 단일 진실 소스 + 슬러그 매핑 분석` (622줄)

**결론**: B-pre 코드 변경 0 (모두 문서 작업). plan 정정 + sweep 발견사항 baked-in.

### [4] B1 의존성 + .env 6 vars (commit 1건)

> **사용자**: "다음 액선 까지 마무리."

Plan 의 B1 진행:
1. `npm install rss-parser cheerio @google/genai` — 55 패키지 추가 (transitive)
   - rss-parser@3.13.0 (MIT) — RSS/Atom 파싱
   - cheerio@1.2.0 (MIT) — HTML 스크래핑
   - **@google/genai@1.51.0** (Apache-2.0) — plan §4 의 `^0.X` 가정과 달리 1.51.0 latest. peer 충돌 0 (R4 위험 해소).
2. `.env.example` 6 vars 추가 (GEMINI_API_KEY / AGGREGATOR_LLM_DAILY_BUDGET=200 / AGGREGATOR_BOT_USER_AGENT / AGGREGATOR_MAX_ITEMS_PER_SOURCE=20 / FIRECRAWL_API_KEY / PRODUCT_HUNT_TOKEN)
3. **3곳 .env 동기화** (메모리 룰 `feedback_env_propagation.md` 적용):
   - `E:\00_develop\260406_luckystyle4u_server\.env` (windows)
   - `~/dev/ypserver-build/.env` (WSL 빌드 측)
   - `~/ypserver/.env` (운영 측, exclude 보호)
   모두 동일 6 keys 추가, secret 은 빈 값 (운영자 직접 채움).
4. tsc exit 0 검증.

**Commit**: `0d9a225 chore(aggregator): rss-parser/cheerio/@google/genai + .env.example 6 vars (B1)` (656 insertions).

### [5] B2 types + dedupe TDD (commit 1건, TDD 가 spec bug 1건 발견)

spec 의 `types.ts` (94 LOC) + `dedupe.ts` (151 LOC) 를 multi-tenant 적응으로 이식.

**핵심 변경 (multi-tenant 적응)**:
- `import { prisma } from "@/lib/prisma"` → `import { tenantPrismaFor } from "@/lib/db/prisma-tenant-client"`
  → 메모리 룰 `project_workspace_singleton_globalthis.md` 적용. Prisma 7 ALS propagation 깨짐 (commit `9621480` 사고) 회피 위해 closure 패턴 강제.
- `dedupeAgainstDb(items: RawItem[])` → `dedupeAgainstDb(items: RawItem[], ctx: TenantContext)` 시그니처 변경. caller (runner.ts) 가 ctx 명시 전달.
- urlHash unique 는 tenant-scoped (T1.6 마이그 적용 완료) → 같은 URL 이라도 tenant 별 독립 dedupe 자연 작동.

**TDD 25 케이스 작성**:
- canonicalizeUrl (12): trim/fragment/UTM/fbclid/_ga/IDN/trailing slash/포트 80/443/sort 안정성/multi-value
- urlHash (5): 안정성/distinct/canonicalize 흡수/대소문자/trailing slash
- dedupeAgainstDb (8): empty/all-fresh/all-dup/partial/batch 내 중복/canonicalize 영향/tenantPrismaFor closure/SELECT where in 정확

**1차 실행 24/25 통과, 1 fail**: 케이스 12 (`tag=b&tag=a`) 가 expected `tag=a&tag=b` 인데 실제 `tag=a&tag=a&tag=b&tag=b` 반환.

**진단**: spec dedupe.ts:78~80 의 "중복 키 보호" 주석 의도와 정반대 동작. `URLSearchParams.keys()` 가 동일 키 multi-value 를 별도 entry 로 노출 → keepKeys 에 ['tag', 'tag'] 중복 → `for (const k of keepKeys)` 가 `params.getAll('tag')` 를 두 번 호출 → kept = [['tag','b'], ['tag','a'], ['tag','b'], ['tag','a']] → sort 후 [a,a,b,b] 4개 entry. spec 자체의 버그.

**Fix**: `Array.from(new Set(keepKeys))` 로 키 unique 화 후 getAll 1회만 호출. 2차 실행 25/25 PASS.

**Commit**: `a121289 feat(aggregator): port types + dedupe with multi-tenant prisma (TDD, B2)` (545 insertions).

`★ Insight ─────────────────────────────────────`
- **TDD 의 즉시 ROI**: spec port 시점에 spec 자체의 multi-value bug 1건 발견 + fix. spec 의 6 fetcher × 4 외부 API 통합 단계 가서야 발견됐을 잠재 회귀를 commit 단위에서 차단.
- **tenantPrismaFor 패턴 일관성**: spec `import { prisma }` 한 줄을 `tenantPrismaFor(ctx)` closure 로 바꾸기만 했는데 ALS propagation 깨짐 회피의 핵심. 메모리 룰이 명문화되어 있어 "이 한 줄 변경"이 자동 적용됨. Track B 전체 (10 파일 1891 LOC 이식) 가 이 패턴 동일 사용 예정 → 일관성 자동 확보.
`─────────────────────────────────────────────────`

### [6] /cs 의식 — 본 conversation 마감

본 항목 작성 중. 산출 (예정):
- `docs/logs/journal-2026-05-02.md` 본 파일 (신규)
- `docs/logs/2026-05.md` 세션 80 entry
- `docs/handover/260502-session80-aggregator-bpre-b1-b2.md` (신규)
- `docs/handover/_index.md` row 80 추가 + 2026-05-02 그룹 신설
- `docs/handover/next-dev-prompt.md` 갱신 (세션 81 첫 작업 = B3 classify.ts)
- `docs/solutions/2026-05-02-spec-port-tdd-multivalue-bug.md` (신규 CK — spec port 시 TDD 가 bug 발견 패턴)
- 최종 commit + push

**핵심 교훈** (CK 본문 발췌):
1. **Spec port = naive copy 의 함정**: spec 자체에 bug/잘못된 가정이 있을 수 있음. TDD 25 케이스가 multi-value 처리에서 정상 동작 spec 가정과 충돌. 코멘트 vs 코드 의도가 어긋날 때 코드가 진실.
2. **베이스라인 검증의 ROI 두 번째 실증**: plan 가정 (T1.7 미완료 / R1 미확인 / R2 3개 부족) 모두 부분 또는 전부 부정확. plan 작성 직후가 아니라 진입 직전에 한 번 더 검증.
3. **3 commit 분리의 가치**: s79 leftover 회복 2건 + B-pre 1건. 단일 commit 으로 묶으면 git log 가 자기 설명 못 함. 기능 단위 commit 의 정신이 서로 다른 의도를 분리.
4. **스펙 동결판이 source of truth 아님**: classify.ts spec 40 슬러그 vs DB 37 슬러그 컨벤션 완전 차이 → DB가 운영 진실 + spec 은 개념 가이드. plan 의 R2 "3개 부족" 가정은 spec 동결판만 보고 도출됨.

**파일/Commit**: 본 /cs 산출 7 파일 + 최종 commit + push.

---

## 세션 80 (계속) — 동일 세션 B3 추가

> 다른 터미널이 이미 b46918c "/cs 의식 — B-pre+B1+B2 종료" 처리한 후, 같은 세션 80 안에서 B3 (classify.ts port) 작업이 추가됨. 본 후속 entry 가 갱신분.

### [7] B3 진입 — classify.ts port (TDD 40 케이스 + 한글 \\b boundary spec bug 차단)

> **사용자**: "b3 진행"

세션 80 첫 /cs 직후 (b46918c 머지 상태) 동일 세션 안에서 B3 작업 진입. 메모리 룰 `feedback_concurrent_terminal_overlap` 적용 — `git log --oneline -5` 점검: HEAD=`b46918c`, 다른 터미널이 `next-dev-prompt.md` 를 "B3 = 세션 81 첫 작업" 으로 갱신해놓은 상태. 같은 세션 안에서 B3 가 진행되어 정합화 필요.

**B3-1 (RED, 40 케이스 TDD)**:
- `tests/aggregator/classify.test.ts` 신규 (40 케이스 9 그룹 분포):
  - A. getAvailableCategorySlugs DB 시드 37 슬러그 정합 (5)
  - B. classifyItem 트랙 매처 (10)
  - C. 서브카테고리 build 7 슬러그 (8 — rag-agents 병합 2건 포함)
  - D. 서브카테고리 work 6 (3)
  - E. 서브카테고리 hustle 6 (2)
  - F. 서브카테고리 invest 6 (3)
  - G. 서브카테고리 learn 6 (3)
  - H. 서브카테고리 community 6 (3)
  - I. 한국어 + matched 배열 (3)
- 한글 케이스 12개 강제 포함 (test 18/22/25/26/27/28/30/31/33/34/36/37/38) — spec 의 잠재 한글 \\b boundary 버그 차단 게이트.
- 1차 RED 검증: classify.ts 미존재 → import 실패 (기대대로).

**B3-2 (GREEN, classify.ts port)**:
- `src/lib/aggregator/classify.ts` 신규 (308 LOC, spec 317 LOC 보다 줄어든 이유: drop 14 매처 제거).
- **DB 시드 매핑 적용** (slug-mapping-db-vs-spec.md 기준):
  - drop 14: marketing-growth, automation, model-releases, fine-tuning, ai-safety, benchmarks, valuation, earnings, courses, guides, case-studies, explainers, meetups, open-positions
  - 단/복수 정정 7: side-project, indie-hacker, tutorial, deep-dive, conference, hackathon, layoff-restructure
  - 의미 정정/약어 7: saas-bootstrap (← saas-business), productivity (← productivity-tools), team-ops (← team-collaboration), ai-workflow (← ai-at-work), knowledge-mgmt (← knowledge-management), infrastructure (← ai-infrastructure), funding (← ai-funding), paper-summary (← research-papers)
  - 병합 3: rag-agents (rag-vector + agents), public-markets (← earnings 흡수), layoff-restructure (← layoffs 정정 + 정리해고/구조조정 병합)
  - DB 신규 11: no-code, remote-work, korean-tech, data-science, system-design, career-growth, discussion, korean-community, macro-economy, market-analysis, research-paper

- **한글 boundary spec bug 차단**: spec 의 `compilePattern` 이 `\\b` boundary 사용. JS 의 `\\b` 는 ASCII `\\w` (=[A-Za-z0-9_]) 전용이라 가-힣 음절 양쪽이 non-word 로 취급되어 한글 키워드 매처가 사실상 비기능. spec 의 모든 한글 키워드 (TRACK_RULES + 서브카테고리 매처) 가 production 에서 매치되지 않을 잠재 회귀.
- **fix**: `compilePattern` 을 lookbehind/lookahead `(?<![\\w가-힣])(?:키워드)(?![\\w가-힣])` 로 교체. ASCII/한글 통합 word-class 정의.
- **iteration order 결정** (한글 fix 부수효과 회피): build 트랙 SUBCATEGORY 에서 korean-tech 를 infrastructure 보다 앞에 배치. 이유 = 한글 fix 후 '인프라' 키워드가 infrastructure 매처에도 노출되어 한국 회사명 텍스트가 한국 매처 우선 채택 안 되는 경우 회피.

**B3-3 (검증 게이트 + commit)**:
- `npx tsc --noEmit` exit 0
- `npx vitest run tests/aggregator/classify.test.ts` 40/40 PASS
- `npx vitest run` (전체) **437 pass / 60 skipped** (B2 의 397 + 신규 40 = 437, 회귀 0)
- `b46918c` 다른 터미널 commit 머지 상태 점검 — B3 코드 (classify.ts/.test.ts) 와 b46918c 산출 (docs only) 간 파일 충돌 0

**Commit**: `e74f3ef feat(aggregator): port classify with DB slug mapping (TDD 40, B3)` — 769 insertions (308 + 461 LOC).

`★ Insight ─────────────────────────────────────`
- **두 번째 spec port-time bug 차단**: B2 (multi-value) 에 이은 두 번째. spec 의 `\\b` Korean boundary 미작동은 spec 동결판이 ASCII 환경에서만 검증된 흔적. TDD 한글 케이스 12개가 RED phase 에서 즉시 표면화. B4~B7 의 fetcher/llm/promote 통합 단계 가서야 발견됐을 잠재 회귀.
- **iteration order = 분류 우선순위**: 한글 fix 적용 시 '인프라' 같은 일반 한글 키워드가 양쪽 매처(korean-tech, infrastructure)에 동시 노출. first-match-wins 룰에서 한국 특화 매처를 일반 매처보다 앞에 배치하는 결정이 한국 콘텐츠 분류 정확도 좌우. 이런 미묘한 순서 의존성을 코드 주석이 아닌 test 22 로 강제 검증.
- **DB = source of truth, 41 항목 변경**: spec 40 슬러그 → DB 37 슬러그로 14 drop / 7 단복수정정 / 7 의미정정 / 3 병합 / 11 신규 = 41 항목 변경. classify.ts slug 한 글자라도 어긋나면 promote.ts 즉시 FK violation → cron 폭주. test 1 (37개), test 4 (drop 14), test 5 (spec naming 17개 부재) 가 이 게이트.
`─────────────────────────────────────────────────`

### [8] /cs 의식 — 동일 세션 B3 정합화

본 항목 작성 중. b46918c 가 작성한 산출물과 B3 정합화:

- 본 저널 [7]/[8] entry append (위)
- `docs/logs/2026-05.md` 세션 80 entry — 5 commits → 6 commits 갱신, B3 토픽 추가, 검증 결과 397 → 437 갱신
- `docs/handover/260502-session80-aggregator-bpre-b1-b2.md` — ## 후기 섹션 추가 (B3 동일 세션 진입), 수정 파일 표 갱신, 검증 결과 갱신
- `docs/status/current.md` row 80 — 5 commits → 6 commits, B3 1줄 추가
- `docs/handover/_index.md` row 80 — 5 commits → 6 commits, B3 commit hash 추가
- `docs/handover/next-dev-prompt.md` — B3 ✅ 완료 마킹, B4 가 세션 81 첫 작업으로 승격
- `docs/solutions/2026-05-02-classify-korean-boundary-spec-bug.md` (CK 신규) — 한글 \\b boundary spec bug 사례. 자매 CK = 같은 세션 멀티밸류 bug.

**핵심 교훈** (CK 본문 발췌):
1. **두 번째 spec port-time bug**: 같은 세션에서 두 번째 spec 동결판 bug 차단 (B2 multi-value + B3 한글 boundary). spec 가 production-tested 가 아니라 single-environment-tested 일 수 있음.
2. **JS `\\b` 의 ASCII-only 함정**: `\\w` 가 ASCII 전용이므로 한글/CJK 키워드는 lookbehind/lookahead 로 별도 boundary 정의 필수. spec 작성 시 한글 케이스를 unit test 안 했다면 silent regression.
3. **다른 터미널 동시 /cs 의 정합화 비용**: 같은 세션 안에서 다른 터미널이 /cs 를 먼저 처리하면 후속 작업물의 docs 정합화가 ~7 파일 갱신으로 늘어남. 메모리 룰 `feedback_concurrent_terminal_overlap` 강화 — 작업 시작 전 다른 터미널 /cs 진행 상태 확인 필수.

**파일/Commit**: 본 /cs 산출 6~7 파일 (b46918c 후속 보강) + 최종 commit + push.

---

## 세션 80 (계속) — B4 + B5 + B6 추가

> 0ad2f9a (B3 정합화) 직후, 같은 세션 80 안에서 사용자 "b4 진행 / b5,b6 도 진행" 요청으로 Track B P0 본진의 나머지 3 commit 추가. 세션 80 commits 누적 7 → 10. spec port-time bug 누적 2 → 4 건.

### [9] B4 — 4 fetchers (rss/html/api/firecrawl) port + TDD 30

> **사용자**: "b4 진행 ...."

next-dev-prompt 와 직전 assistant 메시지 간 B4 정의 충돌 발견 (assistant: "runner.ts dispatch + cron worker pool" / next-dev-prompt: "4 fetchers + mocked 30"). 권위 있는 plan 파일 (`wave-wiggly-axolotl.md`) + handover 모두 일관되게 fetchers 가리킴 → assistant 가 B4/B6 을 혼동. **baseline 검증 룰이 정확히 이런 케이스 회피**.

spec 파일 4종 read: `fetchers/{index,rss,html,api}.ts` (4 파일 706 LOC). **DB 미터치 → multi-tenant 적응 0줄**. fetcher 는 외부 HTTP only, ContentSource 객체는 caller(runner.ts)가 RLS 컨텍스트에서 fetch 한 결과를 받음.

**TDD 30 케이스 RED → GREEN**:
- rss.ts (7): parseURL 호출 / RawItem 매핑 / MAX_ITEMS / empty 필터 / UA / publishedAt fallback / imageUrl fallback
- html.ts (8): parserConfig 검증 / fetch+UA / listSelector / "selector@attr" / 상대 URL absolute / MAX_ITEMS / empty 제외 / dateFormat=epoch_s
- index.ts (2): kind=RSS dispatch / unknown kind → []
- api.ts (13): HN(3) + Reddit(2) + ProductHunt(3) + ArXiv(2) + Firecrawl(2) + 어댑터 throw(1)

**1차 GREEN 21/30 PASS, 2 distinct issues**:
1. **vi.fn(arrow) 가 new 호출 불가** (rss-parser mock 7건 fail): `vi.fn(() => ({...}))` 의 impl 이 arrow function 이라 `new` 호출 시 "is not a constructor" — V8 reflective construction 시 arrow 거부. **fix**: function expression 으로 변경 (`vi.fn(function MockRssParser() {...})`).
2. **B4 spec port-time bug — ArXiv link regex 순서 의존** (test 27 fail): spec 의 정규식이 `rel="alternate"` 가 `href` **앞**에 있을 때만 매치. 테스트 XML 은 `href` 앞 → 0 items 반환. 실제 ArXiv API 가 순서 변경 시 silent failure. **fix**: `extractAlternateLink()` 신규 — `<link>` 모든 태그 iterate → `rel="alternate"` 검사 → `href` 추출. 양 순서 모두 매치 + `<id>` fallback.

**검증**:
- npx vitest run tests/aggregator/fetchers.test.ts → 30/30 PASS
- 추가 1 fix: cheerio.AnyNode import path 변경 (cheerio 1.x → `domhandler` 직접 import)
- npx tsc --noEmit → exit 0
- 전체: 467 pass / 60 skipped (B3 437 + B4 30, 회귀 0)

**Commit**: `100ae5c feat(aggregator): port 4 fetchers (rss/html/api/firecrawl) with mocked TDD 30 (B4)` — 5 파일 +1,481 LOC.

★ Insight ─────────────────────────────────────
- **세 번째 spec port-time bug**: B2 multi-value + B3 한글 boundary 에 이은 ArXiv link regex order 의존. 실제 ArXiv API 가 attribute 순서 변경 시 silent → 0 items 반환 → cron consecutive failures → 자동 비활성화 함정.
- **vi.fn 함정**: arrow function impl 은 `new` 호출 불가. 프로덕션 클래스 mock 작성 시 `function MockX() {...}` 형태 강제. dedupe.test.ts 의 `tenantPrismaFor` mock 이 정상 작동한 이유는 `new` 호출 안 함.
─────────────────────────────────────────────────

### [10] B5 — llm + promote multi-tenant tx port + TDD 27

> **사용자**: "b4 끝나고 B5, b6도 진행."

B4 commit 직후 B5 진입. spec 2 파일 read.

**llm.ts** (DB 미터치 → spec 그대로): Gemini Flash 래퍼, 6.5초 throttle, 일일 한도 200 default, GEMINI_API_KEY 누락/한도 초과/JSON 파싱 실패 모두 graceful (ruleResult only).

**promote.ts** (multi-tenant 적응 2건):
- `import { prisma }` → `import { tenantPrismaFor, withTenantTx, type TenantContext }`
- 시그니처: `promotePending(batch?)` → `promotePending(ctx, batch?)` (RLS SET LOCAL app.tenant_id 보장)
- findMany 2건 (ingested + categories) → `tenantPrismaFor(ctx)`
- upsert + update → `withTenantTx(ctx.tenantId, async (tx) => ...)` 1 transaction

**TDD 27 케이스 (2 파일)**:
- llm.ts (13): API key 부재 / urlHash 항상 / ruleResult base / budget 초과 / valid response / invalid track fallback / invalid slug fallback / JSON parse 실패 / tags slice+filter / summary+language slice / empty text / user prompt 포함 / getLlmStats shape
- promote.ts (14): empty / tenantPrismaFor 호출 / categoryRows fetch / categoryId resolve / categoryId null / slug Korean+hash / excerpt fallback / track fallback / publishedAt fallback / withTenantTx 호출 / upsert where / update status='promoted' / 단일 실패 batch 미차단 / promoted count

**1차 GREEN llm 13/13 PASS, promote 0/14 fail (vi.mock 호이스팅)**:
- promote.test.ts 의 vi.mock factory 가 outer `tenantPrismaForMock` 을 직접 참조 → vi.mock 호이스팅 시 const 미초기화 → ReferenceError.
- **fix**: `vi.hoisted(() => {...})` 패턴 적용. dedupe.test.ts 의 inline-closure 패턴과 차이 = assertion 위해 mock instance reference 가 필요할 때만 vi.hoisted 필요.

**2차 GREEN 13/14 PASS, 1 fail (B5 spec port-time bug)**:
- promote.ts test 6 (한글 slugify) fail: `'AI 도구 소개'` → expected `ai-도구-소개-<hash8>` / 실제 `ai-<hash8>`. spec slugify 의 `normalize("NFKD")` 가 한글 음절 (가-힣 U+AC00~U+D7A3) 을 jamo (U+1100~U+11FF) 로 분해 → 이후 `[^a-z0-9가-힣]+` regex 가 jamo 매치 실패 → 한글이 모두 hyphen 으로 → 빈 슬러그 (`item-<hash>` 폴백).
- **fix**: `normalize("NFKD")` 후 `normalize("NFC")` 추가하여 한글 음절 재결합. Latin diacritic 제거 효과는 보존 (NFKD → strip combining mark → NFC = base char 만 남음).
- **운영 영향**: 운영에서 발견됐다면 한글 제목 카드 모두 `slug = "item-<urlHash>"` 형태로 promote → 의미 없는 URL + slug 충돌 가능. Almanac 운영 후 24h 한글 콘텐츠 활성화 시 발견될 함정.

**검증**:
- npx vitest run llm.test.ts promote.test.ts → 27/27 PASS
- npx tsc --noEmit → exit 0
- 전체: 494 pass / 60 skipped (B4 467 + B5 27, 회귀 0)

**Commit**: `58a526a feat(aggregator): port llm + promote with multi-tenant tx (TDD 27, B5)` — 4 파일 +981 LOC.

★ Insight ─────────────────────────────────────
- **네 번째 spec port-time bug**: NFKD-only slugify 가 한글 음절을 jamo 로 분해하는 함정. NFKD 의 의도는 Latin diacritic 제거 (é → e) 였지만 한글에는 분해 효과 = 데이터 파괴.
- **vi.mock 호이스팅 패턴 분기 기준**: factory 가 mock 인스턴스를 **직접 참조** (예: `tenantPrismaFor: tenantPrismaForMock`) 하면 vi.hoisted 필수. **closure 안에서 참조** (예: `tenantPrismaFor: vi.fn(() => ({...findManyMock}))`) 하면 dedupe 패턴 OK.
- **module-level state 격리**: llm.ts 의 `client`/`counter`/`lastCallAt` 캐시 → 테스트 간 오염. `vi.resetModules()` + `dynamic import` 패턴으로 매 beforeEach 마다 fresh module.
─────────────────────────────────────────────────

### [11] B6 — runner + cron AGGREGATOR dispatcher + kind union 확장 + TDD 15

B5 commit 직후 B6 진입. cron 인프라 4 파일 read (runner / registry / supabase-clone types / API z.enum 2개).

**4 파일 동시 수정 — 단일 commit 필수** (TS 컴파일 차단 회피):
- `src/lib/types/supabase-clone.ts`: CronKindPayload 에 AGGREGATOR variant 추가
- `src/lib/cron/registry.ts`: ScheduledJob.kind union + 2 cast 모두 AGGREGATOR
- `src/lib/cron/runner.ts`: dispatchCron job.kind union + 신규 dispatchAggregatorOnMain
- `src/app/api/v1/cron/route.ts` + `[id]/route.ts`: z.enum 4-value

**runner.ts 신규 (multi-tenant 적응 + 5 sub-runner)**:
- `runAggregatorModule(ctx, {module, batch?})` — ctx 첫 인자 (memory rule `project_workspace_singleton_globalthis` — Prisma 7 ALS 깨짐 회피)
- 5 sub-runner: rss-fetcher / html-scraper / api-poller / classifier / promoter
- processSingleSource: cross-source 격리 (소스마다 try-catch). 성공 시 consecutiveFailures=0 + lastSuccessAt 갱신
- markSourceFailure: 5 도달 시 active=false 자동 비활성

**cron AGGREGATOR dispatcher** (`src/lib/cron/runner.ts`):
- `dispatchAggregatorOnMain(payload, tenantId, started)`: payload.module 검증 + ctx={tenantId} wrap → runAggregatorModule 호출
- `_tenantId` → `tenantId` (실제 사용)

**TDD 15 케이스 (2 파일)**:
- tests/aggregator/runner.test.ts (10): module 디스패치 5 + 알 수 없는 module + processSingleSource success/failure/임계 도달 active=false/cross-source 격리
- tests/cron/cron-aggregator-dispatch.test.ts (5): kind=AGGREGATOR / payload.module 누락 / tenantId → ctx / batch 전달 / 결과 그대로 반환

**1차 RED → GREEN 단번에 15/15 PASS** (vi.hoisted 패턴 사전 적용 효과). spec port-time bug 추가 발견 0건 (cron 인프라는 직접 작성).

**검증**:
- npx vitest run runner.test.ts cron-aggregator-dispatch.test.ts → 15/15 PASS
- npx tsc --noEmit → exit 0
- 전체: 509 pass / 60 skipped (B5 494 + B6 15, 회귀 0)

**Commit**: `7c50c9f feat(aggregator): port runner + cron AGGREGATOR dispatcher (TDD 15, B6)` — 8 파일 +815/-12 LOC.

### [12] 사용자 질문: B 작업은 몇번까지 있어?

> **사용자**: "B작업은 몇번까지 있어?"

전체 시퀀스 매핑 (`wave-wiggly-axolotl.md` + `next-dev-prompt.md` 기준) 보고:
- B-pre + B1~B8 = **9개**
- 완료: B-pre, B1, B2, B3, B4, B5, B6 = **7개**
- 남음: B7 (시드+배포 ~3h), B8 (소스 활성화 ~2h)
- 후속: 세션 85 24h 관찰, 세션 86~ Track C M2 19 라우트

### [13] /cs 의식 — 세션 80 3차 정합화

본 항목 작성 중. 세션 80 안에서 다른 터미널이 이미 /cs 두 번 처리 (b46918c B-pre+B1+B2 + 0ad2f9a B3 후기). 본 conversation 이 B4+B5+B6 추가 → 3차 정합화 필요.

**산출** (예정):
- 본 저널 [9]~[13] entry append (위)
- `docs/logs/2026-05.md` 세션 80 entry — commit 6→9, B4/B5/B6 토픽 추가, 검증 결과 437→509 갱신
- `docs/handover/260502-session80-aggregator-bpre-b1-b2.md` — `## 후속 후기 — B4 + B5 + B6` 섹션 추가
- `docs/status/current.md` row 80 — commit 6→9, B4/B5/B6 1줄씩 + 누적 LOC 갱신
- `docs/handover/_index.md` row 80 — commit 7→9, B4/B5/B6 추가
- `docs/handover/next-dev-prompt.md` — B3/B4/B5/B6 ✅ 완료 마킹, **세션 81 첫 작업 = B7**
- `docs/solutions/2026-05-02-spec-port-time-bug-pattern-4-cases.md` (CK 통합 신규 — 4건 누적 패턴)
- 메모리 신규: `feedback_vi_mock_hoisted_for_outer_refs.md` (vi.mock 호이스팅 패턴 분기)
- 최종 commit + push

**핵심 교훈** (CK 본문 발췌):
1. **Spec port-time bug 4건 누적**: 같은 세션 안에서 B2 multi-value + B3 한글 boundary + B4 ArXiv link order + B5 slugify NFKD = **4건 모두 spec 동결판이 single-environment-tested + happy-path-only-tested**. TDD 가 매번 RED phase 에서 표면화.
2. **vi.mock 호이스팅 분기 룰**: factory 의 mock 참조 방식이 직접 vs closure 에 따라 vi.hoisted 필요 여부 갈림. assertion 위해 instance reference 필요할 때 vi.hoisted 강제.
3. **단일 commit 으로 4 파일 동시 수정 가치** (B6 kind union 확장): TS literal union 확장은 caller 시그니처 일괄 수정 → 부분 commit 시 타입 에러 cascade. 단일 commit 으로 묶어 처리하면 git revert 도 단순.
4. **세션 안에서 /cs 3회 정합화 비용**: 같은 세션 안에서 다른 터미널이 /cs 를 두 번 (B-pre+B1+B2 / B3) 처리한 후 본 conversation 이 B4+B5+B6 추가 → 3차 정합화. 매 정합화마다 ~7 파일 갱신. **권고**: 같은 세션 안 sub-task 진행 시 /cs 는 모든 sub-task 끝낸 후 1회만.

**파일/Commit**: 본 /cs 산출 8~9 파일 + 최종 commit + push.

---

## 세션 81

> 사용자 단일 요청 "B7,b8 작업 및 C 작업 전체 진행" — wave-wiggly-axolotl 의 5 세션 매핑 (80~86) 을 단일 세션에서 압축 시도. B7 시드 + B8 5 소스 활성화 + 라이브 검증 + Track C M2 19 ops 23 라우트 4 그룹 + (다른 터미널 동시) M3 SSE 채널 + M2 통합 테스트 32 케이스.

### [1] 초기 베이스라인 점검 + 현실적 스코프 설명

> **사용자**: "B7,b8 작업 및 C 작업 전체 진행 .... B7 (~3h): scripts/seed-aggregator-cron.ts + 6 cron jobs (enabled=FALSE) + WSL 빌드 + 배포. B8 (~2h): 5 소스 점진 활성화 + 24h 관찰 + 첫 카드"

베이스라인 4종 점검 (memory `feedback_baseline_check_before_swarm` + `feedback_concurrent_terminal_overlap`): git log/status / current.md / next-dev-prompt / wave plan + m2-detailed-plan §5 라우트 인벤토리.

**현실적 스코프 보고**:
- B7 + B8-activation = 즉시 가능
- B8 24h 관찰 = 실시간 압축 불가 (cron schedule "every 6h" 자연 tick 대기)
- Track C 19 ops = 1+ 세션 분량이지만 헬퍼/스키마 이미 완료 (S67/68) → 패턴 단일화 → 압축 가능
- "Anti-Goldilocks 원칙": 24h 윈도우는 cron 작업 시간이지 Claude 작업 시간 아님 → 활성화 직후 곧바로 Track C 진입 (충돌 면적 = `cron/runner.ts` dispatch switch 1개, 이미 B6 처리 완료)

**결론**: 즉시 B7 → B8 → C1 → C2 → C3 → C4 직진.

### [2] B7 — seed-aggregator-cron.ts + 라이브 적용 + WSL 빌드+배포

`scripts/seed-aggregator-cron.ts` 신규 작성:
- 6 cron jobs 정의 (rss-fetch / html-scrape / api-poll / classify / promote / cleanup)
- `--tenant=<slug>` + `--enabled` 옵션 (default FALSE)
- 멱등 upsert 패턴: `(tenantId, name)` composite unique 기반 findFirst → update / create

tsc 1차 실패: `Type 'Record<string, unknown>' is not assignable to type 'JsonNullClass | InputJsonValue'` — Prisma 7 의 strict Json 타입. fix: `import type { Prisma }` + `payload: Prisma.InputJsonValue`. tsc 0.

**WSL 직접 적용** (memory `feedback_migration_apply_directly`):
- `rsync` scripts/ 동기화 → `npx tsx scripts/seed-aggregator-cron.ts --tenant=almanac` → 6 row CREATE (enabled=FALSE) PASS

**WSL 빌드+배포** (`bash scripts/wsl-build-deploy.sh`):
- [1/8]~[8/8] 전 단계 PASS — npm ci / next build / pack-standalone / rsync to ~/ypserver / install-native-linux / Drizzle 0 신규 / verify-schema OK / PM2 restart pid 220187 ↺=19
- ELF Linux x86-64 검증 통과
- err.log 0건, out.log "Ready in 0ms"

### [3] B8 — 5 소스 활성화 + runNow 라이브 검증

`scripts/b8-list-sources.ts` 작성 + 실행 → 60 sources 인벤토리 (RSS 46 / HTML 3 / API 7 / FIRECRAWL 4). 5 선정:
- anthropic-news / openai-blog / vercel-blog (RSS en build)
- toss-tech (RSS ko build) — **B3 한글 boundary fix 라이브 검증용**
- hn-algolia-front (API en community) — fetchers/api.ts 라이브 검증용

`scripts/b8-activate.ts` 작성 + 실행 → 5 sources active=TRUE + 6 cron enabled=TRUE (consecutiveFailures=0 / circuitState=CLOSED 리셋).

`scripts/b8-check.ts` (1분 후 점검): cron last_status 모두 'never' (다음 자연 tick 까지 6h~30m 대기).

**즉시 라이브 검증을 위해 `scripts/b8-runnow.ts` 작성** — registry.runNow(jobId) 직접 호출. 3 모듈 순차 force-run:

```
runNow almanac-rss-fetch  SUCCESS (13s)  — sources=4 fetched=60 inserted=60 dup=0 errors=1 (anthropic-news 404)
runNow almanac-classify   SUCCESS (237ms) — pending=50 classified=50 errors=0
runNow almanac-promote    SUCCESS (303ms) — promoted=50 errors=0
```

**결과**: items=50 (첫 production Almanac 카드), 4개월 P0 본진 라이브 검증 완료.

`★ Insight ─────────────────────────────────────`
- **첫 §3 격리 production 실증**: anthropic-news RSS URL 404 (외부 사실 — 사이트가 RSS 경로 변경) → consecutiveFailures=1, 다른 4 소스 fetch 차단 0. 임계 5 도달 시 `source.active=false` 자동 전환. ADR-022 §3 "한 컨슈머 실패 격리" 첫 실증.
- **B3 한글 boundary fix 라이브 통과**: toss-tech (RSS ko) 의 한글 콘텐츠가 fetch+classify+promote 전 경로 통과 — spec port-time bug (regex `\b` ASCII-only) 가 production 차단되지 않음 증명.
- **runNow = 24h 윈도우 압축 패턴**: cron schedule 자연 tick 까지 대기 대신 runNow 강제 실행으로 결정적 회귀 검증을 즉시 수행. 자연 tick 의 의미적 동등 (스케줄 무시 + circuit/lock 무시).
`─────────────────────────────────────────────────`

### [4] Track C M2 — 23 ops 19 routes 4 그룹 직진

m2-detailed-plan.md §5 인벤토리 + 헬퍼 4개 (conversations/messages/blocks/reports) + Zod 스키마 3개 + types.ts 모두 사전 완비 (S67/68). 신규 작성은 라우트 layer 만.

**공용 유틸**: `src/lib/messenger/route-utils.ts` — MessengerError → HTTP status 매핑 (20 코드, NOT_FOUND=404 / FORBIDDEN=403 / DUPLICATE_*=409 / *_EXPIRED/*_LIMIT_*=422) + `emitMessengerAudit` (audit/safe.ts wrapper).

**C1 conversations CRUD (5 ops, 2 files)**:
- `conversations/route.ts` — GET (본인 멤버 conv 100개) / POST (DIRECT 멱등 + GROUP)
- `conversations/[id]/route.ts` — GET (멤버 검증 + myMembership) / PATCH (title|archive, OWNER/ADMIN) / DELETE (archive, OWNER 만)

**C2 message CRUD (5 ops, 3 files)**:
- `conversations/[id]/messages/route.ts` — GET listMessages / POST sendMessage (rate 60/min/user)
- `conversations/[id]/messages/[msgId]/route.ts` — PATCH editMessage (15min) / DELETE recallMessage (sender 24h or admin 무제한, role 자동 판정)
- `messages/search/route.ts` — GET searchMessages (rate 30/min/user)

**C3 members/typing/receipts (5 ops, 5 files)**:
- `members/route.ts` — POST addMembers (OWNER/ADMIN, 부분 성공 added/skipped)
- `members/[userId]/route.ts` — DELETE removeMember (admin 또는 self, role 자동 판정)
- `members/me/route.ts` — PATCH updateMemberSelf (pin/mute, 정적 segment "me" 가 [userId] 보다 우선 매칭)
- `typing/route.ts` — POST stub (M3 SSE publish 도입 전, rate 1/sec/user)
- `receipts/route.ts` — POST MessageReceipt upsert + cross-conversation 침투 방어 (msg.conversationId 일치)

**C4 safety+admin (8 ops, 6 files)**:
- `user-blocks/route.ts` — GET listMyBlocks / POST blockUser
- `user-blocks/[id]/route.ts` — DELETE unblockUser (cross-user 방어 헬퍼 내부)
- `abuse-reports/route.ts` — POST fileReport (rate 5/min/user)
- `notification-preferences/route.ts` — GET (default fallback) / PATCH upsert
- `admin/reports/route.ts` — GET listOpenReports (`withTenantRole(["OWNER","ADMIN"])`)
- `admin/reports/[id]/resolve/route.ts` — POST resolveReport

총 23 ops 13 files + route-utils.ts.

**검증**:
- tsc --noEmit exit 0 (1회 만에 통과 — 헬퍼 시그니처 + Zod 스키마 + getCurrentTenant() 패턴 모두 사전 검증된 상태)
- vitest run: 509 pass / 60 skip / 회귀 0 (라우트는 helper 호출만, helper/test 변경 0)
- WSL 빌드+배포 PASS — pid 226263 ↺=20, ELF Linux x86-64, err.log 0건
- 17 라우트 unauth ping → 모두 401 (handler 등록 + withTenant 가드 + 부팅 0 errors)

`★ Insight ─────────────────────────────────────`
- **사전 헬퍼/스키마 완비의 ROI**: M2-Step1 (S69) 에서 도메인 헬퍼 4개 + Zod 스키마 3개 + 단위 테스트 32 케이스를 미리 작성한 결과, M2-Step2 (라우트 layer) 가 단일 세션 안에 압축 가능. 라우트 핸들러 = 가드 + Zod parse + helper 호출 + audit + errorMapping 의 5-step 패턴 반복 (~13 파일).
- **memory `project_workspace_singleton_globalthis` 적용 검증**: 모든 라우트가 `withTenant` 가드 → 핸들러 → 헬퍼 호출 흐름. 헬퍼 내부에서 `tenantPrismaFor(getCurrentTenant())` 직접 closure 캡처 → ALS propagation 깨짐 회피 (Prisma 7). 라우트 본체는 ALS 호출 0.
`─────────────────────────────────────────────────`

### [5] 다른 터미널 M3 SSE + M2 통합 테스트 32 케이스 (commit `069705c`)

본 conversation /cs 호출 직전 다른 터미널이 push (+1,332 LOC, 4 신규 파일 + 5 라우트 갱신):

- `src/lib/messenger/sse.ts` (70 LOC) — `convChannelKey(tenantId, conversationId)` + `userChannelKey(tenantId, userId)` + `publishConvEvent` / `publishUserEvent` (try-catch fail-soft, bus.ts 싱글턴 활용, ADR-022 §1 Phase 2 트리거 멀티 인스턴스 시 Redis pubsub 교체)
- `conversations/[id]/events/route.ts` (115 LOC) — SSE GET endpoint
- `tests/messenger/m2-integration.test.ts` (969 LOC) — M2 통합 테스트 32 케이스
- `tests/messenger/sse.test.ts` (134 LOC) — SSE 단위 테스트
- 5 라우트에 `publishConvEvent` 통합: messages POST (`message.created`) / messages PATCH (`message.updated`) / messages DELETE (`message.deleted`) / members POST (`member.joined`) / members DELETE (`member.left`) / typing POST (`typing.started`, M3 stub → 실제 publish 로 활성화) / receipts POST (`receipt.updated`)

**M3 SSE 진입 + M2 머지 게이트 통합 테스트 32 케이스 즉시 충족** — 이번 세션의 handover 가 "별도 세션 RLS_TEST_DATABASE_URL 셋업 후 통합 테스트" 로 미루어 둔 항목이 다른 터미널에 의해 즉시 처리됨.

memory `feedback_concurrent_terminal_overlap` 룰: 동일 영역 동시 진행 — 단 본 conversation 의 라우트 layer (handler + 가드 + Zod + helper 호출) 와 다른 터미널의 M3 SSE 통합 (publish 호출 1줄 추가) 이 정확히 보완 관계로 충돌 0. 후속 머지 시 publishConvEvent import 추가만 자동 병합됨.

### [6] commit & 자동 push (이번 conversation 2 commits)

- `ffdd2dd feat(aggregator): B7+B8 — seed 6 cron jobs + 5 sources 점진 활성화 (almanac live)` (5 files +477)
- `72b5ebd feat(messenger): Track C M2 — 23 라우트 19 ops 4 그룹 (M2 게이트 진입)` (17 files +1,360)
- `069705c feat(messenger): M3 SSE 채널 + M2 통합 테스트 32 케이스 (+1,332 LOC)` — 다른 터미널 push (4 신규 + 5 갱신)

세션 81 누적 commit 3건 / +3,164 LOC / 26 파일. 4개월 P0 본진 라이브 + Track C M2 라우트 + M3 SSE + 통합 테스트 32 케이스 모두 1세션 압축.

---

## 세션 82

> 단일 사용자 메시지 "다음작업 개시(기다려야 하는 작업은 건너뛰고)" 로 시작. M3 user 채널 wiring 후속 "남은 작업도 여기서 진행" 으로 M2 라이브 활성화 + M3 SSE wire-format 검증까지 수렴. 3 commits + Prisma extension + RLS + timezone latent bug 4건 동시 노출/fix.

### [1] 다음작업 개시 — M3 user 채널 wiring (mention/dm/report/block 4 이벤트)

> **사용자**: "다음작업 개시(기다려 여야하는 작업은 건너뛰고) ..."

세션 81 handover 의 "M3 user 채널 5 이벤트 wiring 남음" 진입 (실제 4 이벤트 + typing.stopped 미사용).

**baseline 검증** (memory `feedback_baseline_check_before_swarm` + `feedback_concurrent_terminal_overlap`):
- `git log --oneline -10` + `git status --short` → commit `1c28dd3 docs(s81): /cs 세션 종료` 가 다른 터미널에 의해 추가 발견. commit 메시지에 "다른 터미널의 진행 중 src/ 코드 변경 (M3 user 채널 wiring) 은 본 commit 에서 의도적 제외" 명시 — 동시성 충돌 회피 룰이 양 터미널에서 정상 작동했고 충돌 없이 분리 commit 됨.
- PRD api-surface §4.3 4 이벤트 spec 확인: `mention.received` `dm.received` `report.resolved` `block.created` 모두 user 채널.

**라이브러리 변경** (`src/lib/messenger/messages.ts`):
- `sendMessage` 반환에 `conversationKind` + `otherMemberId` 추가. 라우트가 user 채널 publish 결정에 추가 DB query 없이 활용. helper 가 이미 conv kind 와 peer 를 내부에서 query 하므로 반환만 확장 (가장 비용 효율).

**라우트 wiring**:
- `messages POST` → DM 이면 peer 에 `dm.received`, mention 각자에게 `mention.received`. snippet 80자 컷 (TEXT 한정).
- `admin/reports/[id]/resolve POST` → `result.report.reporterId` 에게 `report.resolved`.
- `user-blocks POST` → blocker 본인에게 `block.created` (cross-device sync 목적, 차단당한 사람에게는 노출 X — stalker risk 차단).

**테스트** (tests/messenger/sse.test.ts +5 / messages.test.ts +2 env-gated): 4 PRD payload 계약 + cross-tenant 격리 + sendMessage DIRECT/GROUP 분기.

**검증**: tsc 0 / vitest 525 pass + 91 skip / 5-run 회귀 0 (totp.test.ts AES-GCM tamper 플레이크는 base64 padding bits 한정 flip 으로 인한 pre-existing 사안).

**결론**: commit `152562d feat(messenger): M3 user 채널 wiring — mention/dm/report/block 4 이벤트 (+292 LOC)`.

### [2] 남은 작업도 여기서 진행 — 3 잔여 진행

> **사용자**: "남은 작업도 여기서 진행."

S81 handover 의 6 후보 중 진행 가능 3건 동시 착수: A. M2 통합 테스트 32 라이브 활성화 / B. M3 SSE 라이브 검증 / C. M4 PRD 확인.

### [3] (A) M2 통합 테스트 라이브 활성화 — Prisma + RLS 함정 4건 동시 노출/fix

WSL postgres 의 `luckystyle4u_test` 신규 DB 셋업 시도 → 4 latent bug 노출.

**3.1 fixture 단순 버그**:
- `tenant_memberships_tenant_id_user_id_key` constraint 가 INDEX 형태 (CONSTRAINT 명 미실재) → `ON CONFLICT (tenant_id, user_id)` 컬럼 기반으로 변경.
- `resetMessengerData` 가 files/folders 미정리 → `users` DELETE 시 FK 차단. mxtest-* pattern cascade cleanup 추가.

**3.2 schema 누락** (`prisma/schema.prisma`):
- `AbuseReport.targetKind` 의 `@map("target_kind")` 누락. Prisma 가 literal `targetKind` 컬럼 기대 / DB 는 snake_case → mismatch. **prod 미운영 라우트라 latent**. session 80 Track C M2 추가 시 누락된 채 머지된 흔적. fix 후 `prisma generate` 재실행.

**3.3 Prisma extension query escape (가장 큰 발견)**:
- `tenantPrismaFor`/`prismaWithTenant` 의 `$allOperations` 콜백이 `basePrisma.$transaction` 으로 감싸 `tx.$executeRawUnsafe('SET LOCAL app.tenant_id = ...')` 적용 후 `query(args)` 호출하는 패턴. 그러나 Prisma extension 의 `query(args)` 는 우리가 연 tx connection 을 사용하지 않고 base client 의 새 connection 으로 escape → **SET LOCAL 적용된 tx 와 실제 query 가 다른 conn → RLS always-fail (0 rows)**.
- **prod 가 BYPASSRLS postgres superuser 사용해서 가려져 있던 latent bug**. 비-bypass role(`app_test_runtime`)로 테스트하는 첫 시도에서 노출.
- 수정: tx 안에서 `tx[modelCamel][operation](args)` 직접 호출. raw operation (`$executeRawUnsafe` 등)은 `args` array spread 로 tx 에 binding. tenantPrismaFor + prismaWithTenant 양쪽 동일 적용.

**3.4 PrismaPg + Asia/Seoul timezone 함정**:
- `tenantPrismaFor` 수정 후에도 `editMessage 15분 1초 경과 → EDIT_WINDOW_EXPIRED` 가 통과 (실제로는 throw 해야 함). 추적 결과 — admin pool (raw pg) 로 INSERT 한 createdAt = `2026-05-02T01:25:55.248Z` (UTC) 이 Prisma 로 read 시 `2026-05-02T10:25:55.248Z` 로 +9hr 시프트 (KST offset).
- 원인: WSL postgres session timezone = `Asia/Seoul`. PrismaPg adapter 가 TIMESTAMPTZ 의 `+09` offset 을 ignore 하고 local 시각을 UTC 로 mis-parse → 9시간 시프트.
- prod 환경에서는 read/write 양방향 동시 시프트라 cancel 되어 보이지 않으나, **admin pool (raw pg) 직접 INSERT + Prisma 읽기 혼용** 시 노출. 회피: `RLS_TEST_DATABASE_URL` 에 `?options=-c%20TimeZone%3DUTC` 추가.
- **prod 영향 가시화 follow-up 별도 필요** — rate-limit window / edit/recall window / session expiry 등 모든 timestamp 비교 로직에 잠재 영향.

**3.5 세션 67/80 latent test 버그**:
- `message_receipts.lastReadAt` 미실재 (스키마는 @updatedAt 자동) → upsert 인자에서 제거.
- `notification_preferences` 의 `id`/`created_at` 컬럼 미실재 (composite PK) → admin pool INSERT 에서 제거.
- `DUPLICATE_REPORT` regex match → `toMatchObject({code})` 로 변경 (Korean message `이미 신고된 대상입니다` 우회).
- editMessage/recallMessage 1초 margin → 5초로 (TIMESTAMPTZ ms 절삭 안전).

**테스트 인프라 영구 정착**:
- `scripts/setup-test-db-role.sh` — `app_test_runtime` password 발급 + login 검증 (BYPASSRLS=false).
- `scripts/run-integration-tests.sh` — bash 러너 (WSL→Win cross-OS env 손실 노트 포함).
- `.env.test.example` — 환경 변수 템플릿 (`.env.test.local` gitignored).
- WSL postgres `luckystyle4u_test` DB (38 tables + 30 RLS policies + role GRANTs).

**검증**: 메신저 92/92 PASS (m2-integration 32 + messages 12 + rls 13 + reports 11 + blocks 4 + conversations 4 + sse 13 + fixtures helper). 전체 vitest (env-gate 비활성) 525 pass + 91 skip — prod 경로 회귀 0.

**결론**: commit `8bef896 fix(messenger,db): M2 통합 테스트 32 라이브 PASS — Prisma extension + RLS 함정 4건 동시 fix` (+180 LOC, 8 파일).

### [4] (B) M3 SSE wire format 자동 검증 — encodeSseEvent/Comment 헬퍼 추출

route.ts 의 인라인 SSE 형식 문자열을 `src/lib/messenger/sse.ts` 헬퍼로 추출:
- `encodeSseEvent(event, payload)` → `event: <name>\ndata: <json>\n\n`
- `encodeSseComment(text)` → `: <text>\n\n` (keepalive)

EventSource spec 준수 검증 7 신규 테스트:
- wire format (event/data/종결자 `\n\n`)
- nested payload JSON.stringify 동등
- 한글/이모지 unescape 보존
- bus → encode → 3 publish 순서 보존
- ReadableStream + TextEncoder 통합 — 실제 chunk 디코딩 후 EventSource parser 시뮬레이션
- multiple subscribers (3명) 동일 채널 동시 수신

브라우저 EventSource 라이브 검증 대체 (PM2 상시 운영 ypserver 가 있어 manual 검증도 가능하나 자동화 우선).

**검증**: vitest 525→532 pass (+7), tsc 0.

**결론**: commit `5449f9e test(messenger): M3 SSE wire format 자동 검증 + encodeSseEvent/Comment 헬퍼 추출 (+7 tests)` (+153 LOC, 3 파일).

### [5] (C) M4 push notification PRD 확인 → 진입 안 함

> 사용자 messsage 의 "M4 push notification (web push subscribe)" 표현은 mis-naming. PRD 확인 결과 messenger Phase 1 의 M4 = **UI 보드** (대화목록 + 채팅창 + composer, 5-7 작업일). M6 가 알림 (in-app SSE only, web push 없음).

Phase 1 에 web push (Service Worker / VAPID) 자체 미포함. 이번 세션 chunk 비현실 → 진입 안 함.

### [6] /cs 진입 — 모든 서버 종료 요청

> **사용자**: "모든 서버 종료하고 세션 종료 진행."

Windows 측 leftover node.exe 7개 (vitest workers, 09:55 시작) 종료 — `Get-Process node | Stop-Process -Force` 후 count=0 확인.

WSL PM2 3개 (cloudflared/seaweedfs/ypserver + pm2-logrotate) 는 stylelucky4u.com 운영 24/7 인프라 — 이번 세션에서 띄우지 않은 사전 운영 중 프로세스. 종료 시 외부 사이트 OFF 라 사용자 추가 확인 대기.

### [7] commit & 자동 push (이번 conversation 3 commits)

- `152562d feat(messenger): M3 user 채널 wiring — mention/dm/report/block 4 이벤트 (+292 LOC)` — 6 files
- `8bef896 fix(messenger,db): M2 통합 테스트 32 라이브 PASS — Prisma extension + RLS 함정 4건 동시 fix` — 8 files +180/-26
- `5449f9e test(messenger): M3 SSE wire format 자동 검증 + encodeSseEvent/Comment 헬퍼 추출 (+7 tests)` — 3 files +153/-6

세션 82 누적 3 commits / +625 LOC / 17 파일. M3 user 채널 + M2 라이브 활성화 + Prisma+RLS+timezone 4 latent bug fix + SSE wire format.
